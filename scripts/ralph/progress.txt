# Ralph Progress Log
Started: Tue Mar 10 20:18:10 -03 2026
---

## Codebase Patterns
- All types go in `src/types/index.ts` — no barrel exports, import directly
- New nullable DB fields added to existing interfaces should use `?:` (optional) to avoid breaking mock data and existing code
- Mock data in `src/data/mock.ts` uses the `Pedido` type — any required field addition will break it
- DB column names use snake_case; TypeScript interface fields for DB-mapped types also use snake_case
- Existing separation columns in DB were renamed by US-002: `separado_por` → `separacao_operador_id`, `separado_em` → `separacao_iniciada_em`, `embalado_em` → `embalagem_concluida_em`, `agrupamento_tiny_id` → `agrupamento_expedicao_id`
- When renaming DB columns, update ALL source files that reference them (grep for old name in src/ — includes API routes, components, mock data, lib services)
- PL/pgSQL functions referencing renamed columns must also be updated via CREATE OR REPLACE in the same migration
- Status values changed: `pendente` → `aguardando_separacao`, `expedido` removed (mapped to `embalado`). Valid values: aguardando_nf, aguardando_separacao, em_separacao, separado, embalado, cancelado
- `siso_pedidos.id` is `text` (not uuid) — all PL/pgSQL and API functions accepting pedido IDs must use `text[]`/`text`
- `siso_pedido_itens` has no `unidade` column — default to 'UN' wherever unidade is needed
- Localizacao is in `siso_pedido_item_estoques`, joined via pedido_id + produto_id + empresa_id (for origin empresa)
- Filter by galpao via `empresa_origem_id` → `siso_empresas.galpao_id` join (more robust than `separacao_galpao_id`)
- Order date column in DB is `data` (not `data_pedido`)
- TABLE-returning PL/pgSQL functions return 0 rows for "not found" — callers check `.length === 0` or empty result
- GET /api/separacao returns `{ counts: SeparacaoCounts, pedidos: array }` — not a raw array
- Role-based filtering in separacao APIs uses `X-User-Cargo` header (not session-based galpao_id)
- Supabase FK join syntax: `siso_empresas(nome)` in select — returns `{ nome }` object on each row
- UF/cidade are NOT stored in siso_pedidos — return null (payload_original only stores Tiny order ID)

## 2026-03-10 - US-001
- Added types: StatusSeparacao, ProdutoConsolidado, BipEmbalagemResult, SeparacaoFilter, SeparacaoCounts, PedidoItem
- Extended Pedido interface with 6 optional separation tracking fields
- Files changed: src/types/index.ts
- **Learnings for future iterations:**
  - Existing DB migration (20260311_add_separacao_columns.sql) uses different status values (`pendente` vs PRD's `aguardando_separacao`) and column names — US-002 will need to reconcile
  - PedidoItem type was created from scratch since no TS type existed for `siso_pedido_itens`
  - Making new Pedido fields optional (`?:`) avoids breaking the 11 mock data entries
---

## 2026-03-10 - US-002
- Created migration `supabase/migrations/20260311_separacao_status_columns.sql`
- Dropped old CHECK constraint on status_separacao, added new with PRD values
- Updated existing rows: `pendente` → `aguardando_separacao`, `expedido` → `embalado`
- Renamed columns: `separado_por` → `separacao_operador_id`, `separado_em` → `separacao_iniciada_em`, `embalado_em` → `embalagem_concluida_em`
- Replaced `agrupamento_tiny_id` (bigint) with `agrupamento_expedicao_id` (text)
- Added `separacao_concluida_em` column to siso_pedidos
- Added `separacao_marcado`, `separacao_marcado_em` columns to siso_pedido_itens
- Created index `idx_siso_pedidos_status_empresa`
- Updated PL/pgSQL function `siso_processar_bip` to use new column names and status values
- Updated all source files referencing old column names (12 files)
- Applied migration via Supabase MCP
- Files changed: supabase/migrations/20260311_separacao_status_columns.sql, src/data/mock-separacao.ts, src/components/separacao/pedido-separacao-card.tsx, src/components/separacao/tab-embalados.tsx, src/app/api/separacao/route.ts, src/app/api/separacao/reimprimir/route.ts, src/app/api/separacao/desfazer-bip/route.ts, src/app/api/separacao/[pedidoId]/forcar-pendente/route.ts, src/app/separacao/page.tsx, src/lib/etiqueta-service.ts, src/lib/nf-webhook-handler.ts
- **Learnings for future iterations:**
  - When renaming DB columns, grep all of `src/` for old names — they appear in Supabase `.select()` strings, response mappings, mock data interfaces, and comments
  - The `agrupamento_expedicao_id` is now text (was bigint `agrupamento_tiny_id`), so parseInt() is needed when passing to Tiny API functions
  - `etiqueta_url` already existed from the old migration — skip re-adding
  - The old `expedido` status and `expedir/route.ts` still exist but will be superseded by the new flow
  - Dynamic DO block is needed to drop auto-generated CHECK constraint names in PostgreSQL
---

## 2026-03-10 - US-003
- Created PL/pgSQL function `siso_consolidar_produtos_separacao(p_pedido_ids text[], p_order_by text)`
- Groups siso_pedido_itens by produto_id, SUMs quantidade_pedida across given pedidos
- Joins siso_pedido_item_estoques for localizacao using origin empresa (empresa_origem_id from siso_pedidos)
- Supports ORDER BY localizacao (default), sku, or descricao via CASE expression
- Defaults unidade to 'UN' (no unidade column exists in siso_pedido_itens)
- Applied migration via Supabase MCP and verified with real data
- Files changed: supabase/migrations/20260311_fn_consolidar_produtos_separacao.sql
- **Learnings for future iterations:**
  - `siso_pedidos.id` is `text` not `uuid` — PL/pgSQL functions must use `text[]` for pedido_id arrays
  - `siso_pedido_itens` has no `unidade` column — default to 'UN' in functions and APIs
  - `produto_id` is `bigint` in DB but returned as `text` by the function (cast required)
  - localizacao lives in `siso_pedido_item_estoques`, joined via pedido_id + produto_id + empresa_id matching siso_pedidos.empresa_origem_id
  - When calling this function via Supabase RPC, pass `p_pedido_ids` as a text array and `p_order_by` as string
---

## 2026-03-10 - US-004
- Created PL/pgSQL function `siso_processar_bip_embalagem(p_sku text, p_galpao_id uuid, p_quantidade int)`
- Finds oldest `separado`-status order in galpao by joining `empresa_origem_id` → `siso_empresas.galpao_id`
- Matches item by SKU or GTIN, locks with `FOR UPDATE OF i`
- Increments `quantidade_bipada`, sets `bipado_completo` when `>= quantidade_pedida`
- Checks all items complete → transitions pedido to `embalado` + sets `embalagem_concluida_em`
- Returns TABLE(pedido_id text, produto_id text, quantidade_bipada numeric, bipado_completo boolean, pedido_completo boolean)
- Returns empty result set (no rows) when no matching item found
- Applied migration via Supabase MCP
- Files changed: supabase/migrations/20260311_fn_processar_bip_embalagem.sql
- **Learnings for future iterations:**
  - Use `empresa_origem_id` → `siso_empresas.galpao_id` join to filter by galpao (more robust than `separacao_galpao_id` which may not be set)
  - Order date column is `data` (not `data_pedido`) in `siso_pedidos`
  - `FOR UPDATE OF i` locks just the item row in a joined query — specify the table alias
  - TABLE-returning functions return empty result set (0 rows) for "not found" — callers check for empty result, not NULL
  - When calling via Supabase RPC: `supabase.rpc('siso_processar_bip_embalagem', { p_sku, p_galpao_id, p_quantidade })`
---

## 2026-03-10 - US-005
- Refactored GET /api/separacao to match new requirements
- Returns `{ counts: SeparacaoCounts, pedidos: array }` instead of raw array
- Counts use 5 parallel HEAD queries (one per status) — efficient, no data transfer
- Role-based filtering via X-User-Cargo header: operador_cwb/operador_sp get galpao-filtered results
- Supports query params: status_separacao, empresa_origem_id, sort, busca
- Search via Supabase `.or()` with ilike on numero, id_pedido_ecommerce, cliente_nome
- Each pedido includes empresa_origem_nome via Supabase FK join `siso_empresas(nome)`
- Updated separacao/page.tsx to handle new response shape (`json.pedidos ?? json`)
- Files changed: src/app/api/separacao/route.ts, src/app/separacao/page.tsx
- **Learnings for future iterations:**
  - UF and cidade are NOT stored in siso_pedidos DB — payload_original only stores the Tiny order ID, not the full order object
  - Supabase FK join works with `siso_empresas(nome)` when there's only one FK to that table — no need for `!column_name` disambiguation
  - Supabase `select("*", { count: "exact", head: true })` is efficient for counting — no data transferred
  - Avoid `Function` type in TypeScript generics — ESLint will flag it; inline the logic instead
  - When changing API response shape, update ALL consumers (page.tsx fetch functions) to avoid runtime breakage
---

## 2026-03-10 - US-006
- Already committed (aa0c86f) — marked passes: true in PRD
---

## 2026-03-10 - US-008
- Created POST /api/separacao/concluir/route.ts: checks each pedido's items, moves to 'separado' only if ALL items have separacao_marcado = true, returns { separados, pendentes }
- Created POST /api/separacao/cancelar/route.ts: resets all item checkmarks (separacao_marcado=false, separacao_marcado_em=null), resets pedidos to aguardando_separacao with null operador/iniciada_em
- Files changed: src/app/api/separacao/concluir/route.ts, src/app/api/separacao/cancelar/route.ts
- **Learnings for future iterations:**
  - concluir groups items by pedido_id in memory (Map) to check completeness per pedido — more efficient than N separate queries
  - cancelar updates items first, then pedidos — order doesn't matter since it's not transactional, but items-first is safer (no orphaned state)
  - Both use `.in("pedido_id", pedido_ids)` for batch operations on siso_pedido_itens
---

## 2026-03-10 - US-007
- Created POST /api/separacao/marcar-item/route.ts: toggles separacao_marcado on a single item, validates parent pedido is em_separacao
- Created POST /api/separacao/bipar-checklist/route.ts: finds items by SKU or GTIN across given pedido_ids where separacao_marcado = false, marks all matches
- Files changed: src/app/api/separacao/marcar-item/route.ts, src/app/api/separacao/bipar-checklist/route.ts
- **Learnings for future iterations:**
  - `siso_pedido_itens.id` is bigint — API can accept string, Supabase `.eq("id", stringValue)` handles coercion
  - Use `.or(\`sku.eq.${value},gtin.eq.${value}\`)` for matching by either SKU or GTIN in Supabase
  - marcar-item validates parent pedido status before allowing update — pattern for item-level APIs
---

## 2026-03-10 - US-009
- Created POST /api/separacao/bipar-embalagem/route.ts
- Calls Supabase RPC `siso_processar_bip_embalagem` with { p_sku, p_galpao_id, p_quantidade }
- Returns 404 when RPC returns empty array (TABLE-returning function pattern)
- Maps raw RPC response to typed BipEmbalagemResult
- Validates sku (string, trimmed) and galpao_id (string), defaults quantidade to 1
- Files changed: src/app/api/separacao/bipar-embalagem/route.ts
- **Learnings for future iterations:**
  - TABLE-returning PL/pgSQL functions return `[]` (empty array) not null — check `.length === 0`
  - Supabase RPC returns array for TABLE-returning functions — extract first element with `data[0]`
  - Follow existing pattern: parse body with `.catch(() => null)`, validate required fields, try/catch with logger
---

## 2026-03-10 - US-010
- Created POST /api/separacao/confirmar-item-embalagem/route.ts
- Accepts { pedido_item_id: string, quantidade: number }, validates pedido status is 'separado'
- Increments quantidade_bipada (min 0), sets bipado_completo when >= quantidade_pedida
- Checks ALL items of pedido for completion → transitions to 'embalado' + sets embalagem_concluida_em
- Returns { pedido_item_id, quantidade_bipada, bipado_completo, pedido_completo }
- Files changed: src/app/api/separacao/confirmar-item-embalagem/route.ts
- **Learnings for future iterations:**
  - For manual +/- confirmation, use `Math.max(0, current + delta)` to prevent negative quantities
  - Validate pedido status is 'separado' (not 'em_separacao') for packing operations — packing happens after separation is complete
  - Use head-only count query `select("*", { count: "exact", head: true })` + `.eq("bipado_completo", false)` to efficiently check remaining items
  - Pattern: fetch item → validate parent pedido status → update item → check all siblings → maybe update pedido status
---

## 2026-03-10 - US-011
- Created POST /api/separacao/reiniciar/route.ts: resets item progress for separacao (separacao_marcado) or embalagem (quantidade_bipada/bipado_completo)
- Created POST /api/separacao/forcar-pendente/route.ts: admin-only bulk force from aguardando_nf → aguardando_separacao
- Files changed: src/app/api/separacao/reiniciar/route.ts, src/app/api/separacao/forcar-pendente/route.ts
- **Learnings for future iterations:**
  - An existing single-pedido PATCH endpoint exists at `[pedidoId]/forcar-pendente` — the new POST at `/forcar-pendente` handles bulk operations (multiple pedido_ids)
  - reiniciar validates status before resetting: em_separacao for separacao etapa, separado for embalagem etapa — returns 400 with invalid pedidos list
  - forcar-pendente uses `getSessionUser(request)` from `@/lib/session` for auth + admin check, matching the pattern in iniciar and other protected routes
  - Bulk update with `.eq("status_separacao", "aguardando_nf")` acts as a guard — only updates matching rows, `.select("id")` returns actually affected IDs
---

## 2026-03-10 - US-012
- Already implemented during US-002 (committed fc78b0e and d54bc85)
- `src/lib/nf-webhook-handler.ts` handles full NF webhook flow: dedup, fast-path match via nota_fiscal_id, fallback via Tiny API, idempotent transition aguardando_nf → aguardando_separacao, stores url_danfe + chave_acesso_nf
- `src/app/api/webhook/tiny/route.ts` discriminates `tipo === "nota_fiscal"` before order webhook logic (line 54)
- No code changes needed — marked as passes in PRD
- **Learnings for future iterations:**
  - NF webhook handler was built proactively during US-002 migration — check existing code before implementing
  - The handler also manages race conditions (NF arrives before order is saved) via "aguardando_pedido" status
  - NF webhook payload uses `dados.idNotaFiscalTiny` (not `dados.id` like order webhooks)
---

## 2026-03-10 - US-013
- Already implemented in tiny-api.ts: `criarAgrupamento` (POST /expedicao) and `obterEtiquetasAgrupamento` (GET /expedicao/{id}/etiquetas)
- Types `TinyCriarAgrupamentoResponse` and `TinyEtiquetasAgrupamentoResponse` co-located in tiny-api.ts
- No code changes needed — marked as passes in PRD
- **Learnings for future iterations:**
  - All Tiny API types are co-located in tiny-api.ts (not types/index.ts) — this is the codebase convention
  - All tiny-api.ts functions accept `token` (not `empresa_id`) — callers handle token retrieval via `getValidTokenByEmpresa()`
  - Rate limiting is caller responsibility, not embedded in tiny-api.ts functions
---

## 2026-03-10 - US-014
- Label service already existed as `src/lib/etiqueta-service.ts` (buscarEImprimirEtiqueta)
- Added fire-and-forget trigger to `bipar-embalagem/route.ts` — calls buscarEImprimirEtiqueta when pedido_completo = true
- Added fire-and-forget trigger to `confirmar-item-embalagem/route.ts` — same pattern
- Files changed: src/app/api/separacao/bipar-embalagem/route.ts, src/app/api/separacao/confirmar-item-embalagem/route.ts, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Label service is `etiqueta-service.ts` (not `print-label.ts` as PRD suggests) — uses buscarEImprimirEtiqueta()
  - The service handles full flow: create agrupamento → get labels → PrintNode printing → status tracking
  - Fire-and-forget pattern: `.catch(err => logger.error(...))` — never await, never throw
  - etiqueta-service.ts is idempotent (checks etiqueta_status === 'impresso' before proceeding)
---
