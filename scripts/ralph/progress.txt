# Ralph Progress Log
Started: Wed Apr  1 16:59:27 -03 2026
---

## Codebase Patterns
- STATUS_SEPARACAO_COLORS, STATUS_SEPARACAO_LABELS, and filter OPTIONS arrays exist in BOTH `pedidos/page.tsx` AND `pedidos/[id]/page.tsx` — update both when adding a new status
- COMPRA_STATUS_COLORS and COMPRA_STATUS_LABELS also exist in `pedidos/[id]/page.tsx` — update when adding a new compra_status
- COMPRA_STATUS_MAP in `separacao-card.tsx` is the separacao module's version of compra status display
- SeparacaoCounts initializers exist in 3 places: `api/separacao/route.ts` (2x: emptyCounts + counts), `separacao/page.tsx` (EMPTY_COUNTS)
- COUNT_STATUSES in `api/separacao/route.ts` maps by array index to countResults — adding a new status requires updating both the array and the index-based counts builder
- Amber color scheme for "pending validation" states: `bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-300`
- iniciar/route.ts has a BLINDAGEM + DB guard (.eq/.in on status_separacao) — when allowing new statuses, update ALLOWED_STATUSES, toStart filter, AND the DB update guard
- cancelar/route.ts classifies pedidos into 3 return statuses by compra_status priority: oc_pendente → validacao_oc, other compra → aguardando_compra, else → aguardando_separacao
- voltar-etapa/route.ts STATUS_ORDER array determines valid admin move targets — add new statuses in flow order
- OC creation pattern (find-or-create siso_ordens_compra by fornecedor + galpao): used in produto-esgotado/route.ts and validar-oc-item/route.ts — always match by fornecedor + galpao + status='aguardando_compra'
- EventoPedido type in historico-service.ts must include new event types before they can be used in registrarEvento calls
- TAB_STATUS_MAP in separacao/page.tsx maps tab ID → status_separacao values fetched — use this for badge counts, API params, and display counts
- api/separacao/route.ts supports comma-separated status_separacao param via statusFilters array — use .in() for multiple, .eq() for single
- checklist/page.tsx uses renderProductRow shared renderer — OC vs normal behavior is determined by product.is_oc + ocEsgotadoItems Set + ocValidarLoading state
- Optimistic updates for OC items in checklist: keep compra_status='oc_pendente' in cache, track esgotado in local Set — refetch after auto-transition clears both

---

## 2026-04-01 - US-001
- Added `oc_pendente` to CompraStatus union type
- Added `validacao_oc` to StatusSeparacao union type and SeparacaoCounts interface
- Added validacao_oc to STATUS_SEPARACAO_COLORS/LABELS in pedidos/page.tsx and pedidos/[id]/page.tsx
- Added validacao_oc to STATUS_SEPARACAO_OPTIONS filter array in pedidos/page.tsx
- Added oc_pendente to COMPRA_STATUS_MAP in separacao-card.tsx
- Added oc_pendente to COMPRA_STATUS_COLORS/LABELS in pedidos/[id]/page.tsx
- Fixed SeparacaoCounts initializers in api/separacao/route.ts and separacao/page.tsx
- Files changed: src/types/index.ts, src/app/pedidos/page.tsx, src/app/pedidos/[id]/page.tsx, src/components/separacao/separacao-card.tsx, src/app/api/separacao/route.ts, src/app/separacao/page.tsx
- **Learnings for future iterations:**
  - Adding a field to SeparacaoCounts breaks 3 places (api/separacao/route.ts x2, separacao/page.tsx) — fix all in same commit
  - The counts builder in api/separacao/route.ts uses index-based mapping from countResults — for US-001 I added validacao_oc: 0 as placeholder; US-002 must wire it into COUNT_STATUSES properly
  - pedidos/[id]/page.tsx has both separacao AND compra status maps — both may need updating
---

## 2026-04-01 - US-002
- Added 'validacao_oc' to VALID_STATUSES and COUNT_STATUSES arrays in api/separacao/route.ts
- Wired validacao_oc count to countResults[2] (inserted at position 2, shifted subsequent indices)
- Added oc_pendente counter to compraStats type, init object, and if/else chain
- Added 'validacao_oc' to dashboard/counts separacao .in() query
- Files changed: src/app/api/separacao/route.ts, src/app/api/dashboard/counts/route.ts
- **Learnings for future iterations:**
  - COUNT_STATUSES uses index-based mapping — inserting a new status shifts ALL subsequent indices in the counts builder. Must update both arrays AND the counts object simultaneously.
  - emptyCounts was already updated in US-001 with validacao_oc: 0 — only needed to wire it to countResults.
  - dashboard/counts/route.ts has separate queries for separacao vs compras badges — validacao_oc goes in separacao only (it's a separation-phase status, not a compras status).
---

## 2026-04-01 - US-003
- Added 'validacao_oc' to ALLOWED_STATUSES in iniciar/route.ts
- Added validacao_oc to toStart filter (|| p.status_separacao === 'validacao_oc')
- Changed DB update guard from .eq() to .in() for ['aguardando_separacao', 'validacao_oc']
- Updated error message to mention validacao_oc
- Files changed: src/app/api/separacao/iniciar/route.ts
- **Learnings for future iterations:**
  - iniciar/route.ts has a BLINDAGEM that blocks pedidos with pending compra items (aguardando_compra, comprado) from transitioning. oc_pendente is NOT in that blocklist, which is correct — validacao_oc pedidos need to start separation to validate items.
  - The DB update uses a guard .eq/.in on status_separacao to prevent race conditions — always update the guard when adding new allowed statuses.
---

## 2026-04-01 - US-004
- Expanded concluir/route.ts items select to include compra_status
- Added oc_pendente guard: returns 400 if any item still has compra_status = 'oc_pendente'
- Modified completude check: items with compra_status in ['aguardando_compra', 'comprado'] are excluded from separacao_marcado verification
- Added aguardando_compra transition branch: when all normal items are marcado but compra items exist, pedido transitions to 'aguardando_compra' (not 'separado'). Does NOT set separacao_concluida_em or reset pick state.
- Added 'separacao_aguardando_compra' to EventoPedido type in historico-service.ts
- Response now includes aguardandoCompra array alongside separados and pendentes
- Files changed: src/app/api/separacao/concluir/route.ts, src/lib/historico-service.ts
- **Learnings for future iterations:**
  - concluir/route.ts groups items by pedido and checks completeness — the grouping map type needed expanding to carry compra_status alongside separacao_marcado
  - EventoPedido in historico-service.ts is a string union — add new event types there before using them with registrarEventos
  - The aguardando_compra branch deliberately does NOT set separacao_concluida_em or reset any separacao fields — this preserves the pick state so when compra items arrive, the operator doesn't need to re-pick normal items
  - The oc_pendente guard applies to ALL pedido_ids in the batch — one unresolved pedido blocks the entire request (early return)
---

## 2026-04-01 - US-005
- Modified cancelar/route.ts: added 3-way return status logic (oc_pendente → validacao_oc, other compra → aguardando_compra, else → aguardando_separacao). Queries compra_status values per pedido and uses oc_pendente as highest priority.
- Modified voltar-etapa/route.ts: added 'validacao_oc' to STATUS_ORDER between aguardando_nf and aguardando_separacao.
- Files changed: src/app/api/separacao/cancelar/route.ts, src/app/api/separacao/voltar-etapa/route.ts
- **Learnings for future iterations:**
  - cancelar/route.ts queries compra_status to decide return status — when adding new compra_status values that affect routing, update the classification logic here
  - voltar-etapa STATUS_ORDER determines valid transition targets AND which pedidos can be moved — inserting validacao_oc enables admin to move pedidos to/from this status
  - cancelar still resets separacao_marcado for ALL items including OC items — compra_status is NOT reset, so OC items retain their oc_pendente/aguardando_compra state after cancel
---

## 2026-04-01 - US-006
- Created POST /api/separacao/validar-oc-item endpoint with encontrei/esgotado actions
- encontrei: clears compra fields + marks as picked (separacao_marcado=true, bipado_completo=true)
- esgotado: sets aguardando_compra + auto-creates/links OC (reuses produto-esgotado OC pattern)
- Auto-transitions: FR-9 (all found → propria + aguardando_separacao), FR-8 (100% OC → aguardando_compra)
- Added oc_item_encontrado and oc_item_confirmado to EventoPedido type
- Updated docs/api-reference-complete.md and CLAUDE.md project structure
- Files changed: src/app/api/separacao/validar-oc-item/route.ts, src/lib/historico-service.ts, docs/api-reference-complete.md, CLAUDE.md
- **Learnings for future iterations:**
  - The OC creation pattern (find-or-create by fornecedor + galpao) is reused from produto-esgotado/route.ts — extracted as helper function linkItemToOC
  - EventoPedido in historico-service.ts must be updated BEFORE using new event types in route handlers
  - validar-oc-item uses getSessionUser for auth — same pattern as bipar/marcar-item endpoints
  - FR-8 auto-transition clears separacao_operador_id and separacao_iniciada_em but does NOT set separacao_concluida_em — consistent with concluir aguardando_compra branch
---

## 2026-04-01 - US-007
- Decoupled VisibleSeparacaoTab from StatusSeparacao — explicit union excluding validacao_oc
- Added TAB_STATUS_MAP to map tabs to their fetched statuses (aguardando_compra → [aguardando_compra, validacao_oc])
- Tab badge counts now sum all statuses in TAB_STATUS_MAP[tab]
- API route.ts now supports comma-separated status_separacao values (.in() for multiple)
- Added MOVE_TARGETS for aguardando_compra tab and validacao_oc as target in relevant entries
- Updated docs/api-reference-complete.md with comma-separated param support
- Files changed: src/app/separacao/page.tsx, src/app/api/separacao/route.ts, docs/api-reference-complete.md
- **Learnings for future iterations:**
  - TAB_STATUS_MAP is the single source of truth for tab→status mapping — use it for badge counts, fetch params, and "Na fila" display
  - api/separacao/route.ts now supports comma-separated status_separacao — use statusFilters array (not statusFilter string) when extending
  - MOVE_TARGETS is keyed by VisibleSeparacaoTab (not StatusSeparacao) — validacao_oc appears as a target option, not a key
  - The "Na fila" count uses TAB_STATUS_MAP[activeTab].reduce() to sum correctly for combined tabs
---

## 2026-04-01 - US-008
- Added `oc_pendente` to CompraStatsData interface
- Expanded isAguardandoOC to include validacao_oc (both show in Aguardando OC tab)
- Split statusBadge: validacao_oc shows "OC Pendente" (amber), aguardando_compra shows "OC Confirmada" (red)
- isEngatilhado now only applies to actual aguardando_compra (not validacao_oc)
- Added oc_pendente counter ("pendente validação") to compra progress summary
- Added oc_pendente segment (amber-300) to compra progress bar
- Files changed: src/components/separacao/separacao-card.tsx
- **Learnings for future iterations:**
  - isAguardandoOC is used for card borders and compra stats visibility — expanding it automatically shows compra progress for validacao_oc cards too
  - isEngatilhado (NF + agrupamento ready) must NOT include validacao_oc — those pedidos haven't confirmed their OC items yet
  - CompraStatsData interface in separacao-card.tsx must match the API response shape from api/separacao/route.ts compraStats builder
  - Manual browser verification needed for visual changes (OC Pendente amber tag, OC Confirmada red tag, progress bar oc_pendente segment)
---

## 2026-04-01 - US-009
- Split checklist item consolidation into OC (compra_status='oc_pendente') and normal groups using composite key (`${produto_id}_${isOc ? 'oc' : 'normal'}`)
- Added `is_oc: boolean` to ConsolidatedProduct interface
- Created `renderProductRow` shared renderer with conditional OC logic (click=encontrei, esgotado button=esgotado, grey styling for esgotado items, loading spinner)
- Amber-bordered "Conferência OC" section at top with ShoppingCart icon and pending counter
- OC encontrei: optimistic update keeps compra_status='oc_pendente' so item stays in OC section (green)
- OC esgotado: tracked in local `ocEsgotadoItems` Set (grey, X checkbox, disabled)
- Auto-transition handling: redirects to /separacao if all pedidos transitioned, toast for partial
- Barcode scanner optimistic update excludes OC items (compra_status !== 'oc_pendente')
- Updated handleConcluir to parse aguardandoCompra response + smart redirect
- Progress bar counts OC resolved items (encontrei OR esgotado) alongside normal marcado items
- Files changed: src/app/separacao/checklist/page.tsx
- **Learnings for future iterations:**
  - renderProductRow is a const arrow function defined inside ChecklistPage — it accesses component state/handlers via closure. Both OC and normal sections call it.
  - OC items stay in their section during optimistic updates by NOT changing compra_status in the cache — only separacao_marcado (encontrei) or local Set (esgotado) is updated. The refetch after auto-transition naturally moves items.
  - The consolidated useMemo still produces ALL items (oc+normal) — the split happens in a separate useMemo. This preserves the sort logic working across all items.
  - For large JSX section replacements (250+ lines), the Edit tool's exact-match requirement makes it impractical — Python via Bash is more reliable for such cases.
  - handleConcluir now handles aguardandoCompra in the response (added in US-004) — redirects to aguardando_compra tab when all pedidos went to compras.
---
