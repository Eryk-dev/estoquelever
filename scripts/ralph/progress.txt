# Ralph Progress Log
Started: Wed Mar 11 16:48:51 -03 2026
---

## Codebase Patterns
- Always use `IF NOT EXISTS` for ADD COLUMN and CREATE INDEX in migrations
- Migration files follow naming: `YYYYMMDD_description.sql` in `supabase/migrations/`
- Supabase project ID: `wrbrbhuhsaaupqsimkqz`
- CHECK constraints on siso_pedidos use named constraints (e.g., `siso_pedidos_status_separacao_check`)
- Old status_separacao constraint was last updated in `20260311_modulo_compras.sql` to include `aguardando_compra`
- Backfill migrations should handle NULL values explicitly (e.g., `status=concluido AND status_separacao IS NULL`)
- Cancellation logic is in `src/app/api/webhook/tiny/route.ts`, NOT in `webhook-processor.ts`
- For dual-write: search `.update()` and `.upsert()` on `siso_pedidos` for `status` or `status_separacao` fields
- execution-worker generic success sets status=concluido for ALL decisoes; status_unificado varies by decisao (aguardando_nf for propria/transferencia, aguardando_compra for oc)
- `bipar-embalagem` uses PL/pgSQL RPC `siso_processar_bip_embalagem` — dual-write for that path requires a DB function update, not TypeScript
- `expedir/route.ts` sets `expedido` which is NOT in StatusUnificado CHECK constraint — needs separate handling when expedido is added to the unified model

## 2026-03-11 - US-001
- Created migration `supabase/migrations/20260311_status_unificado.sql`
- Added `status_unificado` column with CHECK constraint (10 valid values) and DEFAULT 'pendente'
- Backfilled all existing rows mapping status+status_separacao → status_unificado
- Created index `idx_siso_pedidos_status_unificado`
- Applied migration via Supabase MCP successfully
- Typecheck passes
- Files changed: `supabase/migrations/20260311_status_unificado.sql`
- **Learnings for future iterations:**
  - The inline CHECK constraint syntax `ADD COLUMN ... CHECK (...)` works without needing a separate `ADD CONSTRAINT` statement
  - `status=concluido` with `status_separacao IS NULL` defaults to `aguardando_separacao` per PRD notes
  - Old `status` and `status_separacao` columns are kept deprecated — do NOT delete them
---

## 2026-03-11 - US-002
- Added `StatusUnificado` type (10 values) to `src/types/index.ts`
- Added `GalpaoEstoque` interface with dynamic galpao stock fields
- Added optional `statusUnificado?: StatusUnificado` to `Pedido` interface
- Kept all existing types untouched (StatusPedido, StatusSeparacao, EstoqueItem legacy fields)
- Files changed: `src/types/index.ts`
- **Learnings for future iterations:**
  - Types are all centralized in `src/types/index.ts` — no barrel exports, import directly
  - New types are additive-only during transition — mark optional with `?` so existing code doesn't break
  - `GalpaoEstoque` uses `string | undefined` for localizacao (not `string | null`) matching existing EstoqueItem pattern
---

## 2026-03-11 - US-003
- Added `status_unificado` dual-write to webhook-processor.ts and route.ts
- 3 write locations updated:
  1. `webhook-processor.ts` main upsert: `status_unificado: isAuto ? "executando" : "pendente"`
  2. `webhook-processor.ts` NF reconciliation: `status_unificado: "aguardando_separacao"`
  3. `route.ts` cancellation handler: `status_unificado: "cancelado"`
- Files changed: `src/lib/webhook-processor.ts`, `src/app/api/webhook/tiny/route.ts`
- **Learnings for future iterations:**
  - Cancellation logic is in `route.ts` NOT in `webhook-processor.ts` — the processor handles creation, the route handles cancellation
  - The NF reconciliation (race condition handler) in webhook-processor.ts sets `status_separacao: "pendente"` which is NOT a valid StatusSeparacao type — this is a pre-existing issue
  - For dual-write stories (US-003 through US-008), search for ALL `.update()` and `.upsert()` calls on `siso_pedidos` that touch `status` or `status_separacao`
---

## 2026-03-11 - US-004
- Added `status_unificado` dual-write to execution-worker.ts
- 3 write locations updated:
  1. Generic success handler: `status_unificado` based on decisao — propria/transferencia → "aguardando_nf", oc → "aguardando_compra"
  2. Max retries error handler: `status_unificado: "erro"`
  3. OC flow (`executarMarcadoresOnly`): `status_unificado: "aguardando_compra"` alongside `status_separacao`
- Files changed: `src/lib/execution-worker.ts`
- **Learnings for future iterations:**
  - The worker does NOT set status=executando on siso_pedidos — that's done by /api/pedidos/aprovar (US-005)
  - The generic success handler (after executeJob) sets status=concluido for ALL decisoes, but status_unificado differs by decisao
  - For propria/transferencia, NF is generated but not yet authorized → status_unificado = "aguardando_nf"
  - The executarMarcadoresOnly (OC) function sets status_separacao on siso_pedidos before the generic handler sets status=concluido
---

## 2026-03-11 - US-005
- Added `status_unificado: "executando"` to the approval update query in `/api/pedidos/aprovar/route.ts`
- 1 write location updated: the `.update()` call at line ~133 that sets `status: "executando"`
- Files changed: `src/app/api/pedidos/aprovar/route.ts`
- **Learnings for future iterations:**
  - The approval route is the ONLY place that sets status=executando on siso_pedidos (worker does NOT do this)
  - The approval route also sets decisao_final, operador info, tipo_resolucao, marcadores, and separacao_galpao_id in the same update
  - There's only ONE update to siso_pedidos in this route — simple dual-write
---

## 2026-03-11 - US-006
- Added `status_unificado` dual-write to all separacao API routes that update `status_separacao` on `siso_pedidos`
- 9 files updated, 10 write locations:
  1. `cancelar/route.ts`: status_unificado = "aguardando_separacao"
  2. `iniciar/route.ts`: status_unificado = "em_separacao"
  3. `concluir/route.ts`: status_unificado = "separado"
  4. `forcar-pendente/route.ts` (batch): status_unificado = "aguardando_separacao"
  5. `[pedidoId]/forcar-pendente/route.ts`: status_unificado = "aguardando_separacao"
  6. `voltar-etapa/route.ts`: status_unificado = novoStatus (dynamic)
  7. `produto-esgotado/route.ts`: status_unificado = "aguardando_compra"
  8. `desfazer-bip/route.ts` (2 locations): em_separacao or aguardando_separacao
  9. `confirmar-item-embalagem/route.ts`: status_unificado = "embalado"
- Files changed: `src/app/api/separacao/{cancelar,iniciar,concluir,forcar-pendente,[pedidoId]/forcar-pendente,voltar-etapa,produto-esgotado,desfazer-bip,confirmar-item-embalagem}/route.ts`
- **Learnings for future iterations:**
  - `expedir/route.ts` sets status_separacao="expedido" which is NOT in StatusUnificado — skipped (expedido not yet in the unified model)
  - `bipar-embalagem/route.ts` uses PL/pgSQL RPC `siso_processar_bip_embalagem` for atomic updates — dual-write needs a DB migration to update the function, not TypeScript
  - `reiniciar/route.ts` does NOT update status_separacao on siso_pedidos — it only resets item-level fields
  - `voltar-etapa/route.ts` status values come from StatusSeparacao which are all valid StatusUnificado values (aguardando_nf, aguardando_separacao, em_separacao, separado, embalado)
  - `desfazer-bip/route.ts` has conditional status writes (embalado→em_separacao or em_separacao→aguardando_separacao depending on context)
---

## 2026-03-11 - US-007
- Added `status_unificado: "executando"` dual-write to `compras-release.ts` release update
- Only 1 write location: the `.update()` call in `checkAndReleasePedidos` that sets status="executando" + status_separacao="aguardando_nf"
- No compras API routes (`src/app/api/compras/`) directly update status_separacao on siso_pedidos — they only update siso_pedido_itens and siso_ordens_compra
- Files changed: `src/lib/compras-release.ts`
- **Learnings for future iterations:**
  - compras-release sets status="executando" (not concluido) because the pedido re-enters the execution queue — the worker will later set it to concluido+aguardando_nf
  - All compras API routes (conferir, ordens, itens actions) operate on siso_pedido_itens and siso_ordens_compra — only compras-release.ts touches siso_pedidos status
---

## 2026-03-11 - US-008
- Added `status_unificado: "aguardando_separacao"` dual-write to nf-webhook-handler.ts
- 1 write location: Step 5 transition `aguardando_nf → aguardando_separacao` (line 140)
- Files changed: `src/lib/nf-webhook-handler.ts`
- **Learnings for future iterations:**
  - nf-webhook-handler.ts has only ONE status_separacao write on siso_pedidos — the aguardando_nf → aguardando_separacao transition
  - The update is idempotent via `.eq("status_separacao", "aguardando_nf")` WHERE clause — only transitions pedidos currently in aguardando_nf
  - This completes all dual-write stories (US-003 through US-008) — every status/status_separacao write now also writes status_unificado
---

## 2026-03-11 - US-009
- Added `statusUnificado: p.status_unificado ?? undefined` to response mapping in `/api/pedidos/route.ts`
- Added `?status_unificado=` query param support — when present, filters on `status_unificado` column instead of `status`
- Old `?status=` filter still works for backwards compatibility (status_unificado takes precedence if both provided)
- Files changed: `src/app/api/pedidos/route.ts`
- **Learnings for future iterations:**
  - The pedidos API returns a flat array (not `{ data }` wrapper) — just `NextResponse.json(result)`
  - Response mapping is in-line in the `.map()` callback — add new fields there
  - `status_unificado` filter takes precedence over `status` when both are provided (else-if chain)
---

## 2026-03-11 - US-010
- US-010 was already implemented in a prior iteration (code in route.ts already fetches siso_pedido_item_estoques, joins with empresas/galpoes, aggregates by galpao, returns estoquesPorGalpao)
- Verified typecheck passes, marked as passes: true
- Files changed: none (code already present)
- **Learnings for future iterations:**
  - The stock aggregation uses a Map<string, Map<string, GalpaoStockAgg>> keyed by `${pedido_id}::${produto_id}` → galpaoId
  - `atende` boolean is calculated server-side as `disponivel >= quantidadePedida`
  - Legacy CWB/SP fields are kept alongside the new estoquesPorGalpao array
---

## 2026-03-11 - US-011
- Added `estoquesPorGalpao?: GalpaoEstoque[]` to `EstoqueItem` interface in `src/types/index.ts`
- Field is optional (?) to maintain backwards compatibility during transition
- Files changed: `src/types/index.ts`
- **Learnings for future iterations:**
  - GalpaoEstoque interface already existed (added in US-002), just needed to be referenced in EstoqueItem
  - Keep legacy fields (estoqueCWB, estoqueSP, cwbAtende, spAtende) until frontend fully migrates to dynamic rendering
---

## 2026-03-11 - US-012
- PedidoCard now renders dynamic stock pills from `estoquesPorGalpao` when available
- EditableStockPill accepts `galpao: string` + `galpaoId?: string` instead of hardcoded `"CWB" | "SP"`
- ProductRow uses `hasDynamic` flag to branch between dynamic and legacy rendering
- Location resolution uses dynamic galpao data (matching by `galpaoNome === filialOrigem`)
- OC locations render all galpao locations dynamically
- Legacy CWB/SP fallback preserved for old data without estoquesPorGalpao
- Files changed: `src/components/pedido/pedido-card.tsx`
- **Learnings for future iterations:**
  - `filialOrigem` is the galpao name (e.g., "CWB", "SP") — can match against `galpaoNome` in estoquesPorGalpao
  - `DepositoEstoque` can be constructed from `GalpaoEstoque` for the EditableStockPill (id=depositoId, nome=depositoNome)
  - The `galpaoId` is passed to the stock adjustment API body for forward compat (US-014 will make the API use it)
  - `isRelevant` for stock pills: propria = origin galpao, transferencia = non-origin, oc = all relevant
---

## 2026-03-11 - US-013
- `decisaoIsAvailable()` now checks estoquesPorGalpao dynamically: propria = origin galpao atende ALL items, transferencia = any non-origin galpao atende ALL items
- Replaced `DECISAO_OPTIONS` (with label functions) with `DECISAO_VALUES` array + `getDecisaoLabel()` function
- `getDecisaoLabel()` for transferencia shows actual target galpao name from dynamic data (e.g., "Transferência SP")
- `getBestAlternativeGalpao()` helper extracts non-origin galpao names from estoquesPorGalpao
- Legacy CWB/SP fallback preserved in both `decisaoIsAvailable` and `getDecisaoLabel`
- No `as "CWB" | "SP"` casts in the new code path
- Files changed: `src/components/pedido/pedido-card.tsx`
- **Learnings for future iterations:**
  - The decision availability for transferencia checks ALL non-origin galpoes — if ANY single galpao atende ALL items, transferencia is available
  - `DECISAO_VALUES` is just a plain array of Decisao values; labels are computed by `getDecisaoLabel(decisao, pedido)`
  - `getDecisaoOption()` was removed; `ActionRow` calls `getDecisaoLabel(decisao, pedido)` directly
---

## 2026-03-11 - US-014
- Stock adjustment endpoint now accepts `galpaoId` (UUID) in addition to `galpao` (name string)
- When `galpaoId` provided: looks up empresa by `galpao_id`, resolves deposit from `siso_pedido_item_estoques`
- When only `galpao` provided: backwards compat — looks up by galpao name (existing behavior)
- `resolvedGalpaoName` used throughout instead of raw `galpao` param
- Legacy DB update (CWB/SP columns) only runs when `resolvedGalpaoName` is "CWB" or "SP"
- Normalized table (`siso_pedido_item_estoques`) always updated regardless of galpao name
- Files changed: `src/app/api/tiny/stock/ajustar/route.ts`
- **Learnings for future iterations:**
  - The endpoint uses `produto_id_suporte` for non-origin galpao Tiny lookups — each empresa may have a different product ID in Tiny
  - Deposit ID lookup: `siso_pedido_item_estoques` filtered by `empresa_id` gives the right deposit for that empresa
  - For new galpoes beyond CWB/SP, only the normalized table update runs (no legacy columns exist)
---

## 2026-03-11 - US-015
- SISO dashboard now fetches with `?status_unificado=` instead of no filter
- Pendente tab: filters by `statusUnificado === "pendente"`
- Concluidos tab: `tipoResolucao === "manual"` with `statusUnificado` in [aguardando_compra, aguardando_nf, aguardando_separacao, em_separacao, separado, embalado]
- Auto tab: `tipoResolucao === "auto"` regardless of status
- Files changed: `src/app/siso/page.tsx`
- **Learnings for future iterations:**
  - SISO fetches all non-cancelled statuses in one API call, then splits client-side
  - The `filtrarPendentes/Concluidos/Auto` functions in `filtrar-pedidos.ts` handle role-based filtering after the status split
  - Concluidos tab uses "manual" type resolution to show operator-approved orders (not auto)
---
