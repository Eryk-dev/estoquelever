// Separação futura usa A MESMA tela de /wms/separacao (familiaridade da equipe).
// O componente detecta a rota via usePathname e ajusta: filtra
// separacao_futura=true, leva futura=1 pro checklist, e para em `separado`
// (sem tabs aguardando_nf/embalado/conferido nem ação de embalar).
export { default } from "../separacao/page";
