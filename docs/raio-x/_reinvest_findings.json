[
 {
  "id": "P006",
  "current_status": "open",
  "status_evidence": "src/lib/historico-service.ts:60 declara evento 'erro' no type EventoPedido, mas grep 'evento: \"erro\"' em src/ só acha src/lib/historico-service.test.ts:29 — NENHUMA rota de separação registra 'erro'. marcar-item/route.ts retorna 409/500 (linhas 175-178, 232-234, 329-330, 343) sem registrarEvento; parcial/route.ts idem nos returns de erro. Quando a baixa atômica falha (pickItemAtomico throw) só há logger.warn (marcar-item:169) — nada no histórico do pedido.",
  "root_cause": "As rotas de separação (marcar-item, parcial, concluir) tratam falha retornando NextResponse de erro + logger.warn/logError em siso_logs, mas nunca chamam registrarEvento({evento:'erro'}) no histórico do pedido (siso_pedido_historico). Operador no detalhe do pedido não vê a causa (qual loc/qty/produto travou).",
  "change_sites": [
   "src/app/api/wms/separacao/marcar-item/route.ts:167-178 — no catch do pickItemAtomico (falha_baixa_estoque), antes do return 409, chamar registrarEvento({pedidoId, evento:'erro', detalhes:{etapa:'marcar-item', item_id, sku, produto_id, loc_id:locId, qty:qtyADescontar, erro:msg}})",
   "src/app/api/wms/separacao/marcar-item/route.ts:124-133 — no return 'sem_saldo_para_baixa' registrar 'erro' com loc/produto",
   "src/app/api/wms/separacao/parcial/route.ts:272-286 — no return 'posicao_reservada' registrar 'erro' com saldo/reservado/disponivel/loc",
   "src/app/api/wms/separacao/parcial/route.ts:1162-1172 — no catch geral registrar 'erro' com erro_id antes do return 500"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/82-marcar-item-falha-registra-erro-historico.ts",
  "red_test": "Dado pedido em em_separacao cujo item não tem saldo na loc/reserva (força sem_saldo_para_baixa ou posicao_reservada), ao POST /api/wms/separacao/marcar-item espera 409 E que GET /api/wms/pedidos/[id]/historico passe a conter 1 evento evento='erro' cujo detalhes inclui item/sku/loc/causa. Hoje o histórico não recebe nenhum evento 'erro'.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "A nota é 'registrar automaticamente todos os erros de separação no histórico'. Infra pronta: registrarEvento é fire-and-forget safe e o type já tem 'erro'. Mudança cirúrgica: inserir registrarEvento nos pontos de erro das rotas. Não muda fluxo, só observabilidade."
 },
 {
  "id": "P007",
  "current_status": "partially_fixed",
  "status_evidence": "src/lib/wms/vendas-cancelamento.ts:61-65 HOJE bloqueia: status_separacao ∈ {em_separacao, separado, embalado} → throw 'pedido em separação ativa — use voltar-etapa antes de cancelar'. Isso é a OPÇÃO 1 (bloquear), mas a NOTA escolheu OPÇÃO 2 (cancelar só o não-pego + devolver pego manualmente). O 'today' do raio-x (libera os dois itens e marca cancelado) já não corresponde ao código atual. Único caller: src/app/api/wms/vendas/[id]/cancelar/route.ts (vendas manuais MAN-*).",
  "root_cause": "O comportamento atual (block total quando há separação ativa) preserva auditoria mas não implementa a decisão da nota: cancelar parcialmente liberando a reserva só dos itens NÃO pegos, deixando os itens já picados (mov_saida) para devolução manual. Hoje é tudo-ou-nada: ou bloqueia, ou (em aguardando_separacao) libera todas as reservas.",
  "change_sites": [
   "src/lib/wms/vendas-cancelamento.ts:60-65 — substituir o throw de 'separação ativa' por um caminho que, em em_separacao, identifica itens com mov_saida (já picados) vs itens sem pick; libera reserva (liberarReserva) só dos não-picados, marca pedido cancelado, e retorna lista de itens-pegos pendentes de devolução manual (sem estornar a S — operador refaz entrada)",
   "src/lib/wms/vendas-cancelamento.ts:70-89 — generalizar o caminho 1 (hoje só aguardando_separacao/aguardando_compra) pra também tratar em_separacao parcial",
   "src/app/api/wms/vendas/[id]/cancelar/route.ts:48-51 — ajustar classificação de erro client (remover 'separação ativa' como 400 se deixar de bloquear) e propagar payload {itens_para_devolver_manual}"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "reserva_wiring_cancel",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/83-cancelar-pedido-em-separacao-parcial.ts",
  "red_test": "Pedido com 2 itens em em_separacao, item1 já picado (mov_saida_id != null), item2 sem pick. POST cancelar: espera que a reserva R do item2 seja liberada (reservado baixa) E que item1 NÃO seja estornado (mov S permanece, auditoria preservada) E que o response liste item1 como 'pendente_devolucao_manual'. Hoje o endpoint retorna 400 bloqueando tudo.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "AMBIGUIDADE NOTA×CÓDIGO: o código atual já adota a OPÇÃO 1 (bloquear) que NÃO é a decisão. A nota pede OPÇÃO 2 (cancelar só o não-pego + devolver pego manual). cancelarVendaManual só é chamado por vendas manuais (MAN-*) hoje; não há rota equivalente pra pedidos de marketplace — para estes o cancelamento real passa por /pedidos/[id]/estornar e /separacao/cancelar (voltar etapa). Vale confirmar com o user se o escopo é a venda manual ou estender ao marketplace. Por ser questão de fluxo de negócio (devolução manual) o esforço sobe."
 },
 {
  "id": "P012",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/iniciar/route.ts: passo 2 (linhas 141-161) faz UPDATE status_separacao='em_separacao' ANTES do passo 3 (linhas 167-182) que chama a RPC siso_consolidar_produtos_separacao. Se a RPC falhar (rpcError), o comentário linha 177 admite 'Orders are already updated' e retorna 500 — pedido fica em em_separacao sem checklist. Re-tentar cai no ALLOWED_STATUSES (linha 100-105 inclui 'em_separacao' pra resume) mas toStart (133-139) só pega aguardando_separacao/validacao_oc → não re-roda o update, e a consolidação volta a falhar.",
  "root_cause": "Ordem invertida: a transição de estado (em_separacao) acontece antes da consolidação (RPC). A consolidação não é pré-condição da escrita de estado, então uma falha na RPC deixa o pedido órfão em em_separacao sem checklist, exigindo admin pra reverter.",
  "change_sites": [
   "src/app/api/wms/separacao/iniciar/route.ts:120-182 — reordenar: rodar a RPC siso_consolidar_produtos_separacao (passo 3) PRIMEIRO; só se retornar sem erro, executar o UPDATE de status_separacao='em_separacao' (passo 2). Se RPC falhar, retornar 500/erro SEM ter mudado nenhum estado (idempotente em retry).",
   "src/app/api/wms/separacao/iniciar/route.ts:123-139 — mover a checagem de pendingCompra pra antes do update, mantida"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/84-iniciar-consolida-antes-de-marcar.ts",
  "red_test": "Forçar siso_consolidar_produtos_separacao a falhar (ex.: pedido_ids contendo um id que dispara erro na RPC) e POST /separacao/iniciar: espera 500 E que GET status do pedido continue 'aguardando_separacao' (NÃO em_separacao). Hoje o pedido fica em_separacao mesmo com a RPC falhando.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = 'fazer a consolidação ANTES de marcar como em separação'. É exatamente reordenar passo 3 antes do passo 2. Risco baixo: a consolidação é read-only (RPC de leitura) e o update é o único write. Sem necessidade de transação — só ordem."
 },
 {
  "id": "P013",
  "current_status": "partially_fixed",
  "status_evidence": "src/app/api/wms/separacao/marcar-item/route.ts:140-179 a baixa do pick já é ATÔMICA (pickItemAtomico → RPC wms_pick_item_atomico, migration 20260528). Se a S falhar, rollback total (L+S). MAS o cenário da nota é frontend: rede cai DEPOIS do servidor commitar a baixa, antes do cliente receber a resposta. O painel mostra item não-marcado, re-tentar dá 409 ('reserva já liberada — pick já realizado', RPC linha 60-63). Não há mecanismo de re-sincronização explícito além do realtime hook genérico.",
  "root_cause": "Não é bug de atomicidade no servidor (já resolvido pela RPC). É UX de reconciliação: quando a resposta se perde, o estado do servidor (item picado) diverge da tela (não marcado). A nota escolheu 'recarregar tela e mostrar estado real' — ou seja, garantir que o painel re-leia o estado pós-reconexão e reflita separacao_marcado=true; e que o 409 de re-tentativa seja tratado como 'já marcado' (sucesso idempotente) em vez de erro.",
  "change_sites": [
   "src/hooks/* (hook realtime de separação que escuta postgres_changes em siso_pedido_itens) — garantir invalidação da query do checklist ao voltar online (window 'online'/visibilitychange) pra refletir separacao_marcado real",
   "src/components/wms/separacao/* (componente do checklist/marcar-item) — ao receber 409 'falha_baixa_estoque' cuja causa é 'reserva já liberada/pick já realizado', tratar como item-já-marcado (refetch + marcar visualmente) em vez de toast de erro",
   "src/app/api/wms/separacao/marcar-item/route.ts:140-179 — opcional: distinguir o erro de 'pick já realizado' (idempotente → 200 com estado atual) do erro de saldo real (409), pra o front saber reconciliar"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/85-marcar-item-idempotente-pos-reconexao.ts",
  "red_test": "Após um marcar-item bem-sucedido (item picado, R liberada), um SEGUNDO POST /separacao/marcar-item {marcado:true} pro mesmo item deve ser tratado idempotentemente: retornar o estado 'já marcado' (200) em vez de 409 que confunde o operador. Hoje a 2ª tentativa cai no 409 'reserva já liberada'.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "A atomicidade do servidor já foi resolvida (RPC wms_pick_item_atomico, commit 93dea14 'Fase 1 baixa atômica'). O que a nota pede é UX de reconexão — predominantemente frontend + tornar o re-POST idempotente. Diferente de P019 (onde o caminho /parcial NÃO é atômico)."
 },
 {
  "id": "P014",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/marcar-item/route.ts:236-332 (desmarcar): itera sortedLinks chamando estornarMovimentacao/estornarLiberacaoReserva um a um em loop (linhas 258-293); cada falha é só logger.warn e o loop CONTINUA (linha 278-292). Após o loop, sempre deleta os links (294-301) e seta separacao_marcado=false (317-325) INDEPENDENTE de quantos estornos falharam. Resultado: estorno parcial (ex.: S estornada, L falhou) deixa estado meia-boca, mas o item é marcado como desmarcado e os links são apagados → impossível re-tentar o estorno faltante.",
  "root_cause": "O desfazimento (estorno de S+L pareados) não é tudo-ou-nada: são N chamadas RPC independentes em loop JS, falhas são engolidas (warn+continue), e o update final do item + delete dos links roda sempre. Não há transação cobrindo o conjunto de estornos.",
  "change_sites": [
   "src/app/api/wms/separacao/marcar-item/route.ts:236-332 — envolver o desfazimento numa RPC plpgsql única (atômica) que: estorna a S, estorna a L (recria R), deleta os links e seta separacao_marcado=false/quantidade_pega=null/mov_saida_id=null. Se qualquer passo falhar, RAISE → rollback total, item permanece marcado intacto.",
   "supabase/migrations/ — nova migration com RPC wms_desmarcar_item_atomico(p_item_id, p_usuario_id, p_motivo) espelhando o padrão de wms_pick_item_atomico/wms_desfazer_guarda_atomico"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_desmarcar_item_atomico — estorna S (cria E), estorna L (recria R via origem_tipo='reserva_pedido' p/ buscarReservaPendente reachar), deleta siso_pedido_item_mov_links do item, reseta colunas do pedido_item; tudo numa transação plpgsql com RAISE em qualquer falha (rollback all-or-nothing). Mantém a ordem S-antes-de-L (fix p3-2.7).",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "test/integration/desmarcar-item-atomico-rpc.test.ts",
  "red_test": "Seed: item picado com links S+L. Chamar a RPC de desmarcar num cenário em que a recriação da R (estorno L) violaria invariante (saldo já consumido por outro pedido → reserva excede saldo). Espera: a RPC faz RAISE e NADA muda — S permanece estornável, links intactos, separacao_marcado continua true. Hoje (loop JS) a S é estornada, L falha com warn, links são apagados e item vira desmarcado → estado inconsistente irreparável.",
  "deps": [],
  "conflicts": [
   "P015"
  ],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = 'desfazimento tudo-ou-nada'. A ordem S-antes-de-L já está correta (fix p3-2.7) e o estornarLiberacaoReserva já é idempotente; o que falta é a transação cobrindo o conjunto. Sobrepõe parcialmente com P015 (a falha de estorno-L por mudança de saldo é o gatilho que P015 também aborda) — coordenar para não duplicar RPC."
 },
 {
  "id": "P015",
  "current_status": "open",
  "status_evidence": "estornarMovimentacao (src/lib/wms/ledger.ts:363-417) e estornarLiberacaoReserva (src/lib/wms/reservas-picking.ts:318-375) recriam a mov inversa via wms_inserir_movimentacao, que calcula posteriores sobre o saldo/reservado ATUAL (migration 20260527, linhas 55-78: SELECT saldo,reservado FOR UPDATE; tipo R RAISE 'reserva excede saldo' se reservado_posterior > saldo_anterior). Logo, desmarcar separação antiga cujo saldo mudou (outro pedido consumiu) faz o estorno-L (recria R) falhar com 'reserva excede saldo'. O ledger é imutável/append-only e NÃO guarda snapshot do saldo do dia da marcação.",
  "root_cause": "O estorno é sempre RELATIVO ao saldo corrente (design do ledger imutável). Quando o saldo livre de hoje é menor que no dia do pick, recriar a R (reservado += qty) pode exceder o saldo e a CHECK reservado<=saldo bloqueia. A ordem S-antes-de-L (marcar-item:246-256) mitiga o caso comum (a E do estorno-S recupera o saldo antes), mas não cobre o caso em que terceiros consumiram o saldo no intervalo.",
  "change_sites": [
   "DECISÃO CONFLITA COM ARQUITETURA — a nota pede 'guardar o número exato de estoque do dia da marcação e usar pra calcular a volta'. O ledger 3D é append-only e estorno é relativo; reproduzir saldo histórico exigiria gravar saldo_anterior/posterior no pick (que JÁ existe em siso_movimentacoes) e usar a RPC de estorno parcial.",
   "src/lib/wms/reservas-picking.ts:318-375 (estornarLiberacaoReserva) — quando a recriação da R falhar por 'reserva excede saldo', degradar para NÃO bloquear o desmarque: recriar a R apenas até o saldo disponível e sinalizar status_alerta no pedido (mesmo padrão de cutover.ts:333-356 reservasFalhadas), em vez de propagar erro que trava o operador.",
   "src/app/api/wms/separacao/marcar-item/route.ts:258-293 — tratar o caso 'reserva excede saldo' como reversão tolerada (estorno S concluído, R parcial), registrar evento e permitir o desmarque, em vez de deixar item travado"
  ],
  "needs_migration": false,
  "rpc_needed": "opcional: wms_estornar_pick_tolerante — recria R clampada ao saldo livre quando o full estorno violaria a CHECK, marcando status_alerta. Sem RPC nova é possível via lógica TS no estornarLiberacaoReserva.",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "test_file": "test/integration/desmarcar-separacao-antiga-saldo-mudou.test.ts",
  "red_test": "Seed: item picado (S+L) 'há 3 dias'. Consumir o saldo livre da loc com outra S de terceiro até reservado>saldo seria violado na recriação da R. Desmarcar o item: espera que o desmarque COMPLETE (S estornada, item desmarcado) e que NÃO trave com 'reserva excede saldo' — registrando status_alerta. Hoje o estornarLiberacaoReserva propaga o erro e o item fica preso.",
  "deps": [],
  "conflicts": [
   "P014"
  ],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": false,
  "notes": "NOTA INCOMPATÍVEL COM A ARQUITETURA: 'guardar o número exato do dia' contraria o ledger imutável onde estorno é relativo (e siso_movimentacoes JÁ persiste saldo_anterior/posterior por mov). A abordagem fiel ao sistema é estorno tolerante (clampar R ao saldo livre + alerta), não reconstruir saldo histórico. respeita_nota=false porque a estratégia técnica viável diverge da letra da nota — precisa confirmar com o user. Conflita/sobrepõe P014 (mesma falha de estorno-L)."
 },
 {
  "id": "P017",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/parcial/route.ts:68-81 valida quantidade_pega < 0 (rejeita negativo) mas ACEITA quantidade_pega === 0 sem exigir loc_zerou. Com qty=0 + loc_zerou=true cria ajuste_pick_zerou (linhas 401-427, deltaAjuste=saldoWms-0-reservadoRestante) → baixa fantasma do saldo todo. Não há guard 'qty=0 só com loc_zerou=true'. O frontend parcial-modal.tsx:98 desabilita o botão quando qty<=0, mas a API aceita o POST direto.",
  "root_cause": "A API /separacao/parcial não trava qty=0 — permite que um operador (ou request malformado) registre saída zero. Quando loc_zerou=false e qty=0, o item fica 'em progresso' sem efeito; quando loc_zerou=true e qty=0 com prateleira que tinha saldo, cria ajuste de perda do saldo inteiro mesmo sem nada ter saído fisicamente.",
  "change_sites": [
   "src/app/api/wms/separacao/parcial/route.ts:68-81 — endurecer a validação: se quantidade_pega === 0, exigir loc_zerou === true (caso contrário 400 'quantidade 0 só é permitida confirmando prateleira vazia'). Mantém o caminho legítimo 'prateleira vazia' (qty=0 + loc_zerou=true) e bloqueia o zero acidental sem confirmação."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/86-parcial-zero-exige-loc-zerou.ts",
  "red_test": "POST /api/wms/separacao/parcial {quantidade_pega:0, loc_zerou:false}: espera 400 (bloqueado). POST {quantidade_pega:0, loc_zerou:true}: espera 200 (prateleira confirmada vazia → ajuste). Hoje o primeiro caso passa (200) sem reclamar.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = 'permitir zero só com confirmação de prateleira vazia'. loc_zerou JÁ é a confirmação de prateleira vazia no modelo — basta exigi-lo quando qty=0. Mudança de 1 condição na validação de entrada. marcar-item não tem esse risco (não recebe qty do operador)."
 },
 {
  "id": "P019",
  "current_status": "open",
  "status_evidence": "O caminho 'Parcial' (src/app/api/wms/separacao/parcial/route.ts, processarParcialItem) NÃO usa a RPC atômica wms_pick_item_atomico. Faz L e S em chamadas SEPARADAS: liberarReservaPicking (L, linhas 342-352) e depois inserirMovimentacao (S, linhas 363-388) — dois inserirMovimentacao independentes. Se cair entre a L e a S (ou entre S e o claim/update do item), a L libera reserva sem S, ou a S sai sem o pareamento de estado, deixando órfão. Contrasta com marcar-item (checkbox) que JÁ é atômico via RPC.",
  "root_cause": "O fluxo parcial monta L+S+ajuste+links+claim como sequência de operações independentes em JS, não numa transação única. A nota pede tudo-ou-nada: a saída de estoque e a liberação/estado devem comitar juntos ou reverter juntos. O rollback existente (linhas 709-806) só dispara em race detectada no claim, não cobre queda de rede/servidor entre L e S.",
  "change_sites": [
   "src/app/api/wms/separacao/parcial/route.ts:317-388 — substituir o par liberarReservaPicking (L) + inserirMovimentacao (S) por chamada à RPC wms_pick_item_atomico (já existe, faz L+S atômico) por pedido do wave, garantindo que L e S comitem juntos. Para o caso loc_zerou (ajuste), incluir o ajuste na mesma transação ou numa RPC dedicada.",
   "src/app/api/wms/separacao/parcial/route.ts:390-433 (ajuste_pick_zerou) — mover o ajuste pra dentro da transação atômica do pick quando loc_zerou=true, pra que saída+liberação+ajuste sejam atômicos",
   "supabase/migrations/ — possível RPC wms_pick_parcial_atomico que aceita múltiplos pedidos do wave + ajuste loc_zerou numa transação (generaliza wms_pick_item_atomico pro caso parcial/wave)"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_pick_parcial_atomico — numa transação plpgsql: para cada pedido do wave libera a R (L) e emite a S total + (opcional) ajuste loc_zerou, validando coerência; RAISE em qualquer falha (rollback). Espelha wms_pick_item_atomico mas cobre o wave consolidado + ajuste.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "test/integration/parcial-pick-atomico-rpc.test.ts",
  "red_test": "Chamar a RPC de pick parcial num cenário em que a S falharia (saldo insuficiente após a L): espera RAISE e rollback total — a reserva R NÃO fica liberada (nenhuma L persistida) e nenhuma S persistida. Hoje (caminho JS) a L é emitida primeiro e, se a S falhar, a reserva fica liberada sem saída (órfão).",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota = op1 'usar transação tudo-ou-nada'. O bloco /parcial é o mais complexo do sistema (2143 linhas, wave consolidado, cascade, FCFS). A RPC wms_pick_item_atomico já prova o padrão pro caso single — generalizá-la pro wave+ajuste é o trabalho. blast_radius high porque /parcial toca reservas, cascade, compras e realocação. P013 (marcar-item) já está coberto pela RPC; P019 é o gêmeo do caminho /parcial que ficou sem atomicidade."
 },
 {
  "id": "P021",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/desfazer-bip/route.ts:84-103 faz read-modify-write em JS: lê currentBipada (item.quantidade_bipada, linha 84), calcula newBipada=currentBipada-1 (linha 93) e UPDATE quantidade_bipada=newBipada (96-103). Dois POSTs concorrentes lêem o mesmo valor → ambos gravam o mesmo decremento → perde um. Não há filtro de concorrência no UPDATE nem decremento atômico no SQL. Existe a RPC wms_acumular_qty_pega (migration 20260518) que faz UPDATE ... += delta atômico — padrão a aplicar aqui.",
  "root_cause": "O decremento de quantidade_bipada é um read-modify-write não-atômico no servidor. Dois cliques rápidos (ou retries) condicionam a corrida: ambos lêem N e gravam N-1, resultado final N-1 em vez de N-2.",
  "change_sites": [
   "src/app/api/wms/separacao/desfazer-bip/route.ts:84-103 — trocar o read+UPDATE por um decremento atômico no banco: UPDATE ... SET quantidade_bipada = GREATEST(quantidade_bipada-1, 0) ... WHERE quantidade_bipada > 0 RETURNING quantidade_bipada (via RPC plpgsql ou update com .gt('quantidade_bipada',0) + select RETURNING). Usar o valor RETURNADO (não o calculado em JS) pra decidir bipado_completo e reversão de status.",
   "src/app/api/wms/separacao/desfazer-bip/route.ts:117-146 — derivar newBipada/newBipadoCompleto do valor retornado pelo decremento atômico"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_desfazer_bip_atomico(p_pedido_id text, p_produto_id) — UPDATE atômico com GREATEST(quantidade_bipada-1,0) e WHERE quantidade_bipada>0, RETURNING o novo valor; opcionalmente já resolve a reversão de status. Alternativa: update direto com filtro .gt + RETURNING, sem RPC nova.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "test/integration/desfazer-bip-decremento-atomico.test.ts",
  "red_test": "Item com quantidade_bipada=3. Disparar 2 desfazer-bip concorrentes (Promise.all): espera quantidade_bipada final = 1 (cada um decrementou exatamente 1). Hoje, com read-modify-write em JS, o resultado pode ficar 2 (um decremento perdido).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = op2 'desfazer sempre diminui exatamente 1, não importa quanto foi lido antes' = decremento atômico no SQL. wms_acumular_qty_pega já prova o padrão (UPDATE += delta). Não usar debounce de frontend (op1 rejeitada). Cirúrgico."
 },
 {
  "id": "P023",
  "current_status": "open",
  "status_evidence": "src/lib/wms/cutover.ts:290-341 reverterCutoverSeRetrocedeu itera saidasArr fazendo, por S: inserirMovimentacao(E, estorno) (300-312) + reservarAtomico(R) (324-330) — chamadas RPC INDEPENDENTES em loop. Falhas no estorno fazem 'continue' (314-321); falhas na reserva incrementam reservasFalhadas e seguem (332-340). O UPDATE estoque_lancado=false (343-346) só roda DEPOIS do loop. Se o servidor cair no meio: parte das S estornadas (saldo volta parcial), estoque_lancado fica TRUE (flag não chegou a virar) → pedido trava ('acha que já lançou'). Exatamente o cenário do raio-x.",
  "root_cause": "A reversão do cutover (N estornos de S + N recriações de R + flip da flag) não é tudo-ou-nada: são 2N chamadas RPC independentes mais o UPDATE final, sem transação cobrindo o conjunto. Queda no meio deixa saldo parcialmente revertido e estoque_lancado=true.",
  "change_sites": [
   "src/lib/wms/cutover.ts:290-346 — mover o loop de estornos+recriações de R + o UPDATE estoque_lancado/nf_estoque_lancado=false pra dentro de UMA RPC plpgsql transacional. Se qualquer estorno/reserva falhar, RAISE → rollback total (nenhuma S estornada, flag permanece true coerente), em vez de meio-revertido.",
   "src/app/api/wms/separacao/desfazer-bip/route.ts:175-185 — o caller só passa a chamar a RPC atômica; manter o catch/warn pra não quebrar o fluxo de bip",
   "supabase/migrations/ — nova RPC wms_reverter_cutover_atomico(p_pedido_id, p_motivo, p_usuario_id) que busca as S (via origem_id + mov_saida_id), estorna cada uma, recria R, e flipa as flags, tudo numa transação"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_reverter_cutover_atomico — numa transação plpgsql: para cada S do pedido (não já estornada) insere E (estorno_de=S.id) + recria R, e seta estoque_lancado=false/nf_estoque_lancado=false; RAISE em qualquer falha → rollback all-or-nothing. Mantém idempotência (pula S já com E counter).",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "test/integration/reverter-cutover-atomico-rpc.test.ts",
  "red_test": "Pedido com estoque_lancado=true e 3 S. Forçar a recriação da 2ª R a falhar (saldo consumido por terceiro). Chamar a RPC de reversão: espera RAISE e que NENHUMA das 3 S tenha sido estornada E estoque_lancado continue true (estado coerente, retentável). Hoje (loop JS) a 1ª S é estornada, a 2ª falha, e estoque_lancado fica true com saldo parcial → pedido travado.",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota = opção 1 'transação tudo-ou-nada, se cai no meio volta ao estado anterior'. reverterCutoverSeRetrocedeu é chamado por desfazer-bip, voltar-etapa, reiniciar-embalagem e ajuste-admin — blast_radius high. O cutover.ts:333-356 já tem o conceito de reservasFalhadas+status_alerta (degradação tolerada), que conflitaria com all-or-nothing estrito; decidir se a RPC faz rollback total ou mantém o alerta. P023 e P019/P014 compartilham a mesma primitiva (atomic_rpc_ledger) e podem reusar helpers plpgsql."
 },
 {
  "id": "P062",
  "current_status": "partially_fixed",
  "status_evidence": "receberTransferencia (transferencias.ts:296-323) já tem claim estrito via UPDATE condicional setando recebimento_em_andamento_por (migration 20260527_p3_transferencia_recebimento_em_andamento.sql). MAS cancelarTransferencia (transferencias.ts:448-562) NÃO lê nem respeita esse lock — qualquer warehouse-user pode cancelar mesmo com recebimento em andamento (route cancelar/route.ts:10 só exige requireWarehouseAccess). Não existe coluna de timestamp do lock (migration só tem recebimento_em_andamento_por uuid, sem _em) → timeout de 30min impossível hoje.",
  "root_cause": "cancelarTransferencia ignora o lock recebimento_em_andamento_por que receberTransferencia respeita. Maria (não-recebedora) cancela enquanto João recebe: cancel estorna legs E/S e marca cancelada, mas o INSERT da mov E de João (que segurou o claim) já materializou a 1a caixa → fica órfã. Não há checagem de 'quem está recebendo é quem cancela' nem timeout de lock stale.",
  "change_sites": [
   "src/lib/wms/transferencias.ts:448-473 — em cancelarTransferencia, após carregar o header, ler recebimento_em_andamento_por; se != null E (now - recebimento_em_andamento_em) < 30min E recebimento_em_andamento_por != usuarioId → throw 409 code='TRANSFERENCIA_RECEBIMENTO_EM_ANDAMENTO'. Se o lock estiver stale (>30min) ou for o próprio recebedor, permite cancelar (e zera o lock no UPDATE final, linhas 552-559).",
   "supabase/migrations/<nova>_transferencia_recebimento_em_andamento_em.sql — ADD COLUMN recebimento_em_andamento_em timestamptz; setar em receberTransferencia no claim (transferencias.ts:301) e usar pro timeout de 30min no cancel.",
   "src/lib/wms/transferencias.ts:299-305 — incluir recebimento_em_andamento_em: now() no UPDATE de claim do receber."
  ],
  "needs_migration": true,
  "migration_desc": "ADD COLUMN recebimento_em_andamento_em timestamptz em siso_transferencias_galpao para suportar timeout automático de 30min do lock de recebimento (libera lock stale se a tela do recebedor cair).",
  "primitive": "atomic_claim_header",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/wms/transferencias.cancel-lock.integration.test.ts",
  "red_test": "Com recebimento_em_andamento_por=João (setado <30min atrás), cancelarTransferencia(transfId, Maria) deve lançar erro code TRANSFERENCIA_RECEBIMENTO_EM_ANDAMENTO e NÃO mudar status (segue em_transito). João (mesmo usuário do lock) consegue cancelar. Após 30min de lock stale, qualquer um cancela.",
  "deps": [
   "P068"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota pede 2 coisas: (1) só quem recebe cancela — implementável comparando recebimento_em_andamento_por com usuarioId; (2) timeout 30min — exige nova coluna timestamp (não existe hoje). Família P062/P066/P068. A opção 'gerente cancela' foi rejeitada pela nota."
 },
 {
  "id": "P063",
  "current_status": "partially_fixed",
  "status_evidence": "A RAIZ da nota (não excluir loc com itens; forçar transferir antes) JÁ está parcialmente implementada: desativarLocalizacao (localizacoes.ts:121-133) bloqueia soft-delete se siso_estoque.saldo>0 ('não é possível desativar: localização tem saldo'); e existe o fluxo substituir-e-excluir (route.ts) que move todo saldo pra outra loc antes de desativar. DELETE de loc é soft (ativo=false), nunca hard — a row persiste, então estorno de cancel ainda acha a loc. GAP: o guard só checa saldo>0; não bloqueia loc que é DESTINO de transferência em_transito (item com localizacao_destino_id setado mas mov_entrada_id ainda nulo) nem loc referenciada por pendência de guarda aberta.",
  "root_cause": "O guard de desativarLocalizacao cobre saldo físico vivo, mas não cobre referências em-voo (transferência em_transito apontando essa loc como destino, ou pendência de guarda). Uma loc com saldo=0 mas sendo destino de transferência pode ser desativada; quando a transferência recebe/cancela e tenta materializar a mov nessa loc, a loc está inativa. Como o delete é soft (row existe), inserirMovimentacao ainda funciona — o impacto real é menor do que o 'today' do raio-x sugere (que assumia hard-delete sumindo a prateleira).",
  "change_sites": [
   "src/lib/wms/localizacoes.ts:121-133 — em desativarLocalizacao, além do check saldo>0, adicionar: bloquear se EXISTS item de siso_transferencia_galpao_itens com localizacao_destino_id=id OU localizacao_origem_id=id em transferência status='em_transito'; e se EXISTS siso_wms_pendencias_guarda apontando a loc em status pendente/em_guarda. Mensagem orienta usar /substituir-e-excluir.",
   "src/app/api/wms/localizacoes/[id]/route.ts:62-83 — nenhuma mudança lógica; só herda o guard reforçado."
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "soft_delete_block_delete",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/wms/localizacoes.block-delete.integration.test.ts",
  "red_test": "desativarLocalizacao(locId) deve lançar quando a loc é destino de uma transferência em_transito (mesmo com saldo=0) e quando há pendência de guarda aberta nela; deve permitir quando não há referências vivas nem saldo. O fluxo correto força mover via substituir-e-excluir antes.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Decisão CUSTOM da nota (raiz = não excluir loc com itens). Já existe o esqueleto (guard saldo + substituir-e-excluir). Trabalho residual = estender o guard pra referências em-voo (transferência em_transito / guarda pendente). A 'opção reverter cancel em erro' das options NÃO é o approach escolhido — a nota redireciona pra prevenção na exclusão de loc."
 },
 {
  "id": "P068",
  "current_status": "partially_fixed",
  "status_evidence": "receberTransferencia (transferencias.ts:261-404) tem claim atômico via recebimento_em_andamento_por (rejeita receber se lock setado). cancelarTransferencia (transferencias.ts:448-562) NÃO seta esse lock nem checa — então cancel e receber podem rodar concorrentes: cancel estorna legs enquanto receber insere movs E novas na mesma tripla. Não há serialização entre os dois caminhos.",
  "root_cause": "Assimetria de lock: receber respeita recebimento_em_andamento_por, mas cancelar nem seta nem respeita. Como o claim do receber é estrito (rejeita se lock!=null), basta o cancel também adquirir/respeitar o MESMO lock para serializar os dois. Hoje cancel roda em paralelo a um receber em andamento, ambos mexendo no ledger da mesma transferência.",
  "change_sites": [
   "src/lib/wms/transferencias.ts:448-473 — em cancelarTransferencia, antes de estornar, fazer claim condicional: UPDATE ... SET recebimento_em_andamento_por (ou nova flag cancelamento_em_andamento) WHERE id=$1 AND status='em_transito' AND recebimento_em_andamento_por IS NULL; se 0 rows → throw 409 'transferência sendo recebida/cancelada, tente em alguns segundos'. Limpar o lock no UPDATE final (linhas 552-559).",
   "src/lib/wms/transferencias.ts:299-305 — o claim do receber já existe; garantir que cancel use o mesmo campo pra mutex compartilhado entre os dois fluxos."
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "atomic_claim_header",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/wms/transferencias.cancel-receber-race.integration.test.ts",
  "red_test": "Disparar cancelarTransferencia e receberTransferencia concorrentes na mesma transferência: exatamente um vence; o outro recebe 409 (TRANSFERENCIA_OUTRO_RECEBIMENTO/cancelando) sem inserir movs. Saldo final consistente (sem dupla-contagem nem sumiço).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: 'travar transferencia durante cancelamento (família P062/P066)'. Reusa o lock recebimento_em_andamento_por já existente — cancel passa a adquiri-lo como mutex. Combina diretamente com P062 (mesmo lock, mesma coluna). Implementar P062+P068 juntos."
 },
 {
  "id": "P072",
  "current_status": "partially_fixed",
  "status_evidence": "wms_pick_item_atomico (migration 20260528:40-63) protege o caminho COM reserva: SELECT FOR UPDATE na R + guard 'reserva já liberada → exception' (idempotência via EXISTS L estorno_de=R.id). Mas marcar-item/route.ts NÃO tem guard de idempotência próprio: não há early-return se item.separacao_marcado já é true (linhas 82-235 re-executam), e o caminho SEM reserva (buscarLocComMaiorSaldoNoGalpao, route:124 → pickItemAtomico com reserva_id=null) cai no ramo ELSE da RPC (migration:82-88) que NÃO tem guard de idempotência — duplo-pick possível em concorrência real. Não existe token de idempotência por marcação (só o índice idx_pedidos_idempotency_key, que é pra webhook/pedido, não pra marcar-item).",
  "root_cause": "A defesa atual contra duplo-clique depende de: (a) reserva consumida (R→L) bloquear via FOR UPDATE — funciona só quando há R viva; e (b) quantidade_pega==quantidade_pedida zerar qtyADescontar no 2o clique sequencial. Sob concorrência real (dois requests lêem quantidade_pega=0 antes do commit) no caminho SEM reserva, ambos inserem S → saldo dobra. Falta o token de idempotência explícito que a nota pede ('ID único por marcação; recusa reprocessar a mesma; clicar 100x conta 1').",
  "change_sites": [
   "src/app/api/wms/separacao/marcar-item/route.ts:34-37 — aceitar idempotency_key (uuid client-gerado) no body.",
   "src/app/api/wms/separacao/marcar-item/route.ts:42-60 — após carregar item, se já existe registro de marcação com esse idempotency_key (nova tabela/coluna), retornar a mesma resposta (no-op). Alternativa mais barata alinhada à nota: gravar o idempotency_key em siso_pedido_item_mov_links e UNIQUE-constraint nele para o 2o INSERT falhar 23505 → tratar como já-processado.",
   "src/lib/wms/reservas-picking.ts:177-206 / supabase/migrations/20260528_wms_pick_item_atomico.sql:82-88 — fechar o gap do caminho sem reserva: passar idempotency_key pra RPC e checar EXISTS S com esse token antes de inserir a S no ramo p_reserva_id IS NULL."
  ],
  "needs_migration": true,
  "migration_desc": "Adicionar coluna/UNIQUE de idempotency_key (uuid) na trilha de marcação (siso_pedido_item_mov_links ou nova tabela siso_separacao_marcacoes) para que a mesma marcação não gere segunda S; e ensinar wms_pick_item_atomico a respeitar o token no ramo sem reserva.",
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "evolução de wms_pick_item_atomico — aceitar p_idempotency_key e, no ramo sem reserva, RAISE/no-op se já existe S com aquele token (fecha o duplo-pick do completar-parcial/OC sem reserva)",
  "test_harness": "integration",
  "test_file": "src/app/api/wms/separacao/marcar-item.idempotency.integration.test.ts",
  "red_test": "Dois POST /marcar-item {marcado:true, idempotency_key:K} concorrentes (caminho sem reserva, qtyADescontar>0) devem resultar em UMA única mov S e saldo reduzido só 1x. Repetir a chamada com o mesmo K retorna a mesma resposta sem nova baixa.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota escolhe a opção 2 (ID único/idempotency token), explicitamente acima do simples 'verificar se já marcado'. O caminho com reserva já está protegido pela RPC (FOR UPDATE + guard); o trabalho é o token explícito + fechar o ramo sem reserva."
 },
 {
  "id": "P073",
  "current_status": "already_fixed",
  "status_evidence": "concluir/route.ts:103-147 computa separados/aguardandoCompra/pendentes a partir de separacao_marcado dos itens (que NÃO é resetado ao concluir). Os UPDATEs (linhas 202/231) têm .eq('status_separacao','em_separacao') — no 2o clique o pedido já está 'separado' e o UPDATE vira no-op, mas a RESPOSTA (linhas 310-315) ainda reporta o pedido em separadosCompletos porque os itens continuam todos separacao_marcado=true. Logo o 2o POST retorna a MESMA resposta do 1o ('separados': [...]). O cenário do raio-x ('responde nenhum foi concluído / marca todos pendentes') NÃO ocorre no código atual — a resposta é estável.",
  "root_cause": "N/A — comportamento já idempotente na resposta. concluir não reseta separacao_marcado, então a recomputação no 2o clique reproduz o mesmo bucket. O .eq('status_separacao','em_separacao') só protege o UPDATE de regressão (no-op), e a resposta deriva do estado dos itens, não do status do pedido.",
  "change_sites": [],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "frontend_idempotency_ux",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/82-concluir-idempotente.ts",
  "red_test": "(teste de regressão / no-regress) Dois POST /separacao/concluir {pedido_ids:[X]} sequenciais com X já 100% marcado: ambas as respostas contêm X em 'separados' (mesma resposta), nenhuma o coloca em 'pendentes'. Pedido permanece 'separado'.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota pede opção 2 (2o clique retorna a mesma resposta). O código atual já satisfaz isso porque separacao_marcado persiste e a resposta é derivada dos itens. Recomendo apenas um cenário E2E no-regress para travar o comportamento. Status: already_fixed (a descrição 'today' do raio-x parece defasada vs o código com guard .eq status)."
 },
 {
  "id": "P074",
  "current_status": "open",
  "status_evidence": "execution-worker-wms.ts:64-78 — quando upsertNotaFiscal falha, faz logger.warn e segue com notaFiscalUuid=null. A S de nf_venda é inserida na linha 158-169 com nota_fiscal_id=notaFiscalUuid (possivelmente null). ledger.ts:146-160 confirma que NF_REQUIRED inclui 'nf_venda' MAS o enforcement foi RELAXADO pra só logger.warn (linha 155, comentário 'Fix-Final A T8 RELAXADO') — NÃO lança. Resultado: S de venda materializa sem nota_fiscal_id, e o pedido é marcado estoque_lancado=true (linha 198). Rastreabilidade fiscal perdida exatamente como descrito.",
  "root_cause": "lancar_estoque é operação que PRECISA de NF (nf_venda). Mas (a) o worker engole a falha de upsertNotaFiscal (warn+continue) e (b) o ledger relaxou o enforcement de NF pra warn. Os dois juntos deixam a S sair sem NF e o pedido 'concluir' sem comprovante.",
  "change_sites": [
   "src/lib/execution-worker-wms.ts:64-78 — se o pedido tem referência de NF (nota_fiscal_id != null || chave_acesso_nf) e upsertNotaFiscal falha, NÃO continuar: throw (deixa o job retornar erro → retry com backoff da fila). Só ignorar/continuar quando a operação comprovadamente não precisa de NF (não é o caso de nf_venda).",
   "src/lib/execution-worker-wms.ts:158-169 — guardar: se origem_tipo='nf_venda' e notaFiscalUuid==null → throw antes de inserir a S (defensa em profundidade, já que ledger só dá warn).",
   "(opcional) src/lib/wms/ledger.ts:154-160 — considerar voltar a throw para nf_venda especificamente, mas a nota pede granularidade ('exigir só quando precisa'); manter o relaxamento pros caminhos que legitimamente não têm NF (recebimento manual, parcial, retroativo) e bloquear apenas nf_venda/nf_compra."
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "preflight_guard",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/execution-worker-wms.nf-required.integration.test.ts",
  "red_test": "Com pedido que tem nota_fiscal_id/chave_acesso_nf mas upsertNotaFiscal falha (NF some/inválida): processar o job lancar_estoque deve THROW (job vira erro/retry), NÃO inserir S sem nota_fiscal_id e NÃO marcar estoque_lancado=true. Para uma operação que não precisa de NF (ex.: ajuste de posição), o fluxo segue normalmente.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 2 com nuance: exigir NF só quando a operação precisa (nf_venda precisa; ajuste de posição não). O caminho lancar_estoque É nf_venda → deve exigir. O relaxamento atual do ledger (warn) cobre os casos legítimos sem NF, então a correção é no worker (não engolir a falha) + guard pontual pra nf_venda."
 },
 {
  "id": "P080",
  "current_status": "already_fixed",
  "status_evidence": "O cross-dock transita o pedido via prepararPedidosDasOcsParaEmbalagem (compras-embalagem.ts:101-122): se pedido.status_separacao já é 'separado' (READY_STATUS), ele entra em jaSeparados (linhas 116-119) e NÃO é re-preparado — só pedidos em PACKABLE_STATUSES ainda-não-separados sofrem o UPDATE (linhas 140-157). Rodar dispararTriggerCrossDock 2x pro mesmo pedido é no-op na 2a vez (status==separado curto-circuita). Logo não há dupla separação do mesmo pedido.",
  "root_cause": "N/A — a idempotência já existe via o marcador de status 'separado' (jaSeparados short-circuit). A nota pede 'anotar que já rodou e ignorar repetição'; o sistema usa o próprio status_separacao='separado' como esse marcador, o que satisfaz o intento.",
  "change_sites": [],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "atomic_claim_header",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/68b-crossdock-trigger-idempotente.ts",
  "red_test": "(no-regress) Chamar dispararTriggerCrossDock 2x para a mesma pendência/pedido: na 2a chamada o pedido já está 'separado' e NÃO é re-processado (jaSeparados), sem duplicar separação nem eventos de histórico.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "file=null no raio-x; a transição real fica em compras-embalagem.ts:prepararPedidosDasOcsParaEmbalagem (chamada por crossdock-trigger.ts:95). A idempotência por status já existe. Recomendo só um cenário no-regress. P081 (atropelar em_separacao) é o gap remanescente, não P080."
 },
 {
  "id": "P081",
  "current_status": "open",
  "status_evidence": "compras-embalagem.ts:7-11 — PACKABLE_STATUSES inclui 'em_separacao'. Em prepararPedidosDasOcsParaEmbalagem, pedido em 'em_separacao' passa o filtro (linha 102), entra em paraPreparar (linha 121) e o UPDATE (linhas 154-157) seta status_separacao='separado' SEM nenhum .eq('status_separacao', ...) condicional — é um update incondicional por id. Atropela separação manual em andamento.",
  "root_cause": "O cross-dock trigger sobrescreve 'em_separacao'→'separado' sem checar o status atual no momento do UPDATE. Um operador separando manualmente (em_separacao) tem o pedido empurrado pra 'separado' pelo trigger, sumindo do fluxo de separação. Falta o guard condicional na escrita (compare-and-set).",
  "change_sites": [
   "src/lib/compras-embalagem.ts:154-157 — adicionar .eq('status_separacao', pedido.status_separacao) (compare-and-set) ao UPDATE; se retornar 0 rows, registrar como ignorado (corrida) em vez de sobrescrever cegamente.",
   "src/lib/compras-embalagem.ts:7-11 — reavaliar incluir 'em_separacao' em PACKABLE_STATUSES: pela nota, NÃO atropelar separação manual; mover 'em_separacao' para um bucket que exige confirmação/ignora em vez de virar separado automaticamente."
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "preflight_guard",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/compras-embalagem.crossdock-status-guard.integration.test.ts",
  "red_test": "Pedido em 'em_separacao' (separação manual em curso): prepararPedidosDasOcsParaEmbalagem NÃO deve sobrescrever para 'separado' — ou rejeita/ignora o pedido (reportando-o em ignorados) ou exige que o status atual bata. Pedido em 'aguardando_separacao' segue virando separado normalmente.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "file=null no raio-x; o mutador real é compras-embalagem.ts (não crossdock-trigger.ts, que só decide se chama). Nota = opção 1 (conferir status antes de mudar / não atropelar em_separacao). Implementar como compare-and-set no UPDATE + reavaliar PACKABLE_STATUSES."
 },
 {
  "id": "P082",
  "current_status": "open",
  "status_evidence": "reconciliador-oc.ts:268-286 — o UPDATE de transição (decisao_final=propria, status=executando, status_separacao=aguardando_nf) com .in('status_separacao',[...]) é condicional (bom contra corrida), mas se pedidoUpdErr ocorre (FK/permissão/constraint) só faz logger.logError + return (linha 285). Não há retry com backoff nem alerta visível. reconciliarEntradaEstoque é fire-and-forget (chamado de ledger.ts:252 e guarda/[id]/confirmar/route.ts:74) → falha morre silenciosa, pedido fica preso em validacao_oc/aguardando_compra.",
  "root_cause": "A transição de etapa do reconciliador não tem durabilidade: roda fora do worker (sem cron drenando), e em erro de banco apenas loga e desiste. Não retenta nem sinaliza. Pedido preso sem ninguém avisado, exatamente como a nota descreve.",
  "change_sites": [
   "src/lib/wms/reconciliador-oc.ts:268-286 — em pedidoUpdErr, em vez de só logError+return, enfileirar um job durável de retry em siso_fila_execucao (tipo novo ex. 'reconciliar_oc_retry') com schedule de backoff 30s→5min→10min (NÃO 1h), apoiado no proximo_retry_em/tentativas da fila; após 3 falhas, marcar pra alerta visível.",
   "src/lib/wms/reconciliador-oc.ts:333-336 — emitir registrarEvento de alerta (ex. 'reconciliacao_oc_falhou') após esgotar retries, pra aparecer no quadro de tarefas/exceções da home.",
   "src/lib/execution-worker-wms.ts (handler da fila) — adicionar handler do novo tipo de job que re-chama a transição idempotente do reconciliador.",
   "(infra) garantir backoff custom 30s/5min/10min para esse tipo — alinhar com o backoff exponencial existente da siso_fila_execucao ou setar proximo_retry_em explícito por tentativa."
  ],
  "needs_migration": false,
  "migration_desc": "Sem schema novo se reaproveitar siso_fila_execucao; talvez ajustar o CHECK de tipo da fila para aceitar o novo tipo de job de retry (verificar constraint de tipo).",
  "primitive": "durable_enqueue_retry",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "src/lib/wms/reconciliador-oc.retry.integration.test.ts",
  "red_test": "Forçar o UPDATE de transição a falhar (ex.: mockar erro de banco): reconciliarEntradaEstoque deve enfileirar um job de retry (não apenas logar) com proximo_retry_em ~30s; após 3 tentativas falhas, registrar um evento/alerta visível. No happy-path do retry, o pedido transita pra aguardando_nf e o lancar_estoque é enfileirado.",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 2 MAS com schedule ajustado: 30s, 5min, 10min (explicitamente NÃO 1h) + alerta visível se falhar 3x. Reaproveitar siso_fila_execucao (fila durável c/ backoff já existente) é o caminho de menor risco; o trabalho está em criar o tipo de job + override do schedule + alerta na home."
 },
 {
  "id": "P085",
  "current_status": "open",
  "status_evidence": "webhook-processor-wms.ts:583-606 — no loop de reservas de propria/transferencia, cada reservarAtomico está dentro de try/catch que só faz logger.warn e CONTINUA (linhas 597-604). Depois, para isAuto (propria) enfileira lancar_estoque (linha 622) como se 100% aprovado. wms_reservar_atomico (migration 20260526:157-180) NÃO é idempotente — sempre insere nova R. early-return de dedup (linha 443) só dispara se estoque_lancado===true, que é false numa falha parcial.",
  "root_cause": "Falha ao reservar (apartar) um item é engolida: o pedido segue pro fluxo como aprovado mesmo com SKU-B sem reserva. O operador vai buscar 2 itens mas só 1 foi apartado. A nota manda rejeitar TODA a aprovação se qualquer item falhar (loja retenta).",
  "change_sites": [
   "src/lib/webhook-processor-wms.ts:597-604 — trocar o catch warn+continue por: coletar a falha e, ao fim do loop, se houver QUALQUER falha, THROW (status do webhook vira 'erro' → Tiny retenta), em vez de enfileirar lancar_estoque. NÃO marcar isAuto/enfileirar quando há item não-reservado.",
   "src/lib/webhook-processor-wms.ts:582-585 — antes de re-reservar num reprocessamento, limpar/estornar as R já existentes do pedido (reservarAtomico não é idempotente; sem cleanup o retry cria R duplicada para o item A que tinha dado certo). Alternativa: tornar a reserva idempotente por (pedido, produto, loc).",
   "src/lib/webhook-processor-wms.ts:615-636 — só executar o bloco isAuto (enfileirar lancar_estoque + kickWorker) se todas as reservas tiverem sucesso."
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "reserva_wiring_cancel",
  "rpc_needed": "(opcional) tornar wms_reservar_atomico idempotente por (pedido_id, produto_id, galpao_id, localizacao_id) para o retro do retry não duplicar R — senão fazer cleanup das R do pedido antes de re-reservar no reprocessamento",
  "test_harness": "integration",
  "test_file": "src/lib/webhook-processor-wms.reserva-all-or-nothing.integration.test.ts",
  "red_test": "Pedido propria com 2 itens onde a reserva do 2o item falha: processWebhookWms NÃO deve enfileirar lancar_estoque e deve sinalizar erro (status webhook 'erro' p/ retry); o pedido não fica num estado meio-aprovado com 1 item reservado e outro não. No retry bem-sucedido, exatamente 1 R por item (sem duplicação).",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (rejeitar tudo se qualquer item falhar; loja retenta) — explicitamente acima da opção 2 (aprovar parcial + avisar). Atenção crítica: como wms_reservar_atomico não é idempotente, o 'throw → retry' exige limpar R existentes antes de re-reservar, ou o item A vira R duplicada a cada retry. Blast_radius high porque mexe no caminho de auto-aprovação do webhook (todos os pedidos propria)."
 },
 {
  "id": "P127",
  "current_status": "partially_fixed",
  "status_evidence": "src/lib/wms/guarda.ts:359-371 — iniciarGuarda() ainda LANCA erro code='PENDENCIA_OUTRA_GUARDA' (409) quando outro operador (mesmo ausente) e dono; nao existe rota de takeover. src/app/api/wms/guarda/[id]/iniciar/route.ts:31-36 propaga o 409. Existe so um paliativo cosmetico no front: src/components/wms/home/quadro-tarefas.tsx:155-159 nula iniciada_por na UI se o dono nao esta na presenca — mas isso NAO destrava o backend; B clicando 'Iniciar' ainda toma 409. Nenhum aviso '>30min', nenhum reset por timeout. Rotas de guarda existentes: route.ts, [id]/iniciar, confirmar, cancelar, desfazer, desvincular-crossdock — nenhuma 'tomar/forcar'.",
  "root_cause": "iniciarGuarda nao tem caminho de force-unlock: quando iniciada_por != usuario_id e status='em_guarda', sempre 409. Item fica preso ao operador original ate cancelar/admin. A nulificacao por presenca e so no render do quadro, nao no contrato da rota.",
  "change_sites": [
   "src/lib/wms/guarda.ts:345-402 — adicionar param opcional `forcar:boolean` em iniciarGuarda; quando true, o UPDATE condicional remove a clausula .or(iniciada_por.is.null|eq.usuario) e reivindica a pendencia em_guarda de outro dono, preservando qty_guardada/qty_pendente (continua do ponto). Sem forcar, mantem o 409 atual.",
   "src/app/api/wms/guarda/[id]/iniciar/route.ts:19-24 — ler `forcar` do body e repassar a iniciarGuarda; manter requireWarehouseAccess.",
   "src/lib/wms/dashboard-tarefas.ts:813-820 e/ou src/lib/wms/guarda.ts (listagem/obterPendencia) — expor `iniciada_em` pra UI computar '>30min' (badge de stale).",
   "src/components/wms/home/cards-detalhe.tsx:59-63 + pagina /wms/guarda — botao 'Tomar de Fulano' (chama iniciar com forcar:true) visivel quando iniciada_por != usuario atual, com aviso se iniciada_em > 30min."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "red_test": "Com pendencia A em_guarda (iniciada_por=op1, qty_guardada=2 de 5), iniciarGuarda({pendencia_id, usuario_id:op2, forcar:true}) deve retornar a pendencia com iniciada_por=op2 e qty_pendente preservado (3), SEM erro PENDENCIA_OUTRA_GUARDA; e iniciarGuarda sem forcar (op2) deve continuar lancando o 409.",
  "test_file": "src/lib/wms/__tests__/guarda-force-unlock.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = forca-unlock visivel 'Tomar de Fulano' (continua do ponto) + aviso >30min. O paliativo de presenca ja existe mas e cosmetico — o gate real esta no iniciarGuarda. A opcao 'timeout 60min' das options foi REJEITADA pela nota; nao implementar reset automatico."
 },
 {
  "id": "P129",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:84-95 — read-modify-write puro (newBipada = currentBipada + quantidade, depois UPDATE), sem chave de idempotencia. Front em src/app/wms/separacao/embalagem/page.tsx:333-352 (confirmarMut) NAO envia request-id, NAO desabilita o botao durante o request, e ainda faz optimistic +delta (linha 366) — duplo clique soma duas vezes. grep idempotency/request_id/nonce em src/app/api/wms/separacao = 0 resultados.",
  "root_cause": "Cada POST e incondicionalmente aplicado (+quantidade). Dois POSTs identicos disparados por duplo-clique (ou retry de rede) somam 2x quantidade_bipada. Sem dedup por janela de tempo nem chave de cliente.",
  "change_sites": [
   "src/app/wms/separacao/embalagem/page.tsx:338-346 — gerar um client_request_id (uuid) por clique do botao e enviar no body de confirmar-item-embalagem.",
   "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:25-38 — aceitar client_request_id obrigatorio; antes do UPDATE, registrar/checar dedup (tabela siso_idempotencia_embalagem ou coluna last_request_id no item) numa janela de 60s; se repetido, retornar o resultado anterior sem reaplicar +quantidade.",
   "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:88-95 — aplicar o incremento so quando a chave e nova (idealmente dentro do mesmo UPDATE atomico do P130)."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_confirmar_item_embalagem_atomico(p_item_id, p_delta, p_client_request_id) — RPC plpgsql que, sob FOR UPDATE da linha do item, ignora se p_client_request_id ja foi visto na janela (60s) e senao faz quantidade_bipada = quantidade_bipada + p_delta de forma atomica (resolve P129+P130+P131 juntos).",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "scenarios_e2e",
  "red_test": "Dois POSTs identicos a /confirmar-item-embalagem com o MESMO client_request_id em <60s (item com quantidade_pedida=11, bipada=10, delta=+1) devem resultar em quantidade_bipada=11 (nao 12); o segundo retorna o mesmo estado sem reaplicar.",
  "test_file": "scripts/wms/cenarios/cenario-embalagem-idempotencia.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = ID unico por clique, ignora repetido (robustez contra rede). Mesmo route/linha de P130/P131. P129 e P131 sao essencialmente o mesmo fix (idempotency key); diferem so na janela (P129 60s embalagem, P131 1s confirmar). A RPC atomica unifica os tres. A opcao 'disable botao 500ms' foi rejeitada (opcao 1); a nota escolheu a opcao do ID unico."
 },
 {
  "id": "P130",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:84-95 — le quantidade_bipada (linha 84), calcula newBipada em JS (85) e sobrescreve via UPDATE .eq('id') (89-95). Nao ha lock de linha nem incremento atomico no banco. Dois operadores concorrentes leem o mesmo valor e o segundo UPDATE sobrescreve o primeiro (lost update).",
  "root_cause": "Padrao read-modify-write nao-atomico: a soma e feita no app server e gravada como valor absoluto. Concorrencia perde incrementos (A:+3, B:+2 ambos leem 0 -> fica 2).",
  "change_sites": [
   "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:84-95 — substituir o read-modify-write por chamada a RPC que faz UPDATE siso_pedido_itens SET quantidade_bipada = GREATEST(0, quantidade_bipada + p_delta) ... RETURNING, atomico sob FOR UPDATE; recomputar bipado_completo a partir do valor retornado.",
   "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:107-117 — recontar pendingCount apos o UPDATE atomico (ja existe; manter)."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_confirmar_item_embalagem_atomico(p_item_id, p_delta, p_client_request_id) — soma atomica (quantidade_bipada + p_delta, clamp >=0) sob lock de linha, retorna quantidade_bipada/bipado_completo novos. Compartilhada com P129/P131.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "red_test": "Disparar concorrentemente dois incrementos no MESMO item (delta=+3 e delta=+2) a partir de quantidade_bipada=0; apos ambos, quantidade_bipada deve ser exatamente 5 (nenhum incremento perdido).",
  "test_file": "src/lib/wms/__tests__/confirmar-embalagem-atomico.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = gravar com SOMA atomica no banco (nao sobrescrever). Opcao 2 escolhida (upsert/UPDATE com soma), nao lock-da-linha-com-desfazer-manual (opcao 1) nem version/hash (opcao 3). Mesma RPC resolve P129/P130/P131."
 },
 {
  "id": "P131",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:88-95 — mesmo read-modify-write nao-idempotente; duplo clique soma 2x e, se atinge bipado_completo, dispara conclusao/etiqueta/registrarEvento 2x (linhas 132,332-336). Front confirmarMut (embalagem/page.tsx:333-352) nao envia ID por clique nem desabilita botao. grep idempotency/request_id = 0.",
  "root_cause": "Identico a P129: ausencia de chave de idempotencia por clique. Como o handler tambem dispara efeitos colaterais (evento de conclusao, etiqueta) quando pedido_completo, o duplo-incremento alem de inflar a contagem dispara avisos/impressao duplicados.",
  "change_sites": [
   "src/app/wms/separacao/embalagem/page.tsx:338-346 — enviar client_request_id por clique (mesmo do P129).",
   "src/app/api/wms/separacao/confirmar-item-embalagem/route.ts:25-95 — dedup do client_request_id em janela de 1s (P131 pede <1s; usar a mesma infra do P129 com janela parametrizavel); so aplicar o incremento e os efeitos de conclusao quando a chave for nova."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_confirmar_item_embalagem_atomico (mesma de P129/P130) — a dedup por client_request_id cobre tanto a janela de 60s (P129) quanto <1s (P131).",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "scenarios_e2e",
  "red_test": "Dois POSTs com mesmo client_request_id em <1s (delta=+1, item de quantidade_pedida=1, bipada=0) -> quantidade_bipada=1 (nao 2) e o evento de conclusao/etiqueta dispara so 1 vez.",
  "test_file": "scripts/wms/cenarios/cenario-embalagem-idempotencia.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = ID unico por clique no confirmar item da separacao, ignora repetido <1s. Mesmo arquivo/linha de P129/P130 — implementar como um unico fix (RPC atomica + idempotency key) que cobre os tres. Opcao 2 (lock da prateleira) rejeitada pela nota."
 },
 {
  "id": "P132",
  "current_status": "partially_fixed",
  "status_evidence": "Existem rotas de estorno (a afirmacao do raio-x 'nao existe rota' esta DEFASADA): src/app/api/wms/separacao/voltar-etapa/route.ts:109,112,114 e src/app/api/wms/separacao/desfazer-bip/route.ts:125 zeram embalagem_concluida_em=null ao reverter de embalado. POReM nenhuma das duas limpa embalagem_operador_id — a coluna fica com o operador da embalagem desfeita. confirmar-item-embalagem/route.ts:219 (e :219 OC) seta AMBOS embalagem_concluida_em E embalagem_operador_id. dashboard-tarefas.ts:802 le embalagem_operador_id (fila 'separado' a embalar), evidenciando que o campo e consultado e fica stale.",
  "root_cause": "O estorno (voltar-etapa/desfazer-bip) limpa a DATA mas nao o OPERADOR. embalagem_operador_id persiste apontando pro operador de uma embalagem que foi desfeita — credito/atribuicao fantasma.",
  "change_sites": [
   "src/app/api/wms/separacao/voltar-etapa/route.ts:109,112,114 — adicionar pedidoUpdate.embalagem_operador_id = null em cada ramo que ja zera embalagem_concluida_em (alvos <= separado).",
   "src/app/api/wms/separacao/desfazer-bip/route.ts:125 — adicionar pedidoUpdates.embalagem_operador_id = null junto do embalagem_concluida_em=null no ramo 'embalado -> em_separacao'."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "scenarios_e2e",
  "red_test": "Pedido em 'embalado' com embalagem_operador_id=op1 e embalagem_concluida_em setado; apos voltar-etapa (ou desfazer-bip) pra em_separacao, ambos embalagem_operador_id E embalagem_concluida_em devem estar NULL.",
  "test_file": "scripts/wms/cenarios/cenario-estorno-embalagem-limpa-operador.ts",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = rota de estorno limpa data + operador e tira credito do ranking. A 'data' ja e limpa hoje; falta so o 'operador'. O ranking de produtividade do motor de insights le eventos (nao o campo direto), entao o fix de integridade e zerar embalagem_operador_id. Referencia do master a 'migration 20260515 linhas 188-192' esta errada (aquele arquivo e funil de insights); o ponto real e as rotas de estorno."
 },
 {
  "id": "P142",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/reimprimir/route.ts:110-122 — catch: marca status 'falhou' (fire-and-forget) e retorna {status:'falhou', error:'erro_interno'} 500 na PRIMEIRA falha do PrintNode (enviarImpressaoZpl). Nao ha retry (nenhum loop/setTimeout/backoff), nem mensagem clara 'impressora indisponivel'. O botao no front permanece ativo (operador re-clica manualmente).",
  "root_cause": "A reimpressao chama enviarImpressaoZpl uma unica vez; qualquer falha transitoria do PrintNode (sem rede/credito/servico fora) vira 500 imediato. Sem retry automatico nem distincao de erro de impressora vs erro generico.",
  "change_sites": [
   "src/app/api/wms/separacao/reimprimir/route.ts:91-122 — envolver enviarImpressaoZpl num retry de ate 3 tentativas com espera de 10s entre elas (so para falhas de envio/PrintNode); persistir 'imprimindo' durante; se as 3 falharem, retornar erro tipado (ex: error:'impressora_indisponivel') com mensagem clara em vez de 'erro_interno'.",
   "src/app/wms/separacao/page.tsx (handler de reimpressao ~546-556) — exibir mensagem clara 'Impressora indisponivel, tente novamente' so apos as 3 tentativas falharem."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "durable_enqueue_retry",
  "test_harness": "vitest_unit",
  "red_test": "Mock de enviarImpressaoZpl que falha 2x e sucede na 3a: a rota deve retornar status:'impresso' (nao falhou) e ter chamado o envio 3x; se falha as 3, retorna erro tipado 'impressora_indisponivel' (nao 'erro_interno').",
  "test_file": "src/app/api/wms/separacao/reimprimir/__tests__/reimprimir-retry.test.ts",
  "effort": "M",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = tenta 3x sozinho (10s cada) depois avisa claro 'impressora indisponivel'. Cuidado: 3x10s = ate 30s no request — manter dentro do timeout da rota (Next standalone). O master cita 'route.ts:110-122' sem path; e o reimprimir/route.ts (catch block bate exatamente nessas linhas)."
 },
 {
  "id": "P143",
  "current_status": "open",
  "status_evidence": "supabase/migrations/20260311_cleanup_etiqueta_zpl_cron.sql:7 — cron 'cleanup-etiqueta-zpl' (0 3 * * *) faz UPDATE ... etiqueta_zpl=NULL ... WHERE etiqueta_status='impresso' AND updated_at < now()-interval '7 days'. Apos wipe, reimprimir/route.ts:65-77 cai no fallback buscarEImprimirEtiqueta, que em src/lib/etiqueta-service.ts:63-83 chama RPC siso_claim_etiqueta; a RPC (supabase/migrations/20260317_claim_etiqueta_add_forma_frete.sql) so reivindica WHERE etiqueta_status IS NULL OR IN ('pendente','falhou'). Como o pedido esta 'impresso', claim retorna null -> skipped:true -> reimprimir devolve 'falhou'. Narrativa do raio-x confirmada.",
  "root_cause": "Etiqueta cacheada e apagada apos 7 dias; o caminho de re-geracao depende de siso_claim_etiqueta que recusa pedidos ja 'impresso'. Resultado: depois de 7 dias a reimpressao quebra. A nota opta pelo fix minimo: ampliar a retencao.",
  "change_sites": [
   "supabase/migrations/20260311_cleanup_etiqueta_zpl_cron.sql:7 — trocar interval '7 days' por interval '30 days' (criar NOVA migration que cron.unschedule('cleanup-etiqueta-zpl') e re-schedule com 30 dias; nao editar migration ja aplicada in-place)."
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "manual_only",
  "red_test": "Apos aplicar a migration, consultar cron.job: o comando agendado de 'cleanup-etiqueta-zpl' deve conter interval '30 days' (nao '7 days'); pedido impresso ha 10 dias mantem etiqueta_zpl (nao e apagado).",
  "test_file": "supabase/migrations/<nova>_etiqueta_zpl_retencao_30d.sql",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = guardar etiqueta por 30 dias (em vez de 7). Opcao 3 escolhida — fix minimo de retencao. NAO resolve a causa estrutural (siso_claim_etiqueta recusa 'impresso'), so empurra a janela de 7->30d; mas e exatamente o que a nota pede. Migration nova deve usar cron.unschedule+cron.schedule (ou cron.alter_job) — nao reaplicar a antiga."
 },
 {
  "id": "P144",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/reimprimir/route.ts:92-93 — comentario 'Safety: ensure we only print one label even if multiple were cached' + const singleLabel = splitZplLabels(pedido.etiqueta_zpl)[0] ?? pedido.etiqueta_zpl; envia so singleLabel pro PrintNode (linha 98). Quando o ZIP do Tiny trouxe NF + Etiqueta de Envio concatenadas, [0] pega so a primeira (NF) e a etiqueta de envio some na reimpressao via cache.",
  "root_cause": "O fast-path de reimpressao deliberadamente fatia e imprime apenas a primeira label do ZPL cacheado, descartando a segunda (etiqueta de envio).",
  "change_sites": [
   "src/app/api/wms/separacao/reimprimir/route.ts:92-99 — remover o [0]; imprimir TODAS as labels do cache. Opcoes: enviar pedido.etiqueta_zpl completo (o PrintNode ja imprime multi-label, como faz buscarEImprimirEtiqueta em etiqueta-service.ts:147-154 que manda o ZPL full), ou iterar splitZplLabels e enviar cada uma. Ajustar o titulo/contagem na resposta.",
   "src/app/wms/separacao/page.tsx (resposta da reimpressao) — opcional: avisar 'reimprimindo NF + etiqueta de envio' (a nota pede avisar)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "vitest_unit",
  "red_test": "Com etiqueta_zpl contendo 2 labels (NF + envio), a reimpressao deve enviar ao PrintNode o conteudo com AS DUAS labels (splitZplLabels(...).length == 2 enviadas), nao apenas a primeira.",
  "test_file": "src/app/api/wms/separacao/reimprimir/__tests__/reimprimir-duas-labels.test.ts",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = reimprimir sempre as 2 etiquetas (NF + envio), avisando; nao pegar so a primeira. Opcao 1. Confirmar que enviarImpressaoZpl aceita ZPL multi-label num unico job (buscarEImprimirEtiqueta ja envia o full sem split — bom precedente). Se P142 tambem mexe no bloco try, coordenar os dois fixes no mesmo arquivo/PR."
 },
 {
  "id": "P153",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/iniciar/route.ts:133-161 — pedidos ja em_separacao sao SILENCIOSAMENTE filtrados de toStart (so transita aguardando_separacao/validacao_oc, linha 136) e a rota retorna 200 com produtos (linha 212), sem sinalizar quais ja estavam em separacao. Front src/app/wms/separacao/page.tsx:461-471 (iniciarMut) nem checa r.ok nem le o body — apenas dispara e retorna ids. Nao ha mensagem 'esses pedidos ja estao em separacao'.",
  "root_cause": "A rota nao reporta no payload quais pedidos ja estavam em_separacao (no-op silencioso) e o front ignora a resposta — operador clica de novo sem feedback.",
  "change_sites": [
   "src/app/api/wms/separacao/iniciar/route.ts:133-161,212 — computar a lista de pedidos que JA estavam em_separacao (status_separacao==='em_separacao' no fetch inicial) e inclui-la na resposta JSON (ex: ja_em_separacao: string[]).",
   "src/app/wms/separacao/page.tsx:461-471 — ler r.ok/body no iniciarMut e, em onSuccess, se ja_em_separacao.length>0, toast.info('Esses N pedidos ja estao em separacao')."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "scenarios_e2e",
  "red_test": "POST /separacao/iniciar com pedido_ids onde 1 ja esta em_separacao deve retornar 200 com ja_em_separacao contendo aquele id (e o front mostraria a mensagem); o pedido nao e re-transicionado.",
  "test_file": "scripts/wms/cenarios/cenario-iniciar-ja-em-separacao.ts",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = mostrar mensagem 'esses pedidos ja estao em separacao' no 2o clique. Opcao 2 (feedback claro), nao bloquear botao 2s (opcao 1). Fix e principalmente de UX/contrato: a rota ja e idempotente no estado; falta so reportar e exibir."
 },
 {
  "id": "P154",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/marcar-item/route.ts:70-76 — ALLOWED inclui em_separacao sem checar ha quanto tempo (grep '24'|'separacao_iniciada_em'|'stale'|'horas' no arquivo = 0). Nenhum cron cancela em_separacao antigo: grep cron.schedule nas migrations nao retorna nada de 'separac/em_separacao stale'; 20260530_wms_detectar_pedidos_inconsistentes nao trata janela de 24h de em_separacao. Pedidos podem ficar dias em em_separacao e ser separados normalmente.",
  "root_cause": "Nao ha gate de staleness: marcar-item aceita qualquer pedido em_separacao independente de separacao_iniciada_em, e nenhum job revisa/cancela pedidos parados >24h. Estoque pode ter sido reorganizado, levando a pick de loc errada.",
  "change_sites": [
   "supabase/migrations/<nova>_cancelar_separacao_stale.sql — cron diario (ou RPC wms_revisar_separacao_stale) que acha siso_pedidos status_separacao='em_separacao' com separacao_iniciada_em < now()-interval '24 hours', reverte pra aguardando_separacao (limpando separacao_operador_id/iniciada_em e estado de itens — reusar logica de reset) e registra evento/notificacao.",
   "src/app/api/wms/separacao/marcar-item/route.ts:70-76 — opcional: bloquear marcar quando o pedido excede 24h em_separacao (defesa em profundidade), orientando re-iniciar."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_revisar_separacao_stale() plpgsql — reverter pedidos em_separacao ha >24h pra aguardando_separacao (reset de operador/itens + evento) e retornar a lista afetada pra notificacao; agendada via cron.",
  "primitive": "preflight_guard",
  "test_harness": "integration",
  "red_test": "Pedido com status_separacao='em_separacao' e separacao_iniciada_em = now()-25h: apos rodar wms_revisar_separacao_stale(), o pedido volta a aguardando_separacao (separacao_operador_id/iniciada_em zerados) e e registrada notificacao; pedido com 23h NAO e tocado.",
  "test_file": "src/lib/wms/__tests__/revisar-separacao-stale.integration.test.ts",
  "effort": "L",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA = cancelar/revisar automaticamente pedido em separacao ha mais de 24h, COM notificacao. Opcao 1 (auto >24h) + exigencia de notificacao das outras options. 'Cancelar' aqui = reverter pra aguardando_separacao (replanejar), nao cancelar o pedido inteiro — alinha com 'forca o operador a replanejar' do detail. Reusar resetarEstadoSeparacaoItens (ja usado em voltar-etapa) pra estornar movs/parciais ao reverter, evitando saldo fantasma."
 },
 {
  "id": "P055",
  "current_status": "partially_fixed",
  "status_evidence": "Frontend guard JÁ existe: src/app/wms/inventario/page.tsx:338 desabilita botão em `criar.isPending`. MAS o guard de banco (a parte vinculante da NOTA) NÃO existe: a única UNIQUE em siso_inventario_sessoes é `uniq_sessao_continua_galpao` (supabase/migrations/20260601_acerto_prateleira_pick.sql:9-11) que só cobre a sessão `continua` (operacional do pick). Sessões cycle_count criadas via criarSessao (src/lib/wms/inventario.ts:45-86) NÃO têm UNIQUE (galpao_id, data). Tabela tem coluna `programada_para date` (20260529_wms_inventario.sql:21) mas criarSessao nunca a popula (grep `programada_para` em src/ = 0 hits), então a única data utilizável é criado_em::date.",
  "root_cause": "criarSessao faz INSERT simples sem nenhuma constraint de unicidade por (galpao_id, dia). Duplo-clique concorrente que vença a janela do isPending (ou 2 requests via API) cria 2 sessões para o mesmo galpão/dia. O banco não rejeita.",
  "change_sites": [
   "supabase/migrations/<nova>_inv_sessao_unique_galpao_dia.sql:novo — CREATE UNIQUE INDEX parcial em siso_inventario_sessoes ((galpao_id), (criado_em::date)) WHERE status NOT IN ('cancelada') AND continua = false (exclui canceladas e a sessão operacional contínua que já tem seu próprio índice)",
   "src/lib/wms/inventario.ts:66 — após erro do INSERT, tratar código 23505 retornando erro de domínio claro ('já existe sessão de inventário para este galpão hoje') em vez de propagar o PostgrestError cru",
   "src/app/api/wms/inventario/route.ts:81-90 — mapear o 23505/erro de duplicidade para HTTP 409 (hoje cai em wmsErrorResponse status 400 genérico)"
  ],
  "needs_migration": true,
  "migration_desc": "UNIQUE parcial em siso_inventario_sessoes por (galpao_id, criado_em::date) excluindo status='cancelada' e continua=true. Decisão sobre coluna: usar criado_em::date (programada_para nunca é setado pelo fluxo atual).",
  "primitive": "db_constraint_unique",
  "rpc_needed": "",
  "test_harness": "integration",
  "test_file": "test/integration/inventario-sessao-unique.test.ts",
  "red_test": "Criar 2 sessões cycle_count para o mesmo galpão no mesmo dia: a 2ª deve falhar com violação de unicidade (23505) e a rota POST /api/wms/inventario deve responder 409. Sessão 'cancelada' anterior no mesmo dia NÃO deve bloquear nova criação.",
  "effort": "S",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "A NOTA é explicitamente DB-level ('banco rejeita sessao duplicada por galpao+data'). FE já cumprido. Ambiguidade: 'data' — programada_para está sempre null, então criado_em::date é a única opção viável; registrar na migration. Cuidado: índice parcial precisa excluir continua=true para não colidir com o uniq_sessao_continua_galpao existente."
 },
 {
  "id": "P056",
  "current_status": "open",
  "status_evidence": "estornarSessaoInventario (src/lib/wms/inventario.ts:1161-1226) faz loop TS: para cada divergência chama estornarMovimentacao (ledger.ts:363) que por sua vez chama inserirMovimentacao → RPC wms_inserir_movimentacao. CADA estorno é uma transação RPC independente. Não há transação envolvendo o lote. Se o 50º estorno falha (ex.: saldo ficaria negativo → validarCoerencia em ledger.ts:50 lança), os 49 anteriores JÁ foram commitados; loop sai por throw na linha 1209. Não há rollback. Nenhuma RPC plpgsql transacional de lote existe (grep confirmou só RPCs por-mov).",
  "root_cause": "Reversão de N movs é feita em N transações separadas no app layer; não há atomicidade tudo-ou-nada. Falha no meio deixa estoque parcialmente estornado.",
  "change_sites": [
   "supabase/migrations/<nova>_rpc_estornar_sessao_inventario.sql:novo — nova RPC plpgsql wms_estornar_sessao_inventario(p_sessao, p_usuario, p_motivo) que num único BEGIN/EXCEPTION: faz preflight de TODAS as divergências aplicadas (valida que cada estorno não deixa saldo negativo), e se qualquer uma inviável, RAISE com o produto/loc que ficaria negativo (rollback total); senão insere todas as contra-movs e reseta divergencias.status='pendente'",
   "src/lib/wms/inventario.ts:1161-1226 — substituir o loop TS por chamada à nova RPC; preservar idempotência (sessão já não-aplicada vira no-op) e a mensagem do produto que ficaria negativo",
   "src/app/api/wms/inventario/[id]/estornar/route.ts:42-56 — mapear o erro de 'ficaria negativo' para 409/400 com mensagem clara do produto"
  ],
  "needs_migration": true,
  "migration_desc": "RPC plpgsql wms_estornar_sessao_inventario: estorna TODAS as movs aplicadas da sessão dentro de uma transação única; preflight de coerência por mov; rollback total se alguma deixaria saldo negativo, anexando o produto culpado na mensagem. Reusa a lógica de wms_inserir_movimentacao para cada contra-mov (chamada interna).",
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_estornar_sessao_inventario(p_sessao uuid, p_usuario uuid, p_motivo text) — desfaz TODAS as movs aplicadas da sessão tudo-ou-nada; aborta com aviso do produto que ficaria negativo sem desfazer nada",
  "test_harness": "integration",
  "test_file": "test/integration/inventario-estorno-atomico.test.ts",
  "red_test": "Aplicar sessão com 2+ divergências (ex.: ganho +20 produto A, ganho +5 produto B). Consumir produto A via saída deixando saldo<5. Chamar estornar: deve falhar tudo-ou-nada — NENHUM dos estornos persiste (saldo de A e B inalterados, divergências continuam 'aplicada'), e a mensagem cita o produto A que ficaria negativo.",
  "effort": "L",
  "blast_radius": "high",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'tudo-ou-nada no desfazer; se uma reversão falha, nada é desfeito + aviso do produto que ficaria negativo. Vale também pro P061 (mesmo código).' P056 e P061 compartilham estornarSessaoInventario → mesma RPC, mesmo PR. blast high porque reescreve a baixa de estoque por RPC plpgsql nova."
 },
 {
  "id": "P057",
  "current_status": "open",
  "status_evidence": "registrarContagemInline (src/lib/wms/contagem-inline.ts:83-198) documenta explicitamente a não-atomicidade nas linhas 99-106: 'NÃO-ATÔMICO (limitação aceita v1): a mov de reconciliação e os inserts de contagem/divergência não estão numa transação única'. Sequência: inserirMovimentacao (RPC, linha 109) → upsert loc (132) → insert contagem (140) → upsert divergencia (154). Cada um é um round-trip separado; falha após a mov deixa ganho/perda sem registro de contagem.",
  "root_cause": "O acerto de prateleira no pick faz mov no ledger + 3 escritas de inventário em chamadas separadas; não há transação englobando tudo. Falha de rede/servidor no meio desacopla saldo de contagem.",
  "change_sites": [
   "supabase/migrations/<nova>_rpc_contagem_inline_atomica.sql:novo — RPC plpgsql wms_contagem_inline_atomica(tripla, qty_contada, contada_por, sessao_id, sku, pedido_id) que num único BEGIN: lê saldo, calcula delta, chama wms_inserir_movimentacao (inventario_ganho/perda) se delta!=0, upsert loc, insert contagem, upsert divergencia, update ultima_contagem_em — tudo-ou-nada",
   "src/lib/wms/contagem-inline.ts:83-198 — substituir o corpo de registrarContagemInline pela chamada única à RPC; remover o comentário de não-atomicidade (99-106); manter o shape de ContagemInlineResult"
  ],
  "needs_migration": true,
  "migration_desc": "RPC plpgsql wms_contagem_inline_atomica: empacota mov de reconciliação + insert de contagem + upsert de divergência (status='aplicada') + update de ultima_contagem_em numa transação única. getOrCreateSessaoOperacional pode permanecer no TS (idempotente) ou ir pra RPC.",
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_contagem_inline_atomica — reconcilia saldo + grava contagem + divergência atomicamente (tudo-ou-nada)",
  "test_harness": "integration",
  "test_file": "test/integration/contagem-inline-atomica.test.ts",
  "red_test": "Após uma contagem inline bem-sucedida, contar nº de movs de reconciliação E nº de linhas em siso_inventario_contagens para a tripla. Asseverar acoplamento: se a mov existe, a contagem existe (1:1) — e que uma falha simulada não deixa mov inventario_ganho/perda sem linha de contagem correspondente (delta da próxima contagem para a mesma qty_contada = 0, sem duplicar ganho).",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'contagem + saldo numa transacao unica (tudo-ou-nada)'. Caminho real: /api/wms/separacao .../acerto que usa contagem-inline (acerto de prateleira no pick). É independente do par aplicar/estornar de sessão (P056/P060/P061)."
 },
 {
  "id": "P058",
  "current_status": "partially_fixed",
  "status_evidence": "registrarContagem (src/lib/wms/inventario.ts:488-555) JÁ bloqueia bipe quando o STATUS DA LOC é 'contada' ou 'aprovada' (linhas 506-510) e quando a loc não está reivindicada / é de outro operador (511-520). MAS NÃO valida o STATUS DA SESSÃO. Se a sessão saiu de 'em_andamento' (foi pra revisao/aprovada/aplicada) mas a loc ficou com lock e status != contada/aprovada (ex.: 'em_contagem' órfã), o bipe ainda passa. A NOTA pede bloquear pelo estado da SESSÃO, não só da loc.",
  "root_cause": "O guard de aceitação de bipe é por-loc; falta um guard explícito que rejeite qualquer contagem quando a sessão não está em 'em_andamento'.",
  "change_sites": [
   "src/lib/wms/inventario.ts:491-520 (início de registrarContagem) — adicionar lookup do status da sessão (siso_inventario_sessoes.status por input.sessao_id) e lançar erro 'sessão já saiu da fase em andamento (status X) — supervisor cria nova para corrigir' se status != 'em_andamento'",
   "src/app/api/wms/inventario/[id]/contagens/route.ts — mapear esse erro para 409 (estado inválido) com mensagem clara"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "preflight_guard",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "red_test": "Com uma sessão em status 'revisao' (ou 'aprovada'/'aplicada'), tentar registrar um bipe via POST contagens: deve ser rejeitado (409) com mensagem indicando que a sessão já saiu de em_andamento. Bipe em sessão 'em_andamento' continua aceito.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'bloquear entrada se a contagem saiu de em_andamento; supervisor cria nova se precisa corrigir'. O bloqueio por-loc existente cobre o caso comum, mas não o caso da sessão fechada com loc órfã — daí partially_fixed. Guard de sessão é a peça que falta. (contagem-inline usa sessão 'continua' que fica permanentemente em_andamento, então não é afetada.)"
 },
 {
  "id": "P059",
  "current_status": "partially_fixed",
  "status_evidence": "reconciliarTemporal (src/lib/wms/inventario-reconciliacao.ts:102-180) JÁ faz reconciliação temporal: reconstrói saldo_esperado pegando saldo_anterior da primeira mov efetiva com criado_em > T_ref (linhas 159-165). MAS o cálculo de saldo_esperado usa T_ref = max(contado_em) da tripla (não 'início do dia') e computarDivergencias (inventario.ts:803-806) só carrega movs a partir de dataLimiteInferior = min(contado_em). A LIMITAÇÃO está documentada em inventario.ts:653-658 (#P6-4.8): movs entre cutoff_em e aprovado_em não são reconciliadas. A NOTA pede 'recalcular pelo histórico do dia (ledger desde início do dia)'.",
  "root_cause": "A janela de reconciliação começa em min(contado_em), não no início do dia. Compra que chega após a contagem (mas antes da aprovação/cutoff) cai numa zona onde o saldo já mudou mas a divergência foi calculada com snapshot do bipe — gera divergência falsa para o operador.",
  "change_sites": [
   "src/lib/wms/inventario.ts:803-806 — ampliar dataLimiteInferior para o início do dia (date_trunc('day') do cutoff/contagem) ao buscar siso_movimentacoes, garantindo que TODA mov do dia entre na reconciliação",
   "src/lib/wms/inventario-reconciliacao.ts:159-166 — confirmar que primeiraMovEfetiva continua usando T_ref correto da tripla mesmo com janela de movs maior (a janela ampliada só alimenta candidatos; o filtro criado_em > t_ref já é por-tripla, então o saldo_esperado permanece no instante do bipe — o ganho é capturar movs pós-bipe que hoje escapam quando min(contado_em) é depois delas)",
   "src/lib/wms/inventario-reconciliacao.test.ts — adicionar caso: compra (E) chegando entre contagem e cutoff não deve gerar divergência falsa"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "rpc_needed": "",
  "test_harness": "vitest_unit",
  "red_test": "reconciliarTemporal: dada uma contagem de N na tripla em T1, uma compra (mov E) em T2>T1 (mesma loc/produto) com criado_em<=cutoff, e saldo_atual = N+compra: o delta da tripla deve ser 0 (saldo_esperado reconstruído = N via saldo_anterior da mov de compra), SEM emitir divergência falsa. Hoje falha se a mov de compra cai fora da janela min(contado_em).",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Parcialmente resolvido: a infra de reconciliação temporal por ledger JÁ existe (commits 546253e/797fe26). O gap é a JANELA (min(contado_em) vs início do dia) e a limitação documentada #P6-4.8 (movs cutoff→aprovado). A NOTA ('ledger desde início do dia') aponta exatamente para ampliar o limite inferior da query. red_test é vitest_unit pois reconciliarTemporal é função pura."
 },
 {
  "id": "P060",
  "current_status": "open",
  "status_evidence": "aplicarSessao (src/lib/wms/inventario.ts:973-1103) faz loop TS sobre divergências aprovadas (linha 1012): para cada uma chama inserirMovimentacao (RPC independente, linha 1036) e marca divergencia.status='aplicada' (1052). Se D3 falha (saldo mudou desde a contagem → validarCoerencia lança), o throw na 1063 sai do loop com D1/D2 JÁ aplicadas e commitadas; a sessão NÃO transiciona pra 'aplicada' (UPDATE condicional 1096-1100 não roda). Estado meio-feito: 2 movs geradas, sessão travada em 'aprovada'. Nenhuma RPC transacional de lote existe.",
  "root_cause": "Aplicação de N divergências é N transações RPC separadas; falha no meio deixa parte aplicada. Sem tudo-ou-nada.",
  "change_sites": [
   "supabase/migrations/<nova>_rpc_aplicar_sessao_inventario.sql:novo — RPC plpgsql wms_aplicar_sessao_inventario(p_sessao, p_usuario) que num único BEGIN: itera divergências aprovadas, preflight de coerência (todas as movs viáveis?), gera todas as movs (E/S com custo_unitario em ganhos) e marca aplicada, transiciona sessão→'aplicada'; se qualquer mov inviável → RAISE (rollback total) citando produto/loc; idempotente para sessão já 'aplicada'",
   "src/lib/wms/inventario.ts:973-1103 — substituir o loop TS por chamada à RPC; preservar o branch de idempotência (status 'aplicada' → conta movs existentes) e o retorno { movsGeradas }",
   "src/app/api/wms/inventario/[id]/aplicar/route.ts:19-28 — mapear erro de 'saldo mudou/ficaria negativo' para 409 com mensagem clara"
  ],
  "needs_migration": true,
  "migration_desc": "RPC plpgsql wms_aplicar_sessao_inventario: aplica TODAS as divergências aprovadas da sessão tudo-ou-nada; preflight + rollback total se alguma é inviável; carrega custo_medio como custo_unitario nos ganhos (preserva a lógica de inventario.ts:1022-1033); idempotente.",
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_aplicar_sessao_inventario(p_sessao uuid, p_usuario uuid) — gera todas as movs de ajuste da sessão tudo-ou-nada; aborta sem aplicar nada se o saldo mudou e alguma ficaria inválida",
  "test_harness": "integration",
  "test_file": "test/integration/inventario-aplicar-atomico.test.ts",
  "red_test": "Sessão aprovada com 3 divergências (D1 perda produto A, D2 ganho produto B, D3 perda produto C). Antes de aplicar, esvaziar o saldo de C (saída externa) para que a perda de C deixe saldo negativo. Chamar aplicar: deve falhar tudo-ou-nada — NENHUMA mov gerada (A e B inalterados), sessão permanece 'aprovada' (não meio-aplicada), mensagem cita produto C.",
  "effort": "L",
  "blast_radius": "high",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'se falhar no meio ao aplicar contagem, descartar tudo e voltar ao estado anterior (tudo-ou-nada)'. Opção de 'bloquear estoque inteiro' (lock) foi descartada na decisão. Mesmo padrão de RPC que P056/P061 (lote de movs transacional), mas para o caminho APLICAR; pode compartilhar helpers plpgsql com a RPC de estorno."
 },
 {
  "id": "P061",
  "current_status": "open",
  "status_evidence": "Mesmo código de P056: estornarSessaoInventario (src/lib/wms/inventario.ts:1161-1226), loop nas linhas 1190-1215 chamando estornarMovimentacao por divergência. A NOTA do P061 supersede a opção 2 anterior ('desfaz-o-que-da') e alinha a P056 (tudo-ou-nada). Hoje: se o 2º estorno falha por saldo insuficiente (alguém mexeu), o 1º já foi commitado e o supervisor recebe 500 sem saber qual item caiu (a rota estornar/route.ts:48-55 só lista mensagens-cliente específicas; 'saldo insuficiente' cai em 500).",
  "root_cause": "Idêntico a P056: reversão por-mov não-transacional; falha no meio deixa estado parcial. P061 é o mesmo defeito visto pelo fluxo 'Desfazer ajustes de inventário'.",
  "change_sites": [
   "Mesmo da P056: supabase/migrations/<nova>_rpc_estornar_sessao_inventario.sql + src/lib/wms/inventario.ts:1161-1226 + src/app/api/wms/inventario/[id]/estornar/route.ts:42-56 (mensagem de erro deve nomear o item que falhou)"
  ],
  "needs_migration": true,
  "migration_desc": "Coberto pela mesma RPC wms_estornar_sessao_inventario de P056 (tudo-ou-nada). Não há migration adicional separada.",
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_estornar_sessao_inventario (compartilhada com P056)",
  "test_harness": "integration",
  "test_file": "test/integration/inventario-estorno-atomico.test.ts",
  "red_test": "Idêntico ao de P056: estorno de sessão com 1 item inviável (saldo mexido) não deve desfazer NENHUM item; mensagem ao supervisor deve nomear exatamente o item/produto que impediu o estorno.",
  "effort": "S",
  "blast_radius": "high",
  "deps": [
   "P056"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA explícita: 'ALINHADO A P056: tudo-ou-nada também no desfazer ajuste (compartilham estornarSessaoInventario). Vira o mesmo PR do P056; op2 anterior (desfaz-o-que-da) superseded.' effort S porque o trabalho real está em P056; aqui só garante a mensagem que nomeia o item e o teste do fluxo de undo de ajuste. deps=[P056]."
 },
 {
  "id": "P070",
  "current_status": "open",
  "status_evidence": "estornarAjuste (src/lib/wms/movimentacoes.ts:466-491) valida só origem_tipo='ajuste_manual' e delega a estornarMovimentacao (ledger.ts:363-417), que cria a contra-mov via inserirMovimentacao → validarCoerencia (ledger.ts:50). Para um ajuste E de 20 com 15 já consumidos em saídas (saldo=5), a contra-mov S de 20 lança 'saldo insuficiente: anterior=5 qty=20 tipo=S' — erro 500. A rota estornar/route.ts:42-47 lista mensagens-cliente mas NÃO inclui 'saldo insuficiente', então retorna 500 críptico. Não há preflight nem mensagem clara ('já foi usado em N saídas').",
  "root_cause": "Não há checagem prévia de disponibilidade antes de estornar um ajuste de entrada já parcialmente consumido. O ledger impede o saldo negativo (correto), mas o erro é 500 obscuro em vez de um 409 amigável instruindo a fazer outro ajuste.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:474-490 (dentro de estornarAjuste, antes de chamar estornarMovimentacao) — preflight: ler saldo atual da tripla da mov original; se a contra-mov (S do qty original) deixaria saldo<0, lançar Error com código de domínio e mensagem 'Não dá pra desfazer este ajuste — já foi usado em N saídas (separação de pedido). Para corrigir o saldo, faça outro ajuste.' (N = qty_original - saldo_disponível)",
   "src/app/api/wms/ajuste/[id]/estornar/route.ts:42-47 — incluir a nova mensagem/código na lista isClient → responder 409 (conflito de estado) em vez de 500"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "preflight_guard",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "red_test": "Criar ajuste de entrada +20 numa tripla (saldo vira 20). Vender/separar 15 (saída). Chamar POST /api/wms/ajuste/{movId}/estornar: deve responder 409 (não 500) com mensagem clara de que o ajuste já foi usado em 15 saídas e sugerindo novo ajuste; o saldo NÃO deve ficar negativo nem travar.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'rejeitar desfazer com aviso claro quando já houve saídas; pra corrigir, fazer outro ajuste'. O ledger JÁ impede o saldo negativo (não há risco de corrupção hoje), mas o status é open porque falta o preflight + mensagem amigável + status 409 (hoje é 500 críptico). ACD-003 do raio-x descreve exatamente entrada 20 → pedido 15 → desfazer falha com -15."
 },
 {
  "id": "P086",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:121-137 chama movimentarEstoque(token, ..., {tipo:'B'}) (balanço no Tiny) e 143-151 só relê o saldo do Tiny; as linhas 154-159 documentam que os writes locais (snapshot e colunas legadas) foram REMOVIDOS. Nenhuma escrita em siso_movimentacoes/siso_estoque acontece. A correção fica só no Tiny — sem trilha auditável no WMS. NOTA do user reverte a decisão original: 'Tiny é legado, NÃO escrever ajuste no Tiny; registrar no ledger próprio do WMS'. O caminho correto JÁ existe: POST /api/wms/ajuste → ajustarEstoque (movimentacoes.ts:431) → inserirMovimentacao origem_tipo='ajuste_manual' (trilha completa: saldo ant/post via RPC, usuario_id, motivo, motivo_categoria). Esta rota legada nem tem caller no frontend (grep 'tiny/stock/ajustar' em src/app/wms = 0 hits).",
  "root_cause": "A rota legada tiny/stock/ajustar escreve só no Tiny e não no ledger. Decisão do user: substituir esse caminho pelo ledger-only ajuste (que já existe). A rota legada é órfã (sem UI) e usa colunas deprecadas (filial_origem, produto_id_suporte, siso_empresas.galpao_id).",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:121-167 — remover a chamada movimentarEstoque/getEstoque ao Tiny e, no lugar, escrever a correção no ledger via ajustarEstoque (origem_tipo='ajuste_manual', direcao derivada de saldo_alvo - saldo_atual, motivo='correção de estoque no painel', usuario_id da sessão). Alternativa preferida pela arquitetura: DEPRECAR a rota e redirecionar a UI para POST /api/wms/ajuste (já é ledger-only e auditável)",
   "src/app/api/wms/tiny/stock/ajustar/route.ts:20-48 — adicionar requireWarehouseAccess (hoje a rota não tem session check) e validar a tripla/galpão como UUID em vez de nome de galpão legado"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "red_test": "Chamar a rota de correção de estoque do painel para setar saldo de uma tripla de X para Y: ao final deve existir UMA mov de ajuste em siso_movimentacoes (origem_tipo='ajuste_manual', com saldo_anterior=X, saldo_posterior=Y, usuario_id e motivo preenchidos) e siso_estoque refletir Y. NENHUMA chamada ao Tiny (TINY_DISABLED) deve ser necessária para a trilha existir.",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA (correção do user): 'Tiny é legado, NÃO escrever ajuste no Tiny. Registrar a correção no ledger próprio do WMS com trilha completa (saldo ant/post, autor, motivo), auditável e reversível.' Isso SUPERSEDE a opção 2 do raio-x ('estorno reverso/dois registros no Tiny'). O alvo ideal é reusar /api/wms/ajuste (já cumpre 100% da NOTA). P086/P087/P088/P089 são todos sobre a MESMA rota tiny/stock/ajustar → provável PR único. Verificar se a rota tem algum caller real antes de só deprecar; hoje grep não acha UI."
 },
 {
  "id": "P087",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:20-48 não tem nenhuma idempotency key: cada POST processa o balanço integralmente. Como é 'balanço' (set absoluto), duplo POST seta o mesmo valor (idempotente por acaso) NO TINY — mas se migrado para ledger (P086) com direcao=delta, duplo POST aplicaria o ajuste 2x. Existe infra de idempotência no projeto (siso_pedidos payload_original->>'idempotency_key' + migration 20260527_idempotency_key_index.sql; padrão em src/app/api/wms/vendas/criar/route.ts:170-177) que pode ser reusada. Nenhum guard de idempotência nesta rota.",
  "root_cause": "A rota de correção não tem chave de idempotência no backend; duplo-clique/retry de rede pode aplicar a correção mais de uma vez (crítico quando virar ajuste por delta no ledger conforme P086).",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:22-48 — aceitar idempotency_key no body (ou header X-Idempotency-Key); antes de aplicar, checar se já existe mov de ajuste com esse key (em origem_detalhes->>'idempotency_key') e, se sim, retornar o resultado anterior (no-op)",
   "src/lib/wms/movimentacoes.ts:431-452 (ajustarEstoque) — propagar idempotency_key para origem_detalhes da mov para o lookup de dedup",
   "frontend da tela de correção (a criar/identificar) — desabilitar botão durante o save (loading state), espelhando o padrão de inventario/page.tsx:338"
  ],
  "needs_migration": false,
  "migration_desc": "Opcional: índice parcial sobre siso_movimentacoes (origem_detalhes->>'idempotency_key') WHERE origem_tipo='ajuste_manual' para lookup rápido (espelha 20260527_idempotency_key_index.sql).",
  "primitive": "frontend_idempotency_ux",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "red_test": "Enviar 2 POSTs de correção idênticos com o MESMO idempotency_key para a mesma tripla: o saldo final deve refletir UMA única aplicação (não dobrar o delta) e a 2ª resposta deve indicar dedup/no-op. Sem idempotency_key hoje, 2 deltas seriam aplicados.",
  "effort": "M",
  "blast_radius": "med",
  "deps": [
   "P086"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'idempotency key no backend + desabilitar botão na tela; aplica uma só vez'. deps=[P086] porque a idempotência só importa de fato quando a correção virar ajuste por delta no ledger (hoje o balanço Tiny é idempotente por ser set absoluto). Reusar o padrão idempotency_key existente (vendas/criar)."
 },
 {
  "id": "P088",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:49-62 lê o pedido (filial_origem, empresa_origem_id) mas NÃO checa status nem status_separacao — permite corrigir em qualquer etapa sem aviso. A NOTA decide manter 'corrigir em qualquer etapa' (opção 1), MAS exige que a UI deixe claro se o operador está preenchendo o saldo ANTES ou DEPOIS da separação da qty necessária do picking. Hoje não há nenhuma sinalização (nem backend retorna o status do pedido/picking para a UI contextualizar).",
  "root_cause": "A correção é permitida em qualquer etapa (decisão mantida), mas sem feedback de contexto: o operador não sabe se já houve separação/reserva da qty necessária, podendo corrigir saldo que a separação em outro galpão já contava.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:49-62 (ou na rota /api/wms/ajuste ao migrar) — incluir na resposta o status_separacao do pedido e/ou se há reserva/pick aberta para a tripla (siso_pedido_itens.quantidade_pega / reservas vivas), para a UI exibir aviso 'esta correção é DEPOIS do início da separação'",
   "frontend da tela de correção — renderizar badge/aviso claro 'antes da separação' vs 'durante/após separação da qty necessária' com base nesse status (data_visibility_ux)"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "data_visibility_ux",
  "rpc_needed": "",
  "test_harness": "manual_only",
  "red_test": "A correção continua permitida em qualquer etapa; a verificação é de UX: a resposta da API de correção deve expor o status_separacao do pedido / existência de pick aberto para a tripla, e a tela deve exibir aviso visível quando a correção ocorre após o início da separação. (Cobertura automatizável só no nível de payload da API; o aviso visual é manual.)",
  "effort": "S",
  "blast_radius": "low",
  "deps": [
   "P086"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA: 'deixar corrigir em qualquer etapa, MAS a UI deve deixar claro se o operador está preenchendo o saldo antes ou depois da separação da qty necessária do picking.' Esta é a ÚNICA das 4 (P086-89) que NÃO bloqueia — é puramente UX/visibilidade. A opção do raio-x 'bloquear após aprovação' foi REJEITADA pela NOTA. deps=[P086] porque a tela/rota será consolidada com a migração para ledger."
 },
 {
  "id": "P089",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:121-138: o fluxo é movimentarEstoque(Tiny) → getEstoque(Tiny) → retorna. Se a 'volta' (re-fetch ou qualquer escrita local) falhar, o Tiny já mudou mas o WMS não registra nada → desync silencioso (Tiny=3, Siso=5). A NOTA do user é CUSTOM e dissolve o problema na raiz: 'gravar SÓ no ledger, atomicamente (sem Tiny) — dissolve o desync; alinhado ao P086 (Tiny legado)'. Ou seja, remover o Tiny do caminho elimina a possibilidade de desync. O ledger-only ajuste (/api/wms/ajuste) já é atômico (RPC wms_inserir_movimentacao).",
  "root_cause": "Two-phase write (Tiny depois local) cria janela de desync quando a 2ª fase falha. A decisão é eliminar a dependência do Tiny: a correção passa a ser uma única escrita atômica no ledger, sem chamada externa — não há mais 'volta' que possa cair.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:121-167 — remover por completo as chamadas movimentarEstoque/getEstoque ao Tiny (mesma mudança de P086) e escrever apenas no ledger via ajustarEstoque (atômico). Sem segunda fase → sem desync. (O retry-com-backoff da opção 2 do raio-x torna-se desnecessário pela NOTA custom.)"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "rpc_needed": "",
  "test_harness": "scenarios_e2e",
  "red_test": "Com TINY_DISABLED=true (sem qualquer ida ao Tiny), uma correção de estoque deve persistir 100% no ledger (1 mov ajuste_manual + siso_estoque atualizado) e retornar 200 — provando que o saldo do WMS é a fonte e não depende de re-fetch externo (sem janela de desync). Não deve existir caminho onde o Tiny muda e o ledger não.",
  "effort": "S",
  "blast_radius": "med",
  "deps": [
   "P086"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "NOTA CUSTOM: 'gravar SÓ no ledger, atomicamente (sem Tiny) — dissolve o desync; alinhado ao P086.' Isso SUPERSEDE a opção 2 (retry+backoff). P089 é essencialmente o mesmo fix de P086 (remover Tiny → escrever ledger); o efeito colateral é que o desync deixa de existir. deps=[P086]; efetivamente o mesmo PR. effort S marginal sobre P086."
 },
 {
  "id": "P113",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/localizacoes/[id]/route.ts PATCH (l.33-60) chama atualizarLocalizacao sem nenhuma checagem; DELETE (l.62-83) chama desativarLocalizacao. src/lib/wms/localizacoes.ts: atualizarLocalizacao (l.106-119) faz update direto sem guarda; desativarLocalizacao (l.121-133) só checa siso_estoque.saldo>0 — NÃO checa lock de contagem ativa. Lock ativo de contagem existe em siso_localizacao_locks (finalizado_em IS NULL), criado em inventario.ts:147-166 ao iniciar sessão.",
  "root_cause": "Nem o PATCH (troca de tipo) nem o DELETE (desativar) consultam siso_localizacao_locks pra ver se a loc está sob contagem ativa (finalizado_em IS NULL). Operador B muda tipo/desativa enquanto A conta.",
  "change_sites": [
   "src/lib/wms/localizacoes.ts:106-119 — em atualizarLocalizacao, se patch.tipo ou patch.ativo===false, antes do update SELECT siso_localizacao_locks WHERE localizacao_id=id AND finalizado_em IS NULL LIMIT 1; se houver, throw 'localização em contagem — não pode alterar tipo/desativar'",
   "src/lib/wms/localizacoes.ts:121-133 — em desativarLocalizacao, adicionar a mesma checagem de lock ativo antes (ou em adição à) checagem de saldo",
   "src/app/api/wms/localizacoes/[id]/route.ts:49-50 — mapear o erro 'em contagem' pra status 409 (conflito) no PATCH/DELETE (wmsErrorResponse já mascara; garantir 4xx explícito)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/46-localizacao-bloqueada-em-contagem.ts",
  "red_test": "Com uma sessão de inventário em_andamento cobrindo a loc X (lock ativo em siso_localizacao_locks), PATCH /api/wms/localizacoes/X {tipo:'quarentena'} deve retornar erro (409) e o tipo NÃO mudar; DELETE também deve falhar. Após finalizar/aprovar a sessão (lock finalizado), a mesma operação passa.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'bloquear troca de tipo/delete enquanto há contagem ativa'. A guarda no service (localizacoes.ts) cobre todos os callers. Manter checagem de saldo existente no delete. Requer permissão requireAdmin já presente na rota."
 },
 {
  "id": "P126",
  "current_status": "open",
  "status_evidence": "Dashboard conta locks órfãos em siso_localizacao_locks (dashboard-geral.ts:54-58: finalizado_em IS NULL AND iniciado_em < ha1h). O cleanup existente recoveryInventario (src/lib/wms/inventario-recovery.ts) só mexe em siso_inventario_localizacoes (status/bloqueada_por) — NUNCA toca siso_localizacao_locks. grep confirma: nenhum cleanup de siso_localizacao_locks fora de aprovar/finalizar/sair-party (que falham → órfão). A cron /api/wms/inventario/cleanup chama recoveryInventario que não fecha esses locks externos.",
  "root_cause": "Locks externos em siso_localizacao_locks ficam com finalizado_em NULL pra sempre quando a liberação síncrona (aprovarSessao/finalizar/sair-party) falha. Não há varredura que feche locks externos antigos. O dashboard então os reporta como 'travado >1h' indefinidamente, e roteamento/sugestao-dinamica os tratam como loc bloqueada.",
  "change_sites": [
   "src/lib/wms/inventario-recovery.ts:138-155 — adicionar passo 5: UPDATE siso_localizacao_locks SET finalizado_em=now(), observacoes='auto-cleanup >24h' WHERE finalizado_em IS NULL AND iniciado_em < now()-interval '24h'; retornar contagem locksExternosLiberados; logger.warn com id/iniciado_por/iniciado_em (registrar quem/o quê)",
   "src/app/api/wms/inventario/cleanup/route.ts — já existe e roda recoveryInventario; garantir que a cron chame a cada 5min (config de cron externa — manual_only no harness, validar via smoke .sql como os outros crons)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "durable_enqueue_retry",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/inventario-recovery.test.ts",
  "red_test": "recoveryInventario deve fechar (set finalizado_em) locks em siso_localizacao_locks com finalizado_em NULL e iniciado_em > 24h atrás, retornando a contagem e logando iniciado_por; locks < 24h permanecem abertos. (vitest com mock do supabase client, asserindo a query de update com cutoff de 24h e o WHERE finalizado_em IS NULL.)",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: limpeza automática de locks de inventário >24h a cada 5min, registrando quem/o quê. O cutoff é 24h (nota), distinto do threshold >1h só de exibição do dashboard. A cron de 5min já existe (inventario/cleanup chama recoveryInventario); só falta o passo de fechar siso_localizacao_locks externos. Vitest unit é viável mockando o client (padrão de inventario-recovery)."
 },
 {
  "id": "P146",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/lancamento-retroativo/route.ts POST (l.24-100) não tem nenhuma chave de idempotência — chama lancarRetroativo (movimentacoes.ts:520-541) que faz inserirMovimentacao tipo 'E' direto. Dois POSTs idênticos criam 2 movs E de qty cada. siso_movimentacoes não tem coluna idempotency_key nem unique constraint pra retroativos. A page atual (retroativos/page.tsx) é só a lista de reconciliação (botão 'Reconciliar', l.160-176) — o 'Lançar' fica em outra UI/modal que chama esse POST.",
  "root_cause": "Rota de lançamento retroativo não tem idempotency key — duplo clique (rede lenta) cria 2 entradas E e infla saldo + recalcula custo médio errado.",
  "change_sites": [
   "supabase/migrations/NOVA_retroativo_idempotency.sql — adicionar coluna idempotency_key text em siso_movimentacoes (nullable) + UNIQUE PARTIAL INDEX (idempotency_key) WHERE idempotency_key IS NOT NULL",
   "src/lib/wms/movimentacoes.ts:520-541 — lancarRetroativo aceita idempotency_key opcional e repassa pra inserirMovimentacao",
   "src/lib/wms/ledger.ts (inserirMovimentacao) — propagar idempotency_key pro insert/RPC; em violação de unique (23505) retornar a mov existente ou erro idempotente claro",
   "src/app/api/wms/lancamento-retroativo/route.ts:80-91 — ler body.idempotency_key (obrigatório ou gerar server-side determinístico) e repassar; em duplicata retornar 200 com a mov original (não criar segunda)",
   "src/app/wms/retroativos/* (form de criação) — gerar/enviar idempotency_key (uuid) por submissão"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/47-retroativo-idempotente.ts",
  "red_test": "Dois POSTs /api/wms/lancamento-retroativo com a MESMA idempotency_key (qty 100) devem resultar em UMA única mov E e saldo +100 (não +200); o segundo retorna a mov original. Sem a key (ou keys diferentes) cria duas. Custo médio só recalcula uma vez.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota explícita: código único (idempotency) por lançamento retroativo, recusa repetição mesmo horas depois — descarta as opções de debounce-2s/janela-de-segundos. Coluna+índice unique no ledger é a forma durável. P184 também pede idempotency no banco local; alinhar o mecanismo (mesma coluna idempotency_key em siso_movimentacoes) pra não duplicar infra."
 },
 {
  "id": "P147",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts reconciliarRetroativo (l.599-633) faz inserirMovimentacao tipo 'S' com qty = Number(m.quantidade) (a qty original total, l.620). Se o saldo livre atual < qty original, inserirMovimentacao/wms_inserir_movimentacao falha por saldo insuficiente — reconciliação trava e o retroativo fica nem estornado nem reconciliado. Nenhuma leitura de saldo disponível atual nem opção de estorno parcial.",
  "root_cause": "reconciliarRetroativo sempre tenta estornar a qty total original; não consulta o saldo disponível atual nem permite estorno parcial quando parte já foi vendida.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:613-628 — antes do inserirMovimentacao 'S', ler saldo disponível atual da tripla em siso_estoque; aceitar parâmetro qty_estorno opcional; default = min(qty_original, disponível); gravar em origem_detalhes a qty_original vs qty_estornada (parcial)",
   "src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:26-37 — retornar (em GET/preflight ou em erro 409) o saldo disponível atual pra UI perguntar 'tem 70 livres; estornar 70?'; aceitar body.qty_estorno",
   "src/app/wms/retroativos/page.tsx:160-186 — fluxo de confirmação: ao reconciliar, se disponível < total, exibir modal com qty disponível e deixar operador confirmar estorno parcial"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/48-retroativo-estorno-parcial.ts",
  "red_test": "Lançar retroativo de 100, vender/baixar 30 (disponível=70), depois reconciliar: o sistema deve ler 70 disponíveis e permitir estornar só 70 (não falhar com 'saldo insuficiente'); a mov de estorno fica com qty 70 e origem_detalhes registrando parcial; o retroativo sai da lista de pendentes (ou marca parcial).",
  "deps": [
   "P148"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: reconciliação avisa saldo disponível e deixa estornar só o que tem (parcial). A opção escolhida (1) pede CONFIRMAÇÃO do operador — não estorno silencioso. Depende de P148 (guard de double-estorno) pra o parcial não ser re-disparado. Estorno parcial muda a semântica de 'reconciliado' — definir se vira status parcial ou some da lista."
 },
 {
  "id": "P148",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts reconciliarRetroativo (l.599-633) NÃO verifica se já existe estorno (siso_movimentacoes.estorno_de = retroativo_mov_id) antes de inserir o 'S'. Compare com listarRetroativosPendentes (l.581-590) que SABE detectar estornos existentes mas só pra filtrar a lista — a função de reconciliar não reusa essa checagem. reverterReplenishment (l.646+) usa estornarMovimentacao que tem guard de double-estorno, mas reconciliarRetroativo usa inserirMovimentacao direto sem guard.",
  "root_cause": "reconciliarRetroativo não checa estorno pré-existente ligado ao lançamento; duplo clique cria dois estornos S, dobrando a baixa de saldo.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:602-613 — no início de reconciliarRetroativo, SELECT id FROM siso_movimentacoes WHERE estorno_de = input.retroativo_mov_id LIMIT 1; se existir, throw/return idempotente 'lançamento já reconciliado' (não criar segundo S)",
   "src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:70-78 — mapear esse erro pra 409/200 idempotente claro ('já reconciliado')"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/49-retroativo-reconciliar-idempotente.ts",
  "red_test": "Reconciliar o mesmo retroativo_mov_id duas vezes: a primeira cria UM estorno S; a segunda é recusada (já existe estorno_de apontando pra ele) e NÃO cria segundo S — saldo baixa exatamente uma vez.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: recusar reconciliação se já existe estorno ligado ao lançamento (opção 2, mais simples que carimbar data). A checagem de estorno_de já existe em listarRetroativosPendentes — basta reusar a mesma lógica como guard antes do insert. Idealmente a checagem+insert deveriam ser atômicos; pra robustez total contra corrida exata, um UNIQUE PARTIAL INDEX em estorno_de WHERE estorno_de IS NOT NULL daria garantia de banco (mencionar, mas a checagem JS atende a decisão)."
 },
 {
  "id": "P160",
  "current_status": "open",
  "status_evidence": "src/lib/wms/inventario.ts aprovarSessao (l.930-967): após checar divergências pendentes, faz UPDATE siso_inventario_sessoes SET status='aprovada' (l.946-949) com WHERE id=sessaoId — SEM .eq('status','revisao'/'em_revisao'). Não há leitura prévia do status atual nem guard contra re-aprovação. Compare com aplicarSessao (l.986-987) que TEM guard de idempotência por status. Duas rotas chamam: aprovar-sessao/route.ts:42 e aprovar/route.ts:60.",
  "root_cause": "aprovarSessao atualiza status sem condicionar à etapa atual; duas aprovações concorrentes ambas passam, reprocessando a contagem.",
  "change_sites": [
   "src/lib/wms/inventario.ts:946-949 — adicionar .eq('status','revisao') no UPDATE (o status pós-finalizar é 'revisao' conforme l.916) e .select() pra checar linhas afetadas; se 0 linhas, ler status atual e throw 'sessão já aprovada / não está em revisão'",
   "src/lib/wms/inventario.ts:951-966 — só liberar locks se a aprovação efetivamente transicionou (evita liberar locks numa 2ª chamada no-op)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/50-aprovar-sessao-idempotente.ts",
  "red_test": "Aprovar a mesma sessão de inventário duas vezes: a primeira transiciona revisao→aprovada; a segunda é recusada/no-op (status já é 'aprovada', não 'revisao') e NÃO reaplica nem dispara efeitos colaterais. Asserir que aprovada_por não é sobrescrito pela 2ª chamada.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: só aprova se etapa ainda é em_revisao (bloqueia 2ª aprovação). Atenção ao valor exato do status: o código usa 'revisao' (l.916 finalizarSessao seta status='revisao'), não 'em_revisao' — usar 'revisao'. O UPDATE condicional .eq('status','revisao')+.select() é o atomic compare-and-set no Postgres (não precisa SELECT FOR UPDATE)."
 },
 {
  "id": "P161",
  "current_status": "open",
  "status_evidence": "Backend: src/app/api/wms/inventario/[id]/divergencias/route.ts PATCH (l.76-87) filtra .eq('status','pendente') — uma divergência já aprovada/rejeitada NÃO pode ser re-decidida pela rota (update casa 0 linhas). Frontend: divergencias/page.tsx os botões Aprovar/Rejeitar só renderizam quando pendente (l.371 'pendente &&', l.313); não há botão 'Reabrir'. Logo só admin/DB consegue reverter — exatamente o 'today'.",
  "root_cause": "Tanto a rota (guard .eq status=pendente) quanto a UI (ações só pra pendente) impedem refazer a decisão; falta um caminho de 'Reabrir' que volte a divergência pra pendente e permita nova aprovação/rejeição.",
  "change_sites": [
   "src/app/api/wms/inventario/[id]/divergencias/route.ts:62-67 — aceitar acao='reabrir' que faz UPDATE status='pendente', limpando resolucao_por/resolucao_em (permitido só enquanto a sessão NÃO está aplicada; bloquear reabrir se sessão já aplicada no ledger)",
   "src/app/api/wms/inventario/[id]/divergencias/route.ts:84-86 — pra 'reabrir', o WHERE deve casar status IN ('aprovada','rejeitada') em vez de 'pendente'",
   "src/app/wms/inventario/[id]/divergencias/page.tsx:367-409 — adicionar botão 'Reabrir' nas linhas com status aprovada/rejeitada (e fora do bulk-bar), chamando a mutation com acao:'reabrir'"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/51-divergencia-reabrir.ts",
  "red_test": "Aprovar uma divergência, depois PATCH acao='reabrir' deve voltar status pra 'pendente' (limpando resolucao_por/em) e permitir nova decisão (aprovar/rejeitar) — sem admin. Reabrir deve ser bloqueado se a sessão já foi aplicada no ledger.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: permitir Reabrir e refazer a decisão sem admin. Guard crítico: só permitir reabrir enquanto a sessão de inventário NÃO foi aplicada (aplicarSessao gera movs no ledger; reabrir depois descasaria o saldo já lançado). requireWarehouseAccess já cobre a permissão da rota. Combina bem com P180 (realtime) pra a reabertura propagar."
 },
 {
  "id": "P164",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:42-47 só valida quantidade < 0 ('não pode ser negativa'). Zero passa: l.122-127 chama movimentarEstoque tipo 'B' (balanço) com quantidade=0 → Tiny seta saldo 0. Não há rejeição de zero. Confirma o 'today'.",
  "root_cause": "Validação só barra negativo; zero é aceito e propaga balanço 0 ao Tiny, zerando o produto sem trilha.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:42-47 — trocar a checagem 'quantidade < 0' por 'quantidade <= 0' (ou adicionar branch que rejeita 0 com mensagem 'pra zerar, use ajuste de inventário'), retornando 400"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/52-ajustar-rejeita-zero.ts",
  "red_test": "POST /api/wms/tiny/stock/ajustar com quantidade=0 deve retornar 400 ('quantidade deve ser positiva / use ajuste de inventário'); quantidade>0 passa normalmente.",
  "deps": [],
  "conflicts": [
   "P166"
  ],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: rejeitar quantidade zero na correção; pra zerar usa ajuste de inventário (opção 2). CONFLITO de coordenação com P166/P086: a nota de P086 manda PARAR de escrever no Tiny e migrar essa rota pro ledger WMS. Se a migração P086 acontecer, este guard de zero deve ser reescrito na nova rota/handler ledger — mas a regra (rejeitar 0) permanece válida. Fix isolado é trivial; idealmente entra junto da migração ledger."
 },
 {
  "id": "P165",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts escreve só no Tiny (movimentarEstoque, l.122) e re-busca (l.136); NÃO escreve no ledger WMS — l.154-159 confirmam que writes locais foram REMOVIDOS (snapshot dropado). Logo a varredura de pedidos parados, que é auto-disparada em inserirMovimentacao tipo 'E' (ledger.ts:240-256 → varrerPedidosAfetadosPorEntrada + reconciliarEntradaEstoque), NUNCA é acionada por essa correção. Pedido parado só sai por refresh manual.",
  "root_cause": "A correção de estoque não passa pelo ledger WMS, então o gatilho de varredura/reconciliação de pedidos parados (que já existe e dispara em toda entrada E) não roda quando o saldo vai de 0→positivo.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:139-167 — após confirmar a correção, se o saldo passou de 0→positivo, enfileirar/disparar a varredura. Caminho preferido: migrar a correção pra inserirMovimentacao (P086), que JÁ chama varrerPedidosAfetadosPorEntrada + reconciliarEntradaEstoque automaticamente (ledger.ts:240-256) — resolvendo P165 sem código novo de varredura",
   "src/app/api/wms/tiny/stock/ajustar/route.ts (fallback se não migrar agora) — chamar explicitamente varrerPedidosAfetadosPorEntrada({produto_id, galpao_id, localizacao_id}) fire-and-forget quando novoSaldo>0 e saldo anterior era 0"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "cache_invalidation",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/53-correcao-libera-pedidos-parados.ts",
  "red_test": "Com um pedido parado por falta (aguardando_compra) de um produto a saldo 0, corrigir o estoque pra positivo deve enfileirar a varredura e o pedido sair da fila (transicionar) sem refresh manual — asserir que varrerPedidosAfetadosPorEntrada/reconciliarEntradaEstoque foi disparada e o pedido mudou de status.",
  "deps": [
   "P166"
  ],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: ao corrigir estoque 0→positivo, enfileirar varredura de pedidos parados (libera sozinho) — opção 2. A INFRA JÁ EXISTE: ledger.ts:240-256 dispara varrerPedidosAfetadosPorEntrada+reconciliarEntradaEstoque em toda mov E. Logo, se a rota migrar pro ledger (P086/P166), P165 cai junto de graça. Marquei dep em P166 porque o caminho limpo é via a migração ledger; o fallback (chamar varredura direto) é a alternativa cirúrgica se P086 não for feito agora."
 },
 {
  "id": "P166",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts não tem lock pessimista nem write no ledger — escreve balanço direto no Tiny (l.122-127) e re-lê (l.136). l.154-159 confirmam que os writes locais foram removidos. Sem mov no ledger, não há trilha (saldo ant/post, autor, motivo) nem serialização entre duas correções concorrentes do mesmo produto. inserirMovimentacao/wms_inserir_movimentacao (ledger) faz SELECT FOR UPDATE pessimista por (produto,galpão,loc) — mas essa rota não o usa.",
  "root_cause": "Correção de estoque concorrente roda direto no Tiny sem lock pessimista e sem registrar movimentação no ledger WMS — duas correções simultâneas competem na sorte e não deixam trilha auditável.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts:120-167 — substituir o write Tiny por uma mov de correção no ledger via inserirMovimentacao (origem_tipo de ajuste/correção, com motivo+autor; saldo ant/post vem do RPC). O RPC wms_inserir_movimentacao já dá o lock pessimista (SELECT FOR UPDATE) e a trilha — atende lock+auditoria da nota",
   "src/lib/wms/ledger.ts / movimentacoes.ts — se não houver helper de 'correção/ajuste de saldo' (balanço → diferença E ou S), adicionar um (calcula delta vs saldo atual e gera E ou S com origem_tipo de ajuste)",
   "src/app/api/wms/tiny/stock/ajustar/route.ts:121-137 — remover/condicionar o movimentarEstoque no Tiny conforme decisão de P086 (Tiny é legado, não escrever ajuste lá)"
  ],
  "needs_migration": true,
  "rpc_needed": "Reusar wms_inserir_movimentacao (já tem lock pessimista + saldo ant/post). Possivelmente um wrapper plpgsql wms_corrigir_saldo(produto,galpao,loc,saldo_alvo,usuario,motivo) que calcula o delta dentro do FOR UPDATE e insere E ou S atomicamente — evita TOCTOU entre ler saldo atual e calcular delta no app.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/54-correcao-concorrente-ledger.ts",
  "red_test": "Duas correções concorrentes do mesmo (produto,galpão,loc) devem serializar (lock pessimista) e ambas gerar mov no ledger com saldo_anterior/posterior coerentes e encadeados (sem perda/sobrescrita); a trilha registra autor+motivo de cada uma. Saldo final = última correção aplicada, sem corrida.",
  "deps": [
   "P086"
  ],
  "conflicts": [
   "P164",
   "P184"
  ],
  "effort": "L",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota: lock pessimista + registrar no ledger na correção concorrente (auditoria; ALINHA P086). Esta é a cabeça da migração: P086 decide migrar a correção do Tiny pro ledger WMS. P164 (rejeitar 0), P165 (varredura auto pós-E) e P184 (idempotency banco local) TODOS dependem/convergem nessa migração. Sugiro tratar P086+P166 como um único PR de migração ledger, e P164/P165/P184 como ajustes dentro dele. blast_radius high: muda o backend da correção de estoque e a fonte de verdade (Tiny→ledger)."
 },
 {
  "id": "P180",
  "current_status": "open",
  "status_evidence": "src/app/wms/inventario/[id]/divergencias/page.tsx usa só useQuery (l.35-41) sem nenhuma assinatura realtime — não importa useInventarioRealtime. O hook src/hooks/use-inventario-realtime.ts existe e assina contagens/localizacoes/operadores/sessoes (l.100-174) mas NÃO assina siso_inventario_divergencias. Logo, aprovação de divergência por B não chega à aba de A; recarregar 'faz a divergência sumir' do lado de A (today confirmado).",
  "root_cause": "A tela de divergências não tem realtime e o hook de inventário não cobre a tabela siso_inventario_divergencias; updates de status (aprovar/rejeitar/reabrir) por outro supervisor não propagam.",
  "change_sites": [
   "src/hooks/use-inventario-realtime.ts:154-173 — adicionar .on('postgres_changes',{event:'UPDATE',table:'siso_inventario_divergencias',filter:`sessao_id=eq.${sessaoId}`}, ...) atualizando estado/invalidando; expor as divergências ou um sinal de invalidação",
   "src/app/wms/inventario/[id]/divergencias/page.tsx:35-41 — assinar realtime (usar o hook ou um pequeno channel local) e invalidateQueries(['wms-inv-div', id]) ao receber UPDATE de divergência, pra a aba de A refletir a decisão de B na hora"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "manual_only",
  "test_file": "scripts/wms/cenarios/smoke-p1-realtime-publication.sql",
  "red_test": "Verificar (smoke SQL) que siso_inventario_divergencias está na publication de Realtime; e (manual/2-abas) que aprovar uma divergência numa aba atualiza a outra aba sem refresh manual. O comportamento de UI não é testável em vitest/scenarios HTTP — manual_only + smoke de publication.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: notificação em tempo real (realtime já existe) quando supervisor aprova divergência. O hook já existe mas não cobre divergencias — precisa adicionar a tabela à subscription E garantir que siso_inventario_divergencias está na publication supabase_realtime (checar migration). Combina com P161 (Reabrir) pra propagar a reabertura também."
 },
 {
  "id": "P184",
  "current_status": "open",
  "status_evidence": "src/lib/tiny-api.ts movimentarEstoque (próximo a l.553-575) e a rota tiny/stock/ajustar não gravam mov no ledger local nem usam idempotency key local — só chamam o Tiny (balanço 'B' é idempotente no Tiny, daí 'sem problema hoje'). Mas a nota antecipa: quando a correção passar a registrar no ledger (P086/P166), duplo Enter geraria 2 movs locais sem uma idempotency key no banco local.",
  "root_cause": "Não há chave de idempotência no banco local para a operação de correção; hoje é inofensivo porque nada é gravado localmente, mas vira duplicação de trilha assim que a correção escrever no ledger.",
  "change_sites": [
   "supabase/migrations/NOVA_retroativo_idempotency.sql (MESMA de P146) — coluna idempotency_key text em siso_movimentacoes + unique partial index; reusar pra correções de saldo",
   "src/app/api/wms/tiny/stock/ajustar/route.ts — ao migrar pro ledger (P166), passar idempotency_key (uuid por submissão) à inserirMovimentacao; duplicata (23505) retorna a mov existente",
   "src/lib/wms/ledger.ts (inserirMovimentacao) — suportar idempotency_key e tratar conflito como no-op idempotente"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "integration",
  "test_file": "scripts/wms/cenarios/catalogo/55-correcao-idempotente-local.ts",
  "red_test": "Após a correção escrever no ledger (P166), dois POSTs com a mesma idempotency_key devem gerar UMA única mov no banco local (não duas), mesmo separados por horas; keys diferentes geram duas.",
  "deps": [
   "P146",
   "P166"
  ],
  "conflicts": [
   "P166"
  ],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: chave de ID único (idempotency) também no banco local, além do Tiny. Severidade leve — só ganha relevância DEPOIS que a correção começar a gravar no ledger (P166/P086). Antes disso é inerte (nada local pra duplicar). Reusar a mesma coluna idempotency_key + índice de P146 evita duplicar infra; daí dep em P146 (mecanismo) e P166 (quando a correção passa a gravar local)."
 },
 {
  "id": "P001",
  "current_status": "open",
  "status_evidence": "src/lib/webhook-processor-wms.ts:540-560 faz upsert dos itens com `quantidade_pedida: item.quantidade` sem nenhuma checagem de qty>0; resolverItensWms (linhas 85-98) e expandirKits (247-256) propagam `quantidade` cru do Tiny. A unica barreira hoje e ledger.ts:46 `if (qty <= 0) throw new Error('qty deve ser > 0')` em validarCoerencia — mas isso so dispara LA NA FRENTE quando a RPC tenta lancar/reservar, deixando o pedido inteiro travado em estado de erro, exatamente o sintoma descrito no `today`. Nao ha validacao na CHEGADA do webhook.",
  "root_cause": "O caminho de entrada do pedido (processWebhookWms) nao valida quantidade dos itens. Itens com quantidade=0 vindos do Tiny sao aceitos, roteados e so falham no momento de reservar/lancar (ledger.ts:46), travando o pedido sem mensagem clara de qual item esta errado.",
  "change_sites": [
   "src/lib/webhook-processor-wms.ts:384-392 — apos resolverItensWms(), antes do roteamento (linha 394), adicionar guarda: se algum item de itensResolvidos tiver quantidade<=0, logar warn nomeando o(s) item(ns) (sku/tinyProdutoId) e rejeitar o pedido inteiro (marcar webhook_logs como 'erro'/'rejeitado' com motivo, NAO gravar siso_pedidos). Retornar early com status indicando rejeicao por qty=0.",
   "src/lib/webhook-processor-wms.ts:638-645 — garantir que o branch de rejeicao atualize siso_webhook_logs com status apropriado (ex: 'rejeitado') e processado_em, em vez de 'concluido'."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/82-webhook-rejeita-item-qty-zero.ts",
  "red_test": "Disparar um webhook (via /api/wms/webhook/tiny ou fake:webhook) com um pedido onde um item tem quantidade=0. Asseverar: o pedido NAO e gravado em siso_pedidos (ou e marcado rejeitado), nenhuma reserva R e criada, o webhook_log fica com status de rejeicao, e o log nomeia o sku/item com qty=0. Hoje falha porque o pedido e aceito e so quebra no ledger.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota = 'OPCAO 1 VALIDAR' (rejeitar pedido inteiro se qualquer item tiver qty=0). Validar na chegada do webhook, nomeando o item errado no log, como a question pede. Decidi colocar a guarda em processWebhookWms (entrada real) e nao em ledger.ts — a validacao de ledger ja existe mas e tarde demais e nao identifica o item."
 },
 {
  "id": "P003",
  "current_status": "open",
  "status_evidence": "src/lib/webhook-processor-wms.ts:437-456 so faz dedup por `estoque_lancado===true` (cobre re-entrega de webhook ja totalmente processado, NAO o caso de duplo-clique de reprocessamento antes do lancamento). O loop de reserva em :583-606 chama reservarAtomico() sem nenhuma checagem de R viva pre-existente. reservarAtomico (src/lib/wms/reservas.ts:20-36) so chama a RPC. A RPC wms_reservar_atomico (supabase/migrations/20260520c_rpc_reservar.sql:24-32) e um wrapper puro de wms_inserir_movimentacao com tipo='R' — SEM idempotencia: sempre insere uma nova R. Dois reprocessos rapidos => duas R por (pedido,produto) => reservado dobrado.",
  "root_cause": "Nenhuma camada (rota, app, ou RPC) checa se ja existe uma reserva R viva por (pedido, produto) antes de inserir nova R. O early-return por estoque_lancado nao cobre o pedido ainda nao lancado sendo reprocessado duas vezes em paralelo.",
  "change_sites": [
   "src/lib/wms/reservas.ts:20-36 — em reservarAtomico, ANTES de chamar a RPC, consultar siso_movimentacoes por (origem_id=pedido_id, origem_tipo='reserva_pedido', tipo='R', produto_id=tripla.produto_id) que NAO tenha L correspondente (R viva, ainda nao liberada); se existir, retornar o id da R existente (skip) sem inserir nova. SEM advisory lock.",
   "src/lib/webhook-processor-wms.ts:583-606 — alternativa/complemento: no loop de reserva, deixar reservarAtomico fazer o skip; nao precisa duplicar a logica aqui. Garantir que o skip nao quebre o try/catch existente."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "integration",
  "test_file": "src/lib/wms/reservas-idempotente.integration.test.ts",
  "red_test": "Para um mesmo (pedido, produto, tripla), chamar reservarAtomico() duas vezes (simulando duplo reprocesso). Asseverar: existe exatamente UMA mov R viva em siso_movimentacoes para esse (pedido,produto) e o `reservado` em siso_estoque reflete a qty uma unica vez (nao dobrado). Hoje falha porque a 2a chamada cria uma 2a R e dobra o reservado.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota e VINCULANTE e explicita: 'RESOLVIDO: dedup idempotente — antes de reservar, checar mov R viva por (pedido,produto) e pular. SEM advisory lock (infra nova descartada).' Ancorei o change_site em reservarAtomico (ponto unico por onde toda reserva de pedido passa: webhook, aprovar, pick-cascade). A nota diz 'RESOLVIDO' mas o codigo ATUAL nao tem a checagem — marquei open. O primitivo de schema mais proximo seria db_constraint_unique (R viva unica por pedido+produto), mas a nota descartou infra nova/lock, entao a estrategia escolhida e check-and-skip na app (rotulei frontend_idempotency_ux por ser dedup idempotente de duplo-disparo; a essencia e idempotencia, nao UX de browser)."
 },
 {
  "id": "P071",
  "current_status": "partially_fixed",
  "status_evidence": "Backend JA idempotente: src/app/api/wms/vendas/criar/route.ts:170-190 consulta siso_pedidos por payload_original->>idempotency_key (origem_pedido='manual', status!=cancelado) e retorna o pedido existente com `idempotente:true`. POReM o frontend gera a key A CADA CLIQUE: src/app/wms/vendas/nova/page.tsx:183 `idempotency_key: crypto.randomUUID()` dentro de submit(). Como dois cliques geram keys diferentes, o backend nao reconhece como mesmo pedido e cria dois. Nao ha useState/useRef estavel pra key (vide declaracoes em page.tsx:114-126).",
  "root_cause": "A idempotency key e gerada por clique (no corpo de submit, linha 183) em vez de uma vez na abertura do formulario. Isso anula a protecao de idempotencia ja existente no backend quando o guard de UI (disabled) falha.",
  "change_sites": [
   "src/app/wms/vendas/nova/page.tsx:114-126 — adicionar um estado/ref estavel para a key: `const idempotencyKey = useRef(crypto.randomUUID())` (ou useState inicializado uma vez no mount do NovaVendaBody).",
   "src/app/wms/vendas/nova/page.tsx:183 — trocar `idempotency_key: crypto.randomUUID()` por `idempotency_key: idempotencyKey.current` (reusar a mesma key em todo clique).",
   "src/app/wms/vendas/nova/page.tsx:166-230 (handler submit) — apos criacao bem-sucedida nao-idempotente, regenerar a key (idempotencyKey.current = crypto.randomUUID()) para permitir uma proxima venda nova, conforme a question (reusar por 5min/ate resposta)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/app/wms/vendas/nova/idempotency-key.test.tsx",
  "red_test": "Renderizar NovaVendaPage e simular dois cliques no botao de criar venda (sem desabilitar). Asseverar que o payload enviado em ambos os cliques contem a MESMA idempotency_key. Hoje falha porque cada submit gera uma key nova (crypto.randomUUID()).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota = 'gerar idempotency key ao abrir o formulario; reusar em todo clique; backend reconhece e retorna pedido ja criado'. O backend ja cumpre sua parte (criar/route.ts:170-190); o fix restante e puramente no frontend (mover a geracao da key pra fora de submit). Por isso partially_fixed e effort S."
 },
 {
  "id": "P091",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/tiny/stock/ajustar/route.ts:108-115 le `deposito_id` AO VIVO de siso_tiny_connections por empresa.id (nao do pedido). A coluna siso_pedidos.deposito_id existe (migration 20260309_add_deposito_columns.sql) mas NAO e escrita no roteamento: grep nao acha nenhum write de pedidos.deposito_id em webhook-processor-wms.ts (o upsert em :502-535 grava separacao_galpao_id mas nao deposito_id). Logo o deposito usado pra falar com o Tiny segue a config global mutavel, nao o snapshot do pedido. OBS: a rota stock/ajustar nao tem nenhum caller no codigo (grep em src/+scripts so acha o proprio arquivo e docs) — caminho efetivamente dormente.",
  "root_cause": "O galpao/deposito nao e congelado no pedido no momento da rota; quem precisa ajustar estoque no Tiny re-deriva o deposito da config viva de siso_tiny_connections. Se admin muda a config entre rota e ajuste, o Tiny recebe deposito errado.",
  "change_sites": [
   "src/lib/webhook-processor-wms.ts:502-535 — no upsert de siso_pedidos, gravar tambem o `deposito_id` (e galpao) resolvido no momento da rota (buscar de siso_tiny_connections da empresa origem uma vez, no roteamento, e persistir).",
   "src/app/api/wms/tiny/stock/ajustar/route.ts:52-115 — ler `deposito_id` (e galpao) do proprio pedido (siso_pedidos) em vez de re-consultar siso_tiny_connections ao vivo; usar o valor congelado pra montar a chamada ao Tiny."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/83-deposito-congelado-no-pedido.ts",
  "red_test": "Rotear/criar um pedido com deposito A; depois mudar a config da empresa pra deposito B; chamar o fluxo de ajuste de estoque. Asseverar que o ajuste usa o deposito A (gravado no pedido), nao o B (config nova). Hoje falha porque a rota le o deposito vivo de siso_tiny_connections.",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "siso_pedidos.deposito_id ja existe (20260309); nao precisa nova migration, so passar a escrever.",
  "respeita_nota": true,
  "notes": "Nota = 'gravar o galpao no pedido ao rotear e usar sempre esse ate o fim (imune a mudanca de config)' (opcao 2). IMPORTANTE: a rota tiny/stock/ajustar referenciada esta SEM CALLERS (dead path) hoje — Tiny virou camada fiscal e o estoque foi internalizado. O blast_radius e baixo e o impacto pratico atual e residual. Recomendo confirmar com o usuario se ainda vale persistir o deposito no pedido agora ou tratar junto da futura sync marketplace<->WMS (mesma area do P092). primitivo melhor encaixe seria 'outro' (snapshot de config no pedido); usei data_visibility_ux por aproximacao."
 },
 {
  "id": "P092",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/webhook/tiny/route.ts:191-245 trata cancelamento liberando as reservas R locais via estornarReservaIndividual (linhas 210-245) mas NUNCA chama o Tiny pra estornar/devolver saldo: grep por movimentarEstoque/getEstoque/estorno tiny na rota retorna vazio (EXIT 1). Logo o saldo no Tiny permanece baixo apos cancelamento.",
  "root_cause": "Por design atual: cancelamento so reverte o ledger interno do WMS; nao ha integracao de volta com o Tiny pra repor saldo do marketplace.",
  "change_sites": [
   "Nenhum agora — decisao do usuario e DEFERIR. (Se fosse implementar: src/app/api/wms/webhook/tiny/route.ts:191-245 chamaria um ajuste no Tiny apos liberar as Rs; mas a nota proibe.)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "outro",
  "test_harness": "manual_only",
  "test_file": "",
  "red_test": "N/A — decisao e nao tratar via estorno no Tiny por ora. Nenhum teste RED deve ser escrito agora; o comportamento 'correto' depende da futura sync marketplace<->WMS que ainda nao existe.",
  "deps": [],
  "conflicts": [
   "P091"
  ],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota CUSTOM e VINCULANTE: 'FUTURO: usuario vai sincronizar o estoque do marketplace direto com o WMS. Nao tratar via estorno no Tiny por ora — resolver quando a sync marketplace<->WMS existir.' Portanto: NAO implementar agora. Marquei current_status=open (o bug existe) mas respeita_nota=true porque a decisao e explicitamente diferir. Conflito leve com P091: ambos tocam a area Tiny<->WMS; a solucao real dos dois sai junto quando a sync marketplace<->WMS for construida. Efeito pratico minimizado porque Tiny ja e so camada fiscal."
 },
 {
  "id": "P093",
  "current_status": "partially_fixed",
  "status_evidence": "FRONTEND parcialmente coberto: na tela de conexoes (src/app/wms/configuracoes/conexoes/page.tsx:807) o botao 'Salvar' do bloco de deposito tem `disabled={saving || selectedId === null}` — nao salva sem deposito selecionado. POReM o BACKEND nao valida: PUT /api/wms/tiny/connections (src/app/api/wms/tiny/connections/route.ts:182-184) aceita `deposito_id` de qualquer valor, inclusive null, sem rejeitar. E a rota stock/ajustar:143-151 ainda faz fallback `estoqueAtualizado.depositos[0]` quando depositoId e null (e :125 omite o deposito da chamada ao Tiny se null => Tiny usa o primeiro). Sem caller hoje, mas a logica de fallback inseguro persiste.",
  "root_cause": "Falta a 'dupla seguranca' pedida: o backend (PUT connections) nao impede salvar conexao sem deposito, e o consumidor (stock/ajustar) cai em depositos[0] silenciosamente quando deposito_id e null, podendo ajustar o galpao errado.",
  "change_sites": [
   "src/app/api/wms/tiny/connections/route.ts:182-197 — no PUT, quando body.deposito_id===null e a conexao esta sendo marcada ativa (ou ja ativa), rejeitar com 400 ('selecione um deposito antes de salvar'); nao permitir persistir conexao ativa sem deposito_id.",
   "src/app/api/wms/tiny/stock/ajustar/route.ts:115 + 143-151 — quando depositoId===null, retornar erro claro (galpao/deposito nao configurado) em vez de cair em depositos[0]; remover o fallback inseguro.",
   "src/app/wms/configuracoes/conexoes/page.tsx:807 — ja cobre o caso happy-path (disabled sem deposito); manter, e garantir que a flag 'ativo' nao possa ser ligada sem deposito."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/auth/09-connection-sem-deposito-bloqueia.ts",
  "red_test": "PUT /api/wms/tiny/connections com deposito_id=null marcando a conexao ativa: asseverar 400/rejeicao. E: chamar o fluxo de ajuste de estoque para uma empresa cuja conexao tem deposito_id=null: asseverar erro claro (nao cair no primeiro deposito da lista). Hoje ambos passam silenciosamente.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota = 'op1+op2: tela nao salva sem galpao/deposito E backend valida (dupla seguranca)'. A tela JA bloqueia salvar deposito sem selecao (conexoes/page.tsx:807) => por isso partially_fixed; falta o backend validar (PUT connections) e remover o fallback inseguro depositos[0] em stock/ajustar. Mesma ressalva do P091: stock/ajustar nao tem callers (dead path), entao o risco real e residual; o ponto que ainda IMPORTA e o PUT connections nao deixar gravar conexao ativa sem deposito."
 },
 {
  "id": "P094",
  "current_status": "open",
  "status_evidence": "src/lib/ml-oauth.ts:320-332: apos o refresh bem-sucedido, faz `await supabase.from('siso_ml_connections').update({...access_token, refresh_token...})` SEM checar o retorno {error}. Se a gravacao falha, o refresh_token novo (single-use) e perdido e a funcao retorna `tokens.access_token` mesmo assim (linha 340) como se tivesse salvo. O bloco de retry existente (linhas 383-427, '__retry_claim__') cobre apenas a reivindicacao do lease, NAO a persistencia do token.",
  "root_cause": "O update de persistencia do token renovado nao trata erro nem tem retry; falha silenciosa deixa o banco com refresh_token velho (ja invalidado pelo ML), desconectando a conta no proximo uso.",
  "change_sites": [
   "src/lib/ml-oauth.ts:320-332 — capturar o {error} do update; se erro, tentar de novo (ate ~3x com pequeno backoff). Se persistir, escrever ultimo_erro na conexao (ou logger.logError category='auth' severity='critical') e lancar/sinalizar falha em vez de retornar o access_token como se tudo ok. Como refresh_token e single-use, a falha de persistencia precisa ser visivel (alerta) para forcar reconexao."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "durable_enqueue_retry",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/ml-oauth-persist-token.test.ts",
  "red_test": "Mockar o supabase update de siso_ml_connections para retornar {error} (ou falhar). Asseverar: a funcao tenta novamente (>1 chamada de update) e, persistindo a falha, registra erro/alerta (logger.logError ou ultimo_erro setado) e NAO retorna silenciosamente o access_token como sucesso. Hoje falha porque o update nao checa error e a funcao retorna ok.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota = 'retentar salvar token ML + alertar se persistir' (opcao 3 = ambas). O bloco de retry existente em ml-oauth.ts e so pro lease (__retry_claim__), nao pro save do token. O fix e cirurgico no update das linhas 320-332. Usei primitivo durable_enqueue_retry por ser retry-com-alerta de uma escrita duravel."
 },
 {
  "id": "P098",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:54-60: quando nao ha mapeamento Tiny ativo, sincronizarProduto apenas `logger.warn(...)` e faz `return;` (void, sem erro). O caller src/app/api/wms/produtos/[id]/sync/route.ts:14-16 chama `await sincronizarProduto(id)` e retorna `{ ok: true }` — entao o operador ve sucesso enquanto NADA foi sincronizado. Mesmo padrao silencioso em backfill-imagens/route.ts:73.",
  "root_cause": "sincronizarProduto trata 'sem mapeamento/conexao Tiny' como no-op silencioso (return void) em vez de erro; a rota nao distingue 'sincronizou' de 'pulou por falta de mapeamento', retornando ok:true em ambos.",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:54-60 — quando !mapeamento (nenhuma siso_produto_empresas ativa / conexao Tiny perdida), em vez de `return;`, lancar Error claro (ex: 'Produto sem mapeamento Tiny ativo — sincronizacao impossivel') OU retornar um resultado tipado {ok:false, motivo} que o caller interprete.",
   "src/app/api/wms/produtos/[id]/sync/route.ts:14-26 — se sincronizarProduto lancar (ou devolver ok:false), retornar erro 4xx via wmsErrorResponse com mensagem clara ('mapeamento Tiny perdido') em vez de {ok:true}. O try/catch ja existe (linhas 17-26); basta o throw na lib chegar ate ele.",
   "src/app/api/wms/produtos/backfill-imagens/route.ts:73 — opcional: agregar/contabilizar produtos pulados por falta de mapeamento e reporta-los no resultado, sem mascarar como sucesso total."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/sync-tiny-sem-mapeamento.test.ts",
  "red_test": "Chamar sincronizarProduto(produtoId) para um produto SEM siso_produto_empresas ativo (mapeamento perdido). Asseverar que lanca Error (ou retorna ok:false) — e que a rota POST /produtos/[id]/sync responde com erro 4xx e mensagem clara, NAO {ok:true}. Hoje falha porque a funcao retorna void e a rota responde sucesso.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota = 'mostrar erro vermelho claro quando mapeamento/conexao Tiny perdido; nunca sync silencioso' (opcao 2). O ponto-chave e a rota /produtos/[id]/sync devolver ok:true mesmo quando a lib pulou silenciosamente (sync-tiny.ts:54-60). Fix cirurgico: a lib lanca, a rota (que ja tem try/catch + wmsErrorResponse) surface o erro. O 'file' do raio-x era null; localizei a fonte real (sincronizarProduto) e o caller."
 },
 {
  "id": "P117",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:54-60 — quando nenhum mapeamento ativo em siso_produto_empresas é encontrado, a função apenas `logger.warn(\"produto sem mapeamento Tiny ativo\")` e `return` (void). O route src/app/api/wms/produtos/[id]/sync/route.ts:15-16 chama `await sincronizarProduto(id)` e SEMPRE retorna `{ ok: true }`. O frontend src/app/wms/produtos/page.tsx:98-99 mostra `toast.success(\"Sincronizado\")`. Logo, sem vínculo Tiny/fornecedor o admin vê 'Sincronizado' mesmo sem nada ter acontecido. Nenhum commit pós-3-jun tocou sync-tiny.ts (último: d88128b, pré-raio-x).",
  "root_cause": "sincronizarProduto retorna void silenciosamente no caso 'sem mapeamento' (early-return na linha 59) em vez de sinalizar erro pro caller. P117/P118/P171 são o MESMO defeito-raiz atingido por entradas/narrativas diferentes (P117=produto editado no fornecedor, P118=produto sem vínculo fornecedor, P171=produto sem mapeamento Tiny). A nota pede 'só avisar' — não bloquear botão, não criar mapeamento automático.",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:54-60 — substituir `logger.warn(...) + return` por `throw new Error(\"Produto sem mapeamento com o fornecedor/Tiny. Crie o vínculo manualmente antes de sincronizar.\")` (mensagem que o toast vai exibir). A função passa a retornar void só no caminho de sucesso.",
   "src/app/api/wms/produtos/[id]/sync/route.ts:15-26 — sem mudança de código necessária: o try/catch já chama wmsErrorResponse com category external_api; mas como a falta-de-mapeamento é erro 4xx do usuário (não 5xx), considerar capturar a mensagem e retornar status 400/409 com `{ error: <msg> }` para não mascarar como internal_error. Site exato: o catch na linha 17-26.",
   "src/app/api/wms/produtos/backfill-imagens/route.ts:73-91 — verificar: já envolve cada sincronizarProduto em try/catch e reporta `ok:false, error:msg` por SKU; o throw novo é absorvido sem quebrar o batch. Nenhuma mudança necessária aqui, só confirmar no teste."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/sync-tiny.test.ts",
  "red_test": "Dado um produto SEM linha ativa em siso_produto_empresas, `sincronizarProduto(id)` deve REJEITAR (throw) com mensagem indicando falta de vínculo — em vez de resolver silenciosamente. (mock do supabase retornando mapeamento=null; assert rejects). Complemento scenarios_e2e: POST /api/wms/produtos/[id]/sync para produto sem mapeamento retorna status != 2xx com corpo `{error}` (hoje retorna {ok:true}).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "needs_migration_desc": "",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "P117/P118/P171 compartilham change_site único (sync-tiny.ts:54-60). Recomendo implementar os três juntos. A nota de P117 menciona 'opcional mostrar antes/depois' — isso é nice-to-have de escopo maior (diff descrição) e NÃO faz parte da decisão vinculante de 'só avisar'; deixar fora do fix mínimo (Karpathy: sem feature além do pedido)."
 },
 {
  "id": "P118",
  "current_status": "open",
  "status_evidence": "Mesmo defeito de P117: src/lib/wms/sync-tiny.ts:54-60 faz early-return silencioso (apenas logger.warn) quando não há mapeamento ativo. O route /api/wms/produtos/[id]/sync (linha 16) retorna {ok:true} e o toast de sucesso dispara em produtos/page.tsx:99. 'loga um aviso interno e continua' descrito no raio-x = exatamente o logger.warn da linha 55. Nenhum commit pós-3-jun em sync-tiny.ts.",
  "root_cause": "Idêntico a P117: ausência de propagação de erro no caso 'sem vínculo fornecedor/Tiny'. A nota pede 'só avisar' (opção 1), NÃO bloquear o botão (opção 2 descartada).",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:54-60 — throw com mensagem clara em vez de warn+return (mesmo change_site de P117).",
   "src/app/api/wms/produtos/[id]/sync/route.ts:17-26 — garantir que a mensagem chegue ao toast (retornar 4xx em vez de mascarar como internal_error 5xx)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/sync-tiny.test.ts",
  "red_test": "Mesmo de P117: sincronizarProduto sem mapeamento deve throw com mensagem de 'falta de vínculo'. Asseverar o comportamento (rejeita), não a implementação.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Duplicata funcional de P117 e P171 — um único fix em sync-tiny.ts:54-60 resolve os três. A opção 2 do raio-x (bloquear botão para produtos sem mapeamento) foi NÃO marcada como decisão; a nota é explícita: 'so avisar'."
 },
 {
  "id": "P171",
  "current_status": "open",
  "status_evidence": "Mesmo early-return de src/lib/wms/sync-tiny.ts:54-60. O título do P171 ('produto sem nenhum mapeamento Tiny') é literalmente o ramo `if (!mapeamento)` da linha 54. O caller que o operador usa é src/app/wms/produtos/page.tsx:95-103 (botão sync por linha), que mostra toast.success('Sincronizado') no onSuccess. Nenhum commit pós-3-jun.",
  "root_cause": "Mesmo defeito-raiz de P117/P118. A nota: 'avisar claramente que falta mapeamento Tiny ao sincronizar (igual P117/P118)' — explicitamente liga os três. Decisão = opção 2 (avisar como erro), NÃO opção 3 (criar mapeamento automático).",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:54-60 — throw com mensagem clara (mesmo change_site de P117/P118).",
   "src/app/api/wms/produtos/[id]/sync/route.ts:17-26 — propagar a mensagem como 4xx pro toast.error existente (produtos/page.tsx:102)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/sync-tiny.test.ts",
  "red_test": "sincronizarProduto(id) para produto sem qualquer mapeamento Tiny deve rejeitar com erro; e o route /sync deve responder não-2xx (hoje responde {ok:true}). Verifica que o operador recebe feedback de falha, não 'Sincronizado'.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Nota cita explicitamente 'igual P117/P118'. Os 3 são o mesmo fix. Opção 3 (auto-criar mapeamento no Tiny) foi rejeitada na decisão — não implementar."
 },
 {
  "id": "P170",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:42-52 — a query de mapeamento usa `.eq(produto_id).eq(ativo,true).limit(1).maybeSingle()` SEM ORDER BY. Postgres não garante ordem sem ORDER BY, então com 2+ empresas mapeadas a escolha é não-determinística (pode trazer dados de A numa execução e de B noutra). Existe a opção `preferEmpresaId` (linhas 47-49) que força uma empresa, mas o route /api/wms/produtos/[id]/sync (linha 15) chama `sincronizarProduto(id)` SEM passar empresa, e o frontend (produtos/page.tsx:97) não tem seletor de empresa. siso_produto_empresas (migration 20260508_wms_foundation.sql:37-44) NÃO tem coluna de empresa-preferida-para-sync. Nenhum commit pós-3-jun.",
  "root_cause": "Seleção de mapeamento sem critério determinístico (sem ORDER BY e sem escolha persistida do operador). A nota é vinculante: 'operador escolhe a empresa e sempre usa aquela (deterministico)'. Como não há onde persistir a escolha nem UI para fazê-la, o fix exige (a) coluna/flag de empresa-sync no vínculo + (b) UI no drawer/listagem para o operador escolher quando há 2+ mapeamentos + (c) sync-tiny passar a priorizar a empresa escolhida (estável).",
  "change_sites": [
   "supabase/migrations/<nova>_produto_empresa_sync_pref.sql — adicionar coluna booleana `sync_preferida boolean NOT NULL DEFAULT false` em siso_produto_empresas (ou `empresa_sync_id` em siso_produtos). Constraint: no máximo uma preferida por produto (partial unique index WHERE sync_preferida).",
   "src/lib/wms/sync-tiny.ts:42-52 — ao montar mapeamentoQuery, sem preferEmpresaId, ordenar deterministicamente: priorizar `sync_preferida=true`, depois `.order(\"empresa_id\")` como tie-break estável, antes do .limit(1). Garante determinismo mesmo sem escolha explícita.",
   "src/components/wms/produto-drawer.tsx (aba de mapeamentos — atualmente inexistente; ver grep) e/ou src/app/wms/produtos/page.tsx:95-103 — quando produto tem 2+ mapeamentos, exigir/expor seletor de empresa-de-sync (decisao=opção 2) e gravar via novo endpoint PATCH siso_produto_empresas.sync_preferida.",
   "src/app/api/wms/produtos/route.ts (GET) — passar a retornar contagem/lista de mapeamentos por produto pra UI saber quando há ambiguidade (hoje não retorna nenhuma info de empresa)."
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "primitive": "outro",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/sync-tiny.test.ts",
  "red_test": "Dado um produto com 2 mapeamentos ativos (empresa A e B), `sincronizarProduto(id)` chamado 5x deve SEMPRE usar a mesma empresa (a marcada sync_preferida, ou o tie-break estável por empresa_id) — assert que getProdutoFull é chamado sempre com o mesmo tiny_produto_id. (mock supabase com 2 mapeamentos em ordem invertida entre chamadas; assert estabilidade).",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "med",
  "migration_desc": "Adicionar coluna `sync_preferida boolean DEFAULT false` em siso_produto_empresas + partial unique index garantindo no máx. 1 preferida por produto. Backfill: para produtos com mapeamento único, marcar como preferida (no-op de comportamento). Para produtos com múltiplos, deixar false (operador escolhe; sync usa tie-break estável até escolherem).",
  "respeita_nota": true,
  "notes": "A nota ('operador escolhe e sempre usa aquela') é mais cara do que um simples ORDER BY. O mínimo 100% fiel exige persistir a escolha (migration + UI). Se o usuário aceitar uma versão reduzida, só o ORDER BY estável em sync-tiny.ts:50 já resolve a IMPREVISIBILIDADE (problema central do 'why') sem migration — mas NÃO entrega a escolha explícita do operador. Flag pra confirmar escopo com o usuário antes de implementar a parte de UI/migration."
 },
 {
  "id": "P141",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/admin/printnode/contas/[id]/route.ts:91-116 — o DELETE remove a conta direto (FKs em siso_galpoes/siso_usuarios são ON DELETE SET NULL, migration 20260519_printnode_multi_contas.sql:38-48), sem NENHUM preview de impacto. O frontend src/app/wms/configuracoes/conexoes/page.tsx:1448-1471 usa um confirm() genérico ('Galpões e usuários ... ficarão sem impressora') — exatamente a opção 'aviso genérico' que a decisão rejeita. Não existe endpoint que conte/liste os galpões+usuários afetados. Nenhum commit pós-3-jun.",
  "root_cause": "Deleção sem preview de blast-radius. A conta é referenciada por até 4 colunas FK (printnode_account_id e printnode_account_id_produto em siso_galpoes e siso_usuarios). A nota: 'mostrar prateleiras/galpoes afetados ANTES de deletar' (opção 1) — NÃO bloquear (opção 2 descartada). Precisa de um preview backend que conte/liste os afetados.",
  "change_sites": [
   "src/app/api/wms/admin/printnode/contas/[id]/route.ts — adicionar GET (ou query param ?preview=1) que retorna `{ galpoes: [{id,nome,uso:'envio'|'produto'}], usuarios: [{id,nome}] }` consultando siso_galpoes WHERE printnode_account_id=id OR printnode_account_id_produto=id, e idem siso_usuarios. Inserir antes/ao lado do DELETE (linhas 91-116).",
   "src/app/wms/configuracoes/conexoes/page.tsx:1448-1471 (função remove()) — antes do DELETE, chamar o preview e exibir a lista concreta ('Estes N galpões e M usuários ficarão sem impressora: ...') em vez do confirm() genérico. Trocar o texto fixo da linha 1452 pela lista real."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/cenario-printnode-delete-preview.ts",
  "red_test": "Criar conta PrintNode, atribuí-la a 2 galpões e 1 usuário, então GET preview do DELETE deve retornar exatamente esses 2 galpões + 1 usuário (com nomes). Hoje não existe endpoint de preview — o teste falha por 404/ausência. Complemento: o DELETE em si continua funcionando (SET NULL).",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "low",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Decisão = opção 1 (mostrar lista), NÃO opção 2 (bloquear deleção). O preview deve cobrir AS QUATRO colunas FK (envio + produto, em galpões + usuários). siso_impressoes_log também referencia a conta (ON DELETE SET NULL, migration 20260528) mas é histórico — não conta como 'afetado' pra impressão futura, não incluir no preview."
 },
 {
  "id": "P183",
  "current_status": "open",
  "status_evidence": "src/lib/printnode.ts:281-282 (printerCache, TTL 5min) e 360 (printerProdutoCache) cacheiam a api_key resolvida por 5 minutos. Os invalidadores existem (invalidarCacheImpressora linha 352-354 e invalidarCacheImpressoraProduto linha 464-465) mas grep confirma que NUNCA são chamados fora de printnode.ts — em particular o PATCH de api_key em src/app/api/wms/admin/printnode/contas/[id]/route.ts:42-48 NÃO os chama. Logo, após salvar key nova, a próxima impressão usa a key antiga cacheada por até 5 min. Nenhum commit pós-3-jun.",
  "root_cause": "Ausência de invalidação de cache na escrita. A api_key é atualizada (route PATCH linhas 42-48) mas os dois caches in-memory (printerCache + printerProdutoCache) não são limpos. A nota: 'invalidar cache da chave PrintNode ao salvar a nova (sem espera de 5min)'.",
  "change_sites": [
   "src/app/api/wms/admin/printnode/contas/[id]/route.ts:57-82 — após o UPDATE bem-sucedido em siso_printnode_contas, chamar `invalidarCacheImpressora()` E `invalidarCacheImpressoraProduto()` (importar de @/lib/printnode). Disparar pelo menos quando body.api_key ou body.ativo mudou (idealmente sempre, é barato — Map.clear).",
   "src/app/api/wms/admin/printnode/contas/[id]/route.ts:104-116 (DELETE) — chamar os mesmos invalidadores após delete, pra cobertura (a conta deletada pode estar cacheada). Reforça P141.",
   "src/lib/printnode.ts:352-354 e 464-465 — nenhuma mudança no corpo; só passam a ser efetivamente chamados."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "cache_invalidation",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/printnode-cache.test.ts",
  "red_test": "Resolver impressora (popula printerCache + printerProdutoCache com keyA), trocar api_key para keyB e chamar invalidarCacheImpressora()+invalidarCacheImpressoraProduto(); a próxima chamada de resolverImpressora/resolverImpressoraProduto deve refazer o lookup e retornar keyB (não keyA cacheada). Asseverar que ambos os caches são limpos — hoje só printerCache é limpo por invalidarCacheImpressora; provar que o caminho de invalidação no PATCH cobre os DOIS.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Atenção: invalidarCacheImpressora() limpa SÓ printerCache; printerProdutoCache precisa de invalidarCacheImpressoraProduto() separado. O PATCH deve chamar AMBOS senão a etiqueta de PRODUTO continua com key antiga. Cache é por-instância (serverless) — em ambiente multi-instância só a instância que serviu o PATCH é invalidada; mitiga mas não elimina 100% (TTL de 5min ainda cobre o resto). Mencionar essa limitação, mas a nota só pede invalidar ao salvar — fix atende a nota."
 },
 {
  "id": "P168",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/ml/oauth/route.ts:30-45 grava um único cookie `ml_oauth_state` (linha 39); um 2º clique sobrescreve o state. O callback src/app/api/wms/ml/oauth/callback/route.ts:43-45 compara cookie vs state retornado e falha com 'state_mismatch' se diferem. No frontend src/app/wms/configuracoes/conexoes/page.tsx, o botão 'Conectar conta ML' (linhas 1992-2004, função connect() linha 1971-1977) faz `window.location.href = \"/api/wms/ml/oauth\"` SEM estado de 'conectando' (só disabled={!appConfigured}). Há um 2º gatilho idêntico: reauthorize() linha 2092-2094. Nenhum commit pós-3-jun.",
  "root_cause": "Botão Conectar/Reautorizar ML não desabilita durante o processamento, permitindo duplo clique que gera 2 códigos OAuth (state Y sobrescreve X) → callback retorna com X mas cookie tem Y → 'state_mismatch'. A nota é vinculante: 'desabilitar botao Conectar ML durante a conexao' (opção 1), NÃO multi-state (opção 2 descartada).",
  "change_sites": [
   "src/app/wms/configuracoes/conexoes/page.tsx:1971-2004 — adicionar estado local `const [connecting, setConnecting] = useState(false)`; em connect(), setConnecting(true) antes do redirect; aplicar `disabled={!appConfigured || connecting}` no botão (linha 1996) e trocar o label/spinner enquanto connecting. Como window.location.href causa navegação full-page, o disable basta pra bloquear o 2º clique na janela entre o 1º clique e a navegação.",
   "src/app/wms/configuracoes/conexoes/page.tsx:2092-2094 (reauthorize) e o botão correspondente — mesmo tratamento de disable-during-processing."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "manual_only",
  "test_file": "",
  "red_test": "Comportamental (UI): após o 1º clique em 'Conectar conta ML', o botão fica disabled antes da navegação, impedindo um 2º clique que geraria novo state e causaria state_mismatch no callback. Difícil de cobrir em vitest_unit (depende de window.location e navegação); marcar manual_only ou, se quiserem automatizar, um teste de componente verificando que após onClick o botão recebe disabled=true.",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "migration_desc": "",
  "respeita_nota": true,
  "notes": "Decisão = opção 1 (desabilitar botão), NÃO opção 2 (multi-state no backend). Fix é 100% frontend; backend (route/callback) fica intocado. O field 'file' do raio-x cita route.ts:37-45 e callback:43-45 como o MECANISMO da falha, mas o conserto escolhido é na UI. Severidade média, esforço S."
 },
 {
  "id": "P031",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/separacao/concluir-oc/route.ts:242-260 — para cada pedido coleta itemOcGalpaoIds, computa uniqueOcGalpaoIds = [...new Set(...)] e pega SILENCIOSAMENTE uniqueOcGalpaoIds[0] (linha 246). Quando há OCs de galpões diferentes (uniqueOcGalpaoIds.length > 1) não há nenhum bloqueio/aviso — escolhe o 1º e transiciona pra 'separado' (linha 274). git log do arquivo: último commit relevante 4d8d794/29e5cb3 (gating realoc/NF split), nenhum trata mistura de galpões de OC. Bug intacto.",
  "root_cause": "concluir-oc resolve a decisao/galpão de separação por pedido pegando o primeiro galpão de OC encontrado entre os itens (uniqueOcGalpaoIds[0]), sem detectar nem rejeitar o caso de itens vindos de galpões de OC distintos. Itens do galpão C ficam mapeados pra transferência do galpão B silenciosamente.",
  "change_sites": [
   "src/app/api/wms/separacao/concluir-oc/route.ts:235-247 — antes de resolver decisao por pedido, detectar uniqueOcGalpaoIds.length > 1 e, se houver mistura, NÃO marcar o pedido como 'separado'; acumular num set pedidosMultiGalpao e pular o update (continue), removendo o pid de 'separados'.",
   "src/app/api/wms/separacao/concluir-oc/route.ts:144-146 + 347 — incluir os pedidos multi-galpão no payload de resposta como bloqueados (ex.: campo pedido_ids_multi_galpao + status 409 ou flag), no mesmo molde do bloqueio realocacoes_pendentes já existente nas linhas 56-71.",
   "src/app/api/wms/separacao/concluir-oc/route.ts:128-142 — opcional: reverter esses pedidos pra aguardando_compra (mesmo padrão do branch 'pendentes') pra não ficarem em estado intermediário."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/82-concluir-oc-multi-galpao-bloqueia.ts",
  "red_test": "Pedido OC com 2 itens cujos ordem_compra_id apontam pra galpões DIFERENTES (B e C); ao chamar POST /api/wms/separacao/concluir-oc com todos os itens marcados, a rota deve RECUSAR (não transicionar pra 'separado') e retornar o pedido como bloqueado/multi-galpão; status_separacao permanece != 'separado' e decisao_final não vira transferencia do galpão B ignorando C.",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota = op1 (BLOQUEAR e avisar, operador separa manual). Reutilizar exatamente o padrão de early-block já presente no arquivo (realocacoes_pendentes, linhas 56-71) pra consistência. A detecção de mistura precisa rodar por pedido (após montar uniqueOcGalpaoIds), não global. Itens sem ordem_compra_id não contam como galpão de OC ( filter(Boolean) já cobre)."
 },
 {
  "id": "P036",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/trocar-sku/route.ts:50-53 — a query de itens seleciona apenas 'id, pedido_id, produto_id, sku, quantidade_pedida, siso_pedidos(empresa_origem_id)'. NÃO busca compra_status nem ordem_compra_id e NÃO há nenhuma verificação antes do UPDATE (linhas 186-195) que troca sku/produto_id/fornecedor_oc. Logo a troca passa mesmo com compra ativa (compra_status='comprado'/'oc_pendente'/'recebido'). git log do arquivo: 93dea14/bc5a1de/83ac31d — nenhum adiciona guard de status. Bug intacto.",
  "root_cause": "trocar-sku não valida o estado de compra do item antes de sobrescrever o SKU. Conforme o lifecycle (aguardando_compra → comprado [ordem_compra_id setado] → recebido), trocar o SKU de um item já 'comprado' descasa o comprovante da OC do produto do pedido.",
  "change_sites": [
   "src/app/api/wms/compras/trocar-sku/route.ts:50-53 — adicionar compra_status e ordem_compra_id ao select dos itens.",
   "src/app/api/wms/compras/trocar-sku/route.ts:61-78 — após carregar items, validar que TODOS os itens estão em estado trocável (compra_status null OU 'aguardando_compra' OU 'cancelado'); se algum estiver em 'comprado'/'oc_pendente'/'parcialmente'/'recebido'/'equivalente_pendente'/'cancelamento_pendente'/'indisponivel' (ou tiver ordem_compra_id != null), retornar 409 com mensagem 'essa compra já foi marcada, não pode trocar mais' sem nenhum write."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/83-trocar-sku-bloqueia-compra-ativa.ts",
  "red_test": "Item de pedido OC após /compras/comprar (compra_status='comprado', ordem_compra_id setado); POST /api/wms/compras/trocar-sku deve retornar 409 e NÃO alterar sku/produto_id/fornecedor_oc do item. Caso de controle: item ainda em 'aguardando_compra' permite a troca (200).",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota = op1 (impedir/bloquear quando compra ativa). Question define a fronteira exata: troca só permitida em 'aguardando_compra' ou 'cancelado'. Decidir se compra_status null (item normal sem fluxo de compra) deve ser permitido — pela semântica de 'trocar SKU equivalente antes de comprar' provavelmente sim; tratar null como permitido. Como é UPDATE em lote por item_ids, basta UM item travado pra recusar o request inteiro."
 },
 {
  "id": "P039",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:51-67 — o UPDATE seta compra_status='cancelado'/ordem_compra_id=null e limpa flags de bipagem, mas NÃO há chamada a liberarReserva nem estorno de mov R. Comentário linha 47-48 só remove a tabela dropada siso_pedido_item_estoques (não a reserva R do ledger). Contraste: equivalente/confirmar/route.ts:134-149 JÁ chama liberarReserva({motivo:'cancelamento'}) pelo mesmo motivo (R órfã). O reconciliador (src/lib/wms/reconciliador-oc.ts:186) cria R via reservarAtomico (origem_tipo='reserva_pedido', origem_id=pedido_id) pra itens OC reconciliados — essas Rs ficam vivas após o cancel. liberarReserva (reservas.ts:38-49) casa exatamente por origem_id=pedido_id/origem_tipo='reserva_pedido'/tipo='R'. Bug real e intacto.",
  "root_cause": "O fluxo de confirmar cancelamento de item de compra não libera a reserva R que pode existir pra esse pedido (criada pelo reconciliador-oc quando estoque chegou e o item virou própria, ou por aprovação prévia). Sem L de liberação, siso_estoque.reservado nunca baixa — estoque preso até TTL/rotina/admin.",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:7-8 — importar liberarReserva de '@/lib/wms/reservas'.",
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:67-82 — após o UPDATE bem-sucedido (e antes/junto do checkAndCancelPedidoIfAllTerminal), chamar await liberarReserva({ pedido_id: String(item.pedido_id), motivo: 'cancelamento', usuario_id: session.id }) protegido por try/catch+log (mesmo padrão de equivalente/confirmar:134-149).",
   "src/app/api/wms/compras/pedidos/[pedidoId]/cancelar/route.ts:130-160 — (correlato) o cancel de pedido inteiro estorna movs E mas também não chama liberarReserva; incluir a mesma liberação pra o caminho de cancelamento via aba compras."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "reserva_wiring_cancel",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/84-cancelar-item-libera-reserva.ts",
  "red_test": "Pedido OC reconciliado (entrou estoque E → reconciliador-oc criou R viva, siso_estoque.reservado > 0). Marcar item p/ cancelamento (POST cancelamento) e confirmar (POST cancelamento/confirmar). Após confirmar: existe mov L (origem_tipo='liberacao_reserva', origem_id=pedido_id) e siso_estoque.reservado da tripla volta a 0 (não fica preso).",
  "effort": "S",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota = op2 (liberar estoque automaticamente ao confirmar cancelamento). NUANCE: liberarReserva é PEDIDO-scoped (libera todas as Rs do pedido por origem_id), não item-scoped. Pra pedido multi-item onde só um item é cancelado isso pode super-liberar Rs de itens irmãos. Mesma limitação já aceita em equivalente/confirmar (precedente). Se precisar de granularidade por item, seria necessário um estorno R por (pedido, produto) — não pedido por liberarReserva ainda não suportar isso. Registrar essa limitação ao implementar. Atenção: P038 (mesmo tema, fluxo de cancelamento de compra liberar reserva imediato) é correlato — coordenar pra não duplicar a liberação."
 },
 {
  "id": "P041",
  "current_status": "already_fixed",
  "status_evidence": "Nota/decisão = op1: 'Apagar a ligação com a compra (como está agora)'. O código ATUAL já implementa exatamente isso: src/app/api/wms/compras/itens/[itemId]/cancelamento/route.ts:54 seta ordem_compra_id=null no cancelamento pendente, e cancelamento/confirmar/route.ts:55 também seta ordem_compra_id=null. Logo o comportamento corrente CASA com a decisão vinculante (manter o unlink). A 'question' do raio-x pede o oposto (manter ligação/estorno = op2), mas a NOTA do usuário sobrepõe e escolheu op1.",
  "root_cause": "N/A — decisão do usuário foi NÃO mudar (op1 = manter o unlink atual). O bug descrito em 'today' (estoque que chega depois fica órfão sem religar) é um trade-off aceito pela decisão.",
  "change_sites": [],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "outro",
  "test_harness": "manual_only",
  "test_file": "",
  "red_test": "N/A — sem mudança de código pela decisão. (Opcionalmente um teste de regressão poderia asseverar que cancelar item ainda zera ordem_compra_id, mas não há comportamento novo a garantir.)",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "ATENÇÃO: a NOTA (op1) contradiz a 'question' textual do registro (que pede manter a ligação). A nota é vinculante — então NENHUMA mudança. Conflito de redação interno ao próprio registro, não conflito com outro P-id. O efeito colateral (estoque tardio órfão) é tratado parcialmente por P035/reconciliador, mas P041 em si fica como está."
 },
 {
  "id": "P042",
  "current_status": "already_fixed",
  "status_evidence": "Nota = 'permite cancelamento'; decisão = op1 'Permitir cancelamento de tudo, sem validação'. O código ATUAL já permite: src/app/api/wms/compras/itens/[itemId]/cancelamento/route.ts:33-44 busca o item SEM checar compra_status/compra_quantidade_recebida e prossegue pro cancelamento_pendente mesmo se o item já foi recebido 100%. Portanto o comportamento corrente já CASA com a decisão vinculante (sem bloqueio).",
  "root_cause": "N/A — decisão foi NÃO adicionar validação (op1). A 'question' pede bloquear recebidos-na-íntegra (op2), mas a NOTA escolheu op1 (permitir).",
  "change_sites": [],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "outro",
  "test_harness": "manual_only",
  "test_file": "",
  "red_test": "N/A — sem mudança pela decisão. (Regressão opcional: cancelar item já 'recebido' continua retornando ok/200.)",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "CONFLITO INTERNO de redação: a 'question' e o 'why' do registro descrevem o problema e sugerem op2 (bloquear), mas a NOTA do usuário ('permite cancelamento' = op1) é vinculante e diz pra deixar permissivo. Seguindo a nota: nenhum trabalho. Observação de risco: manter permissivo permite cancelar item já no galpão (contagem pode divergir) — mas isso foi explicitamente aceito pelo usuário."
 },
 {
  "id": "P043",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:28-58 — a rota busca o item e faz UPDATE incondicional pra compra_status='aguardando_compra'/ordem_compra_id=null SEM precondição de estado e SEM idempotency key. Um 2º clique re-executa o UPDATE (já no estado-alvo, vira no-op real) mas responde 200 ok:true, sem sinalizar que nada efetivo aconteceu. Não existe infra de idempotency-key genérica no projeto (grep: só idempotência via estado/estorno_de em outros fluxos). git log do arquivo: bc5a1de/83ac31d — nenhum adiciona guard. Bug intacto.",
  "root_cause": "devolver não é idempotente no servidor: faz UPDATE sem WHERE de precondição de estado, então cliques duplicados/concorrentes ambos 'sucedem' e retornam ok, sem o sistema reconhecer/rejeitar a duplicata.",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:44-58 — transformar o UPDATE em conditional/atomic: adicionar precondição de estado no WHERE (ex.: .in('compra_status', ['comprado','oc_pendente','parcialmente_recebido','equivalente_pendente','cancelamento_pendente']) — i.e. estados devolvíveis, NÃO já 'aguardando_compra'/'cancelado') e .select() pra detectar 0 linhas afetadas.",
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:58-60 — se o UPDATE retornar 0 linhas (segunda devolução / item já fora do estado devolvível), retornar 409 'item já devolvido' em vez de 200 ok, tornando a 2ª chamada reconhecidamente rejeitada (mesmo padrão de guard 409 de cancelamento/confirmar:40-44)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/catalogo/85-devolver-item-idempotente.ts",
  "red_test": "Item OC 'comprado'; 1º POST /api/wms/compras/itens/[itemId]/devolver retorna 200 e item vira 'aguardando_compra'; 2º POST (idêntico) deve ser REJEITADO (409 'já devolvido'), não responder ok:true. Asseverar que só houve 1 evento compra_item_devolvido registrado.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota = op2 ('ID único no servidor, descartar cópias idênticas'). O projeto NÃO tem infra de idempotency-key genérica; a forma idiomática e mínima de cumprir o espírito da nota (servidor reconhece e rejeita a duplicata) é o atomic conditional UPDATE usando o próprio estado do item como chave de dedup — o que já é o padrão usado em cancelamento/equivalente confirmar. Se o usuário insistir em UUID de operação literal por request, seria preciso uma tabela/coluna de idempotency_key (escopo maior, marcar como upgrade). Recomendo a versão por estado: cobre cliques simultâneos sem infra nova."
 },
 {
  "id": "P045",
  "current_status": "partially_fixed",
  "status_evidence": "src/lib/compras-utils.ts:178-234 (cancelOcIfEmpty) — já distingue: sem itens → 'cancelado' (199-200); allRecebido → 'recebido' (208-209); someRecebido (qty>0) → 'parcialmente_recebido' (205-211). PORÉM, quando restam itens mas NENHUM foi recebido (someRecebido=false e allRecebido=false), cai no else → return (linha 213) MANTENDO o status atual da OC (possivelmente 'parcialmente_recebido' ou 'comprado' estale), em vez de RESETAR pra 'comprado' (='em compra'/esperando) como a nota op2 exige. Enum da OC (migration 20260311:11-12) tem 'comprado' como o estado 'em compra'. git log: 5412d5f (over-receive) tocou compras-utils mas não esse branch. Parcial.",
  "root_cause": "cancelOcIfEmpty não tem o branch 'nenhum item recebido entre os remanescentes → status volta pra comprado'. Ao devolver um item, se sobram itens não-recebidos, a OC pode permanecer com status estale (ex.: 'parcialmente_recebido') que não descreve a realidade ('esperando'), atrapalhando relatório/cobrança/reorder.",
  "change_sites": [
   "src/lib/compras-utils.ts:201-214 — no ramo final (remainingItems > 0, !allRecebido, !someRecebido), trocar o 'return' (213) por newStatus='comprado' (mapeia 'em compra'/esperando da nota). Manter 'parcialmente_recebido' quando someRecebido=true e 'recebido' quando allRecebido. Garantir que o UPDATE de status sempre roda nesse caso.",
   "src/lib/compras-utils.ts:178-234 — verificar que callers (devolver:60, indisponivel:60, cancelamento:67, cancelar-pedido:140, reconciliador-oc:202) toleram a OC voltar pra 'comprado' (não há regressão nos que esperavam no-op)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/compras-utils.test.ts",
  "red_test": "Unit de cancelOcIfEmpty: dado OC com itens remanescentes onde NENHUM tem compra_quantidade_recebida>0, a função deve atualizar siso_ordens_compra.status pra 'comprado' (não deixar o status anterior/estale). Casos de controle: someRecebido → 'parcialmente_recebido'; allRecebido → 'recebido'; sem itens → 'cancelado'. (Mockar o supabase client como nos demais testes puros do repo, ou cobrir via integration se preferir o banco real.)",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota = op2 ('em compra' se nenhum chegou; 'parcialmente_recebido' se alguns chegaram). 'em compra' = status 'comprado' do enum (não existe literal 'em_compra' na CHECK da migration 20260311; usar 'comprado'). cancelOcIfEmpty é função pura-ish (recebe supabase) usada por devolver/indisponivel/cancelamento/cancelar-pedido/reconciliador — testar isolado dá o RED mais barato. Já existe parte do comportamento (recebido/parcial/cancelado); só falta o reset pra 'comprado' no branch sem recebimento — por isso partially_fixed."
 },
 {
  "id": "P048",
  "current_status": "open",
  "status_evidence": "src/lib/compras-utils.ts:178-234 (cancelOcIfEmpty) — em erro de SELECT (190-195) e em erro de UPDATE da OC (222-228) só faz logger.warn e `return;` (void). Nenhum retry. O caller src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:60 chama `await cancelOcIfEmpty(...)` e NÃO recebe sinal de falha — segue retornando 200/ok (linha 81). git log -- compras-utils.ts mostra que o último toque (5412d5f, P6) só mexeu em getCompraQuantidadeRestante; o swallow de erro segue intacto.",
  "root_cause": "cancelOcIfEmpty engole erros de banco (logger.warn + return void) em vez de tentar de novo e propagar. O update do status da OC pode falhar silenciosamente; a rota devolver responde sucesso mesmo com a OC desatualizada.",
  "change_sites": [
   "src/lib/compras-utils.ts:185-228 — envolver o SELECT de itens restantes e o UPDATE de siso_ordens_compra num loop de até 3 tentativas (com pequeno backoff); ao esgotar, em vez de `return` silencioso, lançar/retornar um resultado de falha (ex.: mudar assinatura de Promise<void> para Promise<{ok:boolean,error?:string}> OU throw) pra o caller distinguir sucesso de falha",
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:60-81 — checar o retorno de cancelOcIfEmpty; se falhou após 3 tentativas, responder erro (500/409) ao operador em vez de {ok:true}",
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/route.ts:67, .../pedidos/[pedidoId]/cancelar/route.ts:140, .../equivalente/route.ts:158, .../indisponivel/route.ts:60, src/lib/wms/reconciliador-oc.ts:202 — ajustar callers à nova assinatura (no reconciliador, manter fire-and-forget tolerante; nas rotas de operador, surfacing do erro)"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "rpc_needed": "",
  "primitive": "durable_enqueue_retry",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/__tests__/compras-utils.cancelOcIfEmpty.test.ts",
  "red_test": "Mockar supabase para o UPDATE da OC falhar 3x: cancelOcIfEmpty deve tentar 3 vezes e então sinalizar falha (throw ou retornar {ok:false}), nunca retornar como se tivesse sucesso. Caso o UPDATE falhe nas 2 primeiras e suceda na 3ª, deve resolver com sucesso (idempotente).",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota P048 = op1+op2: 3 tentativas automáticas E surfacing do erro (nada silencioso). cancelOcIfEmpty é util compartilhado por 6 rotas + reconciliador-oc; mudar a assinatura pra propagar erro tem blast nesses callers. Recomendo manter o reconciliador tolerante (não quebrar o fire-and-forget) e fazer surfacing só nas rotas de operador (devolver é a do raio-x). O 'why' do P045 (status parcial) é vizinho mas é outra lógica."
 },
 {
  "id": "P079",
  "current_status": "open",
  "status_evidence": "supabase/migrations/20260520f_mviews.sql:55-79 — CTE lead_pref puxa pf.lead_time_dias_medio do fornecedor preferencial; a coluna é NOT NULL DEFAULT 14 (20260522_wms_roteamento.sql:28), então NULL só ocorre quando NÃO há fornecedor preferencial (LEFT JOIN miss em lp). No CASE (linhas 69-76) o ramo 'lead_time_risco' exige `lp.lead_time_dias_medio IS NOT NULL`; quando NULL cai direto pra 'ok' — falsa segurança descrita no 'today'. git log mostra a mview criada só uma vez (18f2a55), sem correção posterior.",
  "root_cause": "A mview siso_cobertura_estoque trata lead time ausente (sem fornecedor preferencial → lp.lead_time_dias_medio NULL) como 'ok' em vez de assumir um default conservador, escondendo risco de cobertura.",
  "change_sites": [
   "supabase/migrations/<nova>_cobertura_lead_default.sql — recriar a MATERIALIZED VIEW siso_cobertura_estoque substituindo as referências a lp.lead_time_dias_medio por COALESCE(lp.lead_time_dias_medio, 7): na coluna de saída (linha 68 do arquivo atual) e no ramo do CASE (linhas 73-74) — assim o status 'lead_time_risco' dispara mesmo sem lead time configurado",
   "recriar com DROP MATERIALIZED VIEW IF EXISTS + CREATE (com os índices uq_cobertura e idx_cobertura_status) + manter wms_refresh_cobertura(); aplicar via mcp__supabase__apply_migration no project ehbxpbeijofxtsbezwxd"
  ],
  "needs_migration": true,
  "migration_desc": "Recria a materialized view siso_cobertura_estoque usando COALESCE(lead_time_dias_medio, 7) na CTE lead_pref / no CASE, pra que cobertura sem lead time não vire 'ok' silencioso (assume 7 dias por padrão, conforme nota). Inclui DROP+CREATE da MV, recria os 2 índices e a função wms_refresh_cobertura.",
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "integration",
  "test_file": "tests/integration/cobertura-lead-default.test.ts",
  "red_test": "Seedar produto com giro>0, disponível baixo (ex.: dias_cobertura entre 7 e 14) e SEM fornecedor preferencial (ou sem lead time). Após REFRESH, siso_cobertura_estoque.status_cobertura deve ser 'lead_time_risco' (assumindo 7 dias), NÃO 'ok'. Hoje retorna 'ok'/'atencao' sem considerar lead.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota P079 = assumir 7 dias por padrão quando vazio, NÃO forçar preenchimento. Sutileza: como lead_time_dias_medio é NOT NULL DEFAULT 14 no schema, o caso 'vazio' na prática = sem fornecedor preferencial vinculado (lp NULL). O COALESCE(...,7) resolve esse caso. O default do CLAUDE-fornecedor é 14, mas a nota pede explicitamente 7 — seguir a nota."
 },
 {
  "id": "P124",
  "current_status": "open",
  "status_evidence": "supabase/migrations/20260522_wms_roteamento.sql:45 — idx_pf_preferencial é CREATE INDEX (NÃO-único) ON siso_produto_fornecedores(produto_id) WHERE preferencial AND ativo. Grep em todas as migrations não acha nenhum UNIQUE sobre preferencial. src/app/api/wms/produto-fornecedores/[id]/route.ts:66-84 faz despromoção (UPDATE preferencial=false em todos do produto, 73-76) e promoção (UPDATE preferencial=true no alvo, 79-84) em 2 statements separados, sem atomicidade nem constraint de backstop.",
  "root_cause": "Não existe constraint no banco garantindo ≤1 preferencial por produto; o índice existente é não-único. A troca é feita em 2 passos não-atômicos na rota PATCH, podendo deixar 0 ou 2 preferenciais em corrida.",
  "change_sites": [
   "supabase/migrations/<nova>_pf_preferencial_unique.sql — DROP INDEX IF EXISTS idx_pf_preferencial; CREATE UNIQUE INDEX idx_pf_preferencial ON siso_produto_fornecedores(produto_id) WHERE preferencial AND ativo; (partial unique → no máx 1 preferencial ativo por produto). Aplicar no project ehbxpbeijofxtsbezwxd",
   "src/app/api/wms/produto-fornecedores/[id]/route.ts:66-84 — manter a despromoção-antes-da-promoção (a ordem já evita colisão do unique parcial: zera todos e só então marca o alvo); opcionalmente tratar erro 23505/23P01 do unique retornando 409 amigável via wmsErrorResponse",
   "src/lib/wms/fornecedores.ts:95-100 (vincularProdutoFornecedor) e 227-232 (upsertProdutoFornecedor) — mesma ordem (despromove antes de inserir/upsertar com preferencial=true) já está correta; verificar que continuam compatíveis com o unique parcial"
  ],
  "needs_migration": true,
  "migration_desc": "Converte idx_pf_preferencial de índice comum pra UNIQUE INDEX parcial (produto_id WHERE preferencial AND ativo), enforçando no banco 'no máximo 1 fornecedor preferencial ativo por produto'. DROP + CREATE UNIQUE INDEX. Pré-checar dados existentes (pode haver produtos com 2 preferenciais ativos que quebram o CREATE) e limpar antes.",
  "rpc_needed": "",
  "primitive": "db_constraint_unique",
  "test_harness": "integration",
  "test_file": "tests/integration/pf-preferencial-unique.test.ts",
  "red_test": "Inserir 2 linhas siso_produto_fornecedores do mesmo produto_id ambas com preferencial=true e ativo=true deve falhar com violação de unique (23505). Hoje passa (índice não-único). E o PATCH marcando F2 preferencial deve resultar em exatamente 1 preferencial (F1 despromovido).",
  "effort": "S",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota P124 = constraint no banco (só 1 preferencial por produto). É o backstop estrutural; P125 é a mesma decisão na camada UX/lock. Antes de aplicar o UNIQUE, fazer dedup de dados em staging (produtos com >1 preferencial ativo já existente derrubam o CREATE). P124 e P125 são complementares (mesmo arquivo de rota)."
 },
 {
  "id": "P125",
  "current_status": "open",
  "status_evidence": "Backend: src/app/api/wms/produto-fornecedores/[id]/route.ts:66-84 — sem constraint nem lock; 2 cliques simultâneos rodam os 2 UPDATEs intercalados (last-write-wins, 'funciona por sorte'). Frontend: src/components/wms/produto-drawer.tsx — botão 'Tornar preferencial' (linha ~1175, onClick={() => onPatch({ preferencial: true })}) NÃO tem disabled={atualizar.isPending}; a mutation `atualizar` (969-988) existe mas o botão não consome isPending. git log da rota não mostra fix.",
  "root_cause": "Dupla falta de proteção: (a) frontend permite clicar 2x (botão sem disabled durante mutation pendente); (b) backend não tem unique no banco (mesmo gap do P124) servindo de backstop contra corrida.",
  "change_sites": [
   "src/components/wms/produto-drawer.tsx:~1175 — adicionar disabled={atualizar.isPending} (e passar isPending/isLoading via prop ao FornecedorEditCard) no botão 'Tornar preferencial' pra impedir duplo clique; idealmente desabilitar todos os toggles do card enquanto atualizar.isPending",
   "supabase/migrations/<nova>_pf_preferencial_unique.sql — MESMA migration do P124 (UNIQUE INDEX parcial) serve de backstop no banco contra corrida simultânea",
   "src/app/api/wms/produto-fornecedores/[id]/route.ts:85-93 — mapear violação de unique (23505) pra 409 amigável (via wmsErrorResponse) pra o 2º clique receber erro claro em vez de last-write-wins"
  ],
  "needs_migration": true,
  "migration_desc": "Compartilha a migration do P124 (UNIQUE INDEX parcial em produto_id WHERE preferencial AND ativo). Nenhuma migration adicional além dessa.",
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/components/wms/__tests__/produto-drawer-preferencial.test.tsx",
  "red_test": "Renderizar FornecedorEditCard com a mutation pendente: o botão 'Tornar preferencial' deve estar disabled (não disparar onPatch num 2º clique). Hoje não desabilita. (Complementar: teste integration do unique cobre a corrida no banco, reaproveitando o red_test do P124.)",
  "effort": "S",
  "blast_radius": "low",
  "deps": [
   "P124"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota P125 = desabilitar botão na tela + travar no banco (proteção dupla). A parte 'travar no banco' = o mesmo UNIQUE do P124; por isso deps=[P124] (a migration do P124 cobre o backstop de banco daqui). A parte frontend (disabled) é exclusiva do P125. Fazer P124 primeiro evita duplicar a migration."
 },
 {
  "id": "P155",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/itens/[itemId]/equivalente/confirmar/route.ts:155-183 — o fluxo MUTA in-place o mesmo siso_pedido_itens: troca produto_id/produto_id_tiny/sku/descricao/fornecedor_oc do produto A pro equivalente B, reseta compra_status pra 'aguardando_compra' e zera campos de OC. Não cancela A nem cria linha nova pra B (é exatamente o cenário ambíguo do 'today'). Há até um bloqueio de fusão (113-129) quando já existe item com o produto_id de B. git log mostra só fixes de colunas CWB/SP, não a estratégia de troca.",
  "root_cause": "A confirmação de equivalente reescreve o produto do item existente (A→B) em vez de cancelar A e criar um item/pedido novo de B, deixando histórico ambíguo (mesma linha muda de identidade).",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/equivalente/confirmar/route.ts:155-201 — substituir o UPDATE in-place por: (1) marcar o item A como cancelado/indisponivel (compra_status terminal) com motivo 'substituido_por_equivalente', preservando A no histórico; (2) INSERT de um novo siso_pedido_itens (mesmo pedido_id) com o produto B (produto_id/sku/descricao/fornecedor de `equivalente`), compra_status='aguardando_compra', quantidade = quantidadeNecessariaCompra; (3) registrarEvento ligando A→B (sku_original, sku_equivalente, novo_item_id)",
   "src/app/api/wms/compras/itens/[itemId]/equivalente/confirmar/route.ts:134-149 — a liberação da R do pedido (liberarReserva) já existe e continua válida (A vai pra estado terminal, R de A não deve persistir)",
   "Avaliar impacto em quem lê siso_pedido_itens por pedido (cobertura/separação/dashboards) já que agora haverá 2 linhas (A cancelada + B nova) em vez de 1 mutada — confirmar que filtros por compra_status terminal escondem A"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/equivalente-troca-real.ts",
  "red_test": "Item A em equivalente_pendente com equivalente_sku=B. Após POST .../equivalente/confirmar: o item A original deve continuar existindo em estado terminal (cancelado/substituido, NÃO mutado) E deve existir uma NOVA linha de pedido_item com o produto B em aguardando_compra. Hoje só existe 1 linha (a antiga, com identidade trocada pra B).",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "notes": "Nota P155 = cancela A e cria pedido novo de B (histórico limpo). AMBIGUIDADE: 'pedido novo' pode significar (a) nova LINHA de item no mesmo siso_pedidos, ou (b) um siso_pedidos inteiramente novo. Dado que registrarEvento e o fluxo de compra operam por pedido_id existente e que o id do pedido é text/Tiny (não dá pra inventar um id de pedido novo), a leitura mais segura e cirúrgica é (a): cancelar item A + inserir item B no MESMO pedido. Recomendo confirmar com o usuário antes de implementar. O raio-x marcou file=null (não sabiam onde era); localizei em equivalente/confirmar/route.ts:155-183."
 },
 {
  "id": "P178",
  "current_status": "open",
  "status_evidence": "src/lib/wms/fornecedores.ts:219-275 (upsertProdutoFornecedor) — o payload do upsert (244-251) NÃO inclui ativo:true; faz .upsert(..., {onConflict:'produto_id,fornecedor_id'}). Se o vínculo existente está soft-deletado (ativo=false, via DELETE em produto-fornecedores/[id]/route.ts:106), o upsert atualiza codigo/custo/preferencial mas mantém ativo=false → vínculo segue morto. Grep 'ativo' no arquivo só aparece em filtros .eq('ativo',true), nunca em escrita. git log (701fdf0/d149b99) não adicionou reativação.",
  "root_cause": "Reimport do Tiny via upsertProdutoFornecedor não reativa vínculo soft-deletado: o payload do upsert nunca seta ativo=true, então a linha conflitante permanece ativo=false e o histórico/lead time fica inacessível.",
  "change_sites": [
   "src/lib/wms/fornecedores.ts:244-251 — incluir `ativo: true` no payload do upsert de upsertProdutoFornecedor, de modo que reimportar um vínculo do Tiny reative automaticamente um vínculo previamente soft-deletado (recupera histórico/lead time/custo já existentes)",
   "src/lib/wms/fornecedores.ts:82-140 (vincularProdutoFornecedor) — opcional: como esse path é INSERT-only e bate no UNIQUE(produto_id,fornecedor_id) quando há linha soft-deletada, considerar usar/redirecionar pro upsert; mas P178 é especificamente sobre o fluxo de reimport (upsert) — manter cirúrgico no upsert"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/__tests__/fornecedores.upsert-reativa.test.ts",
  "red_test": "Dado um siso_produto_fornecedores existente com ativo=false para (produto P, fornecedor F): chamar upsertProdutoFornecedor({produto_id:P, fornecedor_id:F, codigo_fornecedor:...}) deve resultar na linha com ativo=true (reativada), preservando lead_time/custo existentes. Hoje a linha permanece ativo=false.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [
   "P179"
  ],
  "respeita_nota": true,
  "notes": "Nota P178 = reativar vínculo automaticamente ao reimportar do Tiny. Fix mínimo = adicionar ativo:true no payload do upsert. CONFLITO POTENCIAL com P179: P179 quer BLOQUEAR delete de fornecedor enquanto há vínculos ativos. Se P179 for cascade, vínculos somem; mas P179 escolheu BLOQUEAR (não cascade), então fornecedor com vínculos não é deletado e P178 (reativar vínculo soft-deletado individualmente) coexiste sem contradição. Atenção: o DELETE do fornecedor (P179) só seta fornecedor.ativo=false, não toca vínculos — então a reativação de vínculo do P178 é ortogonal."
 },
 {
  "id": "P179",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/fornecedores/[id]/route.ts:105-116 (DELETE) — apenas `update({ativo:false}).eq('id',id)` no siso_fornecedores; NÃO conta nem bloqueia por vínculos ativos, e NÃO toca siso_produto_fornecedores. Os vínculos (que filtram por .eq('ativo',true) em fornecedores.ts:73-79) seguem ativos apontando pra um fornecedor inativo. git log (701fdf0/62da07e) não adicionou guard. siso_produto_fornecedores tem coluna ativo (20260522_wms_roteamento.sql:35).",
  "root_cause": "DELETE do fornecedor é soft-delete sem preflight: não verifica vínculos ativos em siso_produto_fornecedores, deixando vínculos órfãos apontando pra fornecedor inativo (aparece em 'estoque de fornecedor preferencial').",
  "change_sites": [
   "src/app/api/wms/fornecedores/[id]/route.ts:105-116 (DELETE) — antes do update ativo:false, fazer SELECT count em siso_produto_fornecedores WHERE fornecedor_id=id AND ativo=true; se count>0, retornar 409 com mensagem 'não posso deletar, fornecedor tem N vínculos ativos' (preflight guard), abortando o soft-delete",
   "src/components/wms/produto-drawer.tsx / UI de fornecedores — tratar o 409 mostrando ao admin quantos vínculos precisam ser removidos antes (mensagem do backend já carrega a contagem)"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/fornecedor-delete-bloqueia-vinculos.ts",
  "red_test": "Fornecedor F com ≥1 vínculo ativo em siso_produto_fornecedores: DELETE /api/wms/fornecedores/[F] deve retornar 409 e NÃO setar F.ativo=false (fornecedor permanece ativo). Sem vínculos ativos, DELETE deve funcionar (200, ativo=false). Hoje sempre seta ativo=false ignorando vínculos.",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [
   "P178"
  ],
  "respeita_nota": true,
  "notes": "Nota P179 = BLOQUEAR delete enquanto há vínculos ativos (alinha P063/P133), explicitamente preferindo bloquear sobre cascade. Decisão coerente com soft_delete_block_delete. Relação com P178: P178 reativa vínculo individual no reimport; P179 bloqueia delete do fornecedor — não colidem de fato (P179 escolheu bloquear, não cascade), mas ambos tocam o mesmo par fornecedor↔vínculo, por isso marquei conflicts cruzado pra revisão conjunta. Vale conferir P063/P133 (mesmo cluster?) pra consistência da regra de soft-delete."
 },
 {
  "id": "P002",
  "current_status": "open",
  "status_evidence": "src/lib/webhook-processor-wms.ts:238-246 — quando componente de kit não tem tinyId mapeado na empresa origem, faz logger.warn e `continue` (DROPA o componente). siso_pedido_itens é gravado com produto_id=tinyProdutoId e onConflict='pedido_id,produto_id' (linha 545/554), logo não há como inserir item sem tiny_produto_id. Não existe coluna sem_cadastro_tiny no schema (grep em migrations só acha produto_id_tiny). Pedido/kit sai incompleto.",
  "root_cause": "expandirKits() (webhook-processor-wms.ts:237-257) só inclui o componente se `tinyIdByComponente.get(c.componente_id)` existir; sem mapeamento Tiny na empresa origem ele pula o componente. O upsert de itens depende de produto_id (=tiny) como chave única.",
  "change_sites": [
   "supabase/migrations/<nova>_pedido_itens_produto_wms_sem_cadastro.sql — ADD COLUMN produto_id_wms uuid (FK siso_produtos), ADD COLUMN sem_cadastro_tiny boolean DEFAULT false em siso_pedido_itens; ajustar unique para (pedido_id, produto_id_wms) OU manter produto_id não-nullable populando-o com tinyProdutoId quando existe e fallback estável quando não (NÃO tornar a chave nullable — conforme nota)",
   "src/lib/webhook-processor-wms.ts:237-257 — não fazer `continue` no componente sem tinyId; incluir o componente no array expandido com tinyProdutoId=null + produtoIdWms=c.componente_id + flag sem_cadastro_tiny=true",
   "src/lib/webhook-processor-wms.ts:540-560 — gravar produto_id_wms e sem_cadastro_tiny no upsert; mudar onConflict para chave que tolere tinyProdutoId ausente",
   "src/components/wms/produto-drawer.tsx / página do pedido — mostrar badge 'sem cadastro Tiny' para o item flagueado (mostrar TODOS os itens do kit/pedido, preferir baixa manual)"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "ALTER TABLE siso_pedido_itens ADD COLUMN produto_id_wms uuid REFERENCES siso_produtos(id), ADD COLUMN sem_cadastro_tiny boolean NOT NULL DEFAULT false; criar índice; (re)definir constraint unique tolerante a item sem tiny_produto_id sem tornar produto_id nullable.",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "red_test": "Dado um kit com 2 componentes onde C1 NÃO tem mapeamento siso_produto_empresas na empresa origem, processWebhookWms/expandirKits deve produzir 2 itens (C1 e C2), com C1 marcado sem_cadastro_tiny=true e produto_id_wms preenchido — nunca dropar C1.",
  "test_file": "src/lib/__tests__/webhook-processor-wms-kit-sem-tiny.test.ts",
  "deps": [
   "P095"
  ],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota é explícita e custom: NÃO rejeitar (contradiz a 'question' do registro, que sugeria rejeitar o pedido inteiro — a nota vence). Mostrar todos os itens, flag sem_cadastro_tiny, uuid WMS interno, NÃO tornar a chave nullable. Blast alto porque mexe na chave única de siso_pedido_itens (gotcha: produto_id é tiny_produto_id, parte do onConflict)."
 },
 {
  "id": "P095",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:175-184 — sincronizarComposicaoKit pula componente sem mapeamento (`if (!componenteId) { logger.warn; continue }`), montando kit só com peças mapeadas. src/lib/wms/produtos.ts:10-100 (listarProdutos) e src/app/wms/produtos/page.tsx:18 (TipoFiltro = todos|kit|simples) NÃO têm filtro 'sem mapeamento' / produtos sem siso_produto_empresas. grep 'sem_mapeamento/nao mapeado' não acha nada.",
  "root_cause": "Sync é best-effort e pula componente sem mapeamento (consistente com P002 pré-fix). Falta a ferramenta que a nota pede: um filtro fácil na aba de produtos para listar produtos SEM mapeamento Tiny (siso_produto_empresas vazio), para o operador completar o cadastro.",
  "change_sites": [
   "src/lib/wms/produtos.ts:10-100 — adicionar filtro `sem_mapeamento?: boolean` em listarProdutos: LEFT JOIN/NOT EXISTS em siso_produto_empresas, retornar só produtos sem nenhuma linha de mapeamento",
   "src/app/api/wms/produtos/route.ts:14-40 — ler query param sem_mapeamento e repassar a listarProdutos",
   "src/app/wms/produtos/page.tsx:18,84,142 — adicionar opção de filtro 'Sem mapeamento Tiny' no seletor TipoFiltro/params"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "red_test": "listarProdutos({ sem_mapeamento: true }) retorna apenas produtos que NÃO têm nenhuma linha em siso_produto_empresas; produto com ≥1 mapeamento é excluído da lista.",
  "test_file": "src/lib/wms/__tests__/produtos-sem-mapeamento.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'TODOS os produtos devem cair, com ou sem mapeamento (igual P002)' + 'adicionar filtro facil de produtos SEM mapeamento na aba de produtos' — vence as options que sugeriam bloquear o sync. O entregável central é o FILTRO; o comportamento de sync já é 'cair best-effort'. Mesma família de P002."
 },
 {
  "id": "P097",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:85 grava patch.eh_kit = (full.tipo === 'K') incondicionalmente; linha 121-124 só chama sincronizarComposicaoKit quando full.tipo==='K' && full.kit.length > 0. Logo um Tiny tipo=K com kit[] vazio resulta em eh_kit=true SEM composição — kit fantasma. Não há bloqueio nem aviso.",
  "root_cause": "patch.eh_kit é setado a partir de full.tipo sem exigir que a composição (full.kit) tenha ≥1 item; kit vazio entra silenciosamente.",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:84-85,121-124 — quando full.tipo==='K' && full.kit.length===0, NÃO setar eh_kit=true; em vez disso logar/retornar aviso 'kit sem peças no Tiny — adicione pelo menos 1 componente antes' e propagar o aviso na resposta da rota de sync",
   "src/app/api/wms/produtos/[id]/sync/route.ts — surfacing do aviso (ex.: { aviso } na resposta) para a UI mostrar"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "preflight_guard",
  "test_harness": "vitest_unit",
  "red_test": "syncProdutoTiny de um produto Tiny tipo='K' com kit=[] NÃO grava eh_kit=true e retorna/loga aviso de kit vazio; produto permanece eh_kit=false até ter ≥1 componente.",
  "test_file": "src/lib/wms/__tests__/sync-tiny-kit-vazio.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'bloquear sync de kit vazio e avisar que precisa de pelo menos 1 peca' — opção 2 do registro. Forte overlap com P120 (mesma invariante kit→≥1 componente, mas na borda do SYNC); ver dep."
 },
 {
  "id": "P120",
  "current_status": "partially_fixed",
  "status_evidence": "Cadastro manual NÃO permite kit vazio: src/lib/wms/kits.ts:234-237 (upsertComponente seta eh_kit=true só ao adicionar 1º componente) e :265-275 (removerComponente desmarca eh_kit ao remover o último). produtos POST/PATCH (src/app/api/wms/produtos/route.ts, [id]/route.ts pickPatchFields) NÃO permitem setar eh_kit. PORÉM sync-tiny.ts:85 ainda cria eh_kit=true com 0 componentes (=P097) e webhook-processor-wms.ts:222-228 mantém o kit como item único na hora do pedido (sintoma de consumo). Não há invariante hard (trigger/CHECK) eh_kit→≥1 componente.",
  "root_cause": "A invariante 'kit exige ≥1 componente' existe só implicitamente no editor manual; não há guard de cadastro/DB que cubra outras escritas (sync, escrita direta). Sintoma de runtime: webhook mantém kit sem composição e roteia pra OC.",
  "change_sites": [
   "supabase/migrations/<nova>_kit_exige_componente.sql — trigger BEFORE on siso_produtos: se NEW.eh_kit=true e não existe linha em siso_produto_kits, RAISE EXCEPTION (ou impedir flip eh_kit→true sem composição)",
   "src/lib/wms/sync-tiny.ts:85 — alinhar com P097 (não setar eh_kit=true sem composição)",
   "src/lib/webhook-processor-wms.ts:222-228 — opcional: tornar o kit-sem-composição um erro visível (status erro/aviso) em vez de só warn que cai em OC"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "Trigger/constraint garantindo que siso_produtos.eh_kit=true só persiste com ≥1 linha em siso_produto_kits (invariante de cadastro).",
  "primitive": "db_constraint_unique",
  "test_harness": "integration",
  "red_test": "Tentar persistir siso_produtos.eh_kit=true sem nenhuma linha em siso_produto_kits deve falhar (trigger/constraint); criar kit pelo editor (add 1º componente) deve passar.",
  "test_file": "src/test/integration/kit-exige-componente.int.test.ts",
  "deps": [
   "P097"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: 'bloquear no cadastro: kit exige >=1 componente'. O editor manual já cumpre; o gap real é a invariante hard cobrindo sync e escrita direta — efetivamente o mesmo primitivo de P097 (que cobre só o sync). Marcado partial por isso."
 },
 {
  "id": "P121",
  "current_status": "partially_fixed",
  "status_evidence": "Frontend JÁ valida: src/components/wms/produto-drawer.tsx:1606 (add: n<=0 → toast 'Quantidade inválida') e :1834 (edição inline: só chama onQtyChange se n>0). Servidor valida em src/lib/wms/kits.ts:217-219 (throw new Error 'Quantidade deve ser maior que zero') MAS a rota src/app/api/wms/produtos/[id]/kit/route.ts:80-97 não seta status, cai em wmsErrorResponse com status default 500 → cliente recebe 'internal_error' genérico (api-errors.ts:69), não um 400 legível.",
  "root_cause": "Validação server existe mas vaza como 500 genérico porque o erro de regra de negócio em upsertComponente é um Error comum tratado como 5xx (mascarado para internal_error).",
  "change_sites": [
   "src/app/api/wms/produtos/[id]/kit/route.ts:72-77 — adicionar checagem explícita `typeof body.quantidade !== 'number' || body.quantidade < 1` retornando 400 com mensagem clara ANTES de chamar upsertComponente; e/ou no catch detectar a mensagem de quantidade e devolver status:400",
   "src/lib/wms/kits.ts:217-219 — manter validação (mensagem clara já existe)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "pointwise_bug",
  "test_harness": "scenarios_e2e",
  "red_test": "POST /api/wms/produtos/[id]/kit com quantidade=0 (ou -5) retorna HTTP 400 com mensagem legível ('quantidade deve ser >= 1'), não 500/internal_error.",
  "test_file": "scripts/wms/cenarios/kit-qty-invalida.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'validar qty componente na tela E no servidor (400 quantidade >= 1)'. Tela já feita; falta o 400 legível no servidor (hoje 500 genérico)."
 },
 {
  "id": "P122",
  "current_status": "open",
  "status_evidence": "src/lib/wms/inventario.ts:522-552 — ao bipar um kit, expande SÓ em contagens dos componentes (registrarContagemSimples por componente, linhas 545-549) e faz `return` sem gravar contagem do SKU kit (comentário linha 523-524 confirma 'Não registra contagem pro próprio SKU do kit'). siso_inventario_contagens (migration 20260529_wms_inventario.sql:70-80) NÃO tem coluna de flag/origem (kit_bipado/expandido). Reconciliação (inventario-reconciliacao.ts) agrupa por (loc,produto) vs saldo — kit não tem saldo em siso_estoque, então um row de kit geraria divergência falsa se não for flagueado.",
  "root_cause": "O histórico de contagem só registra componentes expandidos; o evento 'contei 1 kit' não fica rastreável. Não há coluna para distinguir contagem-de-kit (auditoria) de contagem-real-de-saldo (reconciliação).",
  "change_sites": [
   "supabase/migrations/<nova>_inventario_contagem_kit_audit.sql — ADD COLUMN em siso_inventario_contagens: ex. `eh_kit_bipado boolean DEFAULT false` (e opcional kit_qty numeric) para marcar o bipe-de-kit expandido em N",
   "src/lib/wms/inventario.ts:531-552 — ANTES do return, também inserir 1 contagem para o produto_id do kit marcada eh_kit_bipado=true (auditoria honesta: 1 bipe expandido em N)",
   "src/lib/wms/inventario-reconciliacao.ts / a query que monta `contagens` para reconciliação — excluir rows eh_kit_bipado=true do cálculo de divergência (kit não tem saldo em siso_estoque)",
   "src/app/api/wms/produtos/[id]/ultimas-contagens ou tela de histórico — exibir a linha do kit"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "ALTER TABLE siso_inventario_contagens ADD COLUMN eh_kit_bipado boolean NOT NULL DEFAULT false (marca contagem de kit expandida; excluída da reconciliação de saldo).",
  "primitive": "data_visibility_ux",
  "test_harness": "vitest_unit",
  "red_test": "registrarContagem de um kit (qty=1) com 3 componentes grava 4 rows em siso_inventario_contagens: 1 do kit (eh_kit_bipado=true) + 3 dos componentes; e a reconciliação ignora a row do kit (não gera divergência por saldo 0).",
  "test_file": "src/lib/wms/__tests__/inventario-contagem-kit-audit.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: 'gravar contagem do kit + das pecas, marcando 1 bipe expandido em N (auditoria honesta)'. Cuidado obrigatório: row de kit precisa de flag para NÃO contaminar a reconciliação de divergência (kit não tem siso_estoque)."
 },
 {
  "id": "P133",
  "current_status": "open",
  "status_evidence": "Não existe rota DELETE para galpão: src/app/api/wms/admin/galpoes/[id]/route.ts só exporta PUT (linha 9); grep 'export async function (GET|POST|DELETE)' no arquivo só acha PUT. FK em supabase/migrations/20260514_wms_empresa_galpoes_preferenciais.sql:17 é `galpao_id uuid REFERENCES siso_galpoes(id) ON DELETE CASCADE` — confirma o cascateamento descrito no raio-x. Frontend não tem botão delete galpão (grep DELETE em configuracoes só acha roles/conexoes).",
  "root_cause": "A FK preferenciais.galpao_id usa ON DELETE CASCADE, então deletar um galpão remove silenciosamente os preferenciais. Não há guard. Risco hoje é só via SQL direto (não há rota DELETE), mas a invariante de proteção pedida não existe.",
  "change_sites": [
   "supabase/migrations/<nova>_galpao_pref_restrict.sql — DROP FK ON DELETE CASCADE e RECRIAR como ON DELETE RESTRICT em siso_empresa_galpoes_preferenciais.galpao_id (impede delete de galpão preferencial no nível DB)",
   "src/app/api/wms/admin/galpoes/[id]/route.ts — se/quando expor DELETE: preflight checando preferencial (siso_empresa_galpoes_preferenciais), estoque (siso_estoque saldo>0) e pedidos (separacao_galpao_id), retornando 409 com lista do que impede"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "Trocar ON DELETE CASCADE → RESTRICT na FK galpao_id de siso_empresa_galpoes_preferenciais (e idealmente nas demais FKs de saldo/pedido que referenciam galpão).",
  "primitive": "soft_delete_block_delete",
  "test_harness": "integration",
  "red_test": "DELETE direto de um galpão que é preferencial de alguma empresa (ou tem siso_estoque saldo>0) falha por FK RESTRICT; galpão sem vínculos pode ser removido.",
  "test_file": "src/test/integration/galpao-delete-restrict.int.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: 'bloquear delete de galpao se e preferencial de alguma empresa ou tem estoque/pedido; avisar o que impede'. Como não há rota DELETE, o fix mais durável é DB-level (RESTRICT na FK) + guard caso uma rota DELETE seja criada. Alinhado com a política soft-delete de P177."
 },
 {
  "id": "P163",
  "current_status": "partially_fixed",
  "status_evidence": "API JÁ trata 2º clique: src/app/api/wms/cross/produtos/[sku]/veiculos/[id]/route.ts:30-38 — faz preflight maybeSingle e retorna 404 {error:'Veículo não encontrado'} se já removido (NÃO retorna falso sucesso). PORÉM o frontend descarta a mensagem: src/app/wms/cross/[sku]/page.tsx:192 faz `throw new Error('HTTP '+r.status)` e :200 onError mostra toast 'HTTP 404' genérico, não a mensagem clara da API.",
  "root_cause": "Backend já é idempotente e claro (404 com mensagem). O frontend não lê o body de erro JSON, exibindo 'HTTP 404' em vez de 'Esse veículo já foi removido'.",
  "change_sites": [
   "src/app/wms/cross/[sku]/page.tsx:188-193 — no removeVeicMut, ler o JSON de erro (await r.json()) e `throw new Error(json.error ?? 'HTTP '+r.status)` para o toast mostrar 'Veículo não encontrado'/'Esse veículo já foi removido'",
   "src/app/api/wms/cross/produtos/[sku]/veiculos/[id]/route.ts:37 — opcional: trocar mensagem para 'Esse veículo já foi removido' (mais alinhada à nota)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "red_test": "Mock do sisoFetch retornando 404 {error:'Veículo não encontrado'}: a mutation de remover veículo deve rejeitar com a mensagem da API (e onError mostraria essa mensagem), não 'HTTP 404'.",
  "test_file": "src/app/wms/cross/__tests__/remover-veiculo-msg.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'erro claro esse veiculo ja foi removido no 2o clique'. O contrato HTTP já existe (404 + mensagem); falta só surfacing no frontend. Severidade média."
 },
 {
  "id": "P169",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:84-85 grava patch.eh_kit=(full.tipo==='K'); :121-124 só chama sincronizarComposicaoKit (que limpa+reinsere composição, :158-161) quando full.tipo==='K'. Quando tipo muda de K→não-K, eh_kit vira false mas as linhas antigas em siso_produto_kits NÃO são deletadas (sincronizarComposicaoKit não roda) → composição órfã suja.",
  "root_cause": "A limpeza da composição (delete em siso_produto_kits) só acontece dentro de sincronizarComposicaoKit, que só é chamada quando tipo continua 'K'. Na transição K→não-K não há limpeza.",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:84-91 (ou logo após o update do patch) — quando patch.eh_kit===false (tipo deixou de ser 'K'), executar delete em siso_produto_kits WHERE kit_produto_id = produtoId para apagar a composição antiga"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "cache_invalidation",
  "test_harness": "vitest_unit",
  "red_test": "Produto previamente eh_kit=true com 3 linhas em siso_produto_kits; após syncProdutoTiny com full.tipo!=='K', eh_kit=false E zero linhas restantes em siso_produto_kits para esse kit_produto_id.",
  "test_file": "src/lib/wms/__tests__/sync-tiny-kit-para-naokit.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'apagar composicao antiga do kit ao virar nao-kit (sem dados sujos)' — opção 2. Severidade média. Trivial: 1 delete condicional."
 },
 {
  "id": "P173",
  "current_status": "partially_fixed",
  "status_evidence": "DB JÁ aceita 'packing': supabase/migrations/20260528_loc_tipo_packing.sql:8 recria o CHECK incluindo 'packing' (e cria locs PACKING-<galpao>). MAS o tipo TS e as rotas/form NÃO incluem: src/lib/wms/types.ts:6-11 (TipoLocalizacao sem 'packing'), src/app/api/wms/localizacoes/lote/route.ts:8-14 (TIPOS_VALIDOS sem packing), src/app/api/wms/localizacoes/[id]/route.ts:7-13 (idem), src/app/wms/localizacoes/page.tsx:16-22 (TIPOS sem packing).",
  "root_cause": "A migration adicionou 'packing' ao banco mas não propagou para o type/rotas/form — desalinhamento app↔DB.",
  "change_sites": [
   "src/lib/wms/types.ts:6-11 — adicionar | 'packing' a TipoLocalizacao",
   "src/app/api/wms/localizacoes/lote/route.ts:8-14 — adicionar 'packing' a TIPOS_VALIDOS",
   "src/app/api/wms/localizacoes/[id]/route.ts:7-13 — adicionar 'packing' a TIPOS_VALIDOS",
   "src/app/wms/localizacoes/page.tsx:16-22 — adicionar 'packing' ao array TIPOS (e LocTipoBadge se mapear rótulos por tipo)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "pointwise_bug",
  "test_harness": "scenarios_e2e",
  "red_test": "POST /api/wms/localizacoes/lote com tipo='packing' cria a loc (não retorna 'tipo inválido'); e PATCH /api/wms/localizacoes/[id] com tipo='packing' persiste.",
  "test_file": "scripts/wms/cenarios/loc-tipo-packing.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'adicionar tipo embalagem/packing como valido no formulario — e usado no ciclo de cross-docking (loc PACKING)'. DB já feito (20260528). Termo canônico no DB é 'packing' (não 'embalagem') — usar 'packing'."
 },
 {
  "id": "P174",
  "current_status": "open",
  "status_evidence": "src/lib/wms/produtos.ts:124-139 (criarProduto) faz insert e `if (error) throw error` sem tratar 23505. A rota src/app/api/wms/produtos/route.ts:60-71 manda pro wmsErrorResponse SEM status → default 500 (api-errors.ts:52,69) → cliente recebe {error:'internal_error'} genérico. SKU é UNIQUE (CLAUDE.md/schema), então dup vira 23505 mascarado em 500.",
  "root_cause": "Violação de unique (SKU duplicado) não é mapeada para 409/400 com mensagem clara; cai no caminho 5xx genérico.",
  "change_sites": [
   "src/app/api/wms/produtos/route.ts:60-71 — no catch (ou em criarProduto), detectar code==='23505' e retornar 409 com {error:'Esse código já existe', sku} para a UI oferecer link 'encontrar na lista'",
   "src/lib/wms/produtos.ts:132-138 — opcional: traduzir 23505 em erro tipado para a rota distinguir",
   "frontend de cadastro de produto (modal de novo produto) — ao receber 409, mostrar mensagem + ação de buscar o SKU existente na lista"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "pointwise_bug",
  "test_harness": "scenarios_e2e",
  "red_test": "POST /api/wms/produtos com um sku já existente retorna 409 (ou 400) com mensagem clara 'esse código já existe', não 500/internal_error.",
  "test_file": "scripts/wms/cenarios/produto-sku-duplicado.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'mensagem clara esse codigo ja existe + link pra achar na lista'. O problema 'race de duplo clique' resolve-se pela própria unique do DB; o gap é traduzir 23505 em 409 legível. O 'link pra achar na lista' é UI no modal de cadastro."
 },
 {
  "id": "P175",
  "current_status": "open",
  "status_evidence": "src/lib/wms/produtos.ts:141-154 (atualizarProduto) e src/app/api/wms/produtos/[id]/route.ts:49-66 (PATCH) fazem o update sem registrar autor/antes/depois — nenhum log de auditoria. Não existe tabela de auditoria de produto (grep audit/historico_produto em migrations não acha nada de produto). pickPatchFields (route.ts:22-46) permite editar descricao/gtin/ncm/etc. que são campos sincronizados do Tiny (sync-tiny.ts:72-85), sem bloqueio.",
  "root_cause": "Edição manual de produto não captura quem/quando/antes/depois e não distingue campos auto-sincronizados (que o sync sobrescreve) dos campos editáveis.",
  "change_sites": [
   "supabase/migrations/<nova>_produto_audit_log.sql — criar siso_produto_audit (produto_id, campo, valor_antes, valor_depois, editado_por uuid, editado_em) OU registrar via historico-service/siso_logs",
   "src/app/api/wms/produtos/[id]/route.ts:49-66 (PATCH) — buscar valores atuais (getProduto) antes do update e gravar diff + session.id na auditoria",
   "src/app/api/wms/produtos/[id]/route.ts:22-46 (pickPatchFields) — remover/bloquear campos auto-sincronizados do Tiny (ex.: descricao, gtin, ncm, cest, origem_fiscal) da edição manual, conforme nota"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "Criar tabela siso_produto_audit (ou equivalente) para registrar quem/quando/antes/depois das edições de produto.",
  "primitive": "data_visibility_ux",
  "test_harness": "integration",
  "red_test": "PATCH /api/wms/produtos/[id] alterando um campo editável registra uma linha de auditoria com produto_id, campo, valor_antes, valor_depois e editado_por=session.id; e tentar editar um campo auto-sincronizado é rejeitado/ignorado.",
  "test_file": "src/test/integration/produto-edit-audit.int.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: 'log de auditoria (quem/quando/antes/depois) na edicao de produto; bloquear campos auto-sincronizados'. Dois entregáveis: auditoria + bloqueio de campos do Tiny. Severidade média."
 },
 {
  "id": "P177",
  "current_status": "already_fixed",
  "status_evidence": "Não existe hard-delete de localização: o único caminho é desativarLocalizacao (src/lib/wms/localizacoes.ts:121-133) que faz `update({ativo:false})` (soft-delete), chamado por src/app/api/wms/localizacoes/[id]/route.ts:71 (DELETE) e substituir-e-excluir/route.ts. grep '.delete()' em siso_localizacoes no src/ retorna VAZIO. Como a linha da loc permanece (ativo=false), o INNER JOIN da RPC em 20260526_wms_produto_ultimas_contagens_3d.sql:62 continua resolvendo a loc — histórico de contagem é preservado.",
  "root_cause": "A política da nota ('nunca deletar prateleira, só marcar inativa') já é o comportamento de fato do app: desativarLocalizacao é soft-delete e é o único caminho de remoção. A órfã de contagem só ocorreria via DELETE SQL direto, que o app nunca emite.",
  "change_sites": [],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "soft_delete_block_delete",
  "test_harness": "vitest_unit",
  "red_test": "Garantir invariante: desativarLocalizacao seta ativo=false (não deleta a linha) e a contagem prévia daquela loc continua retornável por wms_produto_ultimas_contagens. (Teste de regressão para impedir reintrodução de hard-delete.)",
  "test_file": "src/lib/wms/__tests__/desativar-localizacao-soft.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: 'nunca deletar prateleira, so marcar inativa (preserva contagens; alinha P063/P115/P133)'. Já implementado via soft-delete; recomendo apenas um teste de regressão para travar a invariante. Alinhado com P133 (mesma família de proteção de delete)."
 },
 {
  "id": "P034",
  "current_status": "partially_fixed",
  "status_evidence": "Existem helpers de liberação (src/lib/wms/reservas.ts:38 liberarReserva, :135 estornarReservaIndividual) e rotas admin (/pedidos/[id]/estornar/route.ts:76 e /pedidos/[id]/liberar-reservas/route.ts:61) que liberam R. O webhook-cancel já libera R (cenário 30-webhook-cancel-libera-r.ts). PORÉM o caminho do operador NÃO libera: (a) bloco 'rejeitado' em pedidos/aprovar/route.ts:68-98 marca status='cancelado' SEM liberar R; um pedido transferencia/propria em 'pendente' já tem R criada pelo webhook (webhook-processor-wms.ts:583-587, ttl 30d). (b) compras/itens/[itemId]/cancelamento/confirmar/route.ts:47-48 removeu o cleanup de snapshot mas NÃO libera a R viva criada pelo reconciliador (reconciliador-oc.ts:186). A R fica presa até morrer por TTL (30d).",
  "root_cause": "O caminho de cancelamento acionado pelo operador (Recusar no painel + confirmar cancelamento de item de compra) não chama estornarReservaIndividual/liberarReserva para as movs R vivas do pedido; só os caminhos admin e webhook fazem.",
  "change_sites": [
   "src/app/api/wms/pedidos/aprovar/route.ts:68-98 — no bloco decisao==='rejeitado', antes do update status='cancelado', listar movs tipo='R' origem_tipo='reserva_pedido' com origem_id=pedidoId e estorno_de IS NULL e estornar cada uma via estornarReservaIndividual (padrão de /liberar-reservas/route.ts:47-66).",
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:50-71 — após marcar item cancelado, liberar a R viva associada ao item/pedido (estornarReservaIndividual) quando o item tinha reserva (reconciliado)."
  ],
  "needs_migration": false,
  "primitive": "reserva_wiring_cancel",
  "test_harness": "scenarios_e2e",
  "red_test": "Pedido transferencia/propria em 'pendente' com R viva (qty reservada=N). Operador chama POST /pedidos/aprovar com decisao='rejeitado'. Assert: pedido status='cancelado' E siso_estoque.reservado volta a 0 imediatamente (R estornada), não espera TTL.",
  "test_file": "scripts/wms/cenarios/catalogo/34-recusar-pedido-libera-r.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "notes": "Reaproveita o loop de /pedidos/[id]/liberar-reservas/route.ts:47-66. Cenário 30 já cobre webhook-cancel; o gap é o Recusar do operador e o confirmar-cancelamento de compra.",
  "respeita_nota": true
 },
 {
  "id": "P035",
  "current_status": "open",
  "status_evidence": "reconciliador-oc.ts:38 STATUS_PEDIDO_OC=['validacao_oc','aguardando_compra'] e :71 filtra pedidos só nesses status. Quando a compra é cancelada e todos os itens viram terminal, compras-utils.ts:125-169 checkAndCancelPedidoIfAllTerminal seta status='cancelado'+status_separacao=null — fora do conjunto que o reconciliador varre. Logo, quando o estoque chega (mov E → reconciliarEntradaEstoque), o pedido cancelado nunca é religado.",
  "root_cause": "Cancelar a compra (todos itens terminais) cancela o pedido permanentemente (status_separacao=null), e o reconciliador de saldo só reconsidera status_separacao IN ('validacao_oc','aguardando_compra'). Não existe transição 'compra cancelada → pedido volta a pendente'.",
  "change_sites": [
   "src/lib/compras-utils.ts:125-169 — em checkAndCancelPedidoIfAllTerminal, quando o motivo dos terminais é 'cancelado' (não 'indisponivel'), em vez de cancelar o pedido, resetar para reentrada: status='pendente', status_separacao=null/pendente, limpar campos de compra dos itens (compra_status=null, ordem_compra_id=null) para roteamento/reconciliador re-avaliar.",
   "src/lib/wms/reconciliador-oc.ts:38 — opcionalmente incluir o estado de reentrada no STATUS_PEDIDO_OC se a estratégia for reconciliar diretamente."
  ],
  "needs_migration": false,
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "red_test": "Pedido OC em 'aguardando_compra', compra criada. Cancelar a compra (item → cancelado). Depois entra estoque (mov E) que cobre o item. Assert: pedido NÃO fica preso em 'cancelado'; volta a 'pendente'/reconcilia e (se há saldo) avança para aguardando_nf.",
  "test_file": "scripts/wms/cenarios/catalogo/35-compra-cancelada-pedido-volta-pendente.ts",
  "deps": [
   "P034"
  ],
  "conflicts": [
   "P040"
  ],
  "notes": "CONFLITO com checkAndCancelPedidoIfAllTerminal: hoje 'todos terminais → cancela pedido'. A nota quer 'compra cancelada → pedido volta a pendente'. Precisa distinguir terminal-por-cancelamento (reentra) de terminal-por-indisponivel (morre). Decisão de produto necessária.",
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P040",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:40-67: só valida compra_status==='cancelamento_pendente' (linha 40) e então zera separacao_marcado/quantidade_bipada/bipado_completo (linhas 58-64) SEM checar se há quantidade já separada/picada nem estornar movs de picking. Bate com o 'today'.",
  "root_cause": "A confirmação de cancelamento de item zera os campos de separação/bipagem sem (a) bloquear quando há quantidade separada, nem (b) estornar as movs de pick — perde rastreabilidade.",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:27-45 — ao buscar o item, incluir quantidade_pega/quantidade_bipada/mov_saida_id; se quantidade separada > 0 (ou mov_saida_id != null), retornar 409 'desfaça a separação antes de cancelar' e NÃO aplicar o update das linhas 51-67.",
   "src/app/api/wms/compras/itens/[itemId]/cancelamento/confirmar/route.ts:51-64 — remover o zeramento cego dos campos de separação (inalcançável após o guard)."
  ],
  "needs_migration": false,
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "red_test": "Item de compra com quantidade_pega>0 (separação iniciada) e compra_status='cancelamento_pendente'. POST /cancelamento/confirmar. Assert: 409, item permanece com compra_status='cancelamento_pendente' e quantidade_pega intacta.",
  "test_file": "scripts/wms/cenarios/catalogo/40e-cancelar-item-com-separacao-bloqueia.ts",
  "deps": [],
  "conflicts": [],
  "notes": "Nota é op2 (bloquear). Guard simples no início da rota. Família de guards de pré-condição com P047.",
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true
 },
 {
  "id": "P047",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts: grep confirma NENHUMA referência a estoque_lancado/lancado/mov_saida_id/estorno. A rota só desvincula o item da OC (linhas 44-58) e volta para 'aguardando_compra', sem checar se o pedido já teve estoque lançado. cutover.ts confirma que siso_pedidos.estoque_lancado existe. Logo, devolver item de pedido já lançado deixa pedido 'pronto' com item solto.",
  "root_cause": "A devolução de item de compra não tem preflight contra estado já-lançado do pedido — desvincula o item independentemente do pedido já ter estoque baixado.",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:28-39 — ao buscar o item, fazer lookup do pedido (siso_pedidos.estoque_lancado via item.pedido_id); se estoque_lancado=true, retornar 409 com instrução clara.",
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:40-60 — esse guard tem PRECEDÊNCIA sobre o estorno do P046 (mesma rota): bloquear antes de qualquer estorno/desvínculo."
  ],
  "needs_migration": false,
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "red_test": "Pedido com estoque_lancado=true e um item OC. POST /compras/itens/[itemId]/devolver. Assert: 409, item permanece vinculado à OC e compra_status inalterado (não vai pra aguardando_compra).",
  "test_file": "scripts/wms/cenarios/catalogo/47-devolver-item-pedido-lancado-bloqueia.ts",
  "deps": [],
  "conflicts": [
   "P046"
  ],
  "notes": "Nota explícita: PRECEDÊNCIA sobre o estorno do P046 na MESMA rota — o guard de P047 deve rodar ANTES da lógica de estorno que P046 quer adicionar.",
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true
 },
 {
  "id": "P067",
  "current_status": "open",
  "status_evidence": "src/lib/wms/transferencias.ts:588-665 desfazerRecebimentoTransferencia: estorna as movs E num loop (619-643), depois faz UPDATE separado dos itens (645-652) e do header (654-662). São 3 statements PostgREST independentes, sem transação/RPC. Se o UPDATE de itens (passo 4) falhar após os estornos (passo 3), o estoque é devolvido mas itens/header continuam 'recebida'.",
  "root_cause": "Desfazer recebimento de transferência não é atômico: estorno de ledger + reset de itens + reset de header em chamadas separadas, sem rollback se uma falhar no meio.",
  "change_sites": [
   "src/lib/wms/transferencias.ts:588-665 — envolver passos 3-5 numa RPC plpgsql única (estorno das legs E + reset itens + reset header) numa transação tudo-ou-nada; falha em qualquer passo aborta/reverte (operador re-tenta limpo)."
  ],
  "needs_migration": true,
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_desfazer_recebimento_transferencia(transferencia_id uuid, usuario_id uuid, motivo text) — estorna legs E + reseta itens (mov_entrada_id/localizacao_destino_id) + volta header a em_transito, numa transação tudo-ou-nada.",
  "test_harness": "integration",
  "red_test": "Transferência 'recebida' com N itens. Simular falha no reset de itens/header após estornar as movs E. Assert: estado final é tudo-ou-nada — ou movs E vivas e header='recebida' (rollback), ou tudo desfeito; nunca movs estornadas com header ainda 'recebida'.",
  "test_file": "src/lib/wms/transferencias-desfazer-atomico.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "Estorno via estornarMovimentacao não é facilmente reversível em JS se o passo seguinte falhar; forma robusta é RPC plpgsql transacional. Mesma classe de P075/P077.",
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P075",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/vendas/criar/route.ts:514-538 rollbackBaixaDireta itera movsCriadas chamando estornarMovimentacao best-effort; em falha de estorno só loga 'DADOS POSSIVELMENTE INCONSISTENTES' (524-534) e MESMO ASSIM deleta itens+pedido (536-537). Não há lock de prateleira nem atomicidade nas 3 saídas.",
  "root_cause": "O rollback de venda baixa_direta é best-effort e segue deletando o pedido mesmo com estorno incompleto; sem advisory lock na prateleira nem operação tudo-ou-nada nas movs S, o estoque fica meio-revertido sob falha de banco.",
  "change_sites": [
   "src/app/api/wms/vendas/criar/route.ts:540-600 (criação das movs S) + 514-538 (rollback) — mover criação das N movs S + rollback para uma RPC plpgsql transacional que tranca as triplas envolvidas (pg_advisory_xact_lock ou SELECT FOR UPDATE em siso_estoque) e garante tudo-ou-nada; trava auto-libera no fim da transação.",
   "src/app/api/wms/vendas/criar/route.ts:536-537 — não deletar pedido/itens se o estorno não completou (inalcançável com a RPC transacional)."
  ],
  "needs_migration": true,
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_vender_baixa_direta_atomico(...) — numa transação: advisory lock por (produto,galpao,loc), insere as N movs S; qualquer erro aborta a transação inteira (nenhuma S persiste).",
  "test_harness": "integration",
  "red_test": "Venda baixa_direta com 3 saídas onde a 4ª/última falha (saldo insuficiente forçado). Assert: nenhuma das 3 movs S anteriores permanece (saldo volta 100% ao original) e o pedido não é persistido.",
  "test_file": "src/lib/wms/vendas-baixa-direta-atomico.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "Nota: trancar prateleira durante o estorno (advisory lock) + tudo-ou-nada nas 3 saídas + trava auto-libera após queda. Caminho transacional plpgsql satisfaz os 3.",
  "effort": "L",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P077",
  "current_status": "open",
  "status_evidence": "src/lib/wms/vendas-cancelamento.ts:105-121 (caminho baixa_direta) itera estornarMovimentacao por mov S num loop; se a 3ª falhar lança erro com as 2 primeiras JÁ estornadas e o pedido ainda não marcado 'cancelado' (update só em 124-130). Não há bloco indivisível. Bate com o 'today'.",
  "root_cause": "Cancelamento de venda (modo baixa_direta) estorna as movs uma a uma sem atomicidade; falha no meio deixa parte das devoluções aplicadas e o pedido não-cancelado.",
  "change_sites": [
   "src/lib/wms/vendas-cancelamento.ts:91-130 — agrupar o estorno das N movs S + o update status='cancelado' numa RPC plpgsql transacional (tudo-ou-nada): só marca cancelado se todas as devoluções forem aplicadas na mesma transação; falha reverte tudo.",
   "src/lib/wms/vendas-cancelamento.ts:70-89 (caminho R) — também garantir atomicidade entre liberarReserva e update de status."
  ],
  "needs_migration": true,
  "primitive": "atomic_rpc_ledger",
  "rpc_needed": "wms_cancelar_venda_atomico(pedido_id text, usuario_id uuid, motivo text) — numa transação: estorna todas as movs S da venda + seta pedido cancelado; aborta inteiro se qualquer estorno falhar.",
  "test_harness": "integration",
  "red_test": "Venda baixa_direta com 3 movs S. Forçar falha no 3º estorno. Assert: nenhuma das 2 primeiras devoluções persiste (saldo inalterado) E o pedido continua status != 'cancelado'.",
  "test_file": "src/lib/wms/vendas-cancelamento-atomico.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "flow-055. Mesma família de P075. Reusar a mesma RPC/abordagem transacional. Nota: 3 devoluções como bloco indivisível.",
  "effort": "L",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P078",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts:646-680 reverterReplenishment chama estornarMovimentacao por mov; o catch (673-677) só engole 'já foi estornada'/'já é um estorno'. ledger.ts:386-392 estornarMovimentacao lança 'tem qty_estornada=N (>0). Use wms_estornar_parcial_movimentacao' quando há estorno parcial prévio — NÃO engolido pelo catch → a reversão inteira falha. A RPC wms_estornar_parcial_movimentacao existe (migration 20260518_realocacao_fix_pack_rpc_estorno_parcial.sql; usada em separacao/desfazer-parcial/route.ts:94) mas reverterReplenishment não a usa.",
  "root_cause": "reverterReplenishment usa só estornarMovimentacao (full estorno), que bloqueia quando a mov já tem estorno parcial (qty_estornada>0); não cai para wms_estornar_parcial_movimentacao para o residual.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:664-678 — no loop, ler também qty_estornada/quantidade; se qty_estornada>0 (e < quantidade), reverter o residual via RPC wms_estornar_parcial_movimentacao (qty = quantidade - qty_estornada) em vez de estornarMovimentacao; full estorno só quando qty_estornada=0."
  ],
  "needs_migration": false,
  "primitive": "estorno_ledger_lookup",
  "rpc_needed": "",
  "test_harness": "integration",
  "red_test": "Replenishment de qty=5 (par S+E). Estornar parcialmente 3 via wms_estornar_parcial_movimentacao. Depois POST /replenishment/[id]/reverter. Assert: as 2 restantes são revertidas (saldo volta integral à loc origem), sem erro 'qty_estornada>0'.",
  "test_file": "src/lib/wms/reverter-replenishment-residual.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "A RPC de estorno parcial já existe e está testada (realoc-fix-pack.test.ts). Mudança cirúrgica em movimentacoes.ts. Nota: reverter só o que sobrou após estorno parcial.",
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P106",
  "current_status": "partially_fixed",
  "status_evidence": "Guard de CÓDIGO em src/lib/wms/ledger.ts:379-384 (estornarMovimentacao busca outra mov com estorno_de=mov_id e lança se existir) — mas é TOCTOU sob concorrência. No banco há apenas um INDEX não-único: migration 20260508_wms_foundation.sql:156 'CREATE INDEX idx_mov_estorno ON siso_movimentacoes(estorno_de) WHERE estorno_de IS NOT NULL'. NÃO existe constraint UNIQUE em estorno_de (grep em supabase/migrations confirma). O 'file' do raio-x (relatorios/movs-por-empresa:53) é ponteiro defasado — essa rota é read-only e não cancela nada.",
  "root_cause": "A proteção contra cancelamento duplo de uma entrada é só no código (estornarMovimentacao), suscetível a corrida; falta a constraint UNIQUE no banco que a nota pede.",
  "change_sites": [
   "supabase/migrations/ (novo arquivo) — CREATE UNIQUE INDEX uq_mov_estorno_unico ON siso_movimentacoes(estorno_de) WHERE estorno_de IS NOT NULL; (promove o idx existente a UNIQUE). Garante que cada mov só pode ser estornada uma vez no banco.",
   "src/lib/wms/ledger.ts:399-416 — tratar o erro 23505 do INSERT como 'já estornada' (idempotente), em vez de propagar erro genérico."
  ],
  "needs_migration": true,
  "primitive": "db_constraint_unique",
  "rpc_needed": "",
  "test_harness": "integration",
  "red_test": "Inserir mov E. Disparar DOIS estornos concorrentes da mesma mov. Assert: exatamente 1 mov S de estorno é criada; a 2ª é rejeitada pela constraint (23505) e tratada como idempotente — saldo anula a entrada apenas uma vez.",
  "test_file": "src/lib/wms/estorno-duplo-constraint.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "Nota explícita pede UNIQUE no banco (não validação de código). O índice atual idx_mov_estorno é não-único; basta promovê-lo a UNIQUE. Cenário 24-cancelar-pedido-oc-recebida já exercita estorno único de mov E.",
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true
 },
 {
  "id": "P119",
  "current_status": "already_fixed",
  "status_evidence": "Não existe endpoint de DELETE real de produto: find em src/app/api/wms/produtos mostra route.ts (GET+PATCH) e [id]/route.ts (GET linha 8 + PATCH linha 48, sem DELETE). O único 'export async function DELETE' em produtos é [id]/kit/route.ts:101 (remove componente de kit, não o produto). Soft-delete já existe: [id]/route.ts:44 aceita 'ativo: boolean' via PATCH; produtos.ts:85 filtra por ativo. O único .delete() em sync-tiny.ts:158-161 é sobre siso_produto_kits (composição), NÃO siso_produtos.",
  "root_cause": "N/A — o comportamento desejado (bloquear delete real, usar desativar via flag ativo) já é o estado atual do backend.",
  "change_sites": [],
  "needs_migration": false,
  "primitive": "soft_delete_block_delete",
  "rpc_needed": "",
  "test_harness": "manual_only",
  "red_test": "Verificação: nenhum endpoint /api/wms/produtos/[id] aceita DELETE; PATCH com {ativo:false} desativa preservando histórico/estoque. Confirmar também que a UI não oferece afordância de 'excluir produto' que mapeie para hard-delete.",
  "test_file": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "notes": "Backend já satisfaz a nota (sem hard-delete, soft via ativo). RESTA verificar o FRONTEND (telas de cadastro/sync) para garantir que não há botão 'deletar' enganoso — fora do escopo de rota. already_fixed no backend; auditar UI separadamente se quiser garantia ponta-a-ponta.",
  "respeita_nota": true
 },
 {
  "id": "P159",
  "current_status": "open",
  "status_evidence": "src/lib/wms/inventario.ts:1161-1226 estornarSessaoInventario estorna a SESSÃO INTEIRA: itera TODAS as divergencias status='aplicada' da sessão (1183-1187) e volta a sessão a 'revisao'. A rota src/app/api/wms/inventario/[id]/estornar/route.ts:36 chama por sessao_id. Não há função/rota de estorno por divergência/contagem individual. Bate com o 'today'.",
  "root_cause": "Só existe estorno em lote por sessão; não há granularidade por divergência/contagem individual.",
  "change_sites": [
   "src/lib/wms/inventario.ts:1161-1226 — adicionar função estornarDivergenciaInventario({ divergencia_id, usuario_id, motivo }) que estorna 1 mov_aplicada_id, reseta aquela divergência para 'pendente' e NÃO mexe nas demais nem força a sessão inteira a 'revisao' (recalcula status da sessão conforme restem aplicadas).",
   "src/app/api/wms/inventario/[id]/estornar/route.ts:18-41 — aceitar opcionalmente um divergencia_id no body para roteamento ao estorno individual; manter o estorno de sessão como caminho separado."
  ],
  "needs_migration": false,
  "primitive": "estorno_ledger_lookup",
  "rpc_needed": "",
  "test_harness": "integration",
  "red_test": "Sessão aplicada com 4 divergências aplicadas (3 corretas, 1 errada). Estornar SÓ a divergência errada. Assert: apenas 1 mov estornada, aquela divergência volta a 'pendente', as outras 3 permanecem 'aplicada' com saldo intacto; a sessão não força recontagem das 3 certas.",
  "test_file": "src/lib/wms/estornar-divergencia-individual.test.ts",
  "deps": [],
  "conflicts": [],
  "notes": "Severidade média. Reusa o guard de double-estorno de estornarMovimentacao. Vizinho: cenário 41-estornar-inventario-aplicado.ts (estorno de sessão) serve de base.",
  "effort": "M",
  "blast_radius": "low",
  "respeita_nota": true
 },
 {
  "id": "P005",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/pedidos/aprovar/route.ts:340-359 — enqueue de 'lancar_estoque' em siso_fila_execucao com `if (queueError) { ...log... // Don't fail — the order status is already updated }`. O erro de enfileiramento é engolido; o pedido fica com status='executando'/status_separacao='aguardando_nf' (linhas 318-326) mas sem job na fila. Não existe sweep/cron que detecte pedidos 'executando' sem job (grep por reclaim/stale/executando-sweep não achou nada). after()+kickWorker (378-385) só drena jobs JÁ enfileirados — não recria o que falhou.",
  "root_cause": "O insert na fila é best-effort: a falha não rola back a transição de status do pedido nem reenfileira. Resultado é estado fantasma 'aprovado mas sem tarefa'. Não há durabilidade (transação única) nem retry do enqueue.",
  "change_sites": [
   "src/app/api/wms/pedidos/aprovar/route.ts:340-359 — trocar o 'Don't fail' por: em queueError, fazer retry curto do insert (ex.: 2-3 tentativas) e, se persistir, retornar 500 com volta do pedido a status anterior (ou marcar para reprocesso); preferir garantir o enqueue (durabilidade) antes de responder ok",
   "src/app/api/wms/pedidos/aprovar/route.ts:328-337 — encadear a transição de status do pedido + enqueue como unidade (idealmente via RPC plpgsql que atualiza siso_pedidos e insere em siso_fila_execucao na mesma transação), pra não deixar pedido aprovado sem job",
   "src/lib/execution-worker-wms.ts:36-130 — confirmar/reforçar idempotência por reserva (já existe convertidasSet via estorno_de em 103-130) pra suportar 'sistema tolerante a duplicatas' da nota caso o enqueue rode 2x"
  ],
  "needs_migration": true,
  "migration_desc": "Opcional mas recomendado pela nota (durabilidade): RPC plpgsql que numa única transação faz UPDATE siso_pedidos (status/status_separacao) + INSERT siso_fila_execucao, garantindo que pedido aprovado SEMPRE tem job. Sem ela, o fix fica no app com retry+rollback de status, sem atomicidade real entre as duas tabelas.",
  "primitive": "durable_enqueue_retry",
  "test_harness": "integration",
  "test_file": "src/app/api/wms/pedidos/aprovar/aprovar-enqueue.integration.test.ts",
  "red_test": "Aprovar um pedido (transferencia/oc) deve garantir que existe exatamente 1 job 'lancar_estoque' em siso_fila_execucao OU que o pedido NÃO ficou em 'executando' (foi revertido) — nunca o par (pedido aprovado, fila vazia). Simular falha do insert na fila e asseverar que a resposta não é ok:true silencioso com pedido órfão.",
  "rpc_needed": "wms_aprovar_e_enfileirar(pedido_id text, decisao text, ...) — atualiza status do pedido e insere job na fila atomicamente",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "high",
  "respeita_nota": true,
  "notes": "Nota escolheu opção 2 (retry automático + tolerância a duplicatas), NÃO a opção 1 (retornar erro e voltar pra pendente). A idempotência por reserva (estorno_de) JÁ existe no worker-wms, então a 'tolerância a duplicatas' está parcialmente coberta; o que falta é a durabilidade/retry do enqueue. blast alto: aprovar é caminho crítico de 500 ped/dia."
 },
 {
  "id": "P008",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/pedidos/[id]/estornar/route.ts:91-94 seta `status_separacao='cancelado'` (NÃO `status`) e NUNCA toca siso_fila_execucao (grep 'fila_execucao' = 0 ocorrências). O guard do worker em src/lib/execution-worker.ts:157-171 checa `siso_pedidos.status === 'cancelado'` — coluna DIFERENTE da que o estornar atualiza — logo o job NÃO é pulado por esse guard. siso_fila_execucao já tem status 'cancelado' no CHECK (supabase/migrations/20260309_create_execution_queue.sql:27).",
  "root_cause": "O estorno admin cancela o pedido (via status_separacao) e estorna movs, mas deixa o job 'lancar_estoque' vivo na fila e não bate no guard do worker (que lê outra coluna). O worker-wms tem idempotência por reserva via estorno_de (execution-worker-wms.ts:103-130) que MITIGA o double-release das MESMAS reservas, mas a nota é vinculante: cancelar a tarefa na fila explicitamente.",
  "change_sites": [
   "src/app/api/wms/pedidos/[id]/estornar/route.ts:91-94 — após marcar pedido cancelado, fazer UPDATE siso_fila_execucao SET status='cancelado' WHERE pedido_id=pedidoId AND status IN ('pendente','executando','erro') pra desativar o job enfileirado",
   "src/app/api/wms/pedidos/[id]/estornar/route.ts:43-50 — (opcional) também setar siso_pedidos.status='cancelado' além de status_separacao, pra alinhar com o guard do worker em execution-worker.ts:163"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "reserva_wiring_cancel",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/cancelar-job-fila-estorno.ts",
  "red_test": "Aprovar pedido (gera job 'lancar_estoque' pendente na fila), chamar POST /pedidos/[id]/estornar, e asseverar: (a) o job em siso_fila_execucao virou status='cancelado'; (b) rodar o worker depois NÃO cria novas movs S/L nem altera saldo/reservado (sem double-release).",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (cancelar a tarefa na fila). O double-release real do 'today' já é parcialmente neutralizado pela idempotência por estorno_de no worker-wms, mas o fix obrigatório da nota é cancelar o job. Atenção ao gap de coluna status vs status_separacao — vale alinhar as duas. Relaciona-se com P034/P038/P039 (release de reserva no cancelamento) mas é independente."
 },
 {
  "id": "P123",
  "current_status": "open",
  "status_evidence": "src/lib/wms/fornecedores.ts:328-351 — loop com SELECT (jaExiste) seguido de INSERT separado, sem upsert: `if (jaExiste) {existentes++; continue} ... const {error}=insert(...); if (!error) criados++`. Sob concorrência ambos veem jaExiste=null, ambos inserem, um falha pelo UNIQUE(nome) e a contagem fica inconsistente (1 criado real reportado como 0/1 dividido). O UNIQUE já existe: supabase/migrations/20260522_wms_roteamento.sql:10 `nome text NOT NULL UNIQUE`.",
  "root_cause": "Padrão check-then-insert (TOCTOU) em vez de upsert. Como a contagem só incrementa quando o insert retorna sem erro, requisições concorrentes produzem contagem incorreta mesmo com o UNIQUE protegendo o dado.",
  "change_sites": [
   "src/lib/wms/fornecedores.ts:328-351 — substituir o SELECT+INSERT por upsert único: sb.from('siso_fornecedores').upsert({nome, prefixo_sku, observacoes}, {onConflict:'nome', ignoreDuplicates:true}); derivar criados/existentes de forma determinística (ex.: select prévio do conjunto existente por nome, ou usar o retorno do upsert) para que dois cliques concorrentes somem o mesmo total correto"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "db_constraint_unique",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/fornecedores.test.ts",
  "red_test": "Com um fornecedor já existente (mesmo nome), chamar autoCriarFornecedoresDosPrefixosSku 2x (ou simular insert que retorna erro de unique) deve resultar em contagem coerente — o fornecedor existente conta como 'existente', não como falha, e o total criados+existentes bate com o PADRAO, sem depender de qual requisição ganhou a corrida.",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (upsert/gravar-ou-atualizar, contagem correta pros dois cliques). UNIQUE(nome) já existe — sem migration. A nota diz 'auto-cadastro de mapeamento' mas o file aponta para o auto-cadastro de fornecedores por prefixo de SKU (mesmo bloco 345-351); o approach (upsert) é o mesmo."
 },
 {
  "id": "P145",
  "current_status": "open",
  "status_evidence": "src/lib/execution-worker.ts:123-149 — processQueue só seleciona jobs com `.eq('status','pendente')` e faz claim atômico `update status='executando' ... .eq('status','pendente')`. Não há nenhuma rotina que devolva jobs 'executando' há muito tempo para 'pendente'. grep por reclaim/stale/'interval 5'/executando-timeout em execution-worker*.ts e migrations de fila = nada. Se o worker morre entre o claim e o update final, o job fica 'executando' para sempre.",
  "root_cause": "Claim sem heartbeat/lease nem reclaim por timeout. Um job que fica 'executando' (worker crashou, deploy no meio, exceção fora do try) nunca volta a ser selecionado porque o SELECT filtra só 'pendente'.",
  "change_sites": [
   "src/lib/execution-worker.ts:122-132 — antes (ou junto) do SELECT de pendentes, reclamar jobs estagnados: UPDATE siso_fila_execucao SET status='pendente', proximo_retry_em=now() WHERE status='executando' AND atualizado_em < now() - interval '5 minutes' (atualizado_em já é escrito no claim em 142-145, serve de marca de início)",
   "src/lib/execution-worker.ts:138-154 — garantir que atualizado_em é refrescado no claim (já é) pra o cálculo de 5min ser confiável"
  ],
  "needs_migration": false,
  "migration_desc": "Sem coluna nova: atualizado_em já existe (20260309_create_execution_queue.sql:22) e é setado no claim. Opcionalmente índice parcial em (status, atualizado_em) WHERE status='executando' para performance, mas não é obrigatório no volume atual.",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "test_file": "src/app/api/wms/worker/reclaim-timeout.integration.test.ts",
  "red_test": "Inserir job com status='executando' e atualizado_em = now()-6min; rodar processQueue; asseverar que o job foi reclamado (voltou a 'pendente' e/ou foi processado) em vez de ficar preso para sempre. Job 'executando' com atualizado_em recente (<5min) NÃO deve ser reclamado.",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (timeout 5min volta pra fila e reprocessa). executeJob/worker-wms já é idempotente por reserva (estorno_de) e por estoque_lancado, então reprocessar um job que de fato completou parte do trabalho é seguro. Independente de P005/P149 mas todos tocam a mesma fila."
 },
 {
  "id": "P149",
  "current_status": "open",
  "status_evidence": "src/lib/wms/ledger.ts:234-263 — em mov tipo='E', dispara `void (async () => { ... varrerPedidosAfetadosPorEntrada(...); reconciliarEntradaEstoque(...) ... } catch { logger.warn('varredura pós-entrada falhou (não-fatal)') })()`. É fire-and-forget puro: se a varredura/reconciliação lançar (erro de banco, timeout) antes de enfileirar os jobs internos, o erro só vira warn e NUNCA é retentado. Os jobs lancar_estoque criados DENTRO do reconciliador têm retry, mas a varredura externa em si não.",
  "root_cause": "A varredura pós-entrada roda como side-effect não-durável dentro do write do ledger. Falha transitória = pedidos em validacao_oc não recebem flag_saldo_apareceu e o banner 'saldo apareceu' nunca surge; nada reexecuta.",
  "change_sites": [
   "src/lib/wms/ledger.ts:234-263 — em vez de só rodar inline com try/catch+warn, enfileirar a varredura pós-entrada como tarefa durável (novo tipo de job, ex.: 'varredura_pos_entrada', payload={produto_id,galpao_id,localizacao_id}) em siso_fila_execucao, deixando o worker retentar 3x; ou envolver a chamada inline num helper com retry(3) + backoff que, no esgotamento, registra job de retry",
   "src/lib/wms/varredura-validacao-oc.ts — expor a varredura de forma que o worker possa chamá-la a partir do payload do job",
   "src/lib/execution-worker-wms.ts / execution-worker.ts — adicionar handler para o novo tipo de job 'varredura_pos_entrada' (chk_fila_tipo precisa aceitar o novo valor)"
  ],
  "needs_migration": true,
  "migration_desc": "Relaxar CONSTRAINT chk_fila_tipo (atual `tipo IN ('lancar_estoque')` em 20260309_create_execution_queue.sql:24) para incluir o novo tipo 'varredura_pos_entrada'. Sem isso, o insert do job de varredura na fila viola o CHECK.",
  "primitive": "durable_enqueue_retry",
  "test_harness": "integration",
  "test_file": "src/lib/wms/varredura-pos-entrada-retry.integration.test.ts",
  "red_test": "Forçar a varredura pós-entrada a falhar na 1ª tentativa (mock de erro) e asseverar que ela é reexecutada (até 3x) — após sucesso, pedidos em validacao_oc do produto+galpão recebem flag_saldo_apareceu. Hoje a falha some em warn e o pedido fica sem aviso.",
  "rpc_needed": "",
  "deps": [
   "P145"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (fila com retry automático 3x). Depende de P145 no sentido de reaproveitar a infra de retry/reclaim da fila (mesmo mecanismo). Alternativa mais barata sem migration: retry(3) inline com backoff dentro do IIFE — porém menos durável que a fila pedida pela nota. file da raio-x (ledger.ts:234-263) bate exatamente com o bloco atual."
 },
 {
  "id": "P150",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts:599-633 (reconciliarRetroativo) — carrega a mov retroativo, valida origem_tipo, e chama inserirMovimentacao tipo='S' estorno_de=m.id, SEM checar se já existe estorno (S com estorno_de=m.id) e SEM lock. A checagem de estorno existente só vive em listarRetroativosPendentes:581-590 (a LISTAGEM), não na ação. Logo 2º clique tenta sair de novo 10un, falha por saldo insuficiente. O route src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:63-69 não adiciona guard de idempotência.",
  "root_cause": "reconciliarRetroativo não é idempotente: não verifica existência de estorno prévio ligado ao lançamento antes de criar o S de estorno. Repetição do mesmo clique tenta o estorno de novo.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:609-628 — antes de inserirMovimentacao, SELECT em siso_movimentacoes WHERE estorno_de=m.id AND tipo='S' (origem_tipo='estorno'); se existir, retornar sucesso (no-op) em vez de tentar novo estorno",
   "src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:63-69 — garantir que o no-op idempotente retorne {ok:true} (sucesso na 2ª vez) em vez de 400"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/movimentacoes.test.ts",
  "red_test": "Chamar reconciliarRetroativo 2x para o mesmo retroativo_mov_id deve produzir exatamente 1 mov de estorno (S) e a 2ª chamada retornar sucesso sem tentar novo S (sem erro de saldo). Asseverar que só 1 estorno_de=m.id existe ao final.",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (detectar ação repetida e responder sucesso sem refazer). P150/P151/P152 são a MESMA função (movimentacoes.ts:599-633) por 3 ângulos: P150 duplo-clique imediato, P151 reclique dias depois, P152 concorrência 2 operadores. O lookup de estorno existente resolve P150 e P151; P152 precisa adicionalmente de lock. Implementar como um pacote coeso."
 },
 {
  "id": "P151",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts:599-633 — mesma função reconciliarRetroativo. Não persiste nenhuma marca de 'já acertada' na mov retroativo nem checa estorno prévio; um reclique dias depois recarrega os dados e tenta o estorno S de novo, agora falhando por saldo (o estoque já foi consumido). Nenhuma flag tipo 'reconciliado_em' existe na mov.",
  "root_cause": "Falta marca persistente de reconciliação concluída. A única evidência de 'já acertou' é a existência do mov de estorno (estorno_de), mas reconciliarRetroativo não consulta isso, então não consegue responder 'já processada' num reclique tardio.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:609-628 — mesmo guard de P150 (SELECT estorno existente por estorno_de=m.id antes de criar) cobre o reclique tardio: se já há estorno, responder 'já processada com sucesso' (no-op idempotente) independentemente do tempo decorrido",
   "src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:63-79 — mapear o no-op para resposta de sucesso clara ('já reconciliado') em vez de 400 'saldo insuficiente'"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "vitest_unit",
  "test_file": "src/lib/wms/movimentacoes.test.ts",
  "red_test": "Reconciliar um retroativo, depois (simulando dias e estoque já consumido — saldo agora insuficiente) chamar reconciliarRetroativo de novo: deve responder sucesso/'já processada' sem tentar novo estorno e sem erro de saldo. Verificar que continua existindo exatamente 1 estorno.",
  "rpc_needed": "",
  "deps": [
   "P150"
  ],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (marcar nota como acertada; reclique dias depois responde 'já processada'). O lookup por estorno_de (mesmo de P150) já serve de 'marca' sem precisar de coluna nova. Se quiser a marca literal pedida na nota ('nota como acertada'), poderia setar um campo em origem_detalhes da mov retroativo, mas o estorno existente é fonte de verdade suficiente. Mesma função de P150/P152."
 },
 {
  "id": "P152",
  "current_status": "open",
  "status_evidence": "src/lib/wms/movimentacoes.ts:599-633 — reconciliarRetroativo não tem nenhum lock (advisory/row) nem claim atômico; duas requisições concorrentes passam ambas pela leitura (603-608) e chamam inserirMovimentacao em paralelo. A atomicidade de wms_inserir_movimentacao protege o saldo de ir negativo (uma das duas falha por saldo), mas não garante coordenação limpa: uma duplica/erra. Não há guard de 'acertando'.",
  "root_cause": "Ausência de exclusão mútua na reconciliação. O lookup de estorno (P150) sozinho ainda tem janela TOCTOU sob concorrência real (ambos leem 'sem estorno' antes de qualquer um inserir). Precisa de cadeado de banco serializando por lançamento retroativo.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:599-628 — envolver a reconciliação num advisory lock por retroativo_mov_id (pg_advisory_xact_lock com hash do uuid) OU executar a checagem-de-estorno + insert de S dentro de uma RPC plpgsql transacional que faz SELECT ... FOR UPDATE da mov retroativo, garantindo que só 1 acerto roda por vez (2º vê o estorno já criado e vira no-op)",
   "src/app/api/wms/lancamento-retroativo/[id]/reconciliar/route.ts:63-79 — tratar o caso 'já sendo processada/já processada' como sucesso/aviso, não 500"
  ],
  "needs_migration": true,
  "migration_desc": "RPC plpgsql wms_reconciliar_retroativo(p_retroativo_id uuid, p_compra_mov_id uuid, p_usuario_id text) que: trava a mov retroativo (advisory lock ou SELECT FOR UPDATE), checa estorno prévio (no-op se existir), e cria o S de estorno via wms_inserir_movimentacao — tudo numa transação. Serializa acertos concorrentes da mesma compra.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "src/lib/wms/reconciliar-retroativo-concorrencia.integration.test.ts",
  "red_test": "Disparar 2 reconciliações concorrentes do MESMO retroativo (Promise.all): exatamente 1 deve criar o estorno; a outra deve ser no-op/rejeitada com 'já sendo processada/já processada'. Saldo final reflete UM único estorno (não +20/-20 nem double).",
  "rpc_needed": "wms_reconciliar_retroativo — serializa o acerto por lançamento (lock) + checagem idempotente + estorno na mesma transação",
  "deps": [
   "P150"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (cadeado de banco: só 1 acerto por vez; 2º vê 'já sendo processada'). Diferente de P150/P151 (idempotência simples por lookup), P152 exige exclusão mútua real — daí RPC com lock. Recomendo implementar a RPC e fazer P150/P151 caírem dentro dela (a checagem de estorno fica protegida pelo lock), unificando os três num único ponto correto."
 },
 {
  "id": "P176",
  "current_status": "open",
  "status_evidence": "src/lib/wms/sync-tiny.ts:28-125 (sincronizarProduto) — sem lock de in-flight; src/app/api/wms/produtos/[id]/sync/route.ts:14-16 chama direto sem guard. Pior: sincronizarComposicaoKit faz DELETE em siso_produto_kits (sync-tiny.ts:159-160) seguido de INSERT (198) — duas syncs paralelas do mesmo kit podem deletar/recriar concorrentemente e deixar componentes órfãos/duplicados. siso_produtos só tem coluna sincronizado_em (timestamp do último sync, 20260508_wms_foundation.sql:25), não há flag 'sincronizando'.",
  "root_cause": "Nenhuma serialização por produto durante a sincronização. O delete-then-insert da composição do kit não é atômico nem protegido contra concorrência, então duas syncs simultâneas corrompem o kit.",
  "change_sites": [
   "src/lib/wms/sync-tiny.ts:28-40 — claim atômico no início: UPDATE siso_produtos SET sincronizando_em=now() WHERE id=produtoId AND (sincronizando_em IS NULL OR sincronizando_em < now()-interval '5 min') RETURNING id; se não reclamou, lançar erro 'já sincronizando'",
   "src/lib/wms/sync-tiny.ts:121-125 (fim) e no catch — em finally, limpar sincronizando_em (release) pra não travar permanentemente",
   "src/app/api/wms/produtos/[id]/sync/route.ts:14-26 — quando sincronizarProduto sinalizar 'já sincronizando', retornar 409 com mensagem 'já tá sincronizando, espere' (em vez de 500 genérico)",
   "src/app/wms/produtos/page.tsx:97 — (opcional, defesa em profundidade) desabilitar o botão de sync enquanto a mutation isPending"
  ],
  "needs_migration": true,
  "migration_desc": "ALTER TABLE siso_produtos ADD COLUMN sincronizando_em timestamptz NULL — usada como lease/claim de sincronização (com staleness de 5min para auto-liberar travas órfãs de crash). Sem ela não há como o claim atômico 'marcar produto como sincronizando'.",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "test_file": "src/lib/wms/sincronizar-produto-lock.integration.test.ts",
  "red_test": "Disparar 2 sincronizarProduto concorrentes do mesmo produto (kit com componentes): apenas 1 executa a sync de fato; a 2ª é recusada com 'já sincronizando'. Asseverar que siso_produto_kits termina com a composição íntegra (sem componentes órfãos/duplicados), o que hoje pode quebrar pelo delete+insert paralelo.",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (marcar produto 'sincronizando' e recusar nova sync até terminar; só 1 por vez). file da raio-x é vago ('titulo/situacao do item 4'), mas o alvo é o sync de produto (sincronizarProduto + composição de kit). Precisa de migration (coluna de claim) porque hoje só existe sincronizado_em. O claim com staleness de 5min evita trava permanente se a sync crashar."
 },
 {
  "id": "P185",
  "current_status": "open",
  "status_evidence": "src/app/wms/dashboard/page.tsx:58-73 — useQuery com refetch; em erro renderiza <ErrorBanner message=... onRetry={() => refetch()} />. O botão de retry vive em src/components/ui/error-banner.tsx — um <button onClick={onRetry}> SEM prop disabled/estado de loading. Cada clique chama refetch() imediatamente; cliques rápidos disparam várias queries. isFetching do useQuery não é passado nem usado para desabilitar.",
  "root_cause": "Botão de retry sem feedback de in-flight: não fica desabilitado enquanto a query está buscando, permitindo N refetch() consecutivos.",
  "change_sites": [
   "src/app/wms/dashboard/page.tsx:58-73 — extrair isFetching do useQuery e passá-lo ao ErrorBanner (ex.: retrying={isFetching}) para desabilitar o botão enquanto recarrega",
   "src/components/ui/error-banner.tsx:8-30 — adicionar prop opcional `retrying`/`disabled` no ErrorBanner e aplicar disabled={retrying} + estilo desabilitado no <button>; manter retrocompat (default false) pra não afetar outros usos"
  ],
  "needs_migration": false,
  "migration_desc": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "manual_only",
  "test_file": "",
  "red_test": "Com a query em estado de erro, clicar 'Tentar de novo' várias vezes em sequência rápida deve disparar no máximo 1 refetch enquanto a anterior está em voo (botão desabilitado/cinza durante isFetching). Verificação por inspeção de chamadas de rede / estado disabled do botão.",
  "rpc_needed": "",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = opção 1 (desabilitar botão enquanto carrega). ErrorBanner é componente genérico compartilhado (usado em várias telas) — adicionar prop opcional retrocompatível, não mudar comportamento dos outros consumidores. Severidade leve. Não há um botão 'Recarregar' explícito separado no dashboard além do retry do ErrorBanner e do refetchInterval de 30s."
 },
 {
  "id": "P028",
  "current_status": "partially_fixed",
  "status_evidence": "receberEstoque (src/lib/wms/movimentacoes.ts:196-239) JÁ tem defense-in-depth: se criarPendencia falha, estorna a mov E daquele item (linha 213-238). E há painel de exceção: detectarRecebimentoOrfao (src/lib/wms/dashboard-tarefas.ts:420) detecta 'saldo em RECEBIMENTO sem pendência viva'. MAS: (1) receberItensViaOC (src/lib/wms/receber-oc.ts:161-170,230-246) usa logger+continue em vez de throw — se item 2 falha, item 1 fica comitado (sem tudo-ou-nada entre itens). (2) O estorno é per-item, não trava o recebimento inteiro como pede a nota ('tudo-ou-nada trancado'). (3) Não há rollback all-items: receberEstoque também processa item-a-item num loop (linha 172) sem transação englobante.",
  "root_cause": "Recebimento processa N itens num loop JS de chamadas inserirMovimentacao+criarPendencia separadas; a compensação (estorno) é per-item e best-effort (pode falhar deixando mov órfã, logada como ERROR). Em receberItensViaOC a falha de um item é silenciada com continue, então a operação 'parece OK' (HTTP 200) mesmo com itens faltando — exatamente o que a nota quer bloquear.",
  "change_sites": [
   "src/lib/wms/receber-oc.ts:161-170,230-246,264-278 — trocar os blocos catch+continue por acumulação de erro e, ao fim, se houve QUALQUER falha, estornar todas as movs já criadas no lote (rollback all-items) e throw — tudo-ou-nada trancado por chamada",
   "src/lib/wms/movimentacoes.ts:172-240 — envolver o loop de itens num modo all-or-nothing: coletar mov_ids; se qualquer item falhar, estornar todos os mov_ids já criados antes de throw (hoje só estorna o item corrente)",
   "src/lib/wms/dashboard-tarefas.ts:420 (detectarRecebimentoOrfao) — confirmar que a UI de exceções já consome este card; se não, expor 'entradas com problema' na home (a tela já pedida na nota)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "durable_enqueue_retry",
  "test_harness": "integration",
  "red_test": "Recebimento de lote com 2 itens onde a pendência de guarda do 2º item falha (loc destino inválida): asserir que NENHUM saldo do lote permanece em siso_estoque (mov do 1º item também estornada) e que a operação retorna erro — não 200 silencioso. Hoje o 1º item fica comitado.",
  "test_file": "src/lib/wms/receber-oc.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "notes": "Status defasado vs raio-x: o estorno per-item E o painel de órfãos JÁ existem. O delta real é (a) tornar receberItensViaOC tudo-ou-nada (parar de engolir falhas com continue) e (b) rollback ALL-items, não só o item corrente. Respeita a nota (op1 trancado + tela de problemas).",
  "respeita_nota": true
 },
 {
  "id": "P029",
  "current_status": "open",
  "status_evidence": "confirmarGuarda (src/lib/wms/guarda.ts:434-506) recebe localizacao_destino_id e chama a RPC wms_confirmar_guarda_atomico — NÃO há validação de que o produto físico corresponde à loc bipada. A rota /api/wms/guarda/[id]/confirmar/route.ts:28-35 só valida qty>0 e presença de localizacao_destino_id. Linha 484-499 apenas LOGA divergência entre loc sugerida e a escolhida (audit), não bloqueia. Operador digita/bipa a loc; sistema confia.",
  "root_cause": "No put-away (confirmarGuarda) o operador informa só o código da loc destino. O sistema não exige bipar também o código de barras do produto pra cross-check produto↔loc. Loc errada ⇒ saldo registrado em lugar diferente do físico ⇒ picking futuro busca na loc errada (pega produto errado ou item fica pendente).",
  "change_sites": [
   "src/app/api/wms/guarda/[id]/confirmar/route.ts:24-35 — aceitar novo campo opcional gtin_bipado/sku_bipado no body e passar pra lib",
   "src/lib/wms/guarda.ts:404-460 (ConfirmarGuardaInput + confirmarGuarda) — antes da RPC, se gtin/sku bipado vier, validar contra siso_produtos do produto_id da pendência; rejeitar (throw 400) se não bater. Botão de confirmação manual (override) quando loc não tem etiqueta — espelhar o padrão de inventário (entrada em loc sem etiqueta)",
   "src/app/wms/guarda/**/page.tsx (tablet) — adicionar campo de bipagem do produto + fluxo 'confirmar manual' quando sem etiqueta"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "preflight_guard",
  "test_harness": "scenarios_e2e",
  "red_test": "POST /guarda/[id]/confirmar com gtin_bipado de um produto DIFERENTE do produto da pendência deve retornar 400 e NÃO criar mov S+E. Com gtin correto (ou flag confirmar_manual=true), deve confirmar normalmente.",
  "test_file": "scripts/wms/cenarios/catalogo/XX-guarda-valida-produto-bipado.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "notes": "A nota pede 'op1 + botão de confirmação manual (igual inventário: entrada em loc sem etiqueta)'. Logo a validação produto+loc é exigida MAS com escape-hatch manual para locs sem etiqueta — não é hard-block absoluto. O cross-check do CÓDIGO DA PRATELEIRA já é implícito (o operador bipa a loc); o que falta é o cross-check do PRODUTO.",
  "respeita_nota": true
 },
 {
  "id": "P033",
  "current_status": "partially_fixed",
  "status_evidence": "Ao contrário do 'today' do raio-x, o código ATUAL JÁ registra a qty_real cheia: receberItensViaOC (src/lib/wms/receber-oc.ts:142) insere mov E com qty=itemReq.qty_real (12, não 10), e compra_quantidade_recebida = jaRecebido + qty_real (linha 250). getCompraQuantidadeRestante (src/lib/compras-utils.ts:85-89) inclusive PERMITE valor negativo (over-receive) por design. O front (src/app/wms/receber/oc/[id]/page.tsx:66-74) manda qty_real digitada sem clamp. PORÉM: o excesso (12-10=2) entra todo como origem_tipo='nf_compra' — NÃO há split que categorize as 2 unidades como 'ganho de inventário' (a semântica que a nota op3 pede).",
  "root_cause": "O over-receive é aceito e o saldo bate com o físico (12), mas tudo é gravado como compra (nf_compra). A nota op3 quer que a parcela acima do solicitado seja registrada como movimento separado de 'ganho de inventário' (ajuste_manual categoria 'achado') pra rastreio contábil/auditoria distinto da compra.",
  "change_sites": [
   "src/lib/wms/receber-oc.ts:133-160 — quando qty_real > qty_pendente_solicitada, splitar: inserir mov E nf_compra com a qty solicitada (custo da compra) + inserir mov E ajuste_manual motivo_categoria='achado' com o excedente (sem alimentar custo médio de compra), ambos no mesmo loteId/origem_id pra agrupar",
   "src/lib/compras-utils.ts:85-89 (getCompraQuantidadeRestante) — manter (já tolera negativo); usar pra calcular o excedente"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "red_test": "Receber via OC com solicitado=10 e qty_real=12: asserir que existem DUAS movs E — uma nf_compra qty=10 e uma ajuste_manual (categoria achado/ganho) qty=2 — e que siso_estoque sobe 12. Hoje há 1 mov nf_compra qty=12.",
  "test_file": "src/lib/wms/receber-oc.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "notes": "Status muito defasado: o raio-x diz 'registra só 10' mas o código registra a qty cheia (12). O delta real é a categorização do excedente como ganho de inventário (op3), não a perda das unidades. Respeita a nota op3.",
  "respeita_nota": true
 },
 {
  "id": "P046",
  "current_status": "open",
  "status_evidence": "buildCompraFieldReset (src/lib/compras-utils.ts:28) seta mov_saida_id: null SEM estornar a mov S correspondente. A rota /compras/itens/[itemId]/devolver/route.ts:47 espalha ...buildCompraFieldReset() no UPDATE — logo, se o item tinha mov_saida_id (foi picado/baixado), o estoque NÃO volta: o vínculo é apagado e o saldo permanece deduzido. Existe precedente correto do estorno em validar-oc-item/route.ts:423-446 (estorna mov_saida_id antes de resetar), que NÃO foi replicado na rota devolver.",
  "root_cause": "A rota /devolver desvincula o item da OC e reseta os campos de pick (incluindo mov_saida_id) mas não grava o estorno (E) da saída anterior. Resultado: saldo do sistema fica menor que o físico.",
  "change_sites": [
   "src/app/api/wms/compras/itens/[itemId]/devolver/route.ts:26-58 — ANTES de aplicar buildCompraFieldReset: ler mov_saida_id do item; se !=null E o pedido NÃO está lançado, chamar estornarMovimentacao(mov_saida_id) (grava E que desfaz a S); só então resetar os campos. Se pedido JÁ lançado, cair no guard do P047 (bloqueia a devolução) — P047 roda PRIMEIRO no fluxo da rota",
   "src/lib/compras-utils.ts:5-31 (buildCompraFieldReset) — manter o null de mov_saida_id, mas o estorno passa a ser responsabilidade do caller (rota) antes do reset"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "scenarios_e2e",
  "red_test": "Item de pedido NÃO lançado com mov_saida_id setado (estoque já saiu). POST /compras/itens/[id]/devolver: asserir que (1) existe nova mov E (estorno) que devolve a qty à loc origem, (2) siso_estoque volta ao saldo pré-pick, (3) mov_saida_id do item fica null. Hoje o saldo fica deduzido.",
  "test_file": "scripts/wms/cenarios/catalogo/XX-devolver-item-estorna-saida.ts",
  "deps": [
   "P047"
  ],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "notes": "A nota é explícita: 'Gravar entrada que desfaz a saída — MAS só quando pedido NÃO está lançado. Se lançado, ganha o bloqueio do P047 (ordem do guard: P047 primeiro)'. Logo P047 (guard de bloqueio) deve ser implementado/posicionado antes do estorno na MESMA rota. Dependência dura de P047.",
  "respeita_nota": true
 },
 {
  "id": "P054",
  "current_status": "open",
  "status_evidence": "classificarDevolucao (src/lib/wms/devolucoes.ts:101-340), caso 'avariado' (linhas 209-271): faz o par S+E de quarentena via DUAS chamadas inserirMovimentacao separadas (S linha 238, E linha 248) — atômicas individualmente (FOR UPDATE por linha na RPC) mas NÃO numa transação englobante. O guard de status (linha 118 'if status != aguardando_classificacao throw') é read-then-act (TOCTOU) — dois calls concorrentes leem 'aguardando' e ambos prosseguem. Não há lock pessimista na prateleira/devolução. NOTA: o DB IMPEDE saldo negativo (siso_estoque CHECK saldo>=0 em 20260508_wms_foundation.sql:83 + RPC RAISE 'saldo insuficiente'), então o '-3' do raio-x é na verdade um erro/abort; o dano real é a CONCLUSÃO PARCIAL (E sem o S, ou meio-caminho) entre os dois operadores.",
  "root_cause": "O fluxo avariado (E na loc + S→quarentena + E quarentena) não é atômico como um todo nem serializado por lock. Dois operadores classificando a mesma devolução/loc concorrentemente intercalam as movs: um pode comitar o E inicial e o outro abortar no S (saldo já consumido) deixando estado meio-feito (entrou avariado mas não foi pra quarentena). Falta lock pessimista por (devolução) ou por (produto,galpão,loc).",
  "change_sites": [
   "src/lib/wms/devolucoes.ts:101-340 (classificarDevolucao) — mover toda a sequência de movs do classify pra uma RPC plpgsql única que faz SELECT ... FOR UPDATE no row de siso_devolucoes_pendentes (serializa por devolução) + executa o par/trio S+E na mesma transação; o guard de status passa a ser dentro do lock (sem TOCTOU)",
   "src/app/api/wms/devolucoes/[id]/classificar/route.ts:55-66 — repassar erro de concorrência (ex code='DEVOLUCAO_OUTRO_CLASSIFY') como 409"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_classificar_devolucao_atomico — recebe devolucao_id + classificacao + tripla + qty + tags; FOR UPDATE na devolução; valida status; executa par/trio S+E (avariado→quarentena) numa transação; atualiza status='classificada'. Garante tudo-ou-nada e serialização por devolução.",
  "migration_desc": "Nova RPC plpgsql wms_classificar_devolucao_atomico que envolve o classify inteiro (E+S+E de quarentena) com SELECT FOR UPDATE no row da devolução, espelhando wms_confirmar_guarda_atomico. Migration em supabase/migrations/.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "red_test": "Dois classify 'avariado' concorrentes na mesma devolução/loc com saldo que cobre só um: asserir que exatamente UM completa o trio E+S+E (saldo final consistente, item em quarentena) e o outro recebe 409/erro SEM deixar estado parcial (nenhum E avariado sem o S correspondente). Saldo sempre >=0 e coerente.",
  "test_file": "src/lib/wms/devolucoes-concorrencia.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "L",
  "blast_radius": "high",
  "notes": "A nota pede 'lock pessimista na prateleira'. Tecnicamente o lock por (produto,galpão,loc) já existe DENTRO de cada inserirMovimentacao; o que falta é serializar o EVENTO inteiro (todas as movs do classify) — fazê-lo numa RPC com FOR UPDATE na devolução entrega exatamente 'operações não se cruzam, saldo sempre positivo'. DB já garante saldo>=0; o ganho é eliminar conclusão parcial e double-classify.",
  "respeita_nota": true
 },
 {
  "id": "P065",
  "current_status": "partially_fixed",
  "status_evidence": "desfazerRecebimentoTransferencia (src/lib/wms/transferencias.ts:588-665) estorna cada leg E (linha 631). O estorno gera uma S na loc destino via inserirMovimentacao; se o saldo já foi vendido/consumido, a RPC RAISE 'saldo insuficiente' (CHECK saldo>=0) — ou seja, o '-10' do raio-x NÃO ocorre (saldo negativo é impossível). MAS: (1) o undo falha com erro cru (500/400) sem dizer 'só dá pra devolver 40 de 50'; (2) o loop estorna item-a-item (linhas 628-643) — se falha no meio, deixa itens já estornados (parcial), sem preflight informando o que dá pra desfazer.",
  "root_cause": "Falta um preflight que, antes de tentar o undo, compare a qty de cada leg E com o saldo disponível atual na loc destino e reporte ao operador quanto pode ser desfeito sem violar saldo>=0. Hoje só descobre tentando (erro técnico) ou estornando parcialmente.",
  "change_sites": [
   "src/lib/wms/transferencias.ts:588-643 (desfazerRecebimentoTransferencia) — adicionar passo de preflight: pra cada item, ler siso_estoque(loc destino) e comparar com qty da leg E; montar relatório {desfazivel, total} e, se algum item não cobre, retornar 409 estruturado com a mensagem 'só pode devolver X de Y' (sem mutar nada) ANTES de qualquer estorno",
   "src/app/api/wms/transferencias/[id]/desfazer-recebimento/route.ts — propagar o 409 de preflight com payload itens-bloqueados; aceitar flag force=true pra prosseguir só nos itens que cobrem (transparência, não bloqueio absoluto, conforme nota)"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "preflight_guard",
  "test_harness": "integration",
  "red_test": "Transferência recebida (50) onde 10 já saíram (saldo destino=40). POST /desfazer-recebimento: asserir que a resposta é 409 estruturado informando 'desfazível 40 de 50' e que NADA foi estornado (saldo permanece 40). Hoje a chamada estoura erro cru de saldo insuficiente (ou estorna parcial).",
  "test_file": "src/lib/wms/transferencias-desfazer-preflight.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "notes": "Status defasado: o saldo NEGATIVO descrito no raio-x é impossível (CHECK saldo>=0 + RPC raise). O delta real é UX/transparência (preflight 'quanto dá pra devolver'), exatamente o que a nota pede ('validar e avisar antes; não bloqueia, dá transparência').",
  "respeita_nota": true
 },
 {
  "id": "P066",
  "current_status": "partially_fixed",
  "status_evidence": "receberTransferencia (src/lib/wms/transferencias.ts:284-323) JÁ tem lock anti-race via UPDATE condicional em recebimento_em_andamento_por (migration 20260527_p3_transferencia_recebimento_em_andamento.sql) — protege recebimentos concorrentes entre si. MAS cancelarTransferencia (src/lib/wms/transferencias.ts:448-562) só checa status==='em_transito' (linha 469) e NÃO checa recebimento_em_andamento_por. Como o status permanece 'em_transito' durante a janela de recebimento (só vira 'recebida' no flip final, linha 384), um cancel concorrente passa o check e estorna a leg S enquanto o receive insere a leg E ⇒ as duas versões da verdade do raio-x.",
  "root_cause": "O lock de recebimento existe mas o cancelamento não o respeita. Falta o guard 'não cancelar enquanto recebimento_em_andamento_por IS NOT NULL' em cancelarTransferencia.",
  "change_sites": [
   "src/lib/wms/transferencias.ts:464-473 (cancelarTransferencia) — após o check de status, adicionar: se transf.recebimento_em_andamento_por IS NOT NULL, throw erro code='TRANSFERENCIA_RECEBIMENTO_ATIVO' (selecionar a coluna no SELECT da linha 453-456). Idealmente o cancel também faz claim atômico via UPDATE condicional (status='em_transito' AND recebimento_em_andamento_por IS NULL) pra fechar a janela TOCTOU entre SELECT e UPDATE de cancelada",
   "src/app/api/wms/transferencias/[id]/cancelar/route.ts:13-25 — mapear o novo erro pra 409 com mensagem 'outro operador está recebendo'"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "red_test": "Operador A inicia recebimento (claim recebimento_em_andamento_por setado, status ainda em_transito). Operador B chama cancelar: asserir 409 'recebimento em andamento' e que a transferência NÃO é cancelada (legs S não estornadas). Após A finalizar (status='recebida'), cancelar deve exigir desfazer-recebimento primeiro.",
  "test_file": "src/lib/wms/transferencias-cancel-vs-receber.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "med",
  "notes": "Mesma família do P062 (nota: 'mesma linha do P062'). A coluna recebimento_em_andamento_por já existe — o fix é só fazer o cancel respeitá-la. Idealmente coordenar com P062 (que adiciona timeout 30min pra liberar lock se a tela cair) — implementar juntos. Respeita a nota (bloquear cancel enquanto recebimento ativo).",
  "respeita_nota": true
 },
 {
  "id": "P099",
  "current_status": "open",
  "status_evidence": "receberEstoque (src/lib/wms/movimentacoes.ts:98) gera loteId = crypto.randomUUID() a cada chamada — ID novo por clique. A RPC wms_inserir_movimentacao (latest: 20260527_wms_inserir_mov_motivo_categoria.sql) NÃO tem chave de idempotência; insere sempre uma nova mov. Não há índice único anti-duplicata em siso_movimentacoes pra recebimento (foundation 20260508 só tem índices não-únicos; o único precedente de unique-idempotência é 20260527_p3_movs_unique_inventario_divergencia.sql, restrito a inventário). Logo 2 cliques no recebimento ⇒ 2 movs E idênticas ⇒ estoque dobra.",
  "root_cause": "Recebimento manual não é idempotente: cada POST gera lote/mov novos sem deduplicar por assinatura estável da NF (chave_acesso_nf + produto + qty). Double-click ou retry duplica a entrada.",
  "change_sites": [
   "src/lib/wms/movimentacoes.ts:94-248 (receberEstoque) — derivar uma idempotency key estável da assinatura da NF (chave_acesso_nf OU nota_fiscal_id + galpao_id) em vez de randomUUID puro pro loteId/origem_id; passar pra inserirMovimentacao",
   "src/lib/wms/ledger.ts:179-202 (chamada da RPC) — propagar a idempotency key; antes do INSERT, checar/usar índice único pra rejeitar 2ª chamada como duplicata",
   "supabase/migrations/ (nova) — índice único parcial em siso_movimentacoes (origem_tipo='nf_compra', chave_acesso_nf, produto_id, galpao_id) WHERE estorno_de IS NULL, espelhando o padrão de 20260527_p3_movs_unique_inventario_divergencia.sql"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "Índice único parcial em siso_movimentacoes pra dedup de recebimento por assinatura da NF (chave_acesso_nf + produto + galpão), padrão de 20260527_p3_movs_unique_inventario_divergencia. A RPC pode capturar unique_violation e retornar a mov existente (idempotente).",
  "primitive": "db_constraint_unique",
  "test_harness": "integration",
  "red_test": "Chamar receberEstoque 2x com a MESMA assinatura de NF (mesma chave_acesso_nf, mesmo produto/qty/galpão): asserir que só 1 mov E é criada e o saldo sobe uma vez. Hoje cria 2 movs e o saldo dobra.",
  "test_file": "src/lib/wms/receber-idempotente.integration.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "high",
  "notes": "P099 e P109 compartilham a MESMA primitiva (dedup do recebimento por assinatura da NF). A nota op2 de P099 ('usar assinatura fixa da NF como ID') é exatamente db_constraint_unique. Implementar P099+P109 juntos: o índice único e a captura de unique_violation cobrem ambos. blast_radius high porque mexe no caminho central do ledger.",
  "respeita_nota": true
 },
 {
  "id": "P109",
  "current_status": "open",
  "status_evidence": "O caminho de entrada que recalcula custo médio está em ledger.ts (inserirMovimentacao, src/lib/wms/ledger.ts:179-202 → RPC). A RPC wms_inserir_movimentacao recalcula custo médio em E de origem nf_compra/devolucao_cliente_integra/lancamento_retroativo (20260527 RPC, bloco v_recalcula_custo) mas NÃO deduplica por NF: reenvio do integrador com a mesma NF insere 2ª mov E idêntica e recalcula custo médio 2x. siso_notas_fiscais.chave_acesso é UNIQUE (20260527_siso_notas_fiscais.sql) e upsertNotaFiscal dedup a NF-record — porém isso NÃO impede a 2ª mov no ledger. Não há guard 'NF já recebida'.",
  "root_cause": "A dedup existente é no nível do webhook (siso_webhook_logs.dedup_key) e do registro de NF (siso_notas_fiscais.chave_acesso UNIQUE), mas o LANÇAMENTO de estoque (mov E nf_compra) não checa se aquela NF já gerou entrada. Reprocesso/reenvio dobra saldo e custo médio.",
  "change_sites": [
   "src/lib/wms/ledger.ts:179-202 (inserirMovimentacao) — para origem nf_compra com nota_fiscal_id/chave_acesso_nf, antes da RPC, checar se já existe mov E (estorno_de IS NULL) com a mesma NF+produto+galpão; se sim, bloquear/retornar idempotente (não recalcular custo médio)",
   "supabase/migrations/ (nova) — mesmo índice único parcial do P099 (origem nf_compra + chave_acesso_nf/nota_fiscal_id + produto + galpão) faz cumprir a regra no DB; a RPC captura unique_violation",
   "src/lib/nf-webhook-handler.ts (caminho de lançamento via NF) — reforçar guard 'NF já recebida' antes de enfileirar/lançar estoque"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "migration_desc": "Compartilhado com P099: índice único parcial em siso_movimentacoes por assinatura de NF de compra. Faz com que a 2ª entrada da mesma NF seja rejeitada/idempotente no DB, evitando dobra de saldo e recálculo duplo de custo médio.",
  "primitive": "db_constraint_unique",
  "test_harness": "integration",
  "red_test": "Processar a mesma entrada de NF (mesma chave_acesso) 2x: asserir que o saldo sobe só uma vez e que siso_custo_medio é recalculado uma única vez (não 2x). Hoje cria 2 movs E e recalcula custo médio duas vezes.",
  "test_file": "src/lib/wms/nf-entrada-dedup.integration.test.ts",
  "deps": [
   "P099"
  ],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "high",
  "notes": "Mesma primitiva e migration de P099 — fazer no mesmo PR. A nota op1 ('bloquear entrada se a NF já foi recebida; dedup por NF') é db_constraint_unique sobre a assinatura da NF. dep de P099 só por compartilharem o índice/migration; implementar conjuntamente.",
  "respeita_nota": true
 },
 {
  "id": "P162",
  "current_status": "open",
  "status_evidence": "No modal de transferência inter-galpão (src/components/wms/ui/modals.tsx): inputs de qty usam type=\"number\" min=\"1\" (linhas 986 e 1524) — type=number aceita decimais (12.5). A validação itensValidos só checa Number(l.qty) > 0 (linha 1180), sem Number.isInteger. O backend transferirInterGalpao (src/lib/wms/movimentacoes.ts:302-340) e a RPC wms_inserir_movimentacao (quantidade numeric, CHECK quantidade>0) também aceitam fração. Logo 12.5 passa do front ao ledger.",
  "root_cause": "Quantidade aceita decimais em toda a cadeia (input HTML number sem step/validação inteira, validação JS só >0, coluna numeric no DB). Saldo fracionário quebra faturamento/picking que esperam inteiros.",
  "change_sites": [
   "src/components/wms/ui/modals.tsx:1176-1181 (itensValidos) — trocar Number(l.qty) > 0 por Number.isInteger(Number(l.qty)) && Number(l.qty) > 0 (a menos que o produto seja granel configurável)",
   "src/components/wms/ui/modals.tsx:985-992 e 1522-1530 — adicionar step=\"1\" e bloquear entrada não-inteira (onChange parseInt) com mensagem 'Digite um número inteiro'",
   "src/lib/wms/movimentacoes.ts:302-340 (transferirInterGalpao) e demais entradas de qty — validação server-side: rejeitar qty não-inteira salvo flag de granel do produto",
   "src/lib/wms/ledger.ts:44-63 (validarCoerencia) ou RPC — guard inteiro como rede de segurança final; exceção por produto granel"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "migration_desc": "Opcional: coluna siso_produtos.permite_fracao (bool, default false) pra exceção granel kg/m configurável por produto, conforme a nota. Sem ela, hard-block inteiro.",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "red_test": "Função de validação de qty: asserir que 12.5 é REJEITADA (retorna inválido / mensagem 'número inteiro') para produto normal, e ACEITA para produto marcado granel. No componente, asserir que o botão Enviar fica disabled com qty=12.5.",
  "test_file": "src/components/wms/ui/modals.qty-inteira.test.ts",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "notes": "Severidade média. A nota pede 'só inteiros; granel (kg/m) é exceção configurável por produto'. A primitiva mais próxima do schema é frontend UX (a defesa principal é no input/validação), mas a defesa robusta exige também guard server-side. Recomendo validação dupla (front + lib/RPC).",
  "respeita_nota": true
 },
 {
  "id": "P069",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/ajuste/route.ts:27 usa requireWarehouseAccess (auth.ts:62-88), que faz userCanAny sobre um conjunto AMPLO de perms (transferir, replenishment, devolucoes, receber, guarda, ajuste_manual, inventario.executar/supervisionar, produtos/localizacoes/fornecedores.editar). Logo um usuario SEM operacoes.ajuste_manual mas COM, p.ex., inventario.executar ou produtos.editar passa e consegue ajustar estoque via curl. O frontend src/app/wms/ajuste/page.tsx:18 gateia em can('operacoes.ajuste_manual'), entao so a tela protege. Nenhuma rota em src/app/api/ checa 'operacoes.ajuste_manual' especificamente (grep confirmou).",
  "root_cause": "Guard de rota usa o gate generico requireWarehouseAccess (qualquer perm de armazem) em vez da permissao especifica operacoes.ajuste_manual exigida pela acao. Frontend e o unico ponto que checa a perm correta.",
  "change_sites": [
   "src/app/api/wms/ajuste/route.ts:26-28 — apos requireWarehouseAccess (ou substituindo), adicionar checagem userCan(auth.user, 'operacoes.ajuste_manual') e retornar 403 forbidden('requer permissao operacoes.ajuste_manual') quando falso. Importar userCan de @/lib/permissions.",
   "src/app/api/wms/ajuste/[id]/estornar/route.ts — mesma rota familia de ajuste manual; alinhar gate para tambem exigir operacoes.ajuste_manual (verificar guard atual; estorno de ajuste e a operacao inversa e deve ter o mesmo gate)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "permission_backend_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/auth/18-ajuste-permissao.ts",
  "red_test": "Usuario autenticado SEM operacoes.ajuste_manual mas COM outra warehouse perm (ex.: inventario.executar) faz POST /api/wms/ajuste com body valido -> deve receber 403; mesmo usuario COM operacoes.ajuste_manual -> 200/ok. Hoje o primeiro caso retorna 200 (RED).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota e clara: validar operacoes.ajuste_manual no backend, nao so na tela. Familia P116 (mesma classe de falha permission_backend_guard). Cuidado para nao quebrar requireWarehouseAccess em outras rotas — a mudanca e local a /ajuste."
 },
 {
  "id": "P076",
  "current_status": "open",
  "status_evidence": "Fluxo de REATRIBUICAO: src/app/wms/vendas/[id]/page.tsx:378-385 popula o dropdown com filter((u) => u.cargos?.includes('vendedor')) — SEM checar u.ativo. O type UsuarioOpt (linha 52-56) nem inclui 'ativo'. Logo vendedor inativo APARECE no dropdown de reatribuicao. (Observacao: o form de NOVA venda src/app/wms/vendas/nova/page.tsx:99 JA filtra u.ativo && includes('vendedor') corretamente — mas P076 e sobre reatribuicao.) O backend src/app/api/wms/vendas/[id]/vendedor/route.ts:60-92 valida o cargo do alvo mas NAO valida ativo.",
  "root_cause": "O dropdown de reatribuicao filtra so por cargo 'vendedor', sem excluir usuarios inativos (ativo=false). O type UsuarioOpt nao carrega o campo ativo, e a query nao filtra por ele.",
  "change_sites": [
   "src/app/wms/vendas/[id]/page.tsx:52-56 — adicionar 'ativo: boolean' ao interface UsuarioOpt.",
   "src/app/wms/vendas/[id]/page.tsx:383-384 — corrigir leitura (a rota /api/wms/admin/usuarios retorna ARRAY puro, nao { usuarios }; hoje le j.usuarios que e undefined) e adicionar filtro && u.ativo: const arr = (await r.json()) as UsuarioOpt[]; return (arr ?? []).filter((u) => u.ativo && u.cargos?.includes('vendedor'))."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "permission_backend_guard",
  "test_harness": "vitest_unit",
  "test_file": "src/app/wms/vendas/[id]/page.test.ts",
  "red_test": "Dada lista [{nome:'Carlos',ativo:false,cargos:['vendedor']},{nome:'Ana',ativo:true,cargos:['vendedor']}], a funcao de filtro do dropdown de reatribuicao deve retornar apenas Ana. Hoje retorna ambos (RED).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota = Modo 2: vendedor inativo nem aparece no dropdown — decisao puramente frontend (filtro de fonte de dados). NAO adicionar bloqueio no backend (isso seria Modo 1, rejeitado). primitive marcado como permission_backend_guard por ser o mais proximo no enum, mas a mudanca e frontend_idempotency_ux/data_visibility na pratica — e um filtro de visibilidade de UI. Bug colateral: a query ja esta quebrada (le j.usuarios em vez de array puro), entao o dropdown provavelmente esta vazio hoje; o fix tambem conserta isso. Para extrair a logica testavel, mover o filtro para uma helper pura."
 },
 {
  "id": "P116",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/localizacoes/lote/route.ts:40-42 (POST de importacao em lote de prateleiras) usa requireAuth — qualquer usuario logado cria N prateleiras via curl. O frontend src/app/wms/localizacoes/page.tsx:68 gateia em can('localizacoes.editar'), entao so a tela protege. Distincao importante: o POST de criacao UNICA em src/app/api/wms/localizacoes/route.ts:14-19 usa requireAuth DE PROPOSITO (comentario 14-16: operadores criam loc faltante on-the-fly nos modais de Receber/Ajuste/Transferencia) — esse e gap conhecido/aceito; P116 e especificamente o LOTE.",
  "root_cause": "Rota de importacao em lote (admin tooling) usa requireAuth (qualquer logado) em vez de exigir a permissao localizacoes.editar no backend. Frontend e o unico gate.",
  "change_sites": [
   "src/app/api/wms/localizacoes/lote/route.ts:3 — trocar import requireAuth por requireAuth + userCan, ou importar requireWarehouseAccess; mais cirurgico: manter requireAuth e adicionar userCan(auth.user,'localizacoes.editar').",
   "src/app/api/wms/localizacoes/lote/route.ts:40-42 — apos auth ok, checar userCan(auth.user,'localizacoes.editar') e retornar 403 quando falso (curl sem a perm e rejeitado)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "permission_backend_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/auth/19-localizacoes-lote-permissao.ts",
  "red_test": "Usuario autenticado SEM localizacoes.editar faz POST /api/wms/localizacoes/lote com galpao_id+ranges validos (preview:false) -> deve receber 403 e nenhuma loc criada; usuario COM localizacoes.editar -> 200 e cria. Hoje o primeiro caso cria as prateleiras (RED).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota explicita: 'familia P069' e 'curl tambem rejeitado'. NAO mexer no POST de criacao unica (localizacoes/route.ts) que tem requireAuth intencional pra criacao inline nos modais operacionais — so o LOTE. Mesma primitiva/abordagem de P069."
 },
 {
  "id": "P134",
  "current_status": "open",
  "status_evidence": "src/components/wms/configuracoes/aba-funcionarios.tsx:604-608 — o disabled do botao Criar (desabilitado) so cobre !nome.trim() || pin.length!==4 || roleIds.length===0 || criar.isPending. NAO inclui (precisaGalpao && galpaoIds.length===0). precisaGalpao existe (linha 546-548) mas nao entra no gate. Backend src/app/api/wms/admin/usuarios/route.ts POST (linha 110-114) insere galpoes so 'if galpao_ids.length>0' — aceita lista vazia sem erro. Resultado: operador criado sem nenhum galpao.",
  "root_cause": "O botao Criar nao bloqueia quando a role exige galpao (operador*) mas nenhum galpao foi marcado; e o backend nao exige >=1 galpao para roles operador.",
  "change_sites": [
   "src/components/wms/configuracoes/aba-funcionarios.tsx:604-608 — adicionar a condicao (precisaGalpao && galpaoIds.length===0) ao desabilitado, para o botao Criar so ativar com >=1 loja marcada quando role e operador."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/components/wms/configuracoes/aba-funcionarios.test.ts",
  "red_test": "Funcao pura de validacao do form (extrair calcDisabled({nome,pin,roleIds,galpaoIds,precisaGalpao})): com precisaGalpao=true e galpaoIds=[] -> disabled=true; com galpaoIds=['g1'] -> disabled=false. Hoje retorna false mesmo com galpaoIds vazio (RED).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: exigir >=1 loja/galpao ao criar operador; botao Criar so ativa com loja marcada — decisao frontend. Combina diretamente com P137 (mesma regra de bloqueio-vazio aplicada no form de edicao). Para testabilidade, extrair o calculo de 'desabilitado' para helper pura. Opcional reforcar tambem no backend POST, mas a nota foca no botao."
 },
 {
  "id": "P135",
  "current_status": "already_fixed",
  "status_evidence": "src/app/api/wms/admin/usuarios/route.ts:209-259 DELETE ja e soft-delete: marca ativo=false e renomeia para liberar UNIQUE(nome) (linha 234,246-253), removendo so os vinculos de galpao (232). Docstring 210-213 confirma 'Soft-delete... Preserva auditoria — nao tem como hard-delete usuario com historico (FKs em siso_movimentacoes, inventario, pedidos etc. sem ON DELETE)'. A row do usuario (e portanto nome/uuid referenciado nos historicos) e preservada. Idempotente (245-248). Introduzido pelo commit cd5738f 'fix(wms/admin-usuarios): soft-delete + reconhece cargo operador unificado' (git log -S confirmou).",
  "root_cause": "N/A — ja resolvido. O comportamento atual e exatamente o pedido na nota (soft-delete/desativar, nome e historico preservados, inativo desde X via atualizado_em).",
  "change_sites": [],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "soft_delete_block_delete",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/auth/20-usuario-soft-delete.ts",
  "red_test": "(Regressao, opcional) DELETE /api/wms/admin/usuarios?id=<uuid de usuario com mov> -> usuario NAO desaparece da tabela: ativo=false, nome com sufixo _excluido_<ts>, e nenhuma FK em siso_movimentacoes quebra. Hoje ja passa (verde).",
  "deps": [],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "A referencia do raio-x a migrations/20260310...sql:66 (FK sem ON DELETE) ficou obsoleta: o soft-delete contorna o hard-delete inteiramente, entao o FK nunca e violado. Status virou already_fixed por commit posterior ao raio-x. Nao ha trabalho a fazer; sugiro apenas um teste de regressao."
 },
 {
  "id": "P136",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/admin/usuarios/route.ts PUT (124-207) aplica 'ativo' direto via updates={...rest} (linha 153,166,184-189) SEM nenhum guard anti-lockout. O frontend src/components/wms/configuracoes/aba-funcionarios.tsx:791-808 (toggleAtivo) envia { id, ativo: !funcionario.ativo } — linha 798 — sem checagem. A protecao anti-lockout EXISTENTE so cobre REMOVER a ROLE admin: src/app/api/wms/admin/usuarios/[id]/roles/route.ts:55-82 (verifica se ha outro admin antes de tirar role admin). Mas DESATIVAR (ativo=false) o ultimo admin nao e coberto por nenhum guard.",
  "root_cause": "A protecao 'sistema precisa de >=1 admin' so existe no endpoint de roles (perda de role admin), nao no PUT de ativo. Desativar o ultimo admin via ativo:false passa sem bloqueio e causa lockout.",
  "change_sites": [
   "src/app/api/wms/admin/usuarios/route.ts:165-189 — no PUT, antes de aplicar updates: se body.ativo === false, checar se o usuario alvo (id) e admin (via siso_usuario_roles join siso_roles.codigo='admin') E se nao existe OUTRO usuario ativo com role admin (neq id, ativo=true); se for o ultimo admin ativo, retornar 400/409 com erro 'Sistema precisa de pelo menos 1 admin ativo'. Espelhar o padrao de roles/route.ts:55-82."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "permission_backend_guard",
  "test_harness": "scenarios_e2e",
  "test_file": "scripts/wms/cenarios/auth/21-ultimo-admin-desativar.ts",
  "red_test": "Com exatamente 1 admin ativo, PUT /api/wms/admin/usuarios { id: <esse admin>, ativo: false } -> deve retornar 4xx e o admin permanecer ativo=true. Com 2 admins ativos, desativar 1 -> 200. Hoje o primeiro caso retorna 200 e desativa (RED, lockout).",
  "deps": [],
  "conflicts": [],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: recusar desativacao do ultimo admin (anti-lockout). O guard deve considerar 'admin ativo' — o conjunto de admins que ainda poderiam logar. Cuidado com a definicao de admin: usar role codigo='admin' (RBAC), nao cargo legado. blast_radius med porque toca o caminho PUT generico de usuarios (printnode/galpoes/cargos passam pelo mesmo handler) — o guard deve disparar SO quando ativo===false."
 },
 {
  "id": "P137",
  "current_status": "partially_fixed",
  "status_evidence": "PARTE FEITA: o form de EDICAO ja carrega com as lojas atuais preenchidas — src/components/wms/configuracoes/aba-funcionarios.tsx:749-752 inicializa form.galpaoIds = funcionario.galpoes.map(g=>g.id). PARTE FALTANTE: o botao Salvar do form de edicao (linha 990-991) so tem disabled={salvar.isPending} — NAO bloqueia salvar com galpaoIds vazio quando precisaGalpao (precisaGalpao existe na linha 760-763 mas nao entra no disabled). O backend PUT src/app/api/wms/admin/usuarios/route.ts:196-203 faz delete-all + insert dos galpoes (vazio = remove todos). Logo um clique que limpe galpoes pode salvar e remover acesso do operador.",
  "root_cause": "Metade da decisao (carregar lojas atuais) ja implementada; falta o bloqueio de salvar-vazio para roles operador no form de edicao.",
  "change_sites": [
   "src/components/wms/configuracoes/aba-funcionarios.tsx:990 — adicionar ao disabled do botao Salvar a condicao (precisaGalpao && form.galpaoIds.length===0), reutilizando a mesma helper de validacao de P134. Manter disabled={salvar.isPending || (precisaGalpao && form.galpaoIds.length===0)}."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "frontend_idempotency_ux",
  "test_harness": "vitest_unit",
  "test_file": "src/components/wms/configuracoes/aba-funcionarios.test.ts",
  "red_test": "Helper pura calcDisabled compartilhada: no contexto de edicao com precisaGalpao=true e galpaoIds=[] -> disabled=true (bloqueia salvar vazio); com galpaoIds=['g1'] -> false. E asserir que o form inicia com galpaoIds = funcionario.galpoes (ja verde). Hoje o caso vazio retorna false (RED).",
  "deps": [
   "P134"
  ],
  "conflicts": [],
  "effort": "S",
  "blast_radius": "low",
  "respeita_nota": true,
  "notes": "Nota: form carrega com lojas atuais preenchidas (JA OK) + bloqueia salvar vazio (FALTA), combina P134. Dep P134: compartilhar a mesma helper de bloqueio-vazio entre form de criacao e edicao. Status partially_fixed porque o init com lojas atuais ja foi feito."
 },
 {
  "id": "P138",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/admin/roles/[id]/permissoes/route.ts:65-76 faz delete .eq('role_id', id) seguido de insert dos novos rows — SEM transacao. O proprio docstring 16-18 admite: 'Replace atomico best-effort: delete tudo + insert novo. Se o insert falha apos o delete, devolve 500... permissoes podem ficar vazias temporariamente nesse caso edge.' supabase-js nao tem transacao multi-statement client-side, entao isso nao e atomico. Nenhuma RPC plpgsql para isso existe (grep em supabase/migrations confirmou).",
  "root_cause": "Substituicao de permissoes do role implementada como delete+insert em duas chamadas separadas pelo client, sem atomicidade — falha no insert deixa o role com zero permissoes.",
  "change_sites": [
   "supabase/migrations/<nova>_wms_set_role_permissoes.sql — criar RPC plpgsql wms_set_role_permissoes(p_role_id uuid, p_codigos text[]) que faz, numa unica funcao (transacional por definicao): lock da row do role (SELECT ... FOR UPDATE em siso_roles), DELETE de siso_role_permissoes WHERE role_id=p_role_id, INSERT dos novos codigos; retorna total. Validacao de codigos pode ficar na rota (ja existe) ou na RPC.",
   "src/app/api/wms/admin/roles/[id]/permissoes/route.ts:60-78 — substituir o par delete+insert por uma unica chamada sb.rpc('wms_set_role_permissoes', { p_role_id: id, p_codigos: finalSet }); manter validacao do registry (41-48) e regra admin=todas (60-63) antes da RPC.",
   "supabase/migrations: aplicar via mcp__supabase__apply_migration no project ehbxpbeijofxtsbezwxd."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_set_role_permissoes(p_role_id uuid, p_codigos text[]) — replace atomico das permissoes de um role dentro de uma unica funcao plpgsql (delete+insert na mesma transacao implicita), idempotente.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "src/lib/wms/__tests__/set-role-permissoes.integration.test.ts",
  "red_test": "Apos uma falha simulada no insert (ex.: codigo que viola constraint, ou role inexistente apos delete), as permissoes pre-existentes do role devem permanecer intactas (rollback total) — nunca ficar vazias. Hoje o delete ja commitou e o role fica sem permissoes (RED).",
  "deps": [],
  "conflicts": [
   "P139"
  ],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: troca de permissoes de cargo tudo-ou-nada via transacao no banco — exige RPC (supabase-js nao da transacao client-side). MESMA ROTA e mesma RPC servem P139: ao adicionar o SELECT FOR UPDATE na row do role dentro da RPC, a serializacao concorrente (P139) sai de graca. Implementar P138 e P139 juntos na mesma RPC para evitar retrabalho e conflito de edicao no arquivo."
 },
 {
  "id": "P139",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/admin/roles/[id]/permissoes/route.ts:65-76 (mesmo trecho de P138) nao tem nenhum lock nem controle de concorrencia/versao: dois PUTs concorrentes para o mesmo role fazem delete+insert intercalados e o ultimo a inserir vence (last-write-wins), perdendo as mudancas do primeiro silenciosamente. Nenhum SELECT FOR UPDATE / advisory lock / optimistic version existe (grep em migrations e na rota confirmou). Existe trigger siso_roles_touch_atualizado_em (migration 20260521_roles_permissoes.sql:170) que atualiza atualizado_em — disponivel se quiser optimistic concurrency.",
  "root_cause": "Substituicao de permissoes sem serializacao: edicoes concorrentes do mesmo role nao sao mutuamente exclusivas, gerando last-write-wins e perda silenciosa.",
  "change_sites": [
   "supabase/migrations/<nova>_wms_set_role_permissoes.sql — na MESMA RPC de P138, iniciar com SELECT id FROM siso_roles WHERE id=p_role_id FOR UPDATE (lock pessimista por row); o segundo PUT concorrente fica em fila e re-aplica de forma serializada (ou, se preferir aviso explicito ao 2o, usar pg_advisory_xact_lock(hashtext(p_role_id::text)) com NOWAIT e levantar excecao 'role em edicao' que a rota traduz em 409).",
   "src/app/api/wms/admin/roles/[id]/permissoes/route.ts:60-78 — usar a RPC unica (vide P138); se a RPC sinalizar contencao (NOWAIT), mapear o erro para 409 com mensagem 'Este cargo esta sendo editado por outro admin. Tente de novo.'"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_set_role_permissoes (compartilhada com P138) com SELECT FOR UPDATE na row do role (ou advisory lock por role_id) para serializar/rejeitar edicoes concorrentes.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "test_file": "src/lib/wms/__tests__/set-role-permissoes-concorrencia.integration.test.ts",
  "red_test": "Dois sb.rpc('wms_set_role_permissoes') concorrentes para o mesmo role com conjuntos diferentes (A=[p1,p2], B=[p3]) — o resultado final deve ser exatamente A ou exatamente B (serializado), nunca mesclado/parcial, e nenhum 'vence em cima de delete do outro' deixando vazio. Hoje, com delete+insert intercalado, pode-se observar estado parcial/perda silenciosa (RED).",
  "deps": [
   "P138"
  ],
  "conflicts": [
   "P138"
  ],
  "effort": "M",
  "blast_radius": "med",
  "respeita_nota": true,
  "notes": "Nota: cadeado — um admin edita o cargo por vez; 2o ve aviso e espera. Implementacao natural = SELECT FOR UPDATE na row do role DENTRO da RPC de P138 (serializa) ou advisory lock NOWAIT (rejeita o 2o com 409 'sendo editado'). dep/conflict P138: ambos editam a MESMA rota e a MESMA RPC; devem ser feitos no mesmo PR para nao colidir. A escolha entre 'serializa silencioso' (FOR UPDATE blocking) e 'avisa e espera' (advisory NOWAIT->409) deve seguir a nota literalmente: nota fala em '2o ve aviso e espera', sugerindo o caminho com mensagem ao segundo admin."
 },
 {
  "id": "P004",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/pedidos/aprovar/route.ts:413-589 (criarReservasPedido) — o loop cria R por item sem RE-CHECAR se os itens do pedido mudaram durante o apartamento; não há re-validação pós-reserva. O comentário TOCTOU em :428-436 confirma que só existe o guard pedido.status!=='pendente' (:124), nenhuma checagem de mudança de mercadoria. Logo o guard pedido por options 1/2 NÃO existe.",
  "root_cause": "Aprovação aparta reservas sem verificar se a composição de itens do pedido mudou na origem durante o processamento; reservas podem ficar órfãs em itens que não existem mais. Decisão do raio-x = aceitar e deixar a reconciliação de saldo limpar depois.",
  "change_sites": [
   "NENHUM — NOTA do usuário diz 'mercadoria do pedido não vai mudar — deixar como está' (opcao 3). Won't-fix por decisão de negócio. Não adicionar guard de re-check nem lock."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "outro",
  "test_harness": "manual_only",
  "red_test": "Nenhum teste novo — decisão vinculante é não corrigir. Se algum dia mudar: detector wms_detectar_pedidos_inconsistentes (padrão C) já cobre reserva órfã via a rotina de acerto que a nota delega.",
  "test_file": "",
  "effort": "S",
  "blast_radius": "low",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "needs_migration_desc": "",
  "migration_desc": "",
  "notes": "Won't-fix explícito pela NOTA ('mercadoria do pedido não vai mudar — deixar como está'). O código realmente não tem o re-check, mas a decisão é manter assim e contar com a rotina de acerto de saldo (que já existe: wms_detectar_pedidos_inconsistentes padrão C_forward_com_reserva_viva, ver P084)."
 },
 {
  "id": "P038",
  "current_status": "open",
  "status_evidence": "src/lib/compras-utils.ts:125-170 (checkAndCancelPedidoIfAllTerminal) seta siso_pedidos.status='cancelado' (:149-156) e cancela jobs da fila (:158-162), mas NÃO chama liberarReserva/estornarReservaIndividual — grep 'reserva' em compras-utils.ts não retorna nada. A R viva continua segurando 'reservado' em siso_estoque até cleanupReservasExpiradas (cron ~1h) ou liberação manual. Compare: vendas-cancelamento.ts:76 e webhook/tiny/route.ts:219-225 JÁ liberam R no cancel — esta path é a wiring faltante.",
  "root_cause": "O path operador-cancela-compra (cancelamento/confirmar → checkAndCancelPedidoIfAllTerminal) cancela o pedido sem liberar as reservas R vivas, deixando estoque apartado preso até o cron de expiração. A máquina de liberação (estornarReservaIndividual, idempotente por estorno_de) já existe; só não é invocada aqui.",
  "change_sites": [
   "src/lib/compras-utils.ts:149-167 — após marcar pedido='cancelado' e cancelar a fila, buscar R vivas do pedido (tipo='R', origem_tipo='reserva_pedido', origem_id=pedidoId sem L estornando) e chamar estornarReservaIndividual({reserva_id, motivo:'outro'}) por R; logar liberadas. Espelhar o bloco de webhook/tiny/route.ts:210-245."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "reserva_wiring_cancel",
  "test_harness": "scenarios_e2e",
  "red_test": "Pedido com R viva é cancelado via fluxo de cancelamento de compra (todos itens terminais). Assertar: imediatamente após o cancel, siso_estoque.reservado do produto baixa em qty (não espera cron); siso_movimentacoes ganha L com estorno_de=R.id. Idempotência: chamar 2x não cria 2 L.",
  "test_file": "scripts/wms/cenarios/catalogo/65-compra-cancelada-libera-reserva.ts",
  "effort": "S",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "Reusar estornarReservaIndividual (idempotente via estorno_de=R.id), NÃO liberarReserva (tem short-circuit global temLiberacao que pula tudo se há qualquer L no pedido). Mesma decisão técnica que webhook/tiny e equivalente/confirmar. Família com P034/P039 (mesma primitiva, paths irmãos)."
 },
 {
  "id": "P084",
  "current_status": "partially_fixed",
  "status_evidence": "DETECÇÃO existe: supabase/migrations/20260530_wms_detectar_pedidos_inconsistentes.sql:61-77 (padrão C_forward_com_reserva_viva = pedido separado/embalado/expedido com R sem L) + src/app/api/wms/reconciliacao-pedidos/route.ts (GET worker-secret, read-only, retorna amostras). FALTA a RESOLUÇÃO: nenhuma rota/página consome essas linhas pra o gerente converter R→S (se saiu) ou devolver à prateleira (se cancelado). grep só achou o endpoint de detecção.",
  "root_cause": "O safety-net só DETECTA o pedido embalado com reserva R viva e sem saída; não há ação de resolução pelo gerente. As 20 unidades ficam congeladas (nem em saída, nem livres) até intervenção manual fora do sistema.",
  "change_sites": [
   "src/app/api/wms/reconciliacao-pedidos/ — NOVA rota POST de resolução (ex: /[pedidoId]/resolver) que, dada a confirmação do gerente, ou (saiu) emite L+S pareados convertendo R→saída (espelhar execution-worker-wms.ts:145-170), ou (cancelado) emite L devolvendo à prateleira via estornarReservaIndividual; gate por permissão admin + perm de visibilidade.",
   "src/app/wms/ — página/seção UI listando linhas do padrão C com botões 'confirmar saída' / 'devolver à prateleira' (consome o GET existente)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "red_test": "Setup: pedido status_separacao='embalado' com R viva e sem mov S nf_venda. Detector retorna 1 linha padrão C. (1) Resolver 'saiu' → cria L(estorno_de=R) + S(nf_venda); reservado baixa; disponível volta correto; detector volta 0. (2) Resolver 'cancelado' → cria só L; saldo permanece, reservado baixa; detector volta 0.",
  "test_file": "src/app/api/wms/reconciliacao-pedidos/resolver.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "A nota exige fluxo de gerente: 'confere NF, converte R->S se saiu ou devolve a prateleira se cancelado'. A detecção (migration C + GET endpoint) já cobre metade. Falta a ação humana de resolução. Conversão R→S deve seguir o padrão do worker (L com estorno_de=R + S nf_venda)."
 },
 {
  "id": "P090",
  "current_status": "partially_fixed",
  "status_evidence": "Arquivo citado src/app/api/wms/tiny/stock/ajustar/route.ts:139-167 ainda faz balanço NO TINY (movimentarEstoque tipo='B') e NÃO toca siso_estoque nem checa reservado — zero alerta de reserva a descoberto. PORÉM o path de ajuste em produção migrou pra POST /api/wms/ajuste → ajustarEstoque (src/lib/wms/movimentacoes.ts:431-453) que escreve no ledger WMS via inserirMovimentacao; lá validarCoerencia (ledger.ts:58) JÁ rejeita saída que faria reservado>saldo (BLOQUEIA, não alerta). A rota Tiny citada parece órfã (frontend ajuste/page.tsx:56 e modals.tsx:554 chamam /api/wms/ajuste).",
  "root_cause": "A correção de saldo absoluto via Tiny não considera reservas: pode deixar disponível negativo. A nota quer aceitar o físico (verdade) e ALERTAR reservas a descoberto, sem bloquear. O path moderno (/api/wms/ajuste) faz o oposto da nota: bloqueia via ledger; o path Tiny citado não bloqueia nem alerta.",
  "change_sites": [
   "src/app/api/wms/tiny/stock/ajustar/route.ts — esta rota deveria ser descontinuada (ver P086: 'Tiny é legado, não escrever ajuste no Tiny'); migrar para o ledger WMS.",
   "src/lib/wms/movimentacoes.ts:431-453 (ajustarEstoque) ou wrapper na rota /api/wms/ajuste — após aceitar o ajuste físico, computar reservado vs novo saldo da tripla e, se reservado > saldo, NÃO bloquear (a nota diz não bloquear): retornar/registrar ALERTA listando pedidos com R a descoberto (origem_id das R vivas) pra resolução (recompra/realocação/cancelar)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "vitest_unit",
  "red_test": "Dado saldo físico corrigido para 5 e reservado=8 (pedido X), o ajuste é ACEITO (não rejeitado) e a resposta inclui alerta de reservas a descoberto identificando pedido X com 3 un sem lastro. Não deve lançar erro de coerência nem ocultar a discrepância.",
  "test_file": "src/lib/wms/movimentacoes.test.ts (novo caso ajuste-reserva-descoberto)",
  "effort": "M",
  "blast_radius": "med",
  "deps": [
   "P086"
  ],
  "conflicts": [
   "P086"
  ],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "CONFLITO DE DESIGN com a guarda atual do ledger: validarCoerencia (ledger.ts:58) BLOQUEIA reservado>saldo, mas a nota de P090 diz 'não bloquear, aceitar físico e alertar'. Implementar o ajuste como correção absoluta exige um caminho que tolere reservado>saldo transitório (a invariante CHECK reservado<=saldo de siso_estoque também impede isso no cache — pode exigir liberar/realocar as R a descoberto como parte do ajuste, ou relaxar a invariante). Resolver junto com P086 (mesmo arquivo Tiny, migração pro ledger). Marcado partially_fixed porque o path moderno já protege a invariante (embora bloqueando, não alertando)."
 },
 {
  "id": "P115",
  "current_status": "open",
  "status_evidence": "src/lib/wms/localizacoes.ts:121-133 (desativarLocalizacao) só consulta siso_estoque.saldo>0 (:127); ignora reservado e não dispara limpeza de reservas vencidas antes. cleanupReservasExpiradas existe em reservas.ts:226 mas roda só via cron (/api/wms/reservas/cleanup) — não é chamada na desativação. Nenhum commit tocou desativarLocalizacao (git log mostra só features de lote/altura).",
  "root_cause": "Desativar prateleira não limpa reservas vencidas antes de checar — e como reservado>0 implica saldo>0 (CHECK reservado<=saldo), o guard saldo>0 já bloqueia, MAS bloqueia até cenquanto há reserva expirada que deveria ser limpa automaticamente. Resultado: operador não consegue desativar uma loc cujo único 'estoque' é reserva vencida pendente de cleanup.",
  "change_sites": [
   "src/lib/wms/localizacoes.ts:121-133 — antes do check, disparar limpeza de R vencidas DESSA loc (filtrar cleanupReservasExpiradas por localizacao_id, ou novo helper que emite L pras R com expira_em<now da loc). Depois re-checar saldo>0/reservado>0; só permitir desativar se nada vivo restar. Manter o bloqueio pra reservas ainda válidas (não-vencidas)."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "soft_delete_block_delete",
  "test_harness": "integration",
  "red_test": "Loc com R vencida (expira_em no passado) e saldo=50 reservado=50: desativarLocalizacao deve (1) auto-liberar a R vencida (cria L, reservado→0, saldo→0 após consumo? — na verdade L só libera reservado; saldo segue) — assertar que após limpeza a desativação procede SOMENTE se saldo livre=0; e que loc com R VÁLIDA (não vencida) continua bloqueada com erro claro.",
  "test_file": "src/lib/wms/localizacoes-desativar.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "Sutileza de invariante: CHECK(reservado<=saldo) garante que reservado>0 ⇒ saldo>0, então o guard saldo>0 atual já cobre o caso 'reserva viva'. A nota (op2) é especificamente sobre AUTO-LIMPAR reservas VENCIDAS e DEPOIS permitir desativar — ou seja, o gap real é a falta do trigger de cleanup na desativação, não a ausência do bloqueio. Liberar a R vencida (L) zera reservado mas NÃO zera saldo; se a loc ainda tem saldo livre, deve continuar bloqueada (correto)."
 },
 {
  "id": "P049",
  "current_status": "open",
  "status_evidence": "src/lib/wms/devolucoes.ts:190-323 (classificarDevolucao) emite até 3 movs sequenciais via inserirMovimentacao (cada uma é uma RPC/transação SEPARADA — ledger.ts:179 wms_inserir_movimentacao é 1 mov por call), e só DEPOIS (:325-334) faz o UPDATE de status. Não há wrapper transacional cobrindo as N movs + status. Se a 2ª mov falha (ex: quarentena), a 1ª já persistiu e o status fica 'aguardando_classificacao' → re-run duplica.",
  "root_cause": "Classificação multi-passo (Classe B: E + S quarentena + E quarentena; Classe C: E + S fornecedor) não é atômica: cada inserirMovimentacao é sua própria transação no Postgres. Falha no meio deixa movs órfãs e status não-classificado, abrindo dupla-contagem ao reprocessar.",
  "change_sites": [
   "Nova RPC plpgsql wms_classificar_devolucao(p_devolucao_id, p_classificacao, tripla, p_qty, ...) que faz as N movs (chamando a lógica interna de wms_inserir_movimentacao, ou inline) + o UPDATE de status DENTRO de uma única transação plpgsql (atômica por natureza). Ou wrapper que, em falha, estorna as movs já criadas do mesmo origemCompartilhado.",
   "src/lib/wms/devolucoes.ts:101-340 — classificarDevolucao passa a delegar à RPC única em vez de orquestrar movs no app layer."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_classificar_devolucao — executa as 1..3 movimentações da classificação (E + transferência quarentena / E + S fornecedor) e o UPDATE de status atomicamente numa transação plpgsql; idempotente por status='aguardando_classificacao'.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "red_test": "Classificar 'avariado' num galpão SEM quarentena cadastrada de forma que o passo S falhe: assertar que NENHUMA mov é persistida (E inicial revertida/não-commitada) E status continua 'aguardando_classificacao' coerente para retry — sem movs órfãs com origem_id compartilhado parcialmente aplicado.",
  "test_file": "src/lib/wms/devolucoes-classificar-atomico.integration.test.ts",
  "effort": "L",
  "blast_radius": "high",
  "deps": [
   "P051"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "Criar RPC wms_classificar_devolucao em supabase/migrations/ que envolve movs+status numa transação plpgsql (tudo-ou-nada).",
  "notes": "Atomicidade real só dá pra garantir no DB (plpgsql) — o app layer não tem transação multi-statement com o supabase-js. Mesma raiz de P050/P052. Resolver os três juntos com a RPC atômica + claim de status. P051 (bloquear sem quarentena) muda o que a Classe B faz, então decidir P051 antes (deps)."
 },
 {
  "id": "P050",
  "current_status": "open",
  "status_evidence": "src/lib/wms/devolucoes.ts:194-206 (E da devolução íntegra) e :325-334 (UPDATE status classificada) são operações SEPARADAS; a mov entra via inserirMovimentacao (ledger.ts:123 wms_inserir_movimentacao commita) e o UPDATE de status roda depois, fora de qualquer transação compartilhada. Se o UPDATE falha, saldo já subiu mas status segue 'pendente' → re-run conta o item 2x.",
  "root_cause": "Entrada de saldo (mov E) e transição de etapa da devolução não compartilham transação; falha entre elas deixa saldo incrementado com devolução ainda pendente, permitindo dupla-contagem no retry.",
  "change_sites": [
   "Mesma RPC de P049 (wms_classificar_devolucao): incluir o UPDATE de siso_devolucoes_pendentes.status DENTRO da transação que faz a(s) mov(s). Sub-caso da Classe A (íntegro) = 1 mov + status.",
   "src/lib/wms/devolucoes.ts:190-334 — passar mov(s)+status para a RPC atômica."
  ],
  "needs_migration": true,
  "rpc_needed": "wms_classificar_devolucao (mesma de P049) — garante mov(s) + UPDATE status na mesma transação plpgsql.",
  "primitive": "atomic_rpc_ledger",
  "test_harness": "integration",
  "red_test": "Forçar falha do UPDATE de status após a mov E (ex: id de devolução inválido pós-mov, ou simulação): assertar que a mov E NÃO persiste (saldo não sobe) quando o status não pôde transicionar — estado final é (saldo inalterado, status 'aguardando_classificacao'), nunca (saldo+3, status pendente).",
  "test_file": "src/lib/wms/devolucoes-classificar-atomico.integration.test.ts",
  "effort": "L",
  "blast_radius": "high",
  "deps": [
   "P049"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "Coberto pela RPC de P049 (mesma migration).",
  "notes": "Mesmo defeito de atomicidade que P049, recortado pela ótica 'mov + etapa'. Implementação compartilhada: uma única RPC plpgsql cobre P049+P050+P052. Marcar como um único PR."
 },
 {
  "id": "P051",
  "current_status": "open",
  "status_evidence": "src/lib/wms/devolucoes.ts:209-271 (case 'avariado'): quando NÃO há quarentena (:236-256 else em :257-270) o código NÃO bloqueia — emite uma S origem_tipo='ajuste_manual' (:260-269) removendo o saldo da loc de origem. Logo o 'desaparecer silencioso' do raio-x foi parcialmente mitigado (há trilha de ajuste), MAS a decisão da NOTA ('bloquear se quarentena não existe; validar ANTES de tirar da prateleira') NÃO está implementada — o item ainda sai da prateleira.",
  "root_cause": "Sem localização de quarentena no galpão, a Classe B faz E na origem e então um ajuste_manual S removendo o saldo — em vez de BLOQUEAR a operação antes de mover. A nota exige validar a existência da quarentena ANTES de qualquer movimentação e rejeitar com aviso ao operador.",
  "change_sites": [
   "src/lib/wms/devolucoes.ts:209-271 — no início do case 'avariado' (ANTES da mov E em :212), buscar a loc de quarentena do galpão; se não existir, throw 'quarentena inexistente no galpão X — crie antes de classificar avariado' (sem emitir nenhuma mov). Remover o branch else :257-270 (ajuste_manual) que mascara a ausência de quarentena.",
   "src/app/api/wms/devolucoes/[id]/classificar/route.ts:66-74 — o wmsErrorResponse já propaga o erro como 400 ao operador."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "test_harness": "vitest_unit",
  "red_test": "classificarDevolucao com classificacao='avariado' num galpão sem loc tipo='quarentena': deve lançar erro ANTES de qualquer inserirMovimentacao (mock conta 0 chamadas), com mensagem citando quarentena inexistente. Com quarentena presente: emite o par S+E para a quarentena normalmente.",
  "test_file": "src/lib/wms/devolucoes.test.ts (novo describe 'avariado sem quarentena bloqueia')",
  "effort": "S",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "Precede P049 (deps de P049): a guarda preflight muda o fluxo da Classe B, então deve ser baked na RPC atômica wms_classificar_devolucao. O branch ajuste_manual atual (:257-270) é uma mitigação que CONFLITA com a decisão da nota (silenciosamente faz o item sumir via ajuste em vez de bloquear) — remover."
 },
 {
  "id": "P052",
  "current_status": "partially_fixed",
  "status_evidence": "src/lib/wms/devolucoes.ts:103-118 — classificarDevolucao SELECTa a devolução e checa 'if (d.status !== \"aguardando_classificacao\") throw \"já classificada\"' (:118). Esse guard rejeita o 2º clique SEQUENCIAL. PORÉM é um read-check, não um compare-and-set: o UPDATE de status só ocorre no fim (:325-334), então 2 chamadas CONCORRENTES podem ambas ler 'aguardando_classificacao', passar o guard e duplicar movs. A nota pede 'rejeita 2a chamada' de forma robusta.",
  "root_cause": "O guard de status é TOCTOU: a leitura (linha 118) e a escrita de status (linha 325) estão separadas por todas as movs; sob concorrência real (duplo-clique com rede lenta) ambas as requisições passam o guard e emitem movs duplicadas.",
  "change_sites": [
   "src/lib/wms/devolucoes.ts:101-118 — substituir o SELECT+check por um CLAIM atômico: UPDATE siso_devolucoes_pendentes SET status='classificando' (ou lock) WHERE id=? AND status='aguardando_classificacao' RETURNING ...; se 0 rows afetadas → throw 'já classificada/em classificação' (rejeita a 2ª concorrente). Idealmente embutido na RPC atômica de P049 (claim no início da transação plpgsql)."
  ],
  "needs_migration": true,
  "rpc_needed": "Coberto por wms_classificar_devolucao (P049) — fazer o claim de status como primeiro statement da transação (UPDATE ... WHERE status='aguardando_classificacao'), abortando se nada for claimado.",
  "primitive": "atomic_claim_header",
  "test_harness": "integration",
  "red_test": "Disparar 2 POSTs /classificar concorrentes para a MESMA devolução: exatamente 1 retorna ok e o conjunto de movs gerado corresponde a 1 classificação; o outro retorna erro 'já classificada'. Contar movs com devolucao_id=X = exatamente o esperado de 1 run (não 2x).",
  "test_file": "src/app/api/wms/devolucoes/classificar-concorrente.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [
   "P049"
  ],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "Claim atômico de status dentro da RPC wms_classificar_devolucao (mesma migration de P049/P050).",
  "notes": "Partially_fixed: o guard sequencial existe (devolucoes.ts:118) e cobre duplo-clique simples; o que falta é o claim atômico (compare-and-set) pra fechar a janela concorrente. Naturalmente resolvido junto com a RPC atômica de P049/P050 — o claim de status vira o primeiro statement da transação."
 },
 {
  "id": "P053",
  "current_status": "open",
  "status_evidence": "src/lib/wms/devolucoes.ts:413-485 (desclassificarDevolucao) faz lookup das movs SOMENTE por FK devolucao_id (:447-451: .eq('devolucao_id', input.devolucao_id)). O próprio doc-comment :408-411 admite: 'devoluções classificadas antes deste commit não terão devolucao_id nas movs (coluna era NULL), portanto desclassificar retornará 0 movs estornadas'. Não há fallback por data+tipo. A nota pede exatamente esse fallback.",
  "root_cause": "Desclassificar localiza as movs a estornar apenas via FK devolucao_id, que não existe em devoluções classificadas antes de fix-final-B T11. Para essas devoluções antigas (pré-maio), o lookup retorna 0 → o estoque que entrou nunca volta ao monte ao cancelar a classificação.",
  "change_sites": [
   "src/lib/wms/devolucoes.ts:444-451 — quando o lookup por devolucao_id retornar 0 movs E a devolução foi classificada antes da introdução do FK (classificada_em < data do fix, ou devolucao_id ausente), aplicar fallback: buscar movs por (origem compartilhado / nota_fiscal_id / produto+galpão) dentro da janela [classificada_em ± margem] filtrando por origem_tipo das classes de devolução (devolucao_cliente_*, transferencia_localizacao, devolucao_fornecedor_enviada) com estorno_de IS NULL. Estornar esse conjunto."
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "estorno_ledger_lookup",
  "test_harness": "integration",
  "red_test": "Setup: devolução classificada com movs SEM devolucao_id (simular legado, NULL) mas com nota_fiscal_id/origem e classificada_em conhecidos. desclassificarDevolucao deve encontrar e estornar as movs via fallback data+tipo (movsEstornadas>0) e o saldo da loc retorna ao estado pré-classificação. Devolução nova (com devolucao_id) continua usando o lookup por FK.",
  "test_file": "src/lib/wms/devolucoes-desclassificar-legado.integration.test.ts",
  "effort": "M",
  "blast_radius": "med",
  "deps": [],
  "conflicts": [],
  "respeita_nota": true,
  "migration_desc": "",
  "notes": "A nota (op1) é explícita: 'procurar devoluções antigas por data da transação + tipo, não só por ID'. O fallback deve ser ativado SÓ quando o lookup por devolucao_id falha (preserva determinismo das novas). Cuidado pra não estornar movs de outra devolução do mesmo produto na mesma janela — ancorar por nota_fiscal_id/origem_id compartilhado quando disponível, data como filtro secundário."
 },
 {
  "id": "P105",
  "current_status": "open",
  "status_evidence": "src/app/wms/relatorios/movs-por-empresa/page.tsx:54-57 — default ainda é últimos 30 dias (Date.now() - 30 dias), NÃO os 12 meses pedidos na nota. src/app/api/wms/relatorios/movs-por-empresa/route.ts:42-65 — query NÃO tem .limit()/.range(); puxa TODAS as movs do intervalo e agrega em memória (Map em 69-95). Nenhum commit pós-3-jun mexeu nestes arquivos (git log: último é 97358ee feat troca_sku).",
  "root_cause": "O backend faz SELECT sem paginação de todas as siso_movimentacoes no intervalo e agrega no Node; o frontend default de 30 dias mascara o problema mas não impede o usuário de pedir intervalos enormes. Sem teto de linhas, intervalo grande estoura memória do servidor.",
  "change_sites": [
   "src/app/wms/relatorios/movs-por-empresa/page.tsx:54-57 — trocar default de dataInicio de (Date.now()-30d) para (Date.now()-365d) (últimos 12 meses pré-selecionados, conforme nota op1)",
   "src/app/api/wms/relatorios/movs-por-empresa/route.ts:42-65 — adicionar .limit(5000)/.range(0, 4999) na query (paginação 5 mil registros, conforme nota op2) ou aceitar offset/page param; retornar flag truncado quando atingir o teto",
   "src/app/api/wms/relatorios/movs-por-empresa/route.ts:97 — incluir no response indicador de truncamento (ex: { items, truncado: data.length>=5000 }) pra UI alertar"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "deps": [
   "P107"
  ],
  "conflicts": [],
  "notes": "P105 e P107 batem no MESMO route.ts (movs-por-empresa) — devem ser feitos juntos pra evitar conflito de merge. A nota é op1+op2 (default 12 meses E paginação 5 mil), não escolher um só. O default de 30 dias hoje não satisfaz a nota.",
  "effort": "S",
  "blast_radius": "low",
  "test_harness": "scenarios_e2e",
  "red_test": "GET /api/wms/relatorios/movs-por-empresa com intervalo que tem >5000 movs retorna no máximo 5000 itens agregados (ou flag truncado=true); e a page monta com dataInicio = hoje-365d por padrão. Asserir comportamento: response capado + default de período.",
  "test_file": "scripts/wms/cenarios/relatorio-movs-paginacao.ts",
  "respeita_nota": true,
  "migration_desc": "",
  "status_evidence_extra": ""
 },
 {
  "id": "P107",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/relatorios/movs-por-empresa/route.ts:62-63 — só galpao_id e produto_id são filtrados no SQL (.eq). O filtro de empresa NÃO está no SQL. route.ts:80-83 — a empresa é derivada (E=empresa_compradora_id, S=empresa_vendedora_id) e o filtro empresaId é aplicado em MEMÓRIA (if (empresaId && empresa !== empresaId) continue). Confirma que 90% dos dados podem ser descartados após carregados.",
  "root_cause": "Empresa não é uma coluna única na mov (é compradora pra E, vendedora pra S), então o autor jogou o filtro pro Node. Mas dá pra fazer no SQL com um .or() condicional ao tipo (E→empresa_compradora_id, S→empresa_vendedora_id).",
  "change_sites": [
   "src/app/api/wms/relatorios/movs-por-empresa/route.ts:62-64 — quando empresaId presente, adicionar filtro SQL: query.or(`and(tipo.eq.E,empresa_compradora_id.eq.${empresaId}),and(tipo.eq.S,empresa_vendedora_id.eq.${empresaId})`) (PostgREST .or com and aninhado)",
   "src/app/api/wms/relatorios/movs-por-empresa/route.ts:83 — manter o guard em memória como defense-in-depth, mas o grosso do corte passa a ocorrer no banco"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "deps": [],
  "conflicts": [],
  "notes": "Mesmo arquivo de P105 — fazer no mesmo PR. A nota é clara: filtrar empresa no SQL (E→compradora, S→vendedora), não em memória. Cuidado com sanitização: empresaId vem de query param; é uuid — validar formato antes de interpolar no .or() pra evitar PostgREST injection.",
  "effort": "S",
  "blast_radius": "low",
  "test_harness": "scenarios_e2e",
  "red_test": "GET /api/wms/relatorios/movs-por-empresa?empresa_id=X retorna apenas grupos cuja empresa==X (entradas onde compradora=X, saídas onde vendedora=X); e o número de linhas lidas do banco cai (verificar via count ou que rows de outras empresas não aparecem). Asserir: resultado idêntico ao filtro em-memória atual, provando equivalência semântica do filtro SQL.",
  "test_file": "scripts/wms/cenarios/relatorio-movs-filtro-empresa.ts",
  "respeita_nota": true,
  "migration_desc": "",
  "status_evidence_extra": ""
 },
 {
  "id": "P128",
  "current_status": "open",
  "status_evidence": "supabase/migrations/20260605_wms_excecoes_dashboards.sql:36-81 — CREATE MATERIALIZED VIEW siso_cobertura_estoque referencia empresa_dona_id em giro_30d (linha 38,45), saldo_agregado (linha 48,51), no SELECT (linha 59,77) e no UNIQUE INDEX (linha 81). Mas supabase/migrations/20260520_ledger_simplificado.sql:36 dropou empresa_dona_id de siso_estoque e linha 43 de siso_movimentacoes. supabase/migrations/20260520f_mviews.sql:38-82 JÁ recriou a MV no shape 3D correto (produto_id+galpao_id, sem empresa_dona_id). O 20260605 (commit def452f, cronologicamente DEPOIS) é o ÚLTIMO writer da MV e REGREDIU pro shape 4D quebrado. src/lib/wms/cobertura.ts:14-24 e src/app/api/wms/insights/estoque/route.ts:22 esperam o shape 3D (produto_id, galpao_id). Nenhuma migration pós-20260605 corrige.",
  "root_cause": "Regressão de ordenação de migrations: 20260605 (conteúdo pré-3D) foi commitado depois de 20260520f e re-dropa/recria a MV com empresa_dona_id que não existe mais. Em build-from-migrations a MV fica quebrada (coluna inexistente) → REFRESH falha → dashboard de cobertura lê MV vazia/inválida → 'Crítico: 0, Atenção: 0, OK: 0'. O consumer TS já está 3D, só o SQL está stale.",
  "change_sites": [
   "supabase/migrations/ — NOVA migration (ex: 20260607_fix_cobertura_3d.sql) que DROP MATERIALIZED VIEW IF EXISTS siso_cobertura_estoque e recria idêntica ao bloco 3D de 20260520f_mviews.sql:38-88 (giro_30d/saldo_agregado agrupados só por produto_id+galpao_id, origem_tipo IN ('nf_venda','venda_manual'), UNIQUE INDEX (produto_id,galpao_id), recria wms_refresh_cobertura)",
   "supabase/migrations/20260605_wms_excecoes_dashboards.sql:38-81 — (opcional, se a base for reconstruída do zero) corrigir in-place o mesmo bloco pra 3D, removendo empresa_dona_id de CTEs/SELECT/GROUP BY/USING/UNIQUE INDEX"
  ],
  "needs_migration": true,
  "rpc_needed": "",
  "primitive": "cache_invalidation",
  "deps": [],
  "conflicts": [],
  "notes": "Nota: 'agrupar por produto+galpao; nao ler empresa_dona_id'. O fix canônico já existe em 20260520f_mviews.sql:38-88 — basta replicar numa migration nova (migrations aplicadas são imutáveis; não editar 20260605 in-place se já rodou). HONESTIDADE: não consegui consultar staging (sem MCP supabase nesta sessão) pra confirmar se 20260605 chegou a aplicar com erro ou se a MV viva ainda é a de 20260520f. Independentemente, o ARQUIVO de migration está quebrado e quebra qualquer rebuild — fix obrigatório.",
  "effort": "S",
  "blast_radius": "med",
  "test_harness": "integration",
  "red_test": "Aplicar todas as migrations em DB limpo e rodar SELECT wms_refresh_cobertura() + SELECT * FROM siso_cobertura_estoque LIMIT 1 sem erro; asserir que a MV tem colunas (produto_id, galpao_id, status_cobertura) e NÃO tem empresa_dona_id. Hoje o REFRESH/SELECT falha por coluna inexistente.",
  "test_file": "tests/integration/cobertura-mv-3d.test.ts",
  "respeita_nota": true,
  "migration_desc": "Recria siso_cobertura_estoque no shape 3D (produto+galpao, sem empresa_dona_id), espelhando 20260520f_mviews.sql, revertendo a regressão do 20260605.",
  "status_evidence_extra": ""
 },
 {
  "id": "P158",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/relatorios/movs-por-empresa/route.ts:54-61 — a query inclui na whitelist de origem_tipo: 'nf_compra','nf_venda','venda_manual','devolucao_cliente_integra','devolucao_cliente_troca_sku','devolucao_fornecedor_enviada'. As 3 origens de devolução entram no mesmo relatório que compras de verdade. git log mostra commit 97358ee que ADICIONOU devolucao_cliente_troca_sku — exatamente o que P158 quer remover. Nenhum fix posterior.",
  "root_cause": "O relatório 'Movimentações por Empresa' (usado como relatório de compras/vendas) trata entradas de devolução (devolucao_cliente_integra, troca_sku) e saída de devolução a fornecedor como movs comerciais, contaminando a leitura de compra real e o custo agregado.",
  "change_sites": [
   "src/app/api/wms/relatorios/movs-por-empresa/route.ts:54-61 — remover do array .in('origem_tipo',[...]) as origens de devolução: 'devolucao_cliente_integra','devolucao_cliente_troca_sku','devolucao_fornecedor_enviada' — deixar só compras/vendas reais ('nf_compra','nf_venda','venda_manual')"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "pointwise_bug",
  "deps": [],
  "conflicts": [],
  "notes": "Nota op2: excluir devoluções do relatório de compras (só compras de verdade) — NÃO adicionar coluna 'Origem' (op1 rejeitada). Mesmo arquivo de P105/P107; coordenar no mesmo PR pra não dar 3 conflitos no mesmo route.ts. severidade média.",
  "effort": "S",
  "blast_radius": "low",
  "test_harness": "scenarios_e2e",
  "red_test": "Com movs de nf_compra E de devolucao_cliente_integra pro mesmo produto no intervalo, GET /api/wms/relatorios/movs-por-empresa NÃO retorna o grupo originado da devolução (só a compra real aparece). Asserir ausência das origens de devolução no resultado.",
  "test_file": "scripts/wms/cenarios/relatorio-movs-sem-devolucao.ts",
  "respeita_nota": true,
  "migration_desc": "",
  "status_evidence_extra": ""
 },
 {
  "id": "P182",
  "current_status": "partially_fixed",
  "status_evidence": "src/app/api/wms/insights/pessoas/[id]/route.ts:17 usa .single() — se operador foi deletado, retorna error → route responde 404 com error.message (linha 20-21), NÃO crash 500. src/app/wms/insights/pessoas/[id]/page.tsx:25 renderiza <ErrorBanner message={...}> nesse caso. Logo NÃO quebra a tela (já mitigado), MAS: (1) não há mensagem amigável 'operador saiu da empresa' — mostra erro cru do Postgres; (2) o campo ativo é selecionado (linha 17) mas NUNCA verificado — operador inativo (ativo=false) ainda renderiza insight normalmente, contra a nota.",
  "root_cause": "A nota pede 'conferir se operador existe/ativo antes de exibir; nao quebrar'. O 'existe' já é coberto (.single()→404→banner), mas falta: (a) tratar inativo (ativo=false) com mensagem dedicada, e (b) trocar o erro cru por uma resposta semântica 'operador saiu da empresa' que a UI exibe de forma calma em vez de ErrorBanner de falha.",
  "change_sites": [
   "src/app/api/wms/insights/pessoas/[id]/route.ts:16-23 — quando usuario.error (não encontrado) retornar 404 com payload semântico { error: 'operador_inexistente' }; e adicionar checagem: se usuario.data.ativo === false, retornar 200 com { usuario, serie, inativo: true } (ou 404 semântico) em vez de seguir como se ativo",
   "src/app/wms/insights/pessoas/[id]/page.tsx:24-26,49-53 — distinguir erro 404/inativo de erro real: exibir card calmo 'Operador saiu da empresa' (não ErrorBanner de falha) quando inexistente/inativo"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "data_visibility_ux",
  "deps": [],
  "conflicts": [],
  "notes": "Nota: 'alinha P135' — verificar P135 (não está no meu cluster) pra consistência de UX de operador inativo. Status partial porque o crash já não acontece (.single→404→banner), mas falta o tratamento de ativo=false e a mensagem amigável. Severidade média.",
  "effort": "S",
  "blast_radius": "low",
  "test_harness": "scenarios_e2e",
  "red_test": "GET /api/wms/insights/pessoas/{id de operador deletado} retorna 404 com chave semântica (operador_inexistente), e {id de operador com ativo=false} retorna marcação 'inativo' (não trata como ativo). Asserir: a UI não cai e mostra estado 'operador saiu', não erro de Postgres cru.",
  "test_file": "scripts/wms/cenarios/insights-pessoa-deletada.ts",
  "respeita_nota": true,
  "migration_desc": "",
  "status_evidence_extra": ""
 },
 {
  "id": "P104",
  "current_status": "open",
  "status_evidence": "src/app/api/wms/relatorios/movs-por-empresa/route.ts:93 — valor_total += qty * Number(custo_unitario ?? 0); quando a mov entrou com custo_unitario null/0, valor = 0. Origens de custo zero EXISTEM: src/app/api/wms/compras/receber/route.ts:239 grava mov E com custo_unitario: custo_unitario ?? 0 (default 0) e linha 448 custo_unitario > 0 ? ... : undefined. src/lib/wms/receber-oc.ts:192,222 passam custo_unitario ?? null. Já há um caminho que força (receber/route.ts:58 valida >=0, mas AINDA aceita 0). Nenhum fix pós-3-jun.",
  "root_cause": "Entradas podem ser gravadas com custo_unitario 0/null (compras/receber e receber/oc não exigem custo > 0), então o relatório multiplica por zero e exibe valor R$0,00, impossibilitando reconciliação com NF do fornecedor.",
  "change_sites": [
   "src/app/api/wms/compras/receber/route.ts:239 e 448 — não defaultar custo pra 0; exigir custo_unitario presente quando há qty, OU aplicar fallback no custo médio histórico (siso_custo_medio.custo_medio do produto) quando o operador não informar",
   "src/app/api/wms/receber/route.ts:58-87 — endurecer validação: rejeitar custo 0 quando qty>0 (hoje aceita custo>=0), com fallback ao custo médio histórico se ausente",
   "src/lib/wms/receber-oc.ts:152,192,222 — quando custo_unitario ausente, buscar fallback em siso_custo_medio antes de gravar null"
  ],
  "needs_migration": false,
  "rpc_needed": "",
  "primitive": "preflight_guard",
  "deps": [
   "P108"
  ],
  "conflicts": [],
  "notes": "Nota op1+op2: forçar preço na entrada COM fallback no custo médio histórico. P104 (valor no relatório) e P108 (bloqueio no RPC) compartilham raiz (entrada com custo 0). Decidir camada: P104 ataca os ROUTES de entrada (force + fallback); P108 ataca o RPC (bloqueio dura). Idealmente o fallback de custo médio vive nos routes (P104) e o RPC só bloqueia o caso terminal (P108). Fazer P108 primeiro (rede de segurança no banco), depois P104 (UX/fallback nos routes).",
  "effort": "M",
  "blast_radius": "med",
  "test_harness": "scenarios_e2e",
  "red_test": "Receber via /api/wms/compras/receber sem custo_unitario para um produto com custo médio histórico>0: a mov E é gravada com custo = custo médio histórico (fallback), NÃO 0; e o relatório movs-por-empresa mostra valor_total>0. Para produto NOVO sem histórico, a entrada é REJEITADA pedindo custo. Asserir: valor nunca é 0 quando há qty.",
  "test_file": "scripts/wms/cenarios/receber-custo-fallback.ts",
  "respeita_nota": true,
  "migration_desc": "",
  "status_evidence_extra": ""
 },
 {
  "id": "P108",
  "current_status": "open",
  "status_evidence": "Versão VIVA da RPC é supabase/migrations/20260527_wms_inserir_mov_motivo_categoria.sql (último writer de wms_inserir_movimentacao; supera 20260520b citado no raio-x). Linhas 82-89: v_recalcula_custo := (p_tipo='E' AND p_custo_unitario IS NOT NULL AND origem IN (...)). custo_unitario=0 é NOT NULL → RECALCULA. Linha 96-99: v_custo_medio_novo = (saldo*cm + qty*0)/(saldo+qty) → dilui/zera o custo médio. NÃO há bloqueio de custo zero nem custo mínimo. grep não achou nenhum CHECK custo_unitario em migrations.",
  "root_cause": "A RPC só checa custo_unitario IS NOT NULL, não custo>0. Uma entrada com custo 0 e qty>0 (whitelist nf_compra/ajuste_manual/etc) entra no recálculo ponderado e arrasta o custo médio global pra perto de zero, mesmo havendo estoque físico.",
  "change_sites": [
   "supabase/migrations/ — NOVA migration recriando wms_inserir_movimentacao (espelhando o corpo de 20260527_wms_inserir_mov_motivo_categoria.sql) que, quando p_tipo='E' AND p_quantidade>0 AND v_recalcula_custo AND COALESCE(p_custo_unitario,0)=0, faz RAISE EXCEPTION 'entrada com custo zero não permitida quando há quantidade' (ou aplica custo mínimo = custo médio atual). Inserir o guard logo após a definição de v_recalcula_custo (linha ~89)"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_inserir_movimentacao — recriar com guard de custo-zero (RAISE quando tipo=E, qty>0 e custo_unitario=0 nas origens que compõem custo médio)",
  "primitive": "db_constraint_unique",
  "deps": [],
  "conflicts": [
   "P104"
  ],
  "notes": "Nota: 'bloquear entrada com custo zero quando ha qty (ou usar custo minimo)'. CUIDADO: o guard só deve disparar nas origens que recalculam custo (nf_compra, devolucao_cliente_integra, lancamento_retroativo, ajuste_manual, inventario_inicial) — origens operacionais (S/R/L, reserva, transferência) passam custo null e NÃO devem ser afetadas. Conflito-leve com P104: ambos mexem em 'custo zero na entrada'; alinhar — RPC bloqueia o terminal (P108), routes fazem fallback de custo médio (P104). Fazer P108 antes de P104.",
  "effort": "M",
  "blast_radius": "high",
  "test_harness": "integration",
  "red_test": "Chamar wms_inserir_movimentacao(tipo='E', qty=5, origem='nf_compra', custo_unitario=0) deve RAISE (ou setar custo=custo mínimo), NÃO gravar custo médio 0. E uma mov S/R/L normal sem custo continua passando. Asserir: produto com 5 un nunca fica com custo médio 0 por entrada de custo zero.",
  "test_file": "tests/integration/rpc-custo-zero-block.test.ts",
  "respeita_nota": true,
  "migration_desc": "Recria wms_inserir_movimentacao adicionando guard: entrada (E) com qty>0 e custo_unitario=0 nas origens que compõem custo médio é bloqueada (RAISE) ou usa custo mínimo, evitando custo médio R$0 com estoque presente.",
  "status_evidence_extra": ""
 },
 {
  "id": "P110",
  "current_status": "open",
  "status_evidence": "src/lib/wms/ledger.ts:394-416 — estornarMovimentacao inverte o tipo (E→S via tipoMap) e grava origem_tipo='estorno'. Na RPC viva (20260527_wms_inserir_mov_motivo_categoria.sql:82-89) v_recalcula_custo só é true pra p_tipo='E' com origens whitelisted; 'estorno' NÃO está na whitelist e o tipo é 'S' → custo médio NÃO recalcula. Resultado: estornar uma entrada deixa o custo médio congelado no valor pós-entrada (inflado). Confirmado também no histórico do RPC (20260520b/20260526) — comportamento idêntico desde a origem.",
  "root_cause": "O estorno de uma entrada vira uma saída (tipo S) com origem 'estorno', e o recálculo de custo médio só roda em entradas whitelisted. Logo o efeito da entrada sobre o custo médio nunca é desfeito — custo médio permanece inflado e contamina recálculos futuros.",
  "change_sites": [
   "supabase/migrations/ — NOVA migration recriando wms_inserir_movimentacao com tratamento de estorno de custo: quando p_estorno_de IS NOT NULL E a mov original era 'E' que recalculou custo, reverter o custo médio. Abordagem cirúrgica seguindo a nota ('como se a entrada nunca existisse'): ler custo_medio_anterior da mov original (siso_movimentacoes.custo_medio_anterior do estorno_de) e restaurar siso_custo_medio.custo_medio para esse valor anterior (válido quando o estorno é o último evento de custo do produto)",
   "src/lib/wms/ledger.ts:399-416 — garantir que estornarMovimentacao propague p_estorno_de (já propaga via input.estorno_de) pra RPC poder identificar e reverter"
  ],
  "needs_migration": true,
  "rpc_needed": "wms_inserir_movimentacao — recriar para que estorno (p_estorno_de) de uma entrada que compôs custo médio reverta siso_custo_medio ao custo_medio_anterior da mov original",
  "primitive": "estorno_ledger_lookup",
  "deps": [
   "P108"
  ],
  "conflicts": [],
  "notes": "Nota: 'estorno recalcula o custo médio como se a entrada nunca existisse (reverte o efeito)'. RISCO/HONESTIDADE: restaurar para custo_medio_anterior é exato SÓ se nenhuma outra entrada de custo ocorreu entre a entrada e o estorno; caso contrário a reversão correta exigiria recomputar a média ponderada removendo aquela entrada (mais complexo). Para o caso da nota (entrou tudo, estornou tudo → volta ao anterior) o custo_medio_anterior resolve. Coordenar com P108 (mesmo corpo de RPC; fazer numa migration única que junta os dois guards de custo pra não ter 2 redefinições conflitantes).",
  "effort": "L",
  "blast_radius": "high",
  "test_harness": "integration",
  "red_test": "Custo médio 5 → entrada de qty a custo 8 (custo vira 8) → estornar essa entrada → custo médio volta a 5 (não fica 8). E uma entrada nova posterior recalcula a partir de 5, não de 8. Asserir: estorno reverte o efeito da entrada sobre siso_custo_medio.",
  "test_file": "tests/integration/rpc-estorno-custo-medio.test.ts",
  "respeita_nota": true,
  "migration_desc": "Recria wms_inserir_movimentacao para que, ao estornar (p_estorno_de) uma entrada que compôs o custo médio, o custo médio seja revertido (restaurado ao custo_medio_anterior da mov original), evitando custo inflado pós-estorno. Idealmente unir com o guard de custo-zero do P108 numa migration só.",
  "status_evidence_extra": ""
 }
]